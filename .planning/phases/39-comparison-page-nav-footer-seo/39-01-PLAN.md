---
phase: 39-comparison-page-nav-footer-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/comparison.ts
  - src/app/(marketing)/compare/page.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, NAV-02, NAV-03]

must_haves:
  truths:
    - "Visitor can view /compare and see a feature matrix with NZPOS vs 3-5 NZ POS competitors"
    - "Visitor sees a global pricing disclaimer with last-verified date for FTA compliance"
    - "Visitor can read a Why NZPOS editorial section with key differentiators"
    - "Visitor can expand/collapse FAQ items using native details/summary elements"
    - "Visitor sees CTA buttons after the feature matrix and after the FAQ"
    - "Comparison page has title, description, OG meta tags, and JSON-LD SoftwareApplication schema"
  artifacts:
    - path: "src/data/comparison.ts"
      provides: "Typed competitor data, feature matrix, FAQ items"
      exports: ["competitors", "featureCategories", "faqItems"]
    - path: "src/app/(marketing)/compare/page.tsx"
      provides: "Comparison page with matrix, editorial, FAQ, CTAs"
      contains: "export const metadata"
  key_links:
    - from: "src/app/(marketing)/compare/page.tsx"
      to: "src/data/comparison.ts"
      via: "import"
      pattern: "from.*@/data/comparison"
    - from: "src/app/(marketing)/compare/page.tsx"
      to: "/add-ons/*"
      via: "Link href"
      pattern: "href.*add-ons/"
    - from: "src/app/(marketing)/compare/page.tsx"
      to: "/signup"
      via: "Link href CTA"
      pattern: "href.*signup"
---

<objective>
Create the competitor comparison page at `/compare` with a typed data file, sticky-column feature matrix, FTA-compliant pricing disclaimer, "Why NZPOS" editorial section, FAQ accordion, and CTA buttons. Include full SEO metadata and JSON-LD structured data on the comparison page.

Purpose: Mid-funnel evaluation page that lets visitors compare NZPOS against NZ POS competitors with accurate, sourced data and clear differentiators.
Output: Two files — `src/data/comparison.ts` (data) and `src/app/(marketing)/compare/page.tsx` (page).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-comparison-page-nav-footer-seo/39-CONTEXT.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comparison data file with typed competitor and feature data</name>
  <files>src/data/comparison.ts</files>
  <read_first>
    - DESIGN.md (color tokens, typography for any inline style references)
    - .planning/phases/39-comparison-page-nav-footer-seo/39-CONTEXT.md (decisions D-04, D-05, D-06, D-08, D-09)
    - .planning/REQUIREMENTS.md (COMP-01 through COMP-05 acceptance criteria)
    - src/app/(marketing)/add-ons/xero/page.tsx (data array pattern — withoutItems, features, steps arrays as structural reference)
  </read_first>
  <action>
Create `src/data/comparison.ts` with the following typed exports (per D-05):

**TypeScript interfaces:**
```typescript
interface Competitor {
  name: string       // Text-only, no logos (per D-04, REQUIREMENTS.md out-of-scope)
  slug: string       // URL-safe identifier
  monthlyPrice: string  // e.g., "From $69/mo" or "Custom pricing"
  website: string    // Source URL for pricing verification
}

interface Feature {
  name: string
  category: string   // Groups features into sections
  nzposValue: string | boolean  // true = has it, string for detail like "$9/mo add-on"
  competitorValues: Record<string, string | boolean>  // keyed by competitor slug
  addOnLink?: string  // Optional link to /add-ons/* detail page (per D-03)
}

interface FeatureCategory {
  name: string
  slug: string
}

interface FAQItem {
  question: string
  answer: string
}
```

**Competitor selection (per D-04, Claude's Discretion — select 3-5 NZ-relevant POS competitors):**
- Square (global, strong NZ presence, free POS + transaction fees)
- Lightspeed (formerly Vend, NZ-founded, now Lightspeed Restaurant/Retail)
- Shopify POS (strong online, POS as add-on)
- POSbiz (NZ-specific, local market)

Include `website` field on each competitor with their NZ pricing page URL. Add a comment block at the top:
```
// Competitor pricing last verified: April 2026
// Sources: [URLs for each competitor's pricing page]
// Update pricing disclaimer date in page.tsx if refreshing this data
```

**Feature categories (10-15 features per D-01, grouped by category):**
Categories: "Core POS", "Online Store", "Inventory & Stock", "NZ Compliance", "Add-ons & Extras", "Pricing"

Features should highlight NZPOS differentiators. For features that correspond to NZPOS add-ons, set `addOnLink` to the detail page URL (per D-03):
- Gift Cards → `/add-ons/gift-cards`
- Advanced Reporting / COGS → `/add-ons/advanced-reporting`
- Loyalty Points → `/add-ons/loyalty-points`
- Xero Integration → `/add-ons/xero`
- Inventory Management → `/add-ons/inventory`

**FAQ items (per D-09, 5-8 comparison-focused questions):**
- "How does NZPOS pricing compare to Square/Lightspeed?"
- "Is NZPOS suitable for my type of retail store?"
- "Does NZPOS handle EFTPOS payments?"
- "How does NZPOS handle GST compliance?"
- "Can I switch from my current POS to NZPOS?"
- "Does NZPOS work on iPad?"

Each answer should be 2-3 sentences, confident Kiwi SaaS tone (per Phase 28 D-07).

**Export a `pricingDisclaimerDate` string** (per D-06): `"April 2026"` — used for the global disclaimer.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npx tsc --noEmit src/data/comparison.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/data/comparison.ts exists and exports `competitors`, `featureCategories`, `features`, `faqItems`, `pricingDisclaimerDate`
    - File contains `interface Competitor` with `name`, `slug`, `monthlyPrice`, `website` fields
    - File contains `interface Feature` with `addOnLink` optional field
    - File contains 4 competitor objects (Square, Lightspeed, Shopify POS, POSbiz)
    - File contains at least 10 features across at least 5 categories
    - File contains at least 5 FAQ items with `question` and `answer` fields
    - File contains comment block with "Competitor pricing last verified" and source URLs
    - File contains `pricingDisclaimerDate` export with value `"April 2026"`
    - At least 3 features have `addOnLink` values pointing to `/add-ons/*` paths
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Typed comparison data file exists with 4 competitors, 10+ features, 5+ FAQ items, pricing disclaimer date, and add-on links. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create comparison page with feature matrix, editorial, FAQ, CTAs, and SEO</name>
  <files>src/app/(marketing)/compare/page.tsx</files>
  <read_first>
    - src/data/comparison.ts (data file created in Task 1)
    - DESIGN.md (colors, typography, spacing, border radius, motion)
    - src/app/(marketing)/add-ons/xero/page.tsx (canonical marketing page structure — Metadata export, force-static, Link imports, Tailwind patterns)
    - src/app/(marketing)/components/LandingNav.tsx (details/summary pattern for FAQ accordion per D-08)
    - .planning/phases/39-comparison-page-nav-footer-seo/39-CONTEXT.md (all decisions D-01 through D-14)
  </read_first>
  <action>
Create `src/app/(marketing)/compare/page.tsx` as a Server Component with `export const dynamic = 'force-static'`.

**SEO Metadata (per D-14, NAV-02):**
```typescript
export const metadata: Metadata = {
  title: 'Compare NZPOS vs Square, Lightspeed, Shopify POS — NZ POS Systems',
  description: 'See how NZPOS compares to Square, Lightspeed, Shopify POS and other NZ point-of-sale systems. Feature-by-feature comparison with transparent pricing.',
  openGraph: {
    title: 'Compare NZPOS vs NZ POS Competitors',
    description: 'Feature-by-feature comparison of NZ point-of-sale systems. Transparent pricing, NZ compliance, and honest feature matrix.',
    type: 'website',
    url: 'https://nzpos.co.nz/compare',
  },
}
```

**Page sections (top to bottom):**

1. **Hero section** — `max-w-[1200px] mx-auto` container. Heading: "How NZPOS compares" (font-display font-bold text-3xl md:text-5xl text-[var(--color-navy)]). Subtext explaining this is an honest feature comparison. Include global pricing disclaimer (per D-06): "Competitor pricing verified as of {pricingDisclaimerDate}. Visit their websites for current pricing." styled as `text-sm text-[var(--text-muted)] italic`.

2. **Feature matrix section** (per D-01, D-02):
   - Wrapper div with `overflow-x-auto` for mobile horizontal scroll
   - `<table>` with sticky first column (NZPOS) using `sticky left-0 z-10 bg-white`
   - Table header row: "Feature" | "NZPOS" | competitor names from data
   - NZPOS column header styled with `bg-[var(--color-navy)] text-white` to visually distinguish
   - Feature rows grouped by category — render category name as a spanning header row (`bg-[var(--surface)] font-display font-bold`)
   - Cell values: boolean true = green checkmark SVG, false = red X SVG, string = text value
   - Features with `addOnLink` — wrap NZPOS cell value in a `<Link>` to the add-on detail page (per D-03)
   - Alternating row backgrounds: `even:bg-[var(--surface)]`
   - Mobile: table scrolls horizontally, NZPOS column stays sticky (per D-02)

3. **CTA section 1** (per D-10) — After feature matrix. Centered block with heading "Ready to switch?" and two buttons:
   - Primary: "Start free trial" → `/signup` (bg-[var(--color-amber)] text-white font-bold rounded-md px-6 py-3)
   - Secondary: "Try the POS demo" → `/demo/pos` (border border-[var(--color-navy)] text-[var(--color-navy)] rounded-md px-6 py-3)

4. **Why NZPOS section** (per D-07, COMP-03) — Editorial narrative with subheadings. Use `font-display` for subheadings, `font-sans` for body. Cover key differentiators:
   - Built for New Zealand (GST, EFTPOS, NZD-first)
   - Honest pricing (no transaction fees on POS sales)
   - One inventory, two channels (POS + online store sync)
   - Add what you need (modular add-ons vs forced bundles)
   Confident Kiwi SaaS tone — direct, no marketing fluff.

5. **FAQ section** (per D-08, D-09, COMP-04):
   - Heading: "Common questions" (font-display)
   - Render each FAQ item using native `<details>` / `<summary>` elements (per D-08, matching LandingNav.tsx pattern)
   - Style: `border-b border-[var(--border)]` between items, `summary` with `cursor-pointer font-sans font-bold text-[var(--color-navy)] py-[var(--space-md)]`, answer in `pb-[var(--space-md)] text-[var(--text-muted)]`
   - Import `faqItems` from comparison data file

6. **CTA section 2** (per D-10) — After FAQ. Same layout as CTA 1 but different copy: heading "See it in action" with same two buttons.

**JSON-LD structured data (per D-13, NAV-03):**
Add a `<script type="application/ld+json">` in the JSX (after the closing main div) with SoftwareApplication schema:
```json
{
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "NZPOS",
  "applicationCategory": "BusinessApplication",
  "operatingSystem": "Web",
  "description": "Point-of-sale and online store for NZ small businesses",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "NZD",
    "description": "Free tier available"
  },
  "featureList": "POS, Online Store, GST Compliance, Xero Integration, Gift Cards, Loyalty Points, Advanced Reporting, Inventory Management"
}
```

**Styling notes:**
- All sections use `max-w-[1200px] mx-auto px-[var(--space-md)] md:px-[var(--space-lg)]` container
- Section spacing: `py-[var(--space-2xl)] md:py-[var(--space-3xl)]`
- Page background: default (inherits from layout)
- Links use `text-[var(--color-amber)] hover:underline` for inline links
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npx tsc --noEmit src/app/\(marketing\)/compare/page.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/compare/page.tsx exists
    - File contains `export const dynamic = 'force-static'`
    - File contains `export const metadata: Metadata` with title containing "Compare NZPOS"
    - File contains `openGraph` object with `title`, `description`, `type`, `url` fields
    - File contains `application/ld+json` script with `SoftwareApplication` type
    - File imports from `@/data/comparison` (competitors, features, faqItems, pricingDisclaimerDate)
    - File contains `overflow-x-auto` for mobile matrix scroll
    - File contains `sticky` class for NZPOS column
    - File contains at least 2 Link elements with href="/signup" (two CTA sections)
    - File contains `<details>` and `<summary>` elements for FAQ accordion
    - File contains Link elements with href values matching `/add-ons/` pattern
    - File contains pricing disclaimer text referencing `pricingDisclaimerDate`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Comparison page renders at /compare with sticky-column feature matrix, FTA-compliant pricing disclaimer, Why NZPOS editorial, expandable FAQ, two CTA sections, OG meta tags, and JSON-LD structured data. All NZPOS add-on features link to their detail pages.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for both new files
- `/compare` route exists under (marketing) route group
- Feature matrix has NZPOS as sticky first column with at least 4 competitors
- Pricing disclaimer visible with "April 2026" date
- FAQ items use native details/summary (no JS dependency)
- Two CTA sections with signup and demo links
- JSON-LD script tag present with SoftwareApplication schema
- OG meta tags present in metadata export
</verification>

<success_criteria>
Visitor can view /compare and see a feature matrix comparing NZPOS against 4 NZ POS competitors, read a Why NZPOS editorial, expand/collapse FAQ items, and click CTAs to signup or demo. Page includes FTA-compliant pricing disclaimer, JSON-LD structured data, and full OG meta tags.
</success_criteria>

<output>
After completion, create `.planning/phases/39-comparison-page-nav-footer-seo/39-01-SUMMARY.md`
</output>
