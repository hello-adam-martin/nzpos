---
phase: 39-comparison-page-nav-footer-seo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/components/LandingNav.tsx
  - src/app/(marketing)/components/LandingFooter.tsx
autonomous: true
requirements: [NAV-01, NAV-04]

must_haves:
  truths:
    - "Visitor can navigate to /compare from the desktop nav bar on the landing page"
    - "Visitor can navigate to /compare from the mobile nav overlay on the landing page"
    - "Footer displays grouped columns with Product, Add-ons, and Account sections"
    - "Footer includes links to /compare, all 5 add-on detail pages, and sign in/signup"
    - "Footer retains Privacy and Terms links"
  artifacts:
    - path: "src/app/(marketing)/components/LandingNav.tsx"
      provides: "Compare link in desktop and mobile nav"
      contains: "href=\"/compare\""
    - path: "src/app/(marketing)/components/LandingFooter.tsx"
      provides: "Multi-column footer with Product, Add-ons, Account groups"
      contains: "href=\"/compare\""
  key_links:
    - from: "LandingNav.tsx"
      to: "/compare"
      via: "Link href"
      pattern: "href.*compare"
    - from: "LandingFooter.tsx"
      to: "/add-ons/*"
      via: "Link href"
      pattern: "href.*add-ons/"
    - from: "LandingFooter.tsx"
      to: "/compare"
      via: "Link href"
      pattern: "href.*compare"
---

<objective>
Add "Compare" to the landing page navigation (desktop and mobile) and restructure the footer from a single-row layout into grouped columns with Product, Add-ons, and Account sections.

Purpose: Make the comparison page discoverable from every marketing page and provide structured footer navigation to all key pages.
Output: Updated LandingNav.tsx and LandingFooter.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-comparison-page-nav-footer-seo/39-CONTEXT.md
@DESIGN.md
</context>

<interfaces>
<!-- Current LandingNav.tsx structure (from codebase read) -->
From src/app/(marketing)/components/LandingNav.tsx:
- Desktop nav: line 17-33, `<nav className="hidden md:flex">` with links: Features (#features), Pricing (#pricing), Add-ons (/add-ons), Sign in (/login), Get started (/signup)
- Mobile overlay: line 36-74, `<details className="md:hidden">` with same links in vertical layout
- "Compare" goes between "Add-ons" and "Sign in" per D-11

From src/app/(marketing)/components/LandingFooter.tsx:
- Currently single-row flexbox with: copyright, Sign in, Privacy, Terms
- Restructure to multi-column grid per D-12
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Compare link to LandingNav desktop and mobile sections</name>
  <files>src/app/(marketing)/components/LandingNav.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingNav.tsx (current nav structure — desktop links at lines 18-20, mobile links at lines 57-60)
    - .planning/phases/39-comparison-page-nav-footer-seo/39-CONTEXT.md (decision D-11)
    - DESIGN.md (typography, spacing)
  </read_first>
  <action>
Add a "Compare" link to LandingNav.tsx in two places (per D-11: placed between "Add-ons" and "Sign in"):

**Desktop nav (inside the `hidden md:flex` nav element, after the Add-ons Link, before the Sign in Link):**
```tsx
<Link href="/compare" className="font-sans text-sm text-white/70 hover:text-white transition-colors duration-150 py-2">Compare</Link>
```

**Mobile overlay (inside the `details` mobile menu, after the Add-ons Link, before the Sign in Link):**
```tsx
<Link href="/compare" className="font-sans text-base text-white/70 hover:text-white transition-colors duration-150">Compare</Link>
```

Both follow the exact same className pattern as the existing "Add-ons" link — only difference is mobile uses `text-base` instead of `text-sm` (matching existing mobile link pattern).
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -n 'compare' src/app/\(marketing\)/components/LandingNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LandingNav.tsx contains exactly 2 occurrences of `href="/compare"`
    - First occurrence is in the desktop nav section (inside `className="hidden md:flex"`)
    - Second occurrence is in the mobile overlay section (inside `<details`)
    - "Compare" link appears after "Add-ons" link and before "Sign in" link in both desktop and mobile sections
    - Desktop link uses `text-sm` class
    - Mobile link uses `text-base` class
    - Both links use `Link` component from next/link (not `<a>`)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>"Compare" link visible in both desktop nav bar and mobile hamburger menu overlay, positioned between "Add-ons" and "Sign in".</done>
</task>

<task type="auto">
  <name>Task 2: Restructure LandingFooter into multi-column grouped layout</name>
  <files>src/app/(marketing)/components/LandingFooter.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingFooter.tsx (current minimal footer structure)
    - .planning/phases/39-comparison-page-nav-footer-seo/39-CONTEXT.md (decision D-12 — column groups)
    - DESIGN.md (colors, typography, spacing, border radius)
  </read_first>
  <action>
Replace the current single-row footer with a multi-column grouped layout (per D-12).

**Structure:**
```
Footer container: bg-[var(--color-navy)] py-[var(--space-2xl)]
  Inner: max-w-[1200px] mx-auto px-[var(--space-md)] md:px-[var(--space-lg)]

    Top section: grid grid-cols-2 md:grid-cols-4 gap-[var(--space-xl)]

    Column 1: "Product"
      - Features → /#features
      - Pricing → /#pricing
      - Compare → /compare

    Column 2: "Add-ons"
      - Xero Integration → /add-ons/xero
      - Inventory → /add-ons/inventory
      - Gift Cards → /add-ons/gift-cards
      - Advanced Reporting → /add-ons/advanced-reporting
      - Loyalty Points → /add-ons/loyalty-points

    Column 3: "Account"
      - Sign in → /login
      - Sign up → /signup
      - Try POS Demo → /demo/pos

    Column 4: "Legal"
      - Privacy → #
      - Terms → #

    Bottom section: border-t border-white/10 mt-[var(--space-xl)] pt-[var(--space-lg)]
      - Copyright: "© 2026 NZPOS. Built in New Zealand."
```

**Styling for each column:**
- Column heading: `font-display font-bold text-sm text-white/60 uppercase tracking-wider mb-[var(--space-md)]`
- Column links: `font-sans text-sm text-white/50 hover:text-white transition-colors duration-150` with `block mb-[var(--space-sm)]` for vertical spacing
- Use `<Link>` for internal routes, `<a>` for anchor links (/#features, /#pricing)
- Copyright text: `font-sans text-sm text-white/40`

**Mobile layout:**
- `grid-cols-2` on mobile (2x2 grid), `md:grid-cols-4` on desktop (all 4 columns in a row)
- All columns stack naturally in the 2-column grid

**Retain:** Copyright text at bottom. Privacy and Terms links (currently `href="#"` placeholder — keep as-is).
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c 'href=' src/app/\(marketing\)/components/LandingFooter.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LandingFooter.tsx contains `grid grid-cols-2 md:grid-cols-4` for column layout
    - File contains column headings: "Product", "Add-ons", "Account", "Legal"
    - File contains `href="/compare"` link
    - File contains links to all 5 add-on pages: `/add-ons/xero`, `/add-ons/inventory`, `/add-ons/gift-cards`, `/add-ons/advanced-reporting`, `/add-ons/loyalty-points`
    - File contains `href="/login"` and `href="/signup"` links
    - File contains `href="/demo/pos"` link
    - File contains Privacy and Terms links (href="#")
    - File contains copyright text "2026 NZPOS"
    - File contains `border-t border-white/10` for bottom separator
    - At least 13 total href attributes (3 Product + 5 Add-ons + 3 Account + 2 Legal)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Footer displays 4 grouped columns (Product, Add-ons, Account, Legal) with links to comparison page, all 5 add-on detail pages, auth pages, demo, and legal pages. Copyright retained at bottom.</done>
</task>

</tasks>

<verification>
- LandingNav.tsx has "Compare" link in both desktop and mobile sections
- LandingFooter.tsx has multi-column grid layout with all required links
- `npx tsc --noEmit` passes for both modified files
- Footer includes all 5 add-on page links
- Footer includes /compare link
</verification>

<success_criteria>
Visitor sees "Compare" in the nav bar (desktop and mobile) on every marketing page. Footer displays structured columns with links to comparison page, all 5 add-on detail pages, signup, sign in, demo, privacy, and terms.
</success_criteria>

<output>
After completion, create `.planning/phases/39-comparison-page-nav-footer-seo/39-02-SUMMARY.md`
</output>
