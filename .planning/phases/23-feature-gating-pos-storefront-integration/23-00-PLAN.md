---
phase: 23-feature-gating-pos-storefront-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/pos/__tests__/ProductCard.test.tsx
  - src/components/store/__tests__/AddToCartButton.test.tsx
autonomous: true
requirements: [POS-01, POS-02, POS-03]

must_haves:
  truths:
    - "POS ProductCard test scaffold exists and can be run by vitest"
    - "Storefront AddToCartButton test scaffold exists and can be run by vitest"
  artifacts:
    - path: "src/components/pos/__tests__/ProductCard.test.tsx"
      provides: "Test scaffold for POS stock badge rendering and out-of-stock behavior"
      contains: "describe.*ProductCard"
    - path: "src/components/store/__tests__/AddToCartButton.test.tsx"
      provides: "Test scaffold for storefront sold-out disable behavior"
      contains: "describe.*AddToCartButton"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for POS and storefront components that Phase 23 plans verify.

Purpose: Nyquist compliance requires automated test files to exist before execution begins. The billing and super-admin test files already exist from Phase 15/16. Only the POS ProductCard and storefront AddToCartButton component test files are missing.

Output: Two test scaffold files with describe blocks and placeholder tests covering POS-01, POS-02, POS-03 behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-feature-gating-pos-storefront-integration/23-CONTEXT.md
@.planning/phases/23-feature-gating-pos-storefront-integration/23-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/pos/ProductCard.tsx (props):
```typescript
// Props include: hasInventory, product (with stock_quantity, reorder_threshold, product_type)
// showStockBadge logic: hasInventory && product.product_type === 'physical'
// isOutOfStock: stock_quantity <= 0
// isLowStock: stock_quantity <= reorder_threshold
```

From src/components/store/AddToCartButton.tsx (props):
```typescript
// Props include: hasInventory, product (with stock_quantity, product_type)
// isSoldOut logic: hasInventory && product.product_type === 'physical' && stock_quantity <= 0
// Button disabled when isSoldOut, text changes to "Sold Out"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create POS ProductCard and storefront AddToCartButton test scaffolds</name>
  <files>
    src/components/pos/__tests__/ProductCard.test.tsx,
    src/components/store/__tests__/AddToCartButton.test.tsx
  </files>
  <read_first>
    src/components/pos/ProductCard.tsx,
    src/components/store/AddToCartButton.tsx,
    src/actions/inventory/__tests__/adjustStock.test.ts
  </read_first>
  <action>
    **1. Create `src/components/pos/__tests__/ProductCard.test.tsx`:**

    Create the `__tests__` directory and test file. Follow the existing vitest mock pattern from `src/actions/inventory/__tests__/adjustStock.test.ts`. Use `@testing-library/react` for component rendering.

    Test scaffolds to include:

    ```typescript
    describe('ProductCard', () => {
      describe('stock badges (POS-01)', () => {
        it('shows no stock badge when hasInventory is false')
        it('shows "N in stock" badge for in-stock physical product when hasInventory is true')
        it('shows low-stock badge when stock_quantity <= reorder_threshold')
        it('shows "Out of stock" badge when stock_quantity is 0')
        it('shows no stock badge for service products even when hasInventory is true')
      })
      describe('out-of-stock behavior (POS-02)', () => {
        it('renders out-of-stock visual state when stock_quantity is 0 and hasInventory is true')
      })
    })
    ```

    Each test should render the component with mock props and assert on the presence/absence of badge text. Use `screen.getByText` / `screen.queryByText` assertions.

    **2. Create `src/components/store/__tests__/AddToCartButton.test.tsx`:**

    Create the `__tests__` directory and test file.

    Test scaffolds to include:

    ```typescript
    describe('AddToCartButton', () => {
      describe('sold-out behavior (POS-03)', () => {
        it('renders enabled button when hasInventory is false (no stock gating)')
        it('renders enabled button when hasInventory is true and product is in stock')
        it('renders disabled button with "Sold Out" text when hasInventory is true and stock_quantity is 0')
        it('renders enabled button for service products even when stock_quantity is 0')
      })
    })
    ```

    Each test should render the component with mock props and assert on button disabled state and text content.

    **Mock strategy:** Mock `next/navigation` (useRouter, useSearchParams), any cart context providers, and Supabase client as needed. Follow existing project patterns — check how other component tests mock these dependencies.
  </action>
  <verify>
    <automated>npx vitest run src/components/pos/__tests__/ProductCard.test.tsx src/components/store/__tests__/AddToCartButton.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, vitest discovers and runs them, all scaffold tests pass (or are marked `.todo` if implementation details need Phase 23 execution to finalize). Wave 0 Nyquist gap is closed.</done>
</task>

</tasks>

<verification>
1. `ls src/components/pos/__tests__/ProductCard.test.tsx` -- file exists
2. `ls src/components/store/__tests__/AddToCartButton.test.tsx` -- file exists
3. `npx vitest run src/components/pos/__tests__/ src/components/store/__tests__/ --reporter=verbose` -- tests discovered and run
</verification>

<success_criteria>
- Two test scaffold files exist at the specified paths
- Vitest discovers and runs both files without errors
- Test cases cover POS-01 (stock badges), POS-02 (out-of-stock), POS-03 (sold-out disable)
</success_criteria>

<output>
After completion, create `.planning/phases/23-feature-gating-pos-storefront-integration/23-00-SUMMARY.md`
</output>
