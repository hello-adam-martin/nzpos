---
phase: 28-marketing-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/components/LandingFeatures.tsx
  - src/app/(marketing)/components/LandingNZCallout.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-08
  - MKT-09

must_haves:
  truths:
    - "Features section shows all 15 feature cards across 4 groups: Sell In-Store, Sell Online, Manage Your Business, Stay Compliant"
    - "Each feature card has an inline SVG icon, bold title, and muted description"
    - "Features use compact inline layout (no bordered rich cards)"
    - "NZ Callout strip is a full-width navy section with 3 trust badges: GST-Compliant, NZD Pricing, Built in NZ"
  artifacts:
    - path: "src/app/(marketing)/components/LandingFeatures.tsx"
      provides: "Grouped features with 15 cards across 4 categories"
      contains: "Sell In-Store"
    - path: "src/app/(marketing)/components/LandingNZCallout.tsx"
      provides: "Full-width navy callout strip with 3 NZ trust badges"
      contains: "GST-Compliant"
  key_links:
    - from: "src/app/(marketing)/components/LandingFeatures.tsx"
      to: "section anchor"
      via: "id attribute on section element"
      pattern: 'id="features"'
    - from: "src/app/(marketing)/components/LandingNZCallout.tsx"
      to: "page.tsx"
      via: "default export consumed by page.tsx in plan 03"
      pattern: "export default function LandingNZCallout"
---

<objective>
Rewrite LandingFeatures from a flat 4-card grid to grouped categories with 15 compact feature cards, and create the new LandingNZCallout strip component.

Purpose: The current features section only shows 4 capabilities. The platform has 15+ shipped features that must be showcased to accurately represent the product. The NZ callout strip highlights NZ-specific differentiators (GST compliance, NZD pricing, local origin) as a trust signal between features and pricing.
Output: Rewritten LandingFeatures.tsx, new LandingNZCallout.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-marketing-landing-page/28-CONTEXT.md
@.planning/phases/28-marketing-landing-page/28-RESEARCH.md
@.planning/phases/28-marketing-landing-page/28-UI-SPEC.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite LandingFeatures with grouped categories and 15 compact cards</name>
  <files>src/app/(marketing)/components/LandingFeatures.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingFeatures.tsx
    - DESIGN.md
    - .planning/phases/28-marketing-landing-page/28-UI-SPEC.md (Pattern A, Feature Groups and Content, Typography, Spacing)
  </read_first>
  <action>
  Full rewrite of LandingFeatures.tsx. Replace the flat 4-card rich grid with grouped categories and compact inline icon+text cards.

  **Section wrapper:** Keep `section` element with `bg-[var(--color-bg)]`. Add `id="features"` to the section element for nav anchor link support.

  **Inner container:** `max-w-[1200px] mx-auto px-[var(--space-md)] md:px-[var(--space-lg)] py-[var(--space-3xl)]`

  **Section heading:** H2 with text "Everything your store needs, out of the box" (per UI-SPEC). Classes: `font-sans text-xl font-bold text-[var(--color-text)] text-center leading-[1.3]`

  **Data structure:** Define a `featureGroups` array with 4 groups. Each group has `heading` (string) and `features` array of `{ title, description, icon }`.

  **Group 1: Sell In-Store** (4 features)
  - POS Checkout: "Tap to sell from any product, apply discounts, and confirm EFTPOS payments in seconds." Icon: tablet shape (rect x=4 y=2 w=16 h=20 rx=2 + line x1=12 y1=18 x2=12.01 y2=18 — reuse existing)
  - Barcode Scanning: "Scan product barcodes to add items instantly — no hunting through a product list." Icon: barcode lines (path d="M2 4h2v16H2M6 4h1v16H6M10 4h1v16h-1M14 4h1v16h-1M18 4h2v16h-2M22 4h1v16h-1" — approximate barcode)
  - Receipts: "Email GST-compliant receipts on every sale. IRD-ready, automatically." Icon: receipt (rect x=4 y=2 w=16 h=20 rx=2 + line x1=8 y1=8 x2=16 y2=8 + line x1=8 y1=12 x2=16 y2=12 + line x1=8 y1=16 x2=12 y2=16 — reuse existing)
  - Partial Refunds: "Issue refunds on individual line items, not just whole orders." Icon: undo arrow (polyline points="1 4 1 10 7 10" + path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10" — half of the existing sync icon)

  **Group 2: Sell Online** (4 features)
  - Online Storefront: "Your store is live online from day one. No extra setup, no extra charge." Icon: shopping bag (reuse existing — path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z" + line x1=3 y1=6 x2=21 y2=6 + path d="M16 10a4 4 0 0 1-8 0")
  - Click and Collect: "Customers order online and pick up in-store. You get the notification." Icon: map pin (path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" + circle cx=12 cy=10 r=3)
  - Promo Codes: "Run a sale or reward loyal customers with discount codes." Icon: tag (path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z" + line x1=7 y1=7 x2=7.01 y2=7)
  - Customer Accounts: "Customers can track their orders and save their details for next time." Icon: users (path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" + circle cx=9 cy=7 r=4 + path d="M23 21v-2a4 4 0 0 0-3-3.87" + path d="M16 3.13a4 4 0 0 1 0 7.75")

  **Group 3: Manage Your Business** (4 features)
  - Staff Management: "Add staff, set POS-only or admin roles, and control who can do what." Icon: user-cog (circle cx=12 cy=7 r=4 + path d="M4 21v-2a4 4 0 0 1 4-4h4" + circle cx=19 cy=18 r=2 + path d="M19 14v1" + path d="M19 21v1" — simplified user+gear)
  - Reporting: "Daily sales summaries, top products, and GST totals — all in one place." Icon: bar chart (line x1=12 y1=20 x2=12 y2=10 + line x1=18 y1=20 x2=18 y2=4 + line x1=6 y1=20 x2=6 y2=16)
  - Unified Inventory: "One inventory powers both in-store and online. Sell a product in-store and it's gone online." Icon: sync arrows (reuse existing — polyline points="23 4 23 10 17 10" + polyline points="1 20 1 14 7 14" + path d="M3.51 9a9 9 0 0 1 14.85-3.36L23 10M1 14l4.64 4.36A9 9 0 0 0 20.49 15")
  - Multi-tenant Ready: "Each store is fully isolated. Your data is yours." Icon: shield (path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z")

  **Group 4: Stay Compliant** (3 features)
  - GST Done Right: "15% GST calculated per line on discounted amounts. Compliant with IRD requirements." Icon: calculator (rect x=4 y=2 w=16 h=20 rx=2 + line x1=8 y1=6 x2=16 y2=6 + line x1=8 y1=10 x2=10 y2=10 + line x1=14 y1=10 x2=16 y2=10 + line x1=8 y1=14 x2=10 y2=14 + line x1=14 y1=14 x2=16 y2=14 + line x1=8 y1=18 x2=16 y2=18)
  - NZD Pricing: "Everything in New Zealand dollars. No currency conversion, no surprises." Icon: dollar sign (line x1=12 y1=1 x2=12 y2=23 + path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6")
  - GST Receipts: "Every receipt shows GST breakdowns that satisfy your accountant and the IRD." Icon: file-text (path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" + polyline points="14 2 14 8 20 8" + line x1=16 y1=13 x2=8 y2=13 + line x1=16 y1=17 x2=8 y2=17 + polyline points="10 9 9 9 8 9")

  **Rendering pattern per group:**
  ```tsx
  {featureGroups.map((group, i) => (
    <div key={group.heading} className={i > 0 ? 'mt-[var(--space-2xl)]' : 'mt-[var(--space-xl)]'}>
      <h3 className="font-sans text-sm font-bold text-[var(--color-text-muted)] uppercase tracking-wide mb-[var(--space-md)]">
        {group.heading}
      </h3>
      <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-[var(--space-md)]">
        {group.features.map((f) => (
          <div key={f.title} className="flex gap-[var(--space-sm)] items-start">
            <div className="text-[var(--color-navy)] shrink-0">{f.icon}</div>
            <div>
              <p className="font-sans text-sm font-bold text-[var(--color-text)]">{f.title}</p>
              <p className="font-sans text-sm text-[var(--color-text-muted)]">{f.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  ))}
  ```

  **CRITICAL:** No bordered cards (no bg-white, no border, no shadow, no p-[var(--space-xl)] on feature items). Compact inline icon+text only (per D-02, UI-SPEC Pattern A).

  **All SVG icons must follow the convention:** xmlns, width=24, height=24, viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth="2", strokeLinecap="round", strokeLinejoin="round", aria-hidden="true".
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "Sell In-Store\|Sell Online\|Manage Your Business\|Stay Compliant" src/app/\(marketing\)/components/LandingFeatures.tsx && grep -c 'id="features"' src/app/\(marketing\)/components/LandingFeatures.tsx && grep -c "POS Checkout\|Barcode Scanning\|Receipts\|Partial Refunds\|Online Storefront\|Click and Collect\|Promo Codes\|Customer Accounts\|Staff Management\|Reporting\|Unified Inventory\|Multi-tenant Ready\|GST Done Right\|NZD Pricing\|GST Receipts" src/app/\(marketing\)/components/LandingFeatures.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LandingFeatures.tsx contains id="features" on the section element
    - LandingFeatures.tsx contains all 4 group headings: "Sell In-Store", "Sell Online", "Manage Your Business", "Stay Compliant"
    - LandingFeatures.tsx contains all 15 feature titles (POS Checkout, Barcode Scanning, Receipts, Partial Refunds, Online Storefront, Click and Collect, Promo Codes, Customer Accounts, Staff Management, Reporting, Unified Inventory, Multi-tenant Ready, GST Done Right, NZD Pricing, GST Receipts)
    - LandingFeatures.tsx contains section heading "Everything your store needs, out of the box"
    - LandingFeatures.tsx does NOT contain "bg-white border border-" on feature cards (compact style, not rich cards)
    - LandingFeatures.tsx uses grid-cols-1 sm:grid-cols-2 md:grid-cols-3 for feature grids
    - All icons use aria-hidden="true"
    - No hardcoded hex colors — only var(--color-*) tokens
  </acceptance_criteria>
  <done>Features section shows 15 features across 4 grouped categories with compact inline layout and proper anchor ID.</done>
</task>

<task type="auto">
  <name>Task 2: Create LandingNZCallout strip component</name>
  <files>src/app/(marketing)/components/LandingNZCallout.tsx</files>
  <read_first>
    - DESIGN.md
    - src/app/(marketing)/components/LandingCTA.tsx (reference for full-width navy section pattern)
    - .planning/phases/28-marketing-landing-page/28-UI-SPEC.md (Pattern B, NZ Callout Strip content)
  </read_first>
  <action>
  Create new file `src/app/(marketing)/components/LandingNZCallout.tsx`. This is a full-width navy banner between Features and Pricing (per D-11).

  **Structure:**
  ```tsx
  export default function LandingNZCallout() {
    return (
      <section className="bg-[var(--color-navy)]">
        <div className="max-w-[1200px] mx-auto px-[var(--space-md)] md:px-[var(--space-lg)] py-[var(--space-2xl)]">
          <div className="flex flex-col md:flex-row items-center justify-center gap-[var(--space-xl)] text-center">
            {/* Item 1: GST-Compliant */}
            {/* Item 2: NZD Pricing */}
            {/* Item 3: Built in NZ */}
          </div>
        </div>
      </section>
    )
  }
  ```

  **Each callout item structure:**
  ```tsx
  <div className="flex flex-col items-center gap-[var(--space-sm)]">
    <svg ...amber icon... aria-hidden="true">...</svg>
    <p className="font-sans text-base font-bold text-white">{label}</p>
    <p className="font-sans text-sm text-white/70">{sublabel}</p>
  </div>
  ```

  **Three items with content:**
  1. Label: "GST-Compliant", Sub: "Per-line calculation, IRD-ready". Icon: checkmark in circle (circle cx=12 cy=12 r=10 + polyline points="9 12 12 15 16 9" — check-circle style)
  2. Label: "NZD Pricing", Sub: "All prices in New Zealand dollars". Icon: dollar sign (line x1=12 y1=1 x2=12 y2=23 + path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6")
  3. Label: "Built in NZ", Sub: "Made for Kiwi retailers". Icon: map pin (path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" + circle cx=12 cy=10 r=3)

  **Icon color:** All 3 icons use `className="text-[var(--color-amber)]"` (amber on navy per UI-SPEC). SVGs follow the standard convention: width=24, height=24, viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth="2", strokeLinecap="round", strokeLinejoin="round", aria-hidden="true".

  **CRITICAL:** The section element has NO max-width. Only the inner div has max-w-[1200px]. The navy background must span full viewport width.

  No imports needed (no Link, no external dependencies). Pure stateless component.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && test -f src/app/\(marketing\)/components/LandingNZCallout.tsx && grep -c "GST-Compliant\|NZD Pricing\|Built in NZ" src/app/\(marketing\)/components/LandingNZCallout.tsx && grep -c "export default function LandingNZCallout" src/app/\(marketing\)/components/LandingNZCallout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/(marketing)/components/LandingNZCallout.tsx exists
    - Contains "export default function LandingNZCallout"
    - Contains all 3 labels: "GST-Compliant", "NZD Pricing", "Built in NZ"
    - Contains all 3 sub-labels: "Per-line calculation, IRD-ready", "All prices in New Zealand dollars", "Made for Kiwi retailers"
    - Section uses bg-[var(--color-navy)] (not amber)
    - Inner container has max-w-[1200px] but section element does NOT have max-w
    - Icons use text-[var(--color-amber)] class
    - Responsive: flex-col on mobile, md:flex-row on desktop
    - No hardcoded hex colors
  </acceptance_criteria>
  <done>NZ callout strip component exists as a full-width navy banner with 3 amber-icon trust badges, ready to be wired into page.tsx.</done>
</task>

</tasks>

<verification>
- `npm test` passes (existing test suite unaffected)
- LandingFeatures has 15 feature titles and 4 group headings
- LandingNZCallout file exists with correct content
- No hardcoded hex colors in new/modified files
- All SVGs use aria-hidden="true"
</verification>

<success_criteria>
- Features section displays all 15 shipped capabilities in 4 groups with compact inline layout
- NZ Callout strip has 3 trust badges on a navy background
- Both components follow design system tokens
- Section anchor id="features" exists on Features section
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-marketing-landing-page/28-02-SUMMARY.md`
</output>
