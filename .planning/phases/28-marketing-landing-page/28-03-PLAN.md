---
phase: 28-marketing-landing-page
plan: 03
type: execute
wave: 2
depends_on:
  - 28-02
files_modified:
  - src/app/(marketing)/components/LandingPricing.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - MKT-03
  - MKT-04
  - MKT-05
  - MKT-08
  - MKT-09

must_haves:
  truths:
    - "Pricing section lists 3 add-on cards: Xero $9/mo, Email Notifications $5/mo, Inventory Management $9/mo"
    - "Free tier checklist has exactly 6 items: POS checkout, Online storefront, GST-compliant receipts, Staff management, Customer accounts, Reporting"
    - "Free tier does NOT mention inventory"
    - "Add-on grid is 3 columns on desktop (md:grid-cols-3), not 2"
    - "Page.tsx renders LandingNZCallout between LandingFeatures and LandingPricing"
    - "page.tsx metadata description updated to match new confident tone"
  artifacts:
    - path: "src/app/(marketing)/components/LandingPricing.tsx"
      provides: "Corrected free tier + 3 add-on cards"
      contains: "Inventory Management"
    - path: "src/app/page.tsx"
      provides: "Correct component order with NZCallout inserted"
      contains: "LandingNZCallout"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/(marketing)/components/LandingNZCallout.tsx"
      via: "import and render between Features and Pricing"
      pattern: "import LandingNZCallout"
    - from: "src/app/(marketing)/components/LandingPricing.tsx"
      to: "section anchor"
      via: "id attribute on section element"
      pattern: 'id="pricing"'
---

<objective>
Fix the pricing section (correct free tier list, add Inventory Management as third add-on), wire LandingNZCallout into page.tsx, and update page metadata.

Purpose: The current pricing section incorrectly lists "Inventory management" in the free tier (it's a paid add-on) and only shows 2 of 3 add-ons. The page composition must include the new NZ Callout strip in the correct position. Metadata must match the new confident messaging tone.
Output: Fixed LandingPricing.tsx, updated page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-marketing-landing-page/28-CONTEXT.md
@.planning/phases/28-marketing-landing-page/28-RESEARCH.md
@.planning/phases/28-marketing-landing-page/28-UI-SPEC.md
@DESIGN.md

@.planning/phases/28-marketing-landing-page/28-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LandingPricing — correct free tier and add Inventory Management add-on</name>
  <files>src/app/(marketing)/components/LandingPricing.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingPricing.tsx
    - DESIGN.md
    - .planning/phases/28-marketing-landing-page/28-UI-SPEC.md (Pattern C, Copywriting Contract — Pricing Section)
  </read_first>
  <action>
  Rewrite LandingPricing.tsx with these specific changes:

  **1. Add section anchor:** Add `id="pricing"` to the section element.

  **2. Update sub-copy:** Change "Free to start. Pay only for what you use." to "Free to start. Add only what you need." (per UI-SPEC).

  **3. Fix free tier checklist (CRITICAL — per D-05, MKT-04):**
  Replace the current array `['POS checkout', 'Online storefront', 'Inventory management', 'GST-compliant receipts']` with exactly:
  ```tsx
  ['POS checkout', 'Online storefront', 'GST-compliant receipts', 'Staff management', 'Customer accounts', 'Reporting']
  ```
  "Inventory management" must NOT appear in this list. The list now has 6 items instead of 4.

  **4. Change free tier card max-width:** Change `max-w-md` to `max-w-lg` on the free tier card wrapper to accommodate the longer checklist.

  **5. Change add-on grid from 2 columns to 3 columns:**
  Replace `grid grid-cols-1 md:grid-cols-2 gap-[var(--space-lg)] max-w-lg mx-auto mt-[var(--space-lg)]` with `grid grid-cols-1 md:grid-cols-3 gap-[var(--space-lg)] mt-[var(--space-lg)]` — remove the max-w-lg and mx-auto from the add-on grid wrapper.

  **6. Add Inventory Management as third add-on card:**
  After the Email Notifications card, add a new card with identical structure:
  ```tsx
  <div className="bg-white border border-[var(--color-border)] rounded-lg p-[var(--space-xl)]">
    <h3 className="font-sans text-sm font-bold text-[var(--color-text)]">
      Inventory Management
    </h3>
    <p className="font-sans text-sm font-bold text-[var(--color-amber)] mt-[var(--space-xs)]">
      $9/month NZD
    </p>
    <ul className="mt-[var(--space-md)] space-y-[var(--space-xs)]">
      {[
        'Live stock levels across in-store and online',
        'Low-stock alerts',
        'Inventory reports',
      ].map((item) => (
        <li key={item} className="flex items-start gap-[var(--space-xs)] font-sans text-sm text-[var(--color-text-muted)]">
          <span className="mt-1 shrink-0 text-[var(--color-text-muted)]">·</span>
          {item}
        </li>
      ))}
    </ul>
  </div>
  ```

  **7. No CTA buttons on add-on cards (per D-06).** The existing cards already have no buttons — keep it that way.

  **8. Footer note stays:** "All prices include GST. No hidden fees."

  All other structure (section bg, container, heading, free tier card border-2 style, checkmark SVG) stays the same.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "Inventory Management" src/app/\(marketing\)/components/LandingPricing.tsx && grep -c "md:grid-cols-3" src/app/\(marketing\)/components/LandingPricing.tsx && grep -c 'id="pricing"' src/app/\(marketing\)/components/LandingPricing.tsx && grep "POS checkout\|Online storefront\|GST-compliant receipts\|Staff management\|Customer accounts\|Reporting" src/app/\(marketing\)/components/LandingPricing.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - LandingPricing.tsx contains id="pricing" on the section element
    - LandingPricing.tsx free tier list contains exactly: "POS checkout", "Online storefront", "GST-compliant receipts", "Staff management", "Customer accounts", "Reporting"
    - LandingPricing.tsx free tier list does NOT contain "Inventory management" (case-insensitive check: no "inventory" in the free tier array)
    - LandingPricing.tsx contains "Inventory Management" as an add-on card title (H3)
    - LandingPricing.tsx contains "$9/month NZD" for Inventory Management
    - LandingPricing.tsx contains "Low-stock alerts" and "Inventory reports" in the Inventory Management benefit list
    - Add-on grid uses md:grid-cols-3 (not md:grid-cols-2)
    - Add-on grid wrapper does NOT have max-w-lg
    - No hardcoded hex colors
    - Sub-copy reads "Add only what you need" not "Pay only for what you use"
  </acceptance_criteria>
  <done>Pricing section accurately shows 6 free tier features (no inventory), 3 add-on cards in a 3-column grid, with correct pricing and benefits for all add-ons.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LandingNZCallout into page.tsx and update metadata</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx
    - src/app/(marketing)/components/LandingNZCallout.tsx (verify it exists from plan 02)
    - .planning/phases/28-marketing-landing-page/28-UI-SPEC.md (Copywriting Contract — Hero section for metadata)
  </read_first>
  <action>
  Two changes to src/app/page.tsx:

  **1. Add LandingNZCallout import and render (per D-10):**

  Add import at top with other Landing imports:
  ```tsx
  import LandingNZCallout from './(marketing)/components/LandingNZCallout'
  ```

  Insert `<LandingNZCallout />` between `<LandingFeatures />` and `<LandingPricing />` in the JSX. The final order must be:
  ```tsx
  <LandingNav />
  <LandingHero />
  <LandingFeatures />
  <LandingNZCallout />
  <LandingPricing />
  <LandingCTA />
  <LandingFooter />
  ```

  **2. Update page metadata to match new confident tone:**

  Change the metadata object:
  ```tsx
  export const metadata: Metadata = {
    title: 'NZPOS — The retail platform built for Kiwi businesses',
    description:
      'Sell in-store and online from one dashboard. GST handled correctly on every transaction. Free to start.',
  }
  ```

  The title changes from "The POS built for Kiwi retailers" to "The retail platform built for Kiwi businesses" (matching the new hero H1). The description changes from "Ring up sales in-store" to "Sell in-store and online from one dashboard" (matching the new hero sub-copy).

  **3. Keep `export const dynamic = 'force-static'` — do NOT remove it.**
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "LandingNZCallout" src/app/page.tsx && grep -c "force-static" src/app/page.tsx && grep -c "The retail platform built for Kiwi businesses" src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports LandingNZCallout from './(marketing)/components/LandingNZCallout'
    - page.tsx renders LandingNZCallout between LandingFeatures and LandingPricing (verify ordering in JSX)
    - page.tsx contains `export const dynamic = 'force-static'`
    - page.tsx metadata title contains "The retail platform built for Kiwi businesses"
    - page.tsx metadata description contains "Sell in-store and online from one dashboard"
    - page.tsx does NOT contain "Ring up sales" or "The POS built for Kiwi retailers" in metadata
  </acceptance_criteria>
  <done>Page.tsx renders all 7 components in the correct order with updated metadata matching the new brand tone.</done>
</task>

</tasks>

<verification>
- `npm test` passes (existing test suite unaffected)
- `npm run build` succeeds (static page generation works with new component)
- Free tier list has 6 items and does NOT include "inventory"
- 3 add-on cards render in a 3-column grid
- LandingNZCallout appears between Features and Pricing in page.tsx
- No hardcoded hex colors in modified files
</verification>

<success_criteria>
- Pricing section lists 3 add-ons with correct prices ($9, $5, $9)
- Free tier accurately reflects free features (no inventory)
- Each add-on has a benefit list
- Page composition follows D-10 order: Hero, Features, NZCallout, Pricing, CTA, Footer
- Metadata matches new hero copy
- Build succeeds with force-static
</success_criteria>

<output>
After completion, create `.planning/phases/28-marketing-landing-page/28-03-SUMMARY.md`
</output>
