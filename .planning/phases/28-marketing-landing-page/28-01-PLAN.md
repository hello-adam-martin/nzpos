---
phase: 28-marketing-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/components/LandingHero.tsx
  - src/app/(marketing)/components/LandingCTA.tsx
  - src/app/(marketing)/components/LandingNav.tsx
autonomous: true
requirements:
  - MKT-06
  - MKT-07
  - MKT-08
  - MKT-09

must_haves:
  truths:
    - "Hero headline says 'The retail platform built for Kiwi businesses.' not 'The POS built for Kiwi retailers.'"
    - "Hero sub-copy mentions 'Sell in-store and online from one dashboard' not 'Ring up sales in-store'"
    - "CTA heading says 'Your shop, running smarter.' not 'Ready to run a better shop?'"
    - "CTA supporting line says 'Set up in under 5 minutes. No credit card needed.' not 'Set up in under 5 minutes.'"
    - "Nav has anchor links to #features and #pricing on desktop"
  artifacts:
    - path: "src/app/(marketing)/components/LandingHero.tsx"
      provides: "Updated hero copy with confident SaaS tone"
      contains: "The retail platform built for Kiwi businesses"
    - path: "src/app/(marketing)/components/LandingCTA.tsx"
      provides: "Updated CTA copy with confident SaaS tone"
      contains: "Your shop, running smarter"
    - path: "src/app/(marketing)/components/LandingNav.tsx"
      provides: "Anchor links to features and pricing sections"
      contains: "#features"
  key_links:
    - from: "src/app/(marketing)/components/LandingNav.tsx"
      to: "LandingFeatures section"
      via: "anchor link href=#features"
      pattern: 'href="#features"'
    - from: "src/app/(marketing)/components/LandingNav.tsx"
      to: "LandingPricing section"
      via: "anchor link href=#pricing"
      pattern: 'href="#pricing"'
---

<objective>
Rewrite hero, CTA, and nav copy to reflect a mature SaaS platform with confident Kiwi tone, and add nav anchor links.

Purpose: The current hero says "The POS built for Kiwi retailers" and "Ring up sales in-store" -- MVP-era messaging. The platform now has 27 shipped phases of features. Copy must match the product's maturity. Nav needs anchor links for the longer page.
Output: Updated LandingHero.tsx, LandingCTA.tsx, LandingNav.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-marketing-landing-page/28-CONTEXT.md
@.planning/phases/28-marketing-landing-page/28-UI-SPEC.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite LandingHero copy and LandingCTA copy</name>
  <files>src/app/(marketing)/components/LandingHero.tsx, src/app/(marketing)/components/LandingCTA.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingHero.tsx
    - src/app/(marketing)/components/LandingCTA.tsx
    - DESIGN.md
    - .planning/phases/28-marketing-landing-page/28-UI-SPEC.md (Copywriting Contract section)
  </read_first>
  <action>
  In LandingHero.tsx — copy changes only, structure stays identical (per D-08, iPad mockup stays):

  1. Change the H1 text from "The POS built for Kiwi retailers." to "The retail platform built for Kiwi businesses." (per D-07, MKT-06)
  2. Change the sub-copy paragraph from "Ring up sales in-store, take orders online, and get GST handled correctly — all from one dashboard." to "Sell in-store and online from one dashboard. GST handled correctly on every transaction." (per D-09)
  3. The CTA button text "Get started free", aria-label, and "No credit card required. NZD pricing." line stay unchanged.
  4. All class names, layout, iPad mockup — zero changes.

  In LandingCTA.tsx — copy changes only:

  1. Change the H2 text from "Ready to run a better shop?" to "Your shop, running smarter." (per D-13, MKT-07)
  2. Change the supporting line from "Set up in under 5 minutes." to "Set up in under 5 minutes. No credit card needed." (per UI-SPEC Copywriting Contract)
  3. The CTA button text "Get started free" and aria-label stay unchanged.
  4. All class names — zero changes.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "The retail platform built for Kiwi businesses" src/app/\(marketing\)/components/LandingHero.tsx && grep -c "Your shop, running smarter" src/app/\(marketing\)/components/LandingCTA.tsx && grep -c "No credit card needed" src/app/\(marketing\)/components/LandingCTA.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LandingHero.tsx contains "The retail platform built for Kiwi businesses."
    - LandingHero.tsx contains "Sell in-store and online from one dashboard. GST handled correctly on every transaction."
    - LandingHero.tsx does NOT contain "Ring up sales"
    - LandingHero.tsx does NOT contain "The POS built for Kiwi retailers"
    - LandingHero.tsx still contains "hidden md:flex" (iPad mockup preserved)
    - LandingCTA.tsx contains "Your shop, running smarter."
    - LandingCTA.tsx contains "No credit card needed"
    - LandingCTA.tsx does NOT contain "Ready to run a better shop?"
    - Both files use only var(--color-*) and var(--space-*) tokens — no hardcoded hex values
  </acceptance_criteria>
  <done>Hero reflects mature SaaS platform messaging. CTA matches confident tone. No structural changes to either component.</done>
</task>

<task type="auto">
  <name>Task 2: Add anchor links to LandingNav</name>
  <files>src/app/(marketing)/components/LandingNav.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingNav.tsx
    - .planning/phases/28-marketing-landing-page/28-UI-SPEC.md (Nav section under Copywriting Contract, and Component Inventory)
  </read_first>
  <action>
  Add "Features" and "Pricing" anchor links to LandingNav.tsx. These go between the wordmark and the auth buttons in the desktop nav, and also in the mobile overlay.

  Desktop nav (the `nav` element with `hidden md:flex`):
  1. Before the "Sign in" Link, add two plain anchor links:
     ```tsx
     <a href="#features" className="font-sans text-sm text-white/70 hover:text-white transition-colors duration-150">Features</a>
     <a href="#pricing" className="font-sans text-sm text-white/70 hover:text-white transition-colors duration-150">Pricing</a>
     ```
  2. The existing gap-[var(--space-sm)] on the nav handles spacing.

  Mobile overlay (inside the details > div):
  1. Before the "Sign in" Link, add two anchor links:
     ```tsx
     <a href="#features" className="font-sans text-base text-white/70 hover:text-white transition-colors duration-150">Features</a>
     <a href="#pricing" className="font-sans text-base text-white/70 hover:text-white transition-colors duration-150">Pricing</a>
     ```

  No other changes to LandingNav. Keep all existing structure, wordmark, hamburger icon, auth buttons.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c '#features' src/app/\(marketing\)/components/LandingNav.tsx && grep -c '#pricing' src/app/\(marketing\)/components/LandingNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LandingNav.tsx contains href="#features" (at least 2 occurrences — desktop + mobile)
    - LandingNav.tsx contains href="#pricing" (at least 2 occurrences — desktop + mobile)
    - LandingNav.tsx contains text "Features" and "Pricing"
    - LandingNav.tsx still contains "NZPOS" wordmark
    - LandingNav.tsx still contains href="/login" and href="/signup"
    - LandingNav.tsx still contains the details/summary mobile hamburger
  </acceptance_criteria>
  <done>Nav has Features and Pricing anchor links on both desktop and mobile. Existing structure preserved.</done>
</task>

</tasks>

<verification>
- `npm test` passes (existing test suite unaffected)
- grep confirms old MVP copy is gone from Hero and CTA
- grep confirms anchor links exist in Nav
- No hardcoded hex colors in modified files
</verification>

<success_criteria>
- Hero H1 reads "The retail platform built for Kiwi businesses."
- CTA H2 reads "Your shop, running smarter."
- Nav has #features and #pricing anchor links
- All design tokens used correctly (no hardcoded values)
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/28-marketing-landing-page/28-01-SUMMARY.md`
</output>
