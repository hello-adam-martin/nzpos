---
phase: 30-admin-ui-super-admin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/super-admin/page.tsx
  - src/app/super-admin/analytics/page.tsx
  - src/app/super-admin/tenants/page.tsx
  - src/app/super-admin/tenants/[id]/page.tsx
autonomous: true
requirements: [ADMIN-03, TEST-01]

must_haves:
  truths:
    - "Super admin dashboard adoption cards show only Xero, Domain, and Inventory — no Email Adoption card"
    - "Super admin tenant detail add-ons section shows only Xero, Custom Domain, and Inventory rows"
    - "Super admin analytics ADDON_DISPLAY_NAMES does not contain email_notifications"
    - "Super admin tenants list query does not select has_email_notifications"
  artifacts:
    - path: "src/app/super-admin/page.tsx"
      provides: "Platform overview without email adoption card"
    - path: "src/app/super-admin/analytics/page.tsx"
      provides: "Analytics without email_notifications display name"
    - path: "src/app/super-admin/tenants/page.tsx"
      provides: "Tenants list without email_notifications in select"
    - path: "src/app/super-admin/tenants/[id]/page.tsx"
      provides: "Tenant detail without email_notifications in select"
  key_links:
    - from: "src/app/super-admin/tenants/[id]/page.tsx"
      to: "src/config/addons.ts"
      via: "ADDONS.map() drives PlanOverrideRow rendering — only xero, custom_domain, inventory"
      pattern: "ADDONS\\.map"
---

<objective>
Remove all email_notifications references from super admin pages.

Purpose: The super admin dashboard, analytics, tenants list, and tenant detail pages still reference email_notifications in queries, adoption rate calculations, display name maps, and select statements. Since email is now always-on and free, these references are dead code that could confuse super admins (e.g., showing "Email Adoption: 100%" as an add-on metric is misleading).

Output: Clean super admin pages with no email_notifications references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-admin-ui-super-admin/30-CONTEXT.md

<interfaces>
<!-- From src/config/addons.ts (source of truth, already cleaned in Phase 29): -->
```typescript
export type SubscriptionFeature = 'xero' | 'custom_domain' | 'inventory'

export const ADDONS = [
  { feature: 'xero', name: 'Xero Accounting', ... },
  { feature: 'custom_domain', name: 'Custom Domain', ... },
  { feature: 'inventory', name: 'Inventory Management', ... },
] as const
```

<!-- PlanOverrideRow is driven by ADDONS.map() in tenant detail — already shows only 3 add-ons -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove email_notifications from super admin dashboard and analytics</name>
  <files>
    src/app/super-admin/page.tsx
    src/app/super-admin/analytics/page.tsx
  </files>
  <read_first>
    src/app/super-admin/page.tsx
    src/app/super-admin/analytics/page.tsx
    src/config/addons.ts
  </read_first>
  <action>
**src/app/super-admin/page.tsx** (per D-03, ADMIN-03):
- Line 53: Change the `.select()` from `'has_xero, has_email_notifications, has_custom_domain, has_inventory'` to `'has_xero, has_custom_domain, has_inventory'`.
- Lines 66-68: Remove the `email_notifications` property from the `adoptionRates` object. Remove these 3 lines:
  ```
  email_notifications: Math.round(
    (plans.filter((p) => p.has_email_notifications).length / totalPlans) * 100
  ),
  ```
- Lines 99-103: Remove the entire "Email Adoption" `<DashboardHeroCard>` block:
  ```
  <DashboardHeroCard
    label="Email Adoption"
    value={adoptionRates.email_notifications + '%'}
    subLabel={adoptionRates.email_notifications + '% of tenants'}
  />
  ```
  This leaves 3 adoption cards: Xero, Domain, Inventory. Update the grid from `md:grid-cols-4` to `md:grid-cols-3` on the adoption cards grid (around line 93) since there are now 3 cards instead of 4.

**src/app/super-admin/analytics/page.tsx** (per D-03):
- Lines 7-12: Remove `email_notifications: 'Email Notifications',` from `ADDON_DISPLAY_NAMES`. The remaining entries are: `xero: 'Xero'`, `inventory: 'Inventory'`, `custom_domain: 'Custom Domain'`.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "email_notifications" src/app/super-admin/page.tsx src/app/super-admin/analytics/page.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "email_notifications" in both files returns 0 matches each
    - src/app/super-admin/page.tsx select string is `'has_xero, has_custom_domain, has_inventory'`
    - src/app/super-admin/page.tsx does NOT contain `Email Adoption` DashboardHeroCard
    - src/app/super-admin/page.tsx adoption cards grid uses `md:grid-cols-3`
    - src/app/super-admin/analytics/page.tsx ADDON_DISPLAY_NAMES has exactly 3 entries (xero, inventory, custom_domain)
  </acceptance_criteria>
  <done>Super admin dashboard shows 3 adoption cards (Xero, Domain, Inventory) with no email card. Analytics page display names exclude email_notifications.</done>
</task>

<task type="auto">
  <name>Task 2: Remove email_notifications from super admin tenant pages</name>
  <files>
    src/app/super-admin/tenants/page.tsx
    src/app/super-admin/tenants/[id]/page.tsx
  </files>
  <read_first>
    src/app/super-admin/tenants/page.tsx
    src/app/super-admin/tenants/[id]/page.tsx
    src/config/addons.ts
  </read_first>
  <action>
**src/app/super-admin/tenants/page.tsx** (per D-03):
- Line 23: Change the `.select()` string from `'id, name, slug, is_active, created_at, store_plans(has_xero, has_email_notifications, has_custom_domain)'` to `'id, name, slug, is_active, created_at, store_plans(has_xero, has_custom_domain, has_inventory)'`. Note: also add `has_inventory` to the select since it is a valid add-on now.

**src/app/super-admin/tenants/[id]/page.tsx** (per D-03):
- Lines 34-35: Change the `.select()` string from `'has_xero, has_email_notifications, has_custom_domain, has_inventory, has_xero_manual_override, has_email_notifications_manual_override, has_custom_domain_manual_override, has_inventory_manual_override'` to `'has_xero, has_custom_domain, has_inventory, has_xero_manual_override, has_custom_domain_manual_override, has_inventory_manual_override'`. Remove both `has_email_notifications` and `has_email_notifications_manual_override`.

The tenant detail "Add-ons" card already renders from `ADDONS.map()` which only has xero/custom_domain/inventory, so the PlanOverrideRow section needs no changes. The select query cleanup prevents fetching unused columns.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "email_notifications" src/app/super-admin/tenants/page.tsx src/app/super-admin/tenants/\[id\]/page.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "email_notifications" in both tenant page files returns 0 matches each
    - tenants/page.tsx select string contains `store_plans(has_xero, has_custom_domain, has_inventory)`
    - tenants/[id]/page.tsx select string contains `has_xero, has_custom_domain, has_inventory, has_xero_manual_override, has_custom_domain_manual_override, has_inventory_manual_override`
    - tenants/[id]/page.tsx select string does NOT contain `has_email_notifications` or `has_email_notifications_manual_override`
  </acceptance_criteria>
  <done>Super admin tenant pages select only the 3 active add-on columns. No email_notifications references remain in any super admin file.</done>
</task>

</tasks>

<verification>
- `grep -r "email_notifications" src/app/super-admin/` returns zero matches
- `npx tsc --noEmit --pretty 2>&1 | head -20` shows no type errors
- The super admin dashboard renders 3 adoption cards, not 4
</verification>

<success_criteria>
1. Super admin dashboard shows exactly 3 add-on adoption cards (Xero, Domain, Inventory)
2. Super admin analytics ADDON_DISPLAY_NAMES has 3 entries, no email_notifications
3. Tenant list query selects has_xero, has_custom_domain, has_inventory from store_plans
4. Tenant detail query does not select has_email_notifications or has_email_notifications_manual_override
5. No file under src/app/super-admin/ contains the string "email_notifications"
</success_criteria>

<output>
After completion, create `.planning/phases/30-admin-ui-super-admin/30-02-SUMMARY.md`
</output>
