---
phase: 14-store-setup-wizard-marketing
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/app/admin/setup/page.tsx
  - src/components/wizard/WizardLayout.tsx
  - src/components/wizard/WizardStepIndicator.tsx
  - src/components/wizard/WizardStepCard.tsx
  - src/components/wizard/StoreNameStep.tsx
  - src/components/wizard/LogoBrandStep.tsx
  - src/components/wizard/FirstProductStep.tsx
  - src/components/wizard/LogoUploadZone.tsx
  - src/components/wizard/BrandColorPicker.tsx
  - src/components/wizard/WizardCompletion.tsx
  - src/components/admin/SetupChecklist.tsx
  - src/app/admin/dashboard/page.tsx
  - src/app/admin/settings/page.tsx
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03

must_haves:
  truths:
    - "New merchant visits /admin and is redirected to /admin/setup showing step 1"
    - "Merchant can fill in store name and click Save & Continue to advance to step 2"
    - "Merchant can skip any step and still advance to the next"
    - "Merchant can upload a logo via drag-drop or click-to-browse and see a preview"
    - "Merchant can select from 8 color swatches with visual selection indicator"
    - "Merchant can add a first product with name, price, category, and image"
    - "After completing or skipping all 3 steps, merchant sees completion screen and is redirected to dashboard"
    - "Dashboard shows setup checklist banner with progress bar and 5 items"
    - "Checklist banner auto-hides when all 5 items are complete"
    - "Branding settings are editable at /admin/settings after wizard"
  artifacts:
    - path: "src/app/admin/setup/page.tsx"
      provides: "Wizard entry point — loads store data and renders wizard shell"
      contains: "SetupWizard"
    - path: "src/components/wizard/WizardLayout.tsx"
      provides: "Centered card layout for wizard steps"
      contains: "WizardLayout"
    - path: "src/components/wizard/WizardStepIndicator.tsx"
      provides: "3-dot step indicator with active/completed/pending states"
      contains: "WizardStepIndicator"
    - path: "src/components/wizard/LogoUploadZone.tsx"
      provides: "Drag-drop + click-to-browse logo upload with preview"
      contains: "LogoUploadZone"
    - path: "src/components/wizard/BrandColorPicker.tsx"
      provides: "8 curated color swatches with amber selection ring"
      contains: "BrandColorPicker"
    - path: "src/components/admin/SetupChecklist.tsx"
      provides: "Dashboard banner with progress bar and 5 checklist items"
      contains: "SetupChecklist"
    - path: "src/app/admin/settings/page.tsx"
      provides: "Post-wizard branding settings page"
      contains: "settings"
    - path: "src/app/admin/dashboard/page.tsx"
      provides: "Modified dashboard with SetupChecklist above existing content"
      contains: "SetupChecklist"
  key_links:
    - from: "src/components/wizard/StoreNameStep.tsx"
      to: "src/actions/setup/saveStoreNameStep.ts"
      via: "Server Action call on form submit"
      pattern: "saveStoreNameStep"
    - from: "src/components/wizard/LogoBrandStep.tsx"
      to: "src/actions/setup/saveLogoStep.ts"
      via: "Server Action call on form submit"
      pattern: "saveLogoStep"
    - from: "src/components/wizard/LogoUploadZone.tsx"
      to: "/api/store/logo"
      via: "fetch POST with FormData"
      pattern: "api/store/logo"
    - from: "src/app/admin/dashboard/page.tsx"
      to: "src/lib/setupChecklist.ts"
      via: "import getChecklistState"
      pattern: "getChecklistState"
---

<objective>
Build the wizard UI (3 steps with skip), dashboard setup checklist banner, and admin settings page.

Purpose: Delivers the merchant-facing onboarding experience — the wizard guides new stores through setup while the checklist tracks activation milestones.
Output: Wizard route, 10 wizard components, checklist component, settings page, modified dashboard page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-store-setup-wizard-marketing/14-CONTEXT.md
@.planning/phases/14-store-setup-wizard-marketing/14-RESEARCH.md
@.planning/phases/14-store-setup-wizard-marketing/14-UI-SPEC.md
@.planning/phases/14-store-setup-wizard-marketing/14-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Server Actions and utilities from Plan 01 -->

From src/actions/setup/saveStoreNameStep.ts:
```typescript
export async function saveStoreNameStep(formData: FormData): Promise<{ success?: boolean; error?: string }>
// FormData keys: storeName (string, 1-100 chars)
```

From src/actions/setup/saveLogoStep.ts:
```typescript
export async function saveLogoStep(formData: FormData): Promise<{ success?: boolean; error?: string }>
// FormData keys: logoUrl (string URL or null), primaryColor (string #hex6 or null)
```

From src/actions/setup/saveProductStep.ts:
```typescript
export async function saveProductStep(formData: FormData): Promise<{ success?: boolean; error?: string }>
// FormData keys: name (string, optional), priceCents (number, optional), categoryId (uuid, optional), imageUrl (url, optional)
```

From src/actions/setup/dismissWizard.ts:
```typescript
export async function dismissWizard(): Promise<{ success?: boolean; error?: string }>
```

From src/lib/setupChecklist.ts:
```typescript
export interface ChecklistState {
  storeName: boolean
  logo: boolean
  firstProduct: boolean
  firstPosSale: boolean
  firstOnlineOrder: boolean
  allComplete: boolean
  dismissed: boolean
  completedCount: number
}
export function getChecklistState(store: {
  name: string | null; slug: string; logo_url: string | null; setup_wizard_dismissed: boolean
}, productCount: number, orderChannels: string[]): ChecklistState
```

From src/app/api/store/logo/route.ts:
```typescript
// POST /api/store/logo — accepts FormData with 'image' key
// Returns: { url: string } on success, { error: string } on failure
```

From src/app/admin/dashboard/page.tsx (existing):
```typescript
export const dynamic = 'force-dynamic'
// Uses: supabase.auth.getUser() -> storeId from app_metadata
// Renders: h1 "Dashboard", grid of DashboardHeroCard, LowStockAlertList
```

From src/lib/resolveAuth.ts:
```typescript
export async function resolveAuth(): Promise<{ store_id: string; staff_id: string } | null>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard route, wizard components, and step UI</name>
  <files>
    src/app/admin/setup/page.tsx,
    src/components/wizard/WizardLayout.tsx,
    src/components/wizard/WizardStepIndicator.tsx,
    src/components/wizard/WizardStepCard.tsx,
    src/components/wizard/StoreNameStep.tsx,
    src/components/wizard/LogoBrandStep.tsx,
    src/components/wizard/FirstProductStep.tsx,
    src/components/wizard/LogoUploadZone.tsx,
    src/components/wizard/BrandColorPicker.tsx,
    src/components/wizard/WizardCompletion.tsx
  </files>
  <read_first>
    src/app/admin/dashboard/page.tsx,
    src/app/admin/layout.tsx,
    src/components/admin/dashboard/DashboardHeroCard.tsx,
    src/app/signup/page.tsx,
    src/app/api/products/image/route.ts,
    DESIGN.md,
    .planning/phases/14-store-setup-wizard-marketing/14-UI-SPEC.md,
    .planning/phases/14-store-setup-wizard-marketing/14-CONTEXT.md
  </read_first>
  <action>
    **Wizard entry page (src/app/admin/setup/page.tsx):**
    Server Component. `export const dynamic = 'force-dynamic'`.
    - Call `createSupabaseServerClient()`, get user, extract `storeId` from `app_metadata`.
    - Query `stores` for: `name, slug, logo_url, primary_color, setup_completed_steps, setup_wizard_dismissed`.
    - If `setup_wizard_dismissed === true`, redirect to `/admin/dashboard`.
    - Calculate `initialStep` from `setup_completed_steps` bitmask: if bit 0 not set -> step 1, else if bit 1 not set -> step 2, else if bit 2 not set -> step 3, else -> all complete (redirect to dashboard).
    - Query `categories` for the step 3 dropdown: `supabase.from('categories').select('id, name').eq('store_id', storeId)`.
    - Render `<WizardLayout>` wrapping `<SetupWizard initialStep={initialStep} storeData={storeData} categories={categories} />`.

    **WizardLayout (src/components/wizard/WizardLayout.tsx):**
    Server Component. Renders a centered container with warm stone background (`bg-[var(--color-bg)]`), full viewport height, flex centering. Children rendered inside. No admin sidebar — wizard has its own minimal chrome. Add "NZPOS" wordmark at top in Satoshi 700 / navy / 20px as a subtle branding element.

    **WizardStepIndicator (src/components/wizard/WizardStepIndicator.tsx):**
    Client Component (`'use client'`). Props: `{ currentStep: 1 | 2 | 3, completedSteps: number[] }`.
    - 3 numbered circles in a horizontal row connected by thin lines.
    - Active step: `bg-[var(--color-amber)]` fill, white number text.
    - Completed step: `bg-[var(--color-navy)]` fill, white checkmark SVG (inline).
    - Pending step: `border border-[var(--color-border)]`, `text-[var(--color-text-muted)]`.
    - Connecting line: `h-[2px] bg-[var(--color-border)]` between circles, `bg-[var(--color-navy)]` if both sides complete.
    - Each circle: `aria-label="Step {N} of 3"`, active has `aria-current="step"`.
    - Transition: 150ms ease-out on background-color change.

    **WizardStepCard (src/components/wizard/WizardStepCard.tsx):**
    Client Component. Props: `{ children, title: string }`.
    - Card shell: `max-w-md w-full mx-auto bg-white shadow-md rounded-lg p-[var(--space-xl)]` (matches AuthCard pattern from signup page).
    - Title: `h2` with DM Sans 20px / 700 / `text-[var(--color-text)]`, `mb-[var(--space-lg)]`.

    **SetupWizard (embedded in setup/page.tsx or separate client component):**
    Client Component (`'use client'`). Props: `{ initialStep, storeData, categories }`.
    - `const [step, setStep] = useState(initialStep)` — tracks current step (1, 2, 3, or 4 for completion).
    - `const [completedSteps, setCompletedSteps] = useState<number[]>([])` — derived from `storeData.setup_completed_steps` bitmask.
    - Renders `WizardStepIndicator` + current step component.
    - Step transitions: 250ms ease-in-out slide animation. Current step slides left, next slides in from right. Use CSS transform + transition on a wrapper div with `overflow-hidden`.
    - On step save success: add step to completedSteps, advance to next step.
    - On step skip: call `dismissWizard()` only on step 3 skip (final step). Steps 1-2 skip just advances without saving.
    - Actually per D-03 and D-05: skip on steps 1 and 2 advances to next step without saving data. Skip on step 3 calls `dismissWizard()` and navigates to dashboard.
    - On step 3 save or skip: call `dismissWizard()`, then show completion screen (step 4).
    - Step 4 (completion): show `WizardCompletion`, auto-redirect after 1500ms via `setTimeout(() => router.push('/admin/dashboard'), 1500)`.

    **StoreNameStep (src/components/wizard/StoreNameStep.tsx):**
    Client Component. Props: `{ storeName: string, slug: string, onSave: () => void, onSkip: () => void }`.
    - Title: "Name your store" (per copywriting contract).
    - Store Name input: text, pre-filled from `storeName` prop. Help text: "This appears on your storefront and receipts." Label style: DM Sans 14px / 400.
    - Slug display: read-only field showing `{rootDomain}/{slug}` in `font-mono text-[var(--color-text-muted)]`. Label: "Your store URL (set during signup, cannot be changed)".
    - Primary CTA: "Save & Continue" — `bg-[var(--color-amber)] text-white font-bold` (DM Sans 14px 700), `w-full min-h-[44px] rounded-md`. On click: create FormData, call `saveStoreNameStep(formData)`. If success, call `onSave()`.
    - Skip: "Skip for now" — `text-[var(--color-text-muted)] text-sm` centered below button. On click: call `onSkip()`.

    **LogoBrandStep (src/components/wizard/LogoBrandStep.tsx):**
    Client Component. Props: `{ logoUrl: string | null, primaryColor: string | null, onSave: () => void, onSkip: () => void }`.
    - Title: "Add your logo and brand colour" (NZ spelling per copywriting contract).
    - Renders `LogoUploadZone` and `BrandColorPicker`.
    - Manages local state: `const [currentLogoUrl, setCurrentLogoUrl] = useState(logoUrl)` and `const [currentColor, setCurrentColor] = useState(primaryColor ?? '#1E293B')`.
    - Primary CTA: "Save & Continue". On click: create FormData with `logoUrl` and `primaryColor`, call `saveLogoStep(formData)`. If success, call `onSave()`.
    - Skip: "Skip for now".

    **LogoUploadZone (src/components/wizard/LogoUploadZone.tsx):**
    Client Component. Props: `{ logoUrl: string | null, onChange: (url: string | null) => void }`.
    - Empty state: dashed border zone (`border-dashed border-2 border-[var(--color-border)]`), `rounded-lg`, `min-h-[120px]`, centered content.
      - Upload icon (inline SVG, 24px, `text-[var(--color-text-muted)]`).
      - Text: "Drop your logo here, or click to browse" (DM Sans 14px).
      - Caption: "PNG, JPG or SVG -- max 2MB" (DM Sans 14px, `text-[var(--color-text-light)]`).
    - Hidden `<input type="file" accept=".png,.jpg,.jpeg,.svg" />` triggered on zone click.
    - Drag-drop: `onDragOver` (prevent default, add visual highlight border), `onDrop` (extract file, upload).
    - Upload: `fetch('/api/store/logo', { method: 'POST', body: formData })`. Show spinner during upload (replace icon with rotating SVG). On success: set `logoUrl` via `onChange`. On error: border turns `border-[var(--color-error)]`, show error text "Upload failed -- file may be too large or wrong format. Try again."
    - Preview state: show logo image (`<img>` tag, not `next/image` — handles SVG, max 80px tall, `object-contain`). "Remove" text link (14px, muted) right-aligned. On remove: call `onChange(null)`, reset to empty state.
    - Zone has `role="button"`, `aria-label="Upload logo"`, `tabIndex={0}`, keyboard activation (Enter/Space opens file picker).

    **BrandColorPicker (src/components/wizard/BrandColorPicker.tsx):**
    Client Component. Props: `{ value: string, onChange: (color: string) => void }`.
    - Label: "Brand Colour" (DM Sans 14px / 400).
    - 8 swatches in flex-wrap row, 4px gap. Each swatch: 40x40px circle (`rounded-full`), the swatch's background color.
    - Colors (exact values from UI-SPEC):
      1. `#1E293B` (Navy — default)
      2. `#0F766E` (Teal)
      3. `#0369A1` (Ocean blue)
      4. `#4F46E5` (Indigo)
      5. `#7C3AED` (Violet)
      6. `#BE185D` (Rose)
      7. `#15803D` (Forest green)
      8. `#B45309` (Warm amber/brown)
    - Selected state: `outline: 3px solid var(--color-amber); outline-offset: 2px`. Transition: 100ms ease-out.
    - Each swatch: `<button>` with `aria-label="{color name}"` and `aria-pressed={selected}`.
    - Help text below: "This colour appears on your storefront header." (14px, muted).

    **FirstProductStep (src/components/wizard/FirstProductStep.tsx):**
    Client Component. Props: `{ categories: { id: string; name: string }[], onSave: () => void, onSkip: () => void }`.
    - Title: "Add your first product" (per copywriting contract).
    - Fields:
      - Product Name (text input). Placeholder: "e.g. Ceramic mug". DM Sans 14px label.
      - Price (text input with "$" prefix inline). Placeholder: "0.00". Note below: "Price includes GST." Parse to cents on submit.
      - Category (select dropdown). Options from `categories` prop. Placeholder: "Select a category".
      - Product Image: compact upload area (64px square preview + "Add image" link button). Reuse fetch to `/api/products/image` for upload.
    - Primary CTA: "Add Product & Finish". On click: create FormData, call `saveProductStep(formData)` then `dismissWizard()`, then `onSave()`.
    - Skip: "Skip -- go to dashboard". On click: call `saveProductStep()` (marks step complete with no product) then `dismissWizard()`, then `onSkip()`.

    **WizardCompletion (src/components/wizard/WizardCompletion.tsx):**
    Client Component. Props: none.
    - Full card content (reuse WizardStepCard shell).
    - Large checkmark icon (24px, `text-[var(--color-success)]`).
    - Heading: "Your store is ready." (DM Sans 20px / 700).
    - Body: "You can update all of these details anytime from your dashboard." (DM Sans 16px, muted).
    - Subtle text: "Taking you there..." (14px, `text-[var(--color-text-light)]`).
    - 250ms fade-in animation on mount (CSS `@keyframes fadeIn { from { opacity: 0 } to { opacity: 1 } }`).
    - Auto-redirect to `/admin/dashboard` handled by parent (not this component).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/setup/page.tsx contains `force-dynamic` and `setup_completed_steps` and `WizardLayout`
    - src/components/wizard/WizardLayout.tsx contains `bg-[var(--color-bg)]`
    - src/components/wizard/WizardStepIndicator.tsx contains `aria-current` and `var(--color-amber)`
    - src/components/wizard/StoreNameStep.tsx contains `saveStoreNameStep` and `"Save & Continue"` and `"Skip for now"`
    - src/components/wizard/LogoBrandStep.tsx contains `saveLogoStep` and `"Brand Colour"`
    - src/components/wizard/LogoUploadZone.tsx contains `api/store/logo` and `image/svg` and `role="button"`
    - src/components/wizard/BrandColorPicker.tsx contains `#1E293B` and `#0F766E` and `#0369A1` and `#4F46E5` and `#7C3AED` and `#BE185D` and `#15803D` and `#B45309` and `aria-pressed`
    - src/components/wizard/FirstProductStep.tsx contains `saveProductStep` and `dismissWizard` and `"Add Product & Finish"`
    - src/components/wizard/WizardCompletion.tsx contains `"Your store is ready."` and `"Taking you there..."`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Complete wizard UI with 3 steps, step indicator, skip flow, logo upload, color picker, completion screen, and auto-redirect to dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard setup checklist banner and admin settings page</name>
  <files>
    src/components/admin/SetupChecklist.tsx,
    src/app/admin/dashboard/page.tsx,
    src/app/admin/settings/page.tsx
  </files>
  <read_first>
    src/app/admin/dashboard/page.tsx,
    src/app/admin/layout.tsx,
    src/components/admin/dashboard/DashboardHeroCard.tsx,
    src/lib/setupChecklist.ts,
    DESIGN.md,
    .planning/phases/14-store-setup-wizard-marketing/14-UI-SPEC.md
  </read_first>
  <action>
    **SetupChecklist (src/components/admin/SetupChecklist.tsx):**
    Client Component (`'use client'`). Props: `{ state: ChecklistState }` (import `ChecklistState` from `src/lib/setupChecklist`).

    If `state.dismissed && state.allComplete`: render the congratulations state.
    If `state.dismissed && !state.allComplete`: render the checklist banner.
    If `!state.dismissed`: do not render (wizard handles this phase).

    Checklist banner layout (per UI-SPEC Screen D):
    - Container: `bg-white border border-[var(--color-border)] shadow-sm rounded-lg p-[var(--space-lg)]`.
    - Top row (horizontal on desktop, stacked on mobile):
      - Left: "{N} of 5 steps complete" — DM Sans 20px / 700 / `text-[var(--color-text)]`.
      - Center: Progress bar — `h-2 rounded-full bg-[var(--color-surface)]` track with `bg-[var(--color-amber)]` fill at `width: ${(state.completedCount / 5) * 100}%`. Add `role="progressbar" aria-valuenow={state.completedCount} aria-valuemin={0} aria-valuemax={5}`.
      - Right: "Resume setup" link — `text-sm text-[var(--color-navy)] hover:underline` linking to `/admin/setup`.
    - Checklist items: `<ul>` below progress row. Each `<li>`:
      - Incomplete: circle outline (20px SVG), label in `text-[var(--color-text)]`.
      - Complete: filled checkmark circle in `text-[var(--color-success)]`, label in `text-[var(--color-text-muted)] line-through`.
    - 5 items (exact copy from copywriting contract):
      1. "Set your store name" — complete when `state.storeName`
      2. "Upload your logo" — complete when `state.logo`
      3. "Add your first product" — complete when `state.firstProduct`
      4. "Complete your first POS sale" — complete when `state.firstPosSale`
      5. "Receive your first online order" — complete when `state.firstOnlineOrder`

    All-complete state:
    - Replace full banner with centered text: "Your store is set up." (DM Sans 20px / 700) + "You're all set -- everything is ready to go." (16px, muted).
    - CSS animation: visible for 3 seconds, then `opacity: 0` transition (`animation: fadeOut 400ms ease-out 3s forwards`), then `max-height: 0; overflow: hidden; transition: max-height 400ms ease-out` after animation ends.
    - Use a `useState` to track `collapsed` state — set to `true` after animation, render nothing.
    - Banner never reappears — the `setup_wizard_dismissed: true` flag is already in DB.

    **Dashboard page modification (src/app/admin/dashboard/page.tsx):**
    Add queries for checklist data AFTER existing queries:
    ```typescript
    // Fetch store data for checklist
    const { data: storeData } = await supabase
      .from('stores')
      .select('name, slug, logo_url, setup_wizard_dismissed')
      .eq('id', storeId)
      .single()

    // Fetch order channels for checklist milestones
    const { data: channelOrders } = await supabase
      .from('orders')
      .select('channel')
      .eq('store_id', storeId)
      .in('status', ['completed', 'pending_pickup', 'ready', 'collected'])
      .limit(100)

    const orderChannels = [...new Set((channelOrders ?? []).map(o => o.channel))]
    const productCount = (products ?? []).length

    const checklistState = getChecklistState(storeData!, productCount, orderChannels)
    ```

    Add `import { getChecklistState } from '@/lib/setupChecklist'` and `import { SetupChecklist } from '@/components/admin/SetupChecklist'`.

    Render `<SetupChecklist state={checklistState} />` as the FIRST child inside the outer `<div>`, above the `<h1>Dashboard</h1>`. Only render if `checklistState.dismissed && !checklistState.allComplete` OR if all complete and not yet collapsed (the component handles its own collapse).

    Actually, simpler: always render `<SetupChecklist state={checklistState} />` and let the component itself decide whether to show anything based on state.

    **Admin settings page (src/app/admin/settings/page.tsx):**
    Server Component. `export const dynamic = 'force-dynamic'`.
    - Fetch store data: `name, slug, logo_url, primary_color` from `stores` table.
    - Render a settings form (Client Component child) with:
      - Store Name (text input, editable, pre-filled).
      - Slug (display-only, same as wizard).
      - Logo upload (reuse `LogoUploadZone` component from wizard).
      - Brand color picker (reuse `BrandColorPicker` component from wizard).
      - Save button: calls a new Server Action `updateBranding` that updates `stores.name`, `stores.logo_url`, `stores.primary_color`. This action is a simplified version of the wizard step actions — no bitmask update needed.
    - Create `src/actions/setup/updateBranding.ts`:
      - `'use server'`, `import 'server-only'`, Zod validation, `resolveAuth()`.
      - Schema: `{ storeName: z.string().min(1).max(100), logoUrl: z.string().url().nullable(), primaryColor: z.string().regex(/^#[0-9A-Fa-f]{6}$/).nullable() }`.
      - Update `stores` SET `name`, `logo_url`, `primary_color` WHERE `id = auth.store_id`.
    - Page title: "Store Settings" (DM Sans 20px / 700). Same card layout as wizard step card for consistency.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/SetupChecklist.tsx contains `role="progressbar"` and `aria-valuenow` and `"of 5 steps complete"` and `"Your store is set up."`
    - src/components/admin/SetupChecklist.tsx contains `"Set your store name"` and `"Upload your logo"` and `"Add your first product"` and `"Complete your first POS sale"` and `"Receive your first online order"`
    - src/app/admin/dashboard/page.tsx contains `import { getChecklistState }` and `import` ... `SetupChecklist` and `orderChannels`
    - src/app/admin/settings/page.tsx contains `force-dynamic` and `logo_url` and `primary_color` and `LogoUploadZone` and `BrandColorPicker`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Dashboard shows setup checklist banner with 5 items and progress bar. Settings page allows branding edits post-wizard. Checklist auto-hides on all-complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify wizard flow and checklist</name>
  <files>src/app/admin/setup/page.tsx, src/app/admin/dashboard/page.tsx, src/app/admin/settings/page.tsx</files>
  <action>
    Human verification of the complete wizard flow and dashboard checklist. No code changes — visual and functional QA only.

    Steps to verify:
    1. Reset local DB: `npx supabase db reset`
    2. Create a new merchant via the signup flow (or manually create a store with `setup_wizard_dismissed: false`)
    3. Visit the admin dashboard — verify redirect to `/admin/setup`
    4. Step 1: Verify store name field is pre-filled, slug is display-only. Click "Save and Continue" — verify advance to step 2
    5. Step 2: Upload a logo (drag or click), verify preview appears. Select a color swatch, verify amber ring. Click "Save and Continue"
    6. Step 3: Add a product name and price. Click "Add Product and Finish" — verify completion screen appears, then redirect to dashboard
    7. On dashboard: verify checklist banner shows with progress (at least 3/5 items complete from wizard)
    8. Visit `/admin/settings` — verify logo, color, and store name are editable and persist on save
    9. Test skip flow: create another store, skip all 3 wizard steps — verify dashboard loads with no broken states
    10. Verify step indicator shows correct active/completed/pending states during wizard
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>User confirms wizard flow, skip flow, checklist banner, and settings page all function correctly. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors across all new files
- Wizard flow: new store redirected to setup, 3 steps work, skip works, completion redirects to dashboard
- Checklist: banner visible on dashboard, items reflect real state, auto-hides on all complete
- Settings: branding editable post-wizard
</verification>

<success_criteria>
- New merchant sees wizard on first admin visit
- All 3 wizard steps render with correct fields and CTAs
- Every step is individually skippable
- Logo upload works (drag-drop and click-to-browse) with SVG/PNG/JPG
- Color picker shows 8 swatches with selection indicator
- Dashboard checklist banner shows 5 items with progress bar
- Settings page allows branding edits after wizard
</success_criteria>

<output>
After completion, create `.planning/phases/14-store-setup-wizard-marketing/14-02-SUMMARY.md`
</output>
