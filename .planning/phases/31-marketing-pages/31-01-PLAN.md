---
phase: 31-marketing-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/components/LandingPricing.tsx
  - src/app/(marketing)/add-ons/email-notifications/page.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-04
  - MKT-05

must_haves:
  truths:
    - "Landing pricing section shows exactly 2 paid add-on cards (Xero and Inventory Management)"
    - "Free tier checklist includes 'Email notifications' as a checked item"
    - "Add-on grid on landing page uses a centered 2-column layout"
    - "Navigating to /add-ons/email-notifications returns a 404"
  artifacts:
    - path: "src/app/(marketing)/components/LandingPricing.tsx"
      provides: "Updated pricing section with 2 add-ons and email in free tier"
      contains: "Email notifications"
    - path: "src/app/(marketing)/add-ons/email-notifications/page.tsx"
      provides: "DELETED - must not exist"
  key_links:
    - from: "src/app/(marketing)/components/LandingPricing.tsx"
      to: "free tier checklist"
      via: "array item"
      pattern: "Email notifications"
---

<objective>
Update the landing page pricing section and remove the email notifications detail page.

Purpose: Reflect the new pricing model where email notifications are free. The landing pricing section must show only 2 paid add-ons (Xero and Inventory Management), include "Email notifications" in the free tier checklist, and use a centered 2-column grid for the add-on cards. The email notifications detail page must be deleted.

Output: Updated LandingPricing.tsx, deleted email-notifications detail page directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-marketing-pages/31-CONTEXT.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LandingPricing.tsx - remove email card, add to free tier, fix grid</name>
  <files>src/app/(marketing)/components/LandingPricing.tsx</files>
  <read_first>
    - src/app/(marketing)/components/LandingPricing.tsx
    - DESIGN.md
    - .planning/phases/31-marketing-pages/31-CONTEXT.md
  </read_first>
  <action>
    Three changes to LandingPricing.tsx (per D-01, D-03, D-05):

    1. **Add "Email notifications" to the free tier checklist (per D-03):**
       In the array on lines 23-29, add 'Email notifications' as the last item after 'Reporting'. The array becomes:
       ```
       'POS checkout',
       'Online storefront',
       'GST-compliant receipts',
       'Staff management',
       'Customer accounts',
       'Reporting',
       'Email notifications',
       ```

    2. **Remove the Email Notifications card (per D-05):**
       Delete the entire Email Notifications Link block (lines 87-112) — the `<Link href="/add-ons/email-notifications" ...>` element and everything inside it, including the closing `</Link>`. Keep the Xero Integration card and Inventory Management card intact.

    3. **Change grid to centered 2-column layout (per D-01):**
       On line 57, change:
       ```
       className="grid grid-cols-1 md:grid-cols-3 gap-[var(--space-lg)] mt-[var(--space-md)]"
       ```
       to:
       ```
       className="grid grid-cols-1 md:grid-cols-2 gap-[var(--space-lg)] mt-[var(--space-md)] max-w-3xl mx-auto"
       ```
       The `max-w-3xl` (48rem / 768px) constrains the 2-column grid within the 1200px container so cards don't stretch too wide. `mx-auto` centers the grid.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "Email notifications" src/app/(marketing)/components/LandingPricing.tsx && grep "md:grid-cols-2" src/app/(marketing)/components/LandingPricing.tsx && ! grep "email-notifications" src/app/(marketing)/components/LandingPricing.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - LandingPricing.tsx free tier array contains exactly 7 items ending with 'Email notifications'
    - LandingPricing.tsx contains NO `<Link href="/add-ons/email-notifications"` element
    - LandingPricing.tsx add-on grid uses `md:grid-cols-2` (not `md:grid-cols-3`)
    - LandingPricing.tsx add-on grid wrapper has `max-w-3xl mx-auto` for centering
    - Only 2 `<Link` elements remain in the add-on grid section (Xero and Inventory)
  </acceptance_criteria>
  <done>Landing pricing shows 7 free tier items including "Email notifications", 2 add-on cards in a centered 2-column grid, no email notifications card</done>
</task>

<task type="auto">
  <name>Task 2: Delete email-notifications detail page</name>
  <files>src/app/(marketing)/add-ons/email-notifications/page.tsx</files>
  <read_first>
    - src/app/(marketing)/add-ons/email-notifications/page.tsx
  </read_first>
  <action>
    Delete the entire directory `src/app/(marketing)/add-ons/email-notifications/` (per D-02).

    Run: `rm -rf src/app/(marketing)/add-ons/email-notifications/`

    This removes the route entirely. Next.js will return its standard 404 page for /add-ons/email-notifications. No redirect needed per D-02.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && ! test -d src/app/\(marketing\)/add-ons/email-notifications && echo "PASS: directory deleted"</automated>
  </verify>
  <acceptance_criteria>
    - Directory src/app/(marketing)/add-ons/email-notifications/ does not exist
    - No file named page.tsx exists at that path
  </acceptance_criteria>
  <done>/add-ons/email-notifications route returns 404 because the page directory no longer exists</done>
</task>

</tasks>

<verification>
- `grep -c "Email notifications" src/app/(marketing)/components/LandingPricing.tsx` returns 1
- `grep "md:grid-cols-2" src/app/(marketing)/components/LandingPricing.tsx` matches
- `grep "max-w-3xl" src/app/(marketing)/components/LandingPricing.tsx` matches
- `grep -c "email-notifications" src/app/(marketing)/components/LandingPricing.tsx` returns 0
- `test -d src/app/(marketing)/add-ons/email-notifications` fails (directory gone)
- `npx next build` succeeds (no broken imports)
</verification>

<success_criteria>
Landing pricing section displays 2 add-on cards (Xero, Inventory) in a centered 2-column grid, free tier checklist includes "Email notifications", and /add-ons/email-notifications returns 404.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-pages/31-01-SUMMARY.md`
</output>
