---
phase: 31-marketing-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/add-ons/page.tsx
autonomous: true
requirements:
  - MKT-03
  - MKT-05

must_haves:
  truths:
    - "Add-ons hub page shows exactly 2 add-ons (Xero and Inventory Management)"
    - "Add-on grid on hub page uses a centered 2-column layout"
    - "Page metadata does not mention email notifications"
  artifacts:
    - path: "src/app/(marketing)/add-ons/page.tsx"
      provides: "Updated add-ons hub with 2 add-ons in 2-column grid"
      contains: "md:grid-cols-2"
  key_links:
    - from: "src/app/(marketing)/add-ons/page.tsx"
      to: "/add-ons/xero and /add-ons/inventory"
      via: "addOns array href values"
      pattern: "href.*add-ons/(xero|inventory)"
---

<objective>
Update the add-ons hub page to show only 2 add-ons with a centered 2-column grid.

Purpose: Remove the Email Notifications entry from the hub page and update the layout from 3-column to centered 2-column, matching the updated pricing model where email is now free.

Output: Updated add-ons/page.tsx with 2 add-ons and corrected grid layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-marketing-pages/31-CONTEXT.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update add-ons hub page - remove email entry, fix grid, update metadata</name>
  <files>src/app/(marketing)/add-ons/page.tsx</files>
  <read_first>
    - src/app/(marketing)/add-ons/page.tsx
    - DESIGN.md
    - .planning/phases/31-marketing-pages/31-CONTEXT.md
  </read_first>
  <action>
    Three changes to src/app/(marketing)/add-ons/page.tsx (per D-01, D-04):

    1. **Update metadata description (per D-04):**
       Change line 9 from:
       ```
       'Extend NZPOS with optional features. Xero integration, email notifications, and inventory management — pay only for what you use.',
       ```
       to:
       ```
       'Extend NZPOS with optional features. Xero integration and inventory management — pay only for what you use.',
       ```

    2. **Remove Email Notifications from addOns array (per D-04):**
       Delete the entire second object in the `addOns` array (lines 27-38) — the one with `name: 'Email Notifications'`. The array should contain exactly 2 objects: Xero Integration and Inventory Management. No other changes to these objects.

    3. **Change grid to centered 2-column layout (per D-01):**
       On line 74, change:
       ```
       className="grid grid-cols-1 md:grid-cols-3 gap-[var(--space-lg)]"
       ```
       to:
       ```
       className="grid grid-cols-1 md:grid-cols-2 gap-[var(--space-lg)] max-w-3xl mx-auto"
       ```
       Same `max-w-3xl mx-auto` pattern as the landing pricing section for visual consistency.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && ! grep -i "email.notification" src/app/\(marketing\)/add-ons/page.tsx && grep "md:grid-cols-2" src/app/\(marketing\)/add-ons/page.tsx && grep "max-w-3xl" src/app/\(marketing\)/add-ons/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - add-ons/page.tsx metadata description does NOT contain "email notifications"
    - add-ons/page.tsx addOns array has exactly 2 entries (Xero Integration and Inventory Management)
    - add-ons/page.tsx contains NO string matching "Email Notifications" or "email-notifications"
    - add-ons/page.tsx grid uses `md:grid-cols-2` (not `md:grid-cols-3`)
    - add-ons/page.tsx grid wrapper has `max-w-3xl mx-auto` for centering
    - `force-static` export is still present
  </acceptance_criteria>
  <done>Add-ons hub page displays exactly 2 add-on cards (Xero, Inventory) in a centered 2-column grid with no mention of email notifications</done>
</task>

</tasks>

<verification>
- `grep -c "Email" src/app/(marketing)/add-ons/page.tsx` returns 0
- `grep "md:grid-cols-2" src/app/(marketing)/add-ons/page.tsx` matches
- `grep "max-w-3xl" src/app/(marketing)/add-ons/page.tsx` matches
- `grep "force-static" src/app/(marketing)/add-ons/page.tsx` matches
- `npx next build` succeeds
</verification>

<success_criteria>
Add-ons hub page shows exactly 2 add-on cards (Xero and Inventory Management) in a centered 2-column grid, with metadata description referencing only Xero and inventory management.
</success_criteria>

<output>
After completion, create `.planning/phases/31-marketing-pages/31-02-SUMMARY.md`
</output>
