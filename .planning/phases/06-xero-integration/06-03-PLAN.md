---
phase: 06-xero-integration
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/xero/sync.ts
  - src/lib/xero/__tests__/sync.test.ts
autonomous: true
requirements:
  - XERO-02
  - XERO-03
  - XERO-04

must_haves:
  truths:
    - "Daily sales are aggregated by payment method (cash, eftpos, online) from the orders table"
    - "Sync creates a Xero invoice or updates an existing one for the same date (upsert pattern)"
    - "Refunds for a date range produce a Xero credit note against the original invoice day"
    - "Sync log entry is created for every sync attempt with status, total, invoice ID, and error"
    - "Failed sync writes error message to sync log without throwing"
  artifacts:
    - path: "src/lib/xero/sync.ts"
      provides: "Complete sync orchestration: query sales, build invoice, call Xero API, write sync log"
      exports: ["executeDailySync", "executeManualSync"]
    - path: "src/lib/xero/__tests__/sync.test.ts"
      provides: "Unit tests for sync logic with mocked Xero API and Supabase"
  key_links:
    - from: "src/lib/xero/sync.ts"
      to: "src/lib/xero/buildInvoice.ts"
      via: "buildDailyInvoice and buildCreditNote for Xero API payload"
      pattern: "buildDailyInvoice|buildCreditNote"
    - from: "src/lib/xero/sync.ts"
      to: "src/lib/xero/client.ts"
      via: "getAuthenticatedXeroClient for API calls"
      pattern: "getAuthenticatedXeroClient"
    - from: "src/lib/xero/sync.ts"
      to: "src/lib/xero/dates.ts"
      via: "getNZDayBoundaries and getNZTodayBoundaries for sync window"
      pattern: "getNZDayBoundaries|getNZTodayBoundaries"
---

<objective>
Build the sync orchestration engine that queries daily sales from the orders table, creates/updates Xero invoices, handles refund credit notes, and writes sync log entries. This is the core business logic for XERO-02 (automated sync) and XERO-04 (manual sync), separated from the API/UI layer for testability.

Purpose: The sync engine is the most complex piece — it coordinates sales aggregation, Xero API calls, upsert logic, and error handling. Building it as a tested library module ensures correctness before wiring to cron or manual triggers.
Output: sync.ts orchestration module + comprehensive test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-xero-integration/06-CONTEXT.md
@.planning/phases/06-xero-integration/06-RESEARCH.md
@.planning/phases/06-xero-integration/06-01-SUMMARY.md

@src/lib/xero/types.ts
@src/lib/xero/vault.ts
@src/lib/xero/client.ts
@src/lib/xero/dates.ts
@src/lib/xero/buildInvoice.ts

<interfaces>
From Plan 01 outputs:
```typescript
// src/lib/xero/types.ts
export interface DailySalesData { cashTotalCents: number; eftposTotalCents: number; onlineTotalCents: number; dateLabel: string }
export interface XeroSettings { cashAccountCode: string; eftposAccountCode: string; onlineAccountCode: string; contactId: string }
export interface XeroSyncLogEntry { id: string; store_id: string; sync_date: string; sync_type: 'auto' | 'manual'; status: 'pending' | 'success' | 'failed'; ... }

// src/lib/xero/dates.ts
export function getNZDayBoundaries(referenceDate?: Date): { from: Date; to: Date; label: string }
export function getNZTodayBoundaries(referenceDate?: Date): { from: Date; to: Date; label: string }

// src/lib/xero/buildInvoice.ts
export function buildDailyInvoice(sales: DailySalesData, settings: XeroSettings, existingInvoiceId?: string): Invoice
export function buildCreditNote(refundCents: number, dateLabel: string, settings: XeroSettings, originalInvoiceNumber: string): CreditNote

// src/lib/xero/client.ts
export async function getAuthenticatedXeroClient(storeId: string): Promise<{ xero: XeroClient; tenantId: string } | null>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build sync orchestration engine with sales aggregation, Xero API integration, and sync log management</name>
  <files>
    src/lib/xero/sync.ts,
    src/lib/xero/__tests__/sync.test.ts
  </files>
  <read_first>
    - src/lib/xero/types.ts (shared types)
    - src/lib/xero/buildInvoice.ts (invoice builder)
    - src/lib/xero/dates.ts (NZ boundaries)
    - src/lib/xero/client.ts (authenticated client factory)
    - src/lib/supabase/admin.ts (admin client pattern)
    - src/actions/orders/completeSale.ts (orders table column names — total_cents, payment_method, channel, status)
    - .planning/phases/06-xero-integration/06-RESEARCH.md (daily sales aggregation query, upsert pattern, credit notes)
  </read_first>
  <behavior>
    - Test: aggregateDailySales returns correct totals grouped by payment method from mock order data
    - Test: aggregateDailySales filters to only completed/collected orders (excludes refunded, pending, expired)
    - Test: aggregateDailySales returns zero for payment methods with no orders
    - Test: executeDailySync creates pending sync log entry, calls Xero API, updates to success with invoice ID
    - Test: executeDailySync updates existing invoice when sync_log has prior success for same date (upsert per D-10)
    - Test: executeDailySync writes error_message to sync log on Xero API failure
    - Test: executeDailySync returns early with "no sales" message when all totals are zero
    - Test: executeDailySync skips sync when Xero is not connected (no authenticated client)
    - Test: executeManualSync uses getNZTodayBoundaries for today's sales window
    - Test: syncRefundsAsCreditNotes creates credit notes for refunded orders in the sync period
  </behavior>
  <action>
**src/lib/xero/sync.ts** — Sync orchestration:

Import `server-only`. Import `createSupabaseAdminClient` from `@/lib/supabase/admin`. Import all xero lib modules.

**Internal helper: `aggregateDailySales(storeId: string, from: Date, to: Date): Promise<DailySalesData & { refundTotalCents: number }>`**
- Query orders via admin client: `.from('orders').select('total_cents, payment_method, channel, status').eq('store_id', storeId).gte('created_at', from.toISOString()).lte('created_at', to.toISOString())`
- Filter completed/collected orders: `.in('status', ['completed', 'collected'])`
- Aggregate:
  - `cashTotalCents`: sum of `total_cents` where `payment_method === 'cash'`
  - `eftposTotalCents`: sum of `total_cents` where `payment_method === 'eftpos'`
  - `onlineTotalCents`: sum of `total_cents` where `channel === 'online'`
- Also query refunded orders for the same period: `.eq('status', 'refunded')` — sum their `total_cents` as `refundTotalCents`.
- Return `{ cashTotalCents, eftposTotalCents, onlineTotalCents, dateLabel, refundTotalCents }`.

**Internal helper: `getXeroSettings(storeId: string): Promise<XeroSettings | null>`**
- Query `xero_connections` for `account_code_cash`, `account_code_eftpos`, `account_code_online`, `xero_contact_id` where `store_id = storeId` and `status = 'connected'`.
- Return null if no connection or any account code is null (not yet configured).
- Map to `XeroSettings` type.

**Internal helper: `writeSyncLog(entry: Partial<XeroSyncLogEntry> & { store_id: string; sync_date: string; sync_type: string; status: string }): Promise<string>`**
- Insert into `xero_sync_log` via admin client. Return the new row ID.

**Internal helper: `updateSyncLog(logId: string, updates: Partial<XeroSyncLogEntry>): Promise<void>`**
- Update `xero_sync_log` row by ID via admin client.

**Exported: `executeDailySync(storeId: string, referenceDate?: Date): Promise<{ success: boolean; message: string; invoiceNumber?: string }>`**
- Get authenticated Xero client. If null, return `{ success: false, message: 'Xero not connected' }`.
- Get Xero settings. If null, return `{ success: false, message: 'Account codes not configured' }`.
- Compute boundaries via `getNZDayBoundaries(referenceDate)`.
- Aggregate sales for the period.
- If all totals are zero and refundTotalCents is zero: return `{ success: true, message: 'No sales recorded for this period. Nothing to sync.' }`.
- Check for existing successful sync for this date: query `xero_sync_log` for `sync_date = label, status = 'success'`. If found, get `xero_invoice_id` for upsert.
- Write pending sync log entry.
- Build invoice via `buildDailyInvoice(sales, settings, existingInvoiceId)`.
- Try: call `xero.accountingApi.createInvoices(tenantId, { invoices: [invoice] })` (for new) or `xero.accountingApi.updateInvoice(tenantId, existingInvoiceId, { invoices: [invoice] })` (for upsert).
- Extract `invoiceID` and `invoiceNumber` from response: `result.body.invoices![0]`.
- If refundTotalCents > 0: build credit note via `buildCreditNote(refundTotalCents, label, settings, invoiceNumber)`, call `xero.accountingApi.createCreditNotes(tenantId, { creditNotes: [creditNote] })`.
- Update sync log to success with `xero_invoice_id`, `xero_invoice_number`, `total_cents` (sum of all payment method totals).
- Return `{ success: true, message: 'Synced', invoiceNumber }`.
- Catch: update sync log to failed with `error_message: error.message`. Return `{ success: false, message: error.message }`.

**Exported: `executeManualSync(storeId: string): Promise<{ success: boolean; message: string; invoiceNumber?: string }>`**
- Same logic as `executeDailySync` but uses `getNZTodayBoundaries()` instead of `getNZDayBoundaries()`.
- Sets `sync_type: 'manual'` in sync log.
- The upsert check looks for today's date in sync_log, so re-syncing today updates the existing invoice per D-10.

**Tests (src/lib/xero/__tests__/sync.test.ts):**
- Mock `createSupabaseAdminClient` to return a mock Supabase client with chainable `.from().select().eq()...` pattern.
- Mock `getAuthenticatedXeroClient` to return a mock xero client.
- Mock `xero.accountingApi.createInvoices` to return `{ body: { invoices: [{ invoiceID: 'inv-123', invoiceNumber: 'NZPOS-2026-03-31' }] } }`.
- Test all behaviors listed above.
- Use `vi.mock()` for module-level mocking.
  </action>
  <verify>
    <automated>npx vitest run src/lib/xero/__tests__/sync.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/xero/sync.ts contains `import 'server-only'`
    - src/lib/xero/sync.ts exports `executeDailySync` and `executeManualSync`
    - src/lib/xero/sync.ts contains `aggregateDailySales` function
    - src/lib/xero/sync.ts contains `.in('status', ['completed', 'collected'])` for order filtering
    - src/lib/xero/sync.ts contains `payment_method === 'cash'` aggregation
    - src/lib/xero/sync.ts contains `payment_method === 'eftpos'` aggregation
    - src/lib/xero/sync.ts contains `channel === 'online'` aggregation
    - src/lib/xero/sync.ts contains `xero_sync_log` insert and update operations
    - src/lib/xero/sync.ts contains `createInvoices` and `updateInvoice` calls
    - src/lib/xero/sync.ts contains `createCreditNotes` call for refunds
    - src/lib/xero/sync.ts contains `getNZDayBoundaries` and `getNZTodayBoundaries`
    - src/lib/xero/__tests__/sync.test.ts exists with at least 8 test cases
    - `npx vitest run src/lib/xero/__tests__/sync.test.ts` exits 0
  </acceptance_criteria>
  <done>Sync engine handles daily and manual sync flows: aggregates sales by payment method, builds Xero invoices, handles upsert for same-day re-sync, creates credit notes for refunds, and writes sync log entries with success/failure status. All test cases pass with mocked dependencies.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/xero/__tests__/ --reporter=verbose` — all sync tests + previous tests pass
- `npx tsc --noEmit` — no type errors
- grep confirms all key patterns present in sync.ts
</verification>

<success_criteria>
- executeDailySync correctly aggregates previous NZ day's sales by payment method
- executeManualSync aggregates today's sales (midnight to now)
- Upsert pattern: re-sync for same date updates existing invoice instead of creating duplicate
- Refunded orders produce Xero credit notes
- Every sync attempt creates a sync log entry regardless of success/failure
- Error handling catches Xero API failures and writes to sync log
- All test cases pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-xero-integration/06-03-SUMMARY.md`
</output>
