---
phase: 22-inventory-add-on-core
plan: "04"
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/app/admin/inventory/page.tsx
  - src/components/admin/inventory/InventoryPageClient.tsx
  - src/components/admin/inventory/StockLevelsTab.tsx
  - src/components/admin/inventory/StockAdjustDrawer.tsx
  - src/components/admin/inventory/InventoryHistoryTab.tsx
  - src/components/admin/AdminSidebar.tsx
  - src/components/admin/products/ProductFormDrawer.tsx
autonomous: false

requirements: [STOCK-01, STOCK-03, STOCK-04, STOCK-05]

must_haves:
  truths:
    - "Admin navigates to /admin/inventory and sees three tabs: Stock Levels, Stocktakes, History"
    - "Stock Levels tab shows all physical products with current stock quantities"
    - "Admin can click Adjust stock on a product row and a drawer opens for stock adjustment"
    - "Adjustment drawer supports both delta and absolute modes with reason codes"
    - "History tab shows paginated adjustment rows with product, date, reason, and quantity filters"
    - "AdminSidebar shows Inventory link only when hasInventory is true"
    - "ProductFormDrawer stock_quantity field is read-only for physical products when hasInventory is true"
    - "ProductFormDrawer has an Adjust stock button that opens StockAdjustDrawer (per D-01)"
  artifacts:
    - path: "src/app/admin/inventory/page.tsx"
      provides: "Server component page for /admin/inventory"
      contains: "InventoryPageClient"
    - path: "src/components/admin/inventory/InventoryPageClient.tsx"
      provides: "Client component with tab navigation"
      contains: "use client"
    - path: "src/components/admin/inventory/StockLevelsTab.tsx"
      provides: "Stock levels table with adjust stock buttons"
      contains: "Adjust stock"
    - path: "src/components/admin/inventory/StockAdjustDrawer.tsx"
      provides: "Drawer for manual stock adjustment"
      contains: "Save adjustment"
    - path: "src/components/admin/inventory/InventoryHistoryTab.tsx"
      provides: "Adjustment history with filters"
      contains: "stock_adjustments"
  key_links:
    - from: "src/components/admin/inventory/StockAdjustDrawer.tsx"
      to: "src/actions/inventory/adjustStock.ts"
      via: "import { adjustStock } and call on form submit"
      pattern: "adjustStock"
    - from: "src/components/admin/inventory/InventoryHistoryTab.tsx"
      to: "src/actions/inventory/getAdjustmentHistory.ts"
      via: "import { getAdjustmentHistory } and call with filters"
      pattern: "getAdjustmentHistory"
    - from: "src/components/admin/products/ProductFormDrawer.tsx"
      to: "src/components/admin/inventory/StockAdjustDrawer.tsx"
      via: "Adjust stock button opens StockAdjustDrawer (per D-01)"
      pattern: "StockAdjustDrawer"
---

<objective>
Build the /admin/inventory page with Stock Levels tab, Stock Adjustment drawer, and History tab. Update AdminSidebar and ProductFormDrawer for inventory integration.

Purpose: This is the primary inventory management UI. Stock Levels (STOCK-05), adjustment drawer (STOCK-01), and history (STOCK-03) are the daily-use features for inventory merchants.
Output: Inventory page with two functional tabs (Stock Levels and History), adjustment drawer, sidebar nav, Adjust stock button in product form (per D-01), and read-only stock field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-inventory-add-on-core/22-CONTEXT.md
@.planning/phases/22-inventory-add-on-core/22-RESEARCH.md
@.planning/phases/22-inventory-add-on-core/22-UI-SPEC.md
@.planning/phases/22-inventory-add-on-core/22-02-SUMMARY.md
@DESIGN.md

<interfaces>
From src/actions/inventory/adjustStock.ts (Plan 02):
```typescript
export async function adjustStock(input: unknown): Promise<
  | { success: true; new_quantity: number }
  | { error: 'feature_not_active' | 'validation_failed' | 'product_not_found' | 'server_error'; issues?: any }
>
```

From src/actions/inventory/getAdjustmentHistory.ts (Plan 02):
```typescript
export async function getAdjustmentHistory(filters?: {
  productId?: string; fromDate?: string; toDate?: string; reason?: string; page?: number
}): Promise<
  | { success: true; rows: any[]; total: number; page: number; pageSize: number }
  | { error: 'server_error' }
>
```

From src/actions/inventory/getStockLevels.ts (Plan 02):
```typescript
export async function getStockLevels(): Promise<
  | { success: true; products: Array<{ id: string; name: string; sku: string; barcode: string; stock_quantity: number; reorder_threshold: number; category_id: string; categories: { name: string } }> }
  | { error: 'server_error' }
>
```

From src/schemas/inventory.ts (Plan 01):
```typescript
export const MANUAL_REASON_CODES = ['received', 'damaged', 'theft_shrinkage', 'correction', 'return_to_supplier', 'other'] as const
export const ALL_REASON_CODES = [...MANUAL_REASON_CODES, ...SYSTEM_REASON_CODES] as const
export const REASON_CODE_LABELS: Record<ReasonCode, string>
export type ManualReasonCode = typeof MANUAL_REASON_CODES[number]
```

From src/components/admin/AdminSidebar.tsx:
```typescript
const navLinks = [ { href: '/admin/dashboard', label: 'Dashboard' }, ... ]
interface AdminSidebarProps { userEmail?: string | null }
export default function AdminSidebar({ userEmail }: AdminSidebarProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /admin/inventory page, InventoryPageClient, StockLevelsTab, and StockAdjustDrawer</name>
  <files>
    src/app/admin/inventory/page.tsx,
    src/components/admin/inventory/InventoryPageClient.tsx,
    src/components/admin/inventory/StockLevelsTab.tsx,
    src/components/admin/inventory/StockAdjustDrawer.tsx
  </files>
  <read_first>
    - src/components/admin/reports/ReportsPageClient.tsx (TabButton pattern — copy verbatim; page layout pattern)
    - src/components/admin/products/ProductFormDrawer.tsx (drawer pattern — copy structure: backdrop, escape key, dirty state)
    - src/app/admin/reports/page.tsx (server component page pattern — how it passes data to client component)
    - src/components/admin/reports/StockLevelsTable.tsx (existing stock level display — semantic color logic for stock quantities)
    - src/actions/inventory/adjustStock.ts (import for drawer submit)
    - src/actions/inventory/getStockLevels.ts (import for stock levels tab)
    - src/schemas/inventory.ts (MANUAL_REASON_CODES, REASON_CODE_LABELS for drawer select)
    - DESIGN.md (design system tokens and rules)
    - .planning/phases/22-inventory-add-on-core/22-UI-SPEC.md (exact visual specs)
  </read_first>
  <action>
    **1. Create `src/app/admin/inventory/page.tsx`** (server component):
    - Import `getStockLevels` from `@/actions/inventory/getStockLevels`
    - Call `getStockLevels()` to fetch products server-side
    - Pass products and `searchParams` to `InventoryPageClient`
    - Page title "Inventory" using `text-[2.25rem] font-bold font-display` (matches ReportsPageClient h1)

    **2. Create `src/components/admin/inventory/InventoryPageClient.tsx`** ('use client'):
    - Three tabs using TabButton pattern copied from ReportsPageClient:
      - "Stock Levels" (`?tab=stock-levels`, default)
      - "Stocktakes" (`?tab=stocktakes`)
      - "History" (`?tab=history`)
    - Tab active style: `border-navy text-navy border-b-2`
    - Tab inactive style: `border-transparent text-muted hover:text-primary hover:border-border`
    - Tab container: `flex gap-2 border-b border-border pb-0`
    - URL-driven via `useSearchParams()` — default to 'stock-levels' when no tab param
    - Render `StockLevelsTab` when tab='stock-levels', placeholder div for stocktakes (Plan 05), `InventoryHistoryTab` when tab='history'

    **3. Create `src/components/admin/inventory/StockLevelsTab.tsx`** ('use client'):
    - Receives `products` array as prop
    - Table with columns: Product, SKU, Stock, Reorder Threshold, Action
    - Table header: `bg-navy text-white` per UI-SPEC, 14px bold DM Sans (`text-sm font-bold font-body`)
    - Table rows: alternating `bg-card` / `bg-surface`, 14px DM Sans
    - Stock column: Geist Mono (`font-mono`), semantic colors:
      - stock_quantity === 0: `text-error font-bold`
      - stock_quantity <= reorder_threshold: `text-warning font-bold`
      - else: default text color
    - Low-stock rows: `border-l-4 border-amber` left border per UI-SPEC
    - "Adjust stock" button per row: ghost button (`border border-border bg-transparent text-sm font-bold`), visible on row hover (`opacity-0 group-hover:opacity-100 transition-opacity`)
    - Click "Adjust stock" opens StockAdjustDrawer with selected product
    - Empty state: "No physical products yet" heading, "Add a product and set its type to Physical to track stock here." body, "Add product" amber link to /admin/products
    - `font-feature-settings: 'tnum' 1` on stock quantity column cells (per UI-SPEC Typography section)

    **4. Create `src/components/admin/inventory/StockAdjustDrawer.tsx`** ('use client'):
    - Props: `product: { id: string; name: string; stock_quantity: number }`, `onClose: () => void`, `onSuccess: (newQuantity: number) => void`
    - Fixed right-side overlay, 480px wide, full height (per D-02, matches ProductFormDrawer)
    - Backdrop: `bg-navy/40`
    - Drawer background: `bg-card` (#FFFFFF)
    - Escape key closes drawer (with dirty-state confirm if values changed)
    - Header: product name (20px Satoshi 700 = `text-xl font-bold font-display`), current stock qty (14px Geist Mono = `text-sm font-mono text-muted`)
    - Toggle "Adjust by" / "Set to" modes (per D-04):
      - Container: `rounded-full border border-border` wrapper
      - Active pill: `bg-navy text-white`
      - Inactive pill: `bg-surface text-text border-border`
    - Quantity input: 48px height, centered, Geist Mono 16px (`font-mono text-base`), `border-border rounded-md`
      - "Adjust by" mode: `+` / `-` stepper buttons flanking input, 44x44px, `bg-surface hover:bg-border`
      - "Set to" mode: plain number input
    - Reason code: `<select>` with MANUAL_REASON_CODES mapped to REASON_CODE_LABELS for display. Style: `border-border rounded-md px-4 py-2 text-sm font-body`
    - Notes: `<textarea rows={3}>` same border/radius pattern
    - Preview row: "New stock: {computed}" in 14px Geist Mono bold (`text-sm font-mono font-bold`)
      - "Adjust by" mode: `product.stock_quantity + value`
      - "Set to" mode: `value` (delta sent to server = `value - product.stock_quantity`)
    - Footer: "Save adjustment" button (`bg-navy text-white rounded-md px-4 py-2 font-bold`) + "Close without saving" ghost link
    - Save button shows spinner during submission (SVG animate-spin)
    - On success: call `onSuccess(newQuantity)`, drawer closes
    - On error: inline error below form in `text-error text-sm`
    - Slide-in animation: 250ms ease-out open, ease-in close (per UI-SPEC Motion)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/inventory/page.tsx exists and contains `InventoryPageClient`
    - src/components/admin/inventory/InventoryPageClient.tsx contains `'use client'`
    - src/components/admin/inventory/InventoryPageClient.tsx contains `tab=stock-levels`
    - src/components/admin/inventory/InventoryPageClient.tsx contains `tab=stocktakes`
    - src/components/admin/inventory/InventoryPageClient.tsx contains `tab=history`
    - src/components/admin/inventory/InventoryPageClient.tsx contains `TabButton` or equivalent tab link pattern
    - src/components/admin/inventory/StockLevelsTab.tsx contains `'use client'`
    - src/components/admin/inventory/StockLevelsTab.tsx contains `bg-navy`
    - src/components/admin/inventory/StockLevelsTab.tsx contains `text-error`
    - src/components/admin/inventory/StockLevelsTab.tsx contains `text-warning`
    - src/components/admin/inventory/StockLevelsTab.tsx contains `border-l-4 border-amber`
    - src/components/admin/inventory/StockLevelsTab.tsx contains `Adjust stock`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `'use client'`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `adjustStock`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `Adjust by`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `Set to`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `Save adjustment`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `MANUAL_REASON_CODES` or `REASON_CODE_LABELS`
    - src/components/admin/inventory/StockAdjustDrawer.tsx contains `480` (drawer width)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Inventory page renders with three tabs, stock levels table shows physical products with semantic colors, adjustment drawer supports delta/absolute modes with reason codes.</done>
</task>

<task type="auto">
  <name>Task 2: Create InventoryHistoryTab, update AdminSidebar and ProductFormDrawer</name>
  <files>
    src/components/admin/inventory/InventoryHistoryTab.tsx,
    src/components/admin/AdminSidebar.tsx,
    src/components/admin/products/ProductFormDrawer.tsx
  </files>
  <read_first>
    - src/components/admin/AdminSidebar.tsx (current sidebar — understand navLinks array and props interface)
    - src/components/admin/products/ProductFormDrawer.tsx (current drawer — find stock_quantity input field to make read-only)
    - src/app/admin/layout.tsx (AdminLayout — understand how props are passed to AdminSidebar, find where to read hasInventory from JWT)
    - src/actions/inventory/getAdjustmentHistory.ts (import for history tab)
    - src/schemas/inventory.ts (ALL_REASON_CODES, REASON_CODE_LABELS for filter dropdown)
    - src/components/admin/inventory/StockAdjustDrawer.tsx (just created in Task 1 — import for D-01 button in ProductFormDrawer)
    - DESIGN.md (design tokens)
    - .planning/phases/22-inventory-add-on-core/22-UI-SPEC.md (InventoryHistoryTab section, Copywriting section)
  </read_first>
  <action>
    **1. Create `src/components/admin/inventory/InventoryHistoryTab.tsx`** ('use client'):
    - Filter bar in a single horizontal row: Product picker (search-enabled select or text input) + date range (two `<input type="date">`) + reason code dropdown (select with ALL_REASON_CODES mapped to REASON_CODE_LABELS) + "Clear filters" ghost link (per D-11, UI-SPEC)
    - All filters in `flex items-center gap-4` row
    - Calls `getAdjustmentHistory(filters)` when filters change or on mount
    - Table columns: Date, Product, Reason, Change, New Total, Notes, User (per D-10)
    - Date: formatted with `date-fns` `format(new Date(row.created_at), 'dd MMM yyyy HH:mm')`
    - Product: `row.products.name` from join
    - Reason: `REASON_CODE_LABELS[row.reason]` — system codes show "(system)" suffix per UI-SPEC Copywriting
    - Change column: Geist Mono (`font-mono`), `+N` in `text-success` / `-N` in `text-error`
    - New Total: Geist Mono (`font-mono`)
    - Notes: DM Sans, truncated with title tooltip if > 50 chars
    - User: staff name or "System" for automated
    - Pagination: 50 rows per page. Simple prev/next controls at bottom. Show "Page X of Y".
    - Empty states per UI-SPEC Copywriting:
      - No adjustments: heading "No adjustment history", body "Manual adjustments, sales, and stocktakes will appear here once inventory activity begins."
      - Filters active no results: heading "No matching adjustments", body "Try adjusting the filters or clearing the date range.", "Clear filters" amber link

    **2. Update `src/components/admin/AdminSidebar.tsx`**:
    - Add `hasInventory?: boolean` to `AdminSidebarProps` interface
    - Conditionally include `{ href: '/admin/inventory', label: 'Inventory' }` in nav links when `hasInventory === true`
    - Position the Inventory link after Products in the nav order
    - Use inline SVG icon (heroicons-style Package/Cube icon, strokeWidth 1.5)

    **3. Update `src/app/admin/layout.tsx`** (if needed):
    - Read `hasInventory` from `user.app_metadata.inventory` (JWT claim, already set by auth hook in migration 024)
    - Pass `hasInventory={!!user?.app_metadata?.inventory}` to `AdminSidebar`

    **4. Update `src/components/admin/products/ProductFormDrawer.tsx`** (per D-01: "Stock adjustment is triggered inline from the product edit/detail view via an Adjust stock button"):
    - Accept `hasInventory?: boolean` prop
    - When `hasInventory === true` and product's `product_type === 'physical'`:
      - Make the stock_quantity input read-only (`readOnly` attribute)
      - Show helper text below: "Stock is managed through the inventory add-on." in `text-sm text-muted`
      - Add `cursor-not-allowed bg-surface` styles to the read-only input
      - **Add an "Adjust stock" button** next to or below the stock_quantity field. Style: ghost button matching the drawer's existing patterns (`border border-border bg-transparent text-sm font-bold text-navy hover:bg-surface rounded-md px-3 py-1.5`). Clicking this button opens `StockAdjustDrawer` with the current product's `{ id, name, stock_quantity }`. Import `StockAdjustDrawer` from `@/components/admin/inventory/StockAdjustDrawer`. On adjustment success (`onSuccess` callback), update the displayed stock_quantity in the drawer form state so the user sees the new value without closing and reopening the product form.
    - When `hasInventory === false` or product_type is 'service': leave stock_quantity editable as before (no Adjust stock button)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `'use client'`
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `getAdjustmentHistory`
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `REASON_CODE_LABELS`
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `No adjustment history`
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `No matching adjustments`
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `Clear filters`
    - src/components/admin/inventory/InventoryHistoryTab.tsx contains `font-mono`
    - src/components/admin/AdminSidebar.tsx contains `hasInventory`
    - src/components/admin/AdminSidebar.tsx contains `Inventory`
    - src/components/admin/AdminSidebar.tsx contains `/admin/inventory`
    - src/components/admin/products/ProductFormDrawer.tsx contains `Adjust stock`
    - src/components/admin/products/ProductFormDrawer.tsx contains `StockAdjustDrawer`
    - src/components/admin/products/ProductFormDrawer.tsx contains `readOnly` or `read-only`
    - src/components/admin/products/ProductFormDrawer.tsx contains `hasInventory`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>History tab shows filtered, paginated adjustment rows. AdminSidebar conditionally shows Inventory link. ProductFormDrawer has Adjust stock button (per D-01) that opens StockAdjustDrawer, and stock field is read-only for inventory stores.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify inventory page and adjustment flow</name>
  <files>src/app/admin/inventory/page.tsx</files>
  <action>Human verifies the complete inventory page, adjustment drawer, history tab, sidebar navigation, product form Adjust stock button (D-01), and read-only stock field.</action>
  <what-built>Complete inventory page with Stock Levels tab, History tab, adjustment drawer, sidebar navigation, Adjust stock button in product edit form (per D-01), and read-only stock field.</what-built>
  <how-to-verify>
    1. Log in as a store WITH the inventory add-on active
    2. Verify "Inventory" appears in the admin sidebar
    3. Navigate to /admin/inventory
    4. Verify three tabs visible: Stock Levels, Stocktakes, History
    5. Stock Levels tab: verify physical products listed with stock quantities in monospace font
    6. Hover over a product row — verify "Adjust stock" button appears
    7. Click "Adjust stock" — verify drawer slides open from right
    8. Toggle between "Adjust by" and "Set to" modes — verify preview updates correctly
    9. Select a reason code, enter a value, click "Save adjustment"
    10. Verify drawer closes and stock quantity updates in the table
    11. Switch to History tab — verify the adjustment just made appears as a row
    12. Test filters: select the product, a reason code — verify filtering works
    13. Go to Products, edit a physical product — verify stock_quantity field is read-only
    14. Verify "Adjust stock" button is visible in the product edit drawer (per D-01)
    15. Click "Adjust stock" in product edit — verify StockAdjustDrawer opens
    16. Make an adjustment from within product edit — verify stock_quantity updates in the product form
    17. Log in as a store WITHOUT the inventory add-on — verify "Inventory" link is NOT in sidebar and no "Adjust stock" button in product edit
  </how-to-verify>
  <verify>Manual verification by human</verify>
  <done>Inventory page, adjustment drawer, history tab, sidebar, product form Adjust stock button (D-01), and read-only stock all verified working.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. Visit /admin/inventory — page loads with three tabs
3. Stock Levels tab shows physical products with correct semantic colors
4. Adjustment drawer opens, supports both modes, saves successfully
5. History tab shows adjustment rows with filters working
6. AdminSidebar shows/hides Inventory link based on hasInventory
7. ProductFormDrawer has "Adjust stock" button for physical products when hasInventory is true (per D-01)
</verification>

<success_criteria>
- /admin/inventory page exists with three tabs (Stock Levels, Stocktakes, History)
- Stock Levels tab displays physical products with stock quantities and low-stock highlighting
- Adjustment drawer supports delta and absolute modes per D-04
- History tab shows paginated, filterable adjustment rows per D-09/D-10/D-11
- AdminSidebar conditionally shows Inventory link
- ProductFormDrawer has "Adjust stock" button opening StockAdjustDrawer per D-01
- ProductFormDrawer stock field is read-only when hasInventory is true
- All UI follows DESIGN.md and UI-SPEC tokens
</success_criteria>

<output>
After completion, create `.planning/phases/22-inventory-add-on-core/22-04-SUMMARY.md`
</output>
