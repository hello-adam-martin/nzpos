---
phase: 38-add-on-detail-pages-landing-page-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/add-ons/gift-cards/page.tsx
  - src/app/(marketing)/add-ons/advanced-reporting/page.tsx
  - src/app/(marketing)/add-ons/loyalty-points/page.tsx
autonomous: true
requirements: [MKTG-01, MKTG-02, MKTG-03]
must_haves:
  truths:
    - "Visitor can navigate to /add-ons/gift-cards and see a full detail page with hero, before/after, compliance callout, features, how-it-works, and CTA sections"
    - "Visitor can navigate to /add-ons/advanced-reporting and see a full detail page with hero, before/after, compliance callout, features, how-it-works, and CTA sections"
    - "Visitor can navigate to /add-ons/loyalty-points and see a full detail page with hero, before/after, compliance callout, features, how-it-works, and CTA sections"
    - "Gift Cards page shows NZ Fair Trading Act 2024 compliance callout with 3-year minimum expiry"
    - "Loyalty Points page shows NZ Privacy Amendment Act 2025 IPP 3A compliance callout"
    - "Each page shows correct pricing: Gift Cards $14/month, Advanced Reporting $9/month, Loyalty Points $15/month"
  artifacts:
    - path: "src/app/(marketing)/add-ons/gift-cards/page.tsx"
      provides: "Gift Cards add-on detail page"
      contains: "force-static"
      min_lines: 120
    - path: "src/app/(marketing)/add-ons/advanced-reporting/page.tsx"
      provides: "Advanced Reporting add-on detail page"
      contains: "force-static"
      min_lines: 120
    - path: "src/app/(marketing)/add-ons/loyalty-points/page.tsx"
      provides: "Loyalty Points add-on detail page"
      contains: "force-static"
      min_lines: 120
  key_links:
    - from: "src/app/(marketing)/add-ons/gift-cards/page.tsx"
      to: "/add-ons"
      via: "Back to add-ons link in hero"
      pattern: "href=\"/add-ons\""
    - from: "src/app/(marketing)/add-ons/gift-cards/page.tsx"
      to: "/signup"
      via: "Get started free CTA buttons"
      pattern: "href=\"/signup\""
    - from: "src/app/(marketing)/add-ons/advanced-reporting/page.tsx"
      to: "/signup"
      via: "Get started free CTA buttons"
      pattern: "href=\"/signup\""
    - from: "src/app/(marketing)/add-ons/loyalty-points/page.tsx"
      to: "/signup"
      via: "Get started free CTA buttons"
      pattern: "href=\"/signup\""
---

<objective>
Create three new add-on detail pages (Gift Cards, Advanced Reporting, Loyalty Points) that follow the exact same section structure as the existing Xero detail page. Each page includes a NZ compliance callout section specific to that add-on.

Purpose: Visitors need dedicated pages to evaluate each add-on before purchase. These pages are also link targets for the pricing section and comparison page (Phase 39).
Output: Three new page files under `src/app/(marketing)/add-ons/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-add-on-detail-pages-landing-page-refresh/38-CONTEXT.md
@.planning/phases/38-add-on-detail-pages-landing-page-refresh/38-UI-SPEC.md

<interfaces>
<!-- Canonical template — copy section structure exactly for all 3 new pages -->

From src/app/(marketing)/add-ons/xero/page.tsx:
```tsx
// Structure: metadata + dynamic export + data arrays + default export with sections:
// 1. Hero (navy bg): back link, h1, subtext, CTA + price note
// 2. Before/After (bg): 2-col grid, "Without" panel (X icons) + "With" panel (check icons, amber border)
// 3. Features (surface bg): h2 "What you get", 2-col grid of title+description
// 4. How It Works (bg): h2 "Three steps. Two minutes.", 3-col grid of numbered steps
// 5. CTA (navy bg): centered h2, price note, amber button

export const dynamic = 'force-static'
export const metadata: Metadata = {
  title: '[Name] — NZPOS Add-ons',
  description: '[One sentence problem + benefit]',
}
```

Key CSS patterns (MUST use these exact tokens, never raw Tailwind colors):
- `var(--color-navy)`, `var(--color-amber)`, `var(--color-bg)`, `var(--color-surface)`
- `var(--color-text)`, `var(--color-text-muted)`, `var(--color-text-light)`
- `var(--color-border)`, `var(--color-success)`
- `var(--space-xs)`, `var(--space-sm)`, `var(--space-md)`, `var(--space-lg)`, `var(--space-xl)`, `var(--space-2xl)`, `var(--space-3xl)`
- `font-display` for hero h1 only, `font-sans` for everything else
- Container: `max-w-[1200px] mx-auto px-[var(--space-md)] md:px-[var(--space-lg)]`
- Hero subtext: `text-base text-white/70` (NOT text-lg per UI-SPEC fix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Gift Cards detail page</name>
  <files>src/app/(marketing)/add-ons/gift-cards/page.tsx</files>
  <read_first>
    - src/app/(marketing)/add-ons/xero/page.tsx (canonical template — copy structure exactly)
    - .planning/phases/38-add-on-detail-pages-landing-page-refresh/38-UI-SPEC.md (visual contract)
    - .planning/phases/38-add-on-detail-pages-landing-page-refresh/38-CONTEXT.md (decisions D-01 through D-12)
    - DESIGN.md (design system reference)
  </read_first>
  <action>
Create `src/app/(marketing)/add-ons/gift-cards/page.tsx` by copying the exact section structure from `xero/page.tsx` with Gift Cards content.

**Metadata (per D-01, MKTG-01):**
```tsx
export const dynamic = 'force-static'
export const metadata: Metadata = {
  title: 'Gift Cards — NZPOS Add-ons',
  description: 'Sell and manage gift cards with automatic NZ Fair Trading Act compliance. 3-year minimum expiry tracked for you.',
}
```

**Section 1 — Hero** (identical structure to Xero hero):
- Back link: `← Back to add-ons` linking to `/add-ons`
- H1: Write a compelling headline about gift cards as a revenue channel (Claude's discretion per D-04). Kiwi SaaS tone — direct, second person.
- Subtext: `text-base text-white/70` (NOT text-lg). One sentence about the benefit.
- CTA: `Get started free` amber button + `then enable Gift Cards for $14/month NZD` price note

**Section 2 — Before/After** (per D-02 — pain points, not competitor comparisons):
- `withoutItems` array: 4 pain points about managing gift cards manually (tracking expiry dates, reconciling balances, paper vouchers getting lost, compliance headaches)
- `withItems` array: 4 matching benefits with NZPOS Gift Cards (automatic expiry tracking, real-time balance, digital cards tied to customer, FTA-compliant out of the box)
- Exact same panel structure as Xero: "Without" panel with X icons in `var(--color-text-light)`, "With" panel with checkmarks in `var(--color-success)` and amber border

**Section 3 — Compliance Callout** (NEW section per UI-SPEC, between Before/After and Features):
- Per D-10, D-11, D-12: NZ Fair Trading Act 2024 — 3-year minimum gift card expiry
- Visual: `border border-[var(--color-amber)] rounded-lg p-[var(--space-xl)] bg-[var(--color-navy)]`
- Shield SVG icon (24px, amber colored) — inline SVG with a shield path
- White heading: `font-sans text-base font-bold text-white` — "NZ Fair Trading Act 2024 Compliant"
- Body: `font-sans text-sm text-white/70` — explain that NZPOS automatically enforces the 3-year minimum expiry rule so the store owner never has to think about it
- Section wrapper: `bg-[var(--color-bg)]` with standard container padding

**Section 4 — Features** (per D-03: 4-6 features):
- H2: "What you get"
- 2-col grid (`grid-cols-1 sm:grid-cols-2 gap-[var(--space-lg)]`)
- Feature items (Claude's discretion for count 4-6, bold title + muted description):
  - Physical and digital gift cards
  - Real-time balance tracking
  - Partial redemption support
  - POS and online acceptance
  - Customer-to-customer gifting
  - Automatic expiry enforcement

**Section 5 — How It Works:**
- H2: "Three steps. Two minutes."
- 3 numbered steps in `md:grid-cols-3`:
  - Step 1: Enable — Turn on Gift Cards in Settings
  - Step 2: Sell — Ring up gift cards at the POS or let customers buy online
  - Step 3: Redeem — Scan or enter the code at checkout, balance updates instantly

**Section 6 — CTA** (navy bg, centered):
- H2: Compelling closing line about gift cards driving repeat business
- Price note: `Free to start. Add Gift Cards for $14/month when you are ready.`
- Amber `Get started free` button linking to `/signup`

All CSS must use `var(--color-*)` and `var(--space-*)` tokens. Never raw Tailwind color utilities.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && cat src/app/(marketing)/add-ons/gift-cards/page.tsx | head -5 | grep "force-static" && grep -c "var(--color-" src/app/(marketing)/add-ons/gift-cards/page.tsx | awk '$1 > 15' && grep "Fair Trading Act 2024" src/app/(marketing)/add-ons/gift-cards/page.tsx && grep '\\$14/month' src/app/(marketing)/add-ons/gift-cards/page.tsx && grep 'href="/add-ons"' src/app/(marketing)/add-ons/gift-cards/page.tsx && grep 'href="/signup"' src/app/(marketing)/add-ons/gift-cards/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/(marketing)/add-ons/gift-cards/page.tsx
    - Contains `export const dynamic = 'force-static'`
    - Contains `title: 'Gift Cards — NZPOS Add-ons'`
    - Contains `$14/month NZD` price in hero and CTA sections
    - Contains `Fair Trading Act 2024` in compliance callout section
    - Contains `3-year` reference in compliance callout
    - Contains `href="/add-ons"` back link
    - Contains at least 2 `href="/signup"` CTA links
    - Uses `var(--color-navy)` for hero/CTA backgrounds (no raw hex #1E293B)
    - Uses `var(--color-amber)` for accent elements (no raw hex #E67E22)
    - Contains `font-display` only on h1 element
    - Hero subtext uses `text-base` not `text-lg`
    - Has 6 sections: hero, before/after, compliance callout, features, how-it-works, CTA
    - withoutItems and withItems arrays each have 4 items
    - Features grid uses `sm:grid-cols-2`
    - Steps grid uses `md:grid-cols-3`
  </acceptance_criteria>
  <done>Gift Cards detail page at /add-ons/gift-cards renders with all 6 sections, correct $14/month pricing, NZ Fair Trading Act 2024 compliance callout, and links to /signup and /add-ons</done>
</task>

<task type="auto">
  <name>Task 2: Create Advanced Reporting and Loyalty Points detail pages</name>
  <files>src/app/(marketing)/add-ons/advanced-reporting/page.tsx, src/app/(marketing)/add-ons/loyalty-points/page.tsx</files>
  <read_first>
    - src/app/(marketing)/add-ons/gift-cards/page.tsx (just created in Task 1 — reference for compliance callout pattern)
    - src/app/(marketing)/add-ons/xero/page.tsx (canonical template)
    - .planning/phases/38-add-on-detail-pages-landing-page-refresh/38-UI-SPEC.md (visual contract)
    - .planning/phases/38-add-on-detail-pages-landing-page-refresh/38-CONTEXT.md (decisions)
  </read_first>
  <action>
Create two files following the exact same structure as the Gift Cards page (Task 1) with content specific to each add-on.

---

**FILE 1: `src/app/(marketing)/add-ons/advanced-reporting/page.tsx`**

Metadata:
```tsx
export const dynamic = 'force-static'
export const metadata: Metadata = {
  title: 'Advanced Reporting — NZPOS Add-ons',
  description: 'COGS tracking, profit margins, and detailed sales analytics for NZ retail businesses.',
}
```

Hero: H1 about knowing your real margins (Claude's discretion). Price: `$9/month NZD`.

Before/After (per D-02):
- withoutItems: 4 pain points (guessing margins, spreadsheet tracking, no COGS visibility, end-of-month surprises)
- withItems: 4 benefits (real-time margin data, automatic COGS calculation, product profitability ranking, confident purchasing decisions)

Compliance Callout (per D-10 — contextual, not a legal act):
- Visual: same `bg-[var(--color-navy)] border border-[var(--color-amber)]` pattern as Gift Cards
- Shield icon (amber, 24px inline SVG)
- Heading: "Built for NZ Retail" or similar
- Body: explain COGS and margin tracking designed for NZ business practices, GST-aware calculations
- This is a contextual credibility callout, not a specific act reference

Features (4-6 per D-03):
- COGS tracking per product
- Real-time profit margins
- Sales by category/period
- Product performance ranking
- Export to CSV
- GST-inclusive margin calculations

How It Works: 3 steps — Enable in Settings, Set cost prices on products, View reports in dashboard

CTA: Closing line about knowing your numbers. Price: `$9/month`.

---

**FILE 2: `src/app/(marketing)/add-ons/loyalty-points/page.tsx`**

Metadata:
```tsx
export const dynamic = 'force-static'
export const metadata: Metadata = {
  title: 'Loyalty Points — NZPOS Add-ons',
  description: 'Reward repeat customers with points they can spend in-store and online. NZ Privacy Act compliant.',
}
```

Hero: H1 about turning one-time buyers into regulars (Claude's discretion). Price: `$15/month NZD`.

Before/After (per D-02):
- withoutItems: 4 pain points (no way to reward loyalty, paper stamp cards, no customer purchase history, losing regulars to competitors)
- withItems: 4 benefits (automatic points on every sale, customers see balance at checkout, redeem points in-store or online, purchase history drives smarter promotions)

Compliance Callout (per D-10, D-11):
- NZ Privacy Amendment Act 2025, IPP 3A consent
- Same visual pattern: `bg-[var(--color-navy)] border border-[var(--color-amber)]`
- Shield icon (amber, 24px)
- Heading: "NZ Privacy Amendment Act 2025 Compliant"
- Body: explain that NZPOS handles IPP 3A consent requirements automatically — customers explicitly opt in to the loyalty programme, and their data is handled per NZ privacy law

Features (4-6 per D-03):
- Automatic points earning on purchases
- Points redemption at POS and online
- Customer-facing balance display
- Configurable earn/redeem rates
- Points expiry rules
- Opt-in consent management

How It Works: 3 steps — Enable in Settings, Set points earn rate, Customers earn and redeem automatically

CTA: Closing line about customers who come back. Price: `$15/month`.

---

Both pages must:
- Use identical section structure to Gift Cards page (6 sections)
- Use only `var(--color-*)` and `var(--space-*)` CSS tokens
- Use `font-display` only on h1
- Use `text-base` (not `text-lg`) for hero subtext
- Link to `/signup` and `/add-ons`
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep "force-static" src/app/(marketing)/add-ons/advanced-reporting/page.tsx && grep "force-static" src/app/(marketing)/add-ons/loyalty-points/page.tsx && grep '\\$9/month' src/app/(marketing)/add-ons/advanced-reporting/page.tsx && grep '\\$15/month' src/app/(marketing)/add-ons/loyalty-points/page.tsx && grep "Privacy Amendment Act 2025" src/app/(marketing)/add-ons/loyalty-points/page.tsx && grep "Built for NZ" src/app/(marketing)/add-ons/advanced-reporting/page.tsx && grep 'href="/signup"' src/app/(marketing)/add-ons/advanced-reporting/page.tsx && grep 'href="/signup"' src/app/(marketing)/add-ons/loyalty-points/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/(marketing)/add-ons/advanced-reporting/page.tsx
    - File exists at src/app/(marketing)/add-ons/loyalty-points/page.tsx
    - Advanced Reporting contains `export const dynamic = 'force-static'`
    - Advanced Reporting contains `title: 'Advanced Reporting — NZPOS Add-ons'`
    - Advanced Reporting contains `$9/month NZD` price
    - Advanced Reporting contains COGS-related compliance/contextual callout with navy bg and amber border
    - Loyalty Points contains `export const dynamic = 'force-static'`
    - Loyalty Points contains `title: 'Loyalty Points — NZPOS Add-ons'`
    - Loyalty Points contains `$15/month NZD` price
    - Loyalty Points contains `Privacy Amendment Act 2025` in compliance callout
    - Loyalty Points contains `IPP 3A` reference
    - Both contain `href="/add-ons"` back link
    - Both contain at least 2 `href="/signup"` CTA links
    - Both use `var(--color-navy)` and `var(--color-amber)` tokens (no raw hex)
    - Both have `font-display` only on h1
    - Both have hero subtext at `text-base` not `text-lg`
    - Both have 6 sections matching Gift Cards structure
  </acceptance_criteria>
  <done>Advanced Reporting page at /add-ons/advanced-reporting shows $9/month pricing with COGS contextual callout. Loyalty Points page at /add-ons/loyalty-points shows $15/month pricing with NZ Privacy Amendment Act 2025 compliance callout. Both follow identical section structure to Gift Cards and Xero pages.</done>
</task>

</tasks>

<verification>
All 3 new add-on detail pages:
1. Follow the exact 6-section structure from xero/page.tsx (hero, before/after, compliance callout, features, how-it-works, CTA)
2. Use only CSS custom property tokens (no raw Tailwind colors)
3. Include `export const dynamic = 'force-static'`
4. Include correct metadata with title and description
5. Show correct pricing ($14, $9, $15 respectively)
6. Include NZ compliance callouts with specific act names/years where applicable
7. Link to /signup (CTA) and /add-ons (back link)
8. Use font-display only on h1, font-sans everywhere else
9. Hero subtext uses text-base (not text-lg)

Build check:
```bash
npx next build 2>&1 | tail -20
```
All 3 routes should appear in build output as static pages.
</verification>

<success_criteria>
- /add-ons/gift-cards renders with Gift Cards content, $14/month, Fair Trading Act 2024 callout
- /add-ons/advanced-reporting renders with Advanced Reporting content, $9/month, COGS contextual callout
- /add-ons/loyalty-points renders with Loyalty Points content, $15/month, Privacy Amendment Act 2025 callout
- All pages pass `next build` as static pages
- All pages follow identical section structure to existing Xero detail page
</success_criteria>

<output>
After completion, create `.planning/phases/38-add-on-detail-pages-landing-page-refresh/38-01-SUMMARY.md`
</output>
