---
phase: 34-signup-conversion-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/components/LandingHero.tsx
  - src/app/(marketing)/components/LandingCTA.tsx
  - src/app/__tests__/LandingHero.test.tsx
autonomous: true
requirements: [LAND-01, LAND-02]

must_haves:
  truths:
    - "The landing page hero section has a visible 'Try POS Demo' button"
    - "The demo button navigates to /demo/pos"
    - "The amber 'Get started free' button remains the primary visual CTA"
    - "The ghost demo button is visually secondary (outlined, not filled)"
  artifacts:
    - path: "src/app/(marketing)/components/LandingHero.tsx"
      provides: "Secondary ghost CTA button linking to /demo/pos"
      contains: "/demo/pos"
    - path: "src/app/(marketing)/components/LandingCTA.tsx"
      provides: "Optional secondary demo text link"
      contains: "/demo/pos"
    - path: "src/app/__tests__/LandingHero.test.tsx"
      provides: "Unit tests for LAND-01, LAND-02"
      exports: []
  key_links:
    - from: "src/app/(marketing)/components/LandingHero.tsx"
      to: "/demo/pos"
      via: "Next.js Link href"
      pattern: 'href="/demo/pos"'
---

<objective>
Add a "Try POS Demo" ghost button to the landing page hero section alongside the existing "Get started free" primary CTA, and optionally a text link in the bottom CTA section.

Purpose: Give prospective merchants a one-click path from the landing page to the interactive POS demo, increasing engagement and conversion.
Output: Updated LandingHero with dual CTAs, updated LandingCTA with demo link, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-signup-conversion-landing-page/34-CONTEXT.md
@.planning/phases/34-signup-conversion-landing-page/34-RESEARCH.md

@src/app/(marketing)/components/LandingHero.tsx
@src/app/(marketing)/components/LandingCTA.tsx
@DESIGN.md

<interfaces>
<!-- Current LandingHero structure (full file, 64 lines) -->

From src/app/(marketing)/components/LandingHero.tsx:
```typescript
// Server component, no props
// Left column contains: h1, p, Link to /signup (amber), p (no credit card)
// Right column contains: iPad mockup illustration (desktop only)
// Section uses bg-[var(--color-navy)]
```

From src/app/(marketing)/components/LandingCTA.tsx:
```typescript
// Server component, no props
// Single centered section with h2, Link to /signup (amber), p (set up in 5 min)
// Section uses bg-[var(--color-navy)]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ghost "Try POS Demo" button to LandingHero with tests</name>
  <files>
    src/app/(marketing)/components/LandingHero.tsx,
    src/app/__tests__/LandingHero.test.tsx
  </files>
  <read_first>
    src/app/(marketing)/components/LandingHero.tsx,
    DESIGN.md
  </read_first>
  <behavior>
    - Test 1: LandingHero renders a link with text "Try POS Demo" and href="/demo/pos"
    - Test 2: LandingHero still renders a link with text "Get started free" and href="/signup"
    - Test 3: The "Try POS Demo" link has aria-label containing "demo"
  </behavior>
  <action>
    **Phase 1 (RED): Create test file first.**

    Create `src/app/__tests__/LandingHero.test.tsx`. Mock `next/link` as a simple anchor rendering children with href. Import and render `LandingHero`. Write 3 tests:

    ```typescript
    import { render, screen } from '@testing-library/react'
    import { describe, it, expect, vi } from 'vitest'
    import LandingHero from '../(marketing)/components/LandingHero'

    vi.mock('next/link', () => ({
      default: ({ href, children, ...props }: any) => <a href={href} {...props}>{children}</a>,
    }))

    describe('LandingHero', () => {
      it('renders Try POS Demo link to /demo/pos', () => {
        render(<LandingHero />)
        const demoLink = screen.getByRole('link', { name: /demo/i })
        expect(demoLink).toHaveAttribute('href', '/demo/pos')
      })

      it('still renders Get started free link to /signup', () => {
        render(<LandingHero />)
        const signupLink = screen.getByRole('link', { name: /get started free/i })
        expect(signupLink).toHaveAttribute('href', '/signup')
      })

      it('Try POS Demo link has accessible label', () => {
        render(<LandingHero />)
        const demoLink = screen.getByRole('link', { name: /demo/i })
        expect(demoLink).toBeTruthy()
      })
    })
    ```

    Run tests — Test 1 and Test 3 must FAIL (no demo link exists yet).

    **Phase 2 (GREEN): Add ghost button to LandingHero.**

    In `src/app/(marketing)/components/LandingHero.tsx`, replace the existing single-button layout in the left column with a flex-wrapped dual-CTA row. Replace lines 16-23 (the Link + subtitle paragraph) with:

    ```tsx
    {/* CTA buttons — per D-07: two CTAs in hero, per D-08: ghost outlined secondary */}
    <div className="flex flex-wrap gap-[var(--space-sm)] mt-[var(--space-lg)] items-center">
      <Link
        href="/signup"
        className="inline-block bg-[var(--color-amber)] text-white px-[var(--space-xl)] py-[var(--space-sm)] rounded-md text-sm font-bold hover:opacity-90 transition-opacity duration-150"
        aria-label="Get started free — hero call to action"
      >
        Get started free
      </Link>
      <Link
        href="/demo/pos"
        className="inline-block border border-white/70 text-white px-[var(--space-xl)] py-[var(--space-sm)] rounded-md text-sm font-bold hover:bg-white/10 transition-colors duration-150"
        aria-label="Try the POS demo"
      >
        Try POS Demo
      </Link>
    </div>
    <p className="text-sm text-white/50 mt-[var(--space-sm)]">
      No credit card required. NZD pricing.
    </p>
    ```

    The amber button is identical to the existing one (per D-07, primary action). The ghost button uses white border on navy background per D-08 with `border-white/70` and `hover:bg-white/10`. Navigation to `/demo/pos` via standard `<Link>` per D-09.

    Run tests — all 3 must PASS.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npm test -- --reporter=verbose src/app/__tests__/LandingHero.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - LandingHero.tsx contains `href="/demo/pos"` in a Link element
    - LandingHero.tsx contains text "Try POS Demo"
    - LandingHero.tsx contains `aria-label="Try the POS demo"`
    - LandingHero.tsx contains `border border-white/70` (ghost button styling)
    - LandingHero.tsx still contains `href="/signup"` (existing primary CTA preserved)
    - LandingHero.tsx contains `flex flex-wrap gap-` (dual-CTA row wrapper)
    - LandingHero.test.tsx exists and contains at least 3 test cases
    - `npm test -- src/app/__tests__/LandingHero.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    LandingHero has two side-by-side CTAs: amber "Get started free" (primary) and ghost "Try POS Demo" (secondary). Ghost button navigates to /demo/pos. All 3 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add demo text link to LandingCTA bottom section</name>
  <files>
    src/app/(marketing)/components/LandingCTA.tsx
  </files>
  <read_first>
    src/app/(marketing)/components/LandingCTA.tsx
  </read_first>
  <action>
    In `src/app/(marketing)/components/LandingCTA.tsx`, add `import Link from 'next/link'` if not already a named import (it currently imports as default — confirm). Then add a secondary text link below the existing subtitle paragraph. After the `<p>` tag with "Set up in under 5 minutes. No credit card needed." add:

    ```tsx
    <p className="font-sans text-sm text-white/60 mt-[var(--space-xs)]">
      Want to try it first?{' '}
      <Link
        href="/demo/pos"
        className="underline hover:text-white/80 transition-colors duration-150"
      >
        Try the POS demo
      </Link>
    </p>
    ```

    This is a subtle text link (Claude's Discretion item) — not a full ghost button. Uses `text-white/60` to match the existing subtitle color and `underline` for discoverability. Supports users who scroll to the bottom CTA section before deciding.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - LandingCTA.tsx contains `href="/demo/pos"` in a Link element
    - LandingCTA.tsx contains text "Try the POS demo"
    - LandingCTA.tsx contains `underline` class on the demo link
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    LandingCTA bottom section has a subtle "Try the POS demo" text link below the existing subtitle. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- src/app/__tests__/LandingHero.test.tsx` — all 3 tests pass
2. `npx tsc --noEmit` — no type errors across codebase
3. `npm test` — full test suite passes (no regressions)
</verification>

<success_criteria>
- Landing page hero has two CTAs: amber "Get started free" and ghost "Try POS Demo"
- Ghost button uses outlined style (white border on navy) per D-08
- "Try POS Demo" navigates to /demo/pos via Next.js Link
- Visual hierarchy preserved: amber = primary, outlined = secondary
- LandingCTA has subtle demo text link for bottom-of-page visitors
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-signup-conversion-landing-page/34-02-SUMMARY.md`
</output>
