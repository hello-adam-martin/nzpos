---
phase: 13-merchant-self-serve-signup
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/actions/auth/ownerSignup.ts
  - src/actions/auth/checkSlugAvailability.ts
  - src/actions/auth/retryProvisioning.ts
  - src/actions/auth/resendVerification.ts
  - src/app/api/auth/callback/route.ts
  - src/middleware.ts
  - src/actions/auth/__tests__/ownerSignup.test.ts
  - src/middleware.test.ts
autonomous: true
requirements: [SIGNUP-01, SIGNUP-02, SIGNUP-03, SIGNUP-05]

must_haves:
  truths:
    - "ownerSignup creates auth user via Supabase, then calls provision_store RPC via admin client, and deletes auth user if RPC fails"
    - "checkSlugAvailability returns available/taken/reserved status without requiring authentication"
    - "retryProvisioning re-runs the RPC for an authenticated user whose store provisioning failed"
    - "Middleware redirects unverified users on /admin routes to /signup/verify-email on root domain"
    - "Auth callback exchanges PKCE code, looks up store slug, and redirects to {slug}.{domain}/admin/dashboard"
    - "resendVerification points emailRedirectTo at the root domain /api/auth/callback"
    - "ownerSignup enforces rate limiting (5 per IP per hour) and 1-store-per-email before any DB write"
  artifacts:
    - path: "src/actions/auth/ownerSignup.ts"
      provides: "Atomic signup Server Action"
      exports: ["ownerSignup"]
    - path: "src/actions/auth/checkSlugAvailability.ts"
      provides: "Live slug check Server Action"
      exports: ["checkSlugAvailability"]
    - path: "src/actions/auth/retryProvisioning.ts"
      provides: "Retry provisioning Server Action"
      exports: ["retryProvisioning"]
    - path: "src/app/api/auth/callback/route.ts"
      provides: "PKCE callback with subdomain redirect"
      exports: ["GET"]
    - path: "src/middleware.ts"
      provides: "Email verification gate on admin routes"
      contains: "email_confirmed_at"
  key_links:
    - from: "src/actions/auth/ownerSignup.ts"
      to: "provision_store RPC"
      via: "admin.rpc('provision_store', ...)"
      pattern: "rpc\\('provision_store'"
    - from: "src/actions/auth/ownerSignup.ts"
      to: "src/lib/signupRateLimit.ts"
      via: "checkRateLimit(ip)"
      pattern: "checkRateLimit"
    - from: "src/actions/auth/ownerSignup.ts"
      to: "src/lib/slugValidation.ts"
      via: "SlugSchema in Zod object"
      pattern: "SlugSchema"
    - from: "src/middleware.ts"
      to: "supabase.auth.getUser()"
      via: "email_confirmed_at check"
      pattern: "email_confirmed_at"
    - from: "src/app/api/auth/callback/route.ts"
      to: "stores table"
      via: "admin.from('stores').select('slug').eq('owner_auth_id', ...)"
      pattern: "owner_auth_id"
---

<objective>
Build all Server Actions (ownerSignup, checkSlugAvailability, retryProvisioning), update resendVerification, create the auth callback route, and add the email verification gate to middleware.

Purpose: Wire the provisioning RPC and validation utilities from Plan 01 into the Server Actions and middleware that the UI screens in Plan 03 will call.
Output: Complete server-side signup pipeline — form submission through provisioning through email verification through dashboard redirect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-merchant-self-serve-signup/13-CONTEXT.md
@.planning/phases/13-merchant-self-serve-signup/13-RESEARCH.md
@.planning/phases/13-merchant-self-serve-signup/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (src/lib/slugValidation.ts): -->
export const RESERVED_SLUGS: readonly string[]
export const SLUG_REGEX: RegExp
export function validateSlug(slug: string): { valid: boolean; reason?: string }
export function slugify(name: string): string
export const SlugSchema: z.ZodString  // Zod schema with min/max/regex/refine

<!-- From Plan 01 (src/lib/signupRateLimit.ts): -->
export function checkRateLimit(ip: string): { allowed: boolean; remaining: number }

<!-- From existing codebase (src/lib/supabase/admin.ts): -->
export function createSupabaseAdminClient(): SupabaseClient<Database>

<!-- From existing codebase (src/lib/supabase/server.ts): -->
export async function createSupabaseServerClient(): Promise<SupabaseClient<Database>>

<!-- From existing codebase (src/lib/supabase/middleware.ts): -->
export async function createSupabaseMiddlewareClient(request: NextRequest): Promise<{ supabase: SupabaseClient; response: NextResponse }>

<!-- provision_store RPC signature (from 017 migration): -->
-- provision_store(p_auth_user_id UUID, p_store_name TEXT, p_slug TEXT, p_owner_email TEXT) RETURNS JSONB
-- Returns: { "store_id": UUID, "slug": TEXT }
-- Errors: "SLUG_TAKEN:{slug}" or "PROVISION_FAILED:{message}"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ownerSignup + create checkSlugAvailability + retryProvisioning + update resendVerification</name>
  <files>
    src/actions/auth/ownerSignup.ts,
    src/actions/auth/checkSlugAvailability.ts,
    src/actions/auth/retryProvisioning.ts,
    src/actions/auth/resendVerification.ts
  </files>
  <read_first>
    src/actions/auth/ownerSignup.ts,
    src/actions/auth/resendVerification.ts,
    src/lib/slugValidation.ts,
    src/lib/signupRateLimit.ts,
    src/lib/supabase/admin.ts,
    src/lib/supabase/server.ts,
    src/types/database.ts,
    .planning/phases/13-merchant-self-serve-signup/13-RESEARCH.md
  </read_first>
  <action>
    REWRITE src/actions/auth/ownerSignup.ts (per D-01, D-04, D-05, D-06, D-12):
    - 'use server' directive at top
    - Import: createSupabaseServerClient, createSupabaseAdminClient, z, headers from 'next/headers'
    - Import: SlugSchema from '@/lib/slugValidation', checkRateLimit from '@/lib/signupRateLimit'
    - Define OwnerSignupSchema = z.object({ email: z.string().email(), password: z.string().min(8).max(72), storeName: z.string().min(1).max(100), slug: SlugSchema })
    - Return type: Promise<{ error?: Record<string, string[]>; success?: boolean; slug?: string }>
    - Steps:
      1. Extract IP from headers: `const headersList = await headers(); const ip = headersList.get('x-forwarded-for')?.split(',')[0]?.trim() ?? '127.0.0.1'`
      2. Rate limit check: `const rateCheck = checkRateLimit(ip); if (!rateCheck.allowed) return { error: { _form: ['Too many signup attempts. Please wait an hour and try again.'] } }`
      3. Parse form data with safeParse: email, password, storeName, slug from formData.get()
      4. If parse fails, return `{ error: parsed.error.flatten().fieldErrors }`
      5. Check 1-store-per-email: `const admin = createSupabaseAdminClient(); const { data: existingStore } = await admin.from('stores').select('id').eq('owner_auth_id', ???)` — BUT we don't have the auth_user_id yet. Instead, check after auth.signUp: if signUp returns "User already registered", return `{ error: { email: ['An account already exists for this email.'] } }`
      6. Create auth user: `const supabase = await createSupabaseServerClient(); const { data: authData, error: authError } = await supabase.auth.signUp({ email, password, options: { emailRedirectTo: \`\${process.env.NEXT_PUBLIC_SITE_URL}/api/auth/callback\` } })`
      7. If authError or !authData.user, return appropriate field error
      8. Call RPC: `const { data, error: rpcError } = await admin.rpc('provision_store', { p_auth_user_id: authData.user.id, p_store_name: parsed.data.storeName, p_slug: parsed.data.slug, p_owner_email: parsed.data.email })`
      9. If rpcError: delete orphaned auth user `await admin.auth.admin.deleteUser(authData.user.id)`. Check if error message includes 'SLUG_TAKEN' and return `{ error: { slug: ['{slug} is taken - try another'] } }`, else return `{ error: { _form: ['Something went wrong. Please try again.'] } }`
      10. Refresh session: `await supabase.auth.refreshSession()` — forces auth hook to inject store_id + role claims
      11. Return `{ success: true, slug: parsed.data.slug }`
    - Do NOT call redirect() — the client page handles navigation to /signup/provisioning

    CREATE src/actions/auth/checkSlugAvailability.ts (per D-02):
    - 'use server' directive
    - Import: createSupabaseAdminClient, validateSlug from '@/lib/slugValidation'
    - Export async function checkSlugAvailability(slug: string): Promise<{ available: boolean; reason?: string }>
    - First call validateSlug(slug) — if invalid, return { available: false, reason: validation.reason }
    - Then query: admin.from('stores').select('id').eq('slug', slug).maybeSingle()
    - Return { available: data === null }

    CREATE src/actions/auth/retryProvisioning.ts (per D-06):
    - 'use server' directive
    - Import: createSupabaseServerClient, createSupabaseAdminClient
    - Export async function retryProvisioning(): Promise<{ success?: boolean; slug?: string; error?: string }>
    - Get current user: supabase.auth.getUser() — if no user, return { error: 'Not authenticated' }
    - Check if user already has a store: admin.from('stores').select('id, slug').eq('owner_auth_id', user.id).maybeSingle()
    - If store exists, return { success: true, slug: store.slug } (already provisioned)
    - If no store, need storeName and slug from... The retry case means the form was submitted but RPC failed. Store the slug in a cookie or session metadata during initial signup. SIMPLER: accept slug and storeName as parameters.
    - REVISED: Export async function retryProvisioning(formData: FormData): Promise<...>
    - Parse storeName + slug from formData (validated with Zod)
    - Call admin.rpc('provision_store', { p_auth_user_id: user.id, p_store_name, p_slug, p_owner_email: user.email })
    - On success: await supabase.auth.refreshSession(), return { success: true, slug }
    - On failure: return { error: rpcError.message.includes('SLUG_TAKEN') ? 'Slug taken' : 'Provisioning failed. Please try again.' }

    UPDATE src/actions/auth/resendVerification.ts (per D-09):
    - Change emailRedirectTo from `${process.env.NEXT_PUBLIC_SITE_URL}/account/callback` to `${process.env.NEXT_PUBLIC_SITE_URL}/api/auth/callback`
    - This ensures the verification link routes through the root-domain callback that does subdomain redirect
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/auth/ownerSignup.ts contains "'use server'"
    - src/actions/auth/ownerSignup.ts contains "rpc('provision_store'"
    - src/actions/auth/ownerSignup.ts contains "checkRateLimit"
    - src/actions/auth/ownerSignup.ts contains "SlugSchema"
    - src/actions/auth/ownerSignup.ts contains "admin.auth.admin.deleteUser"
    - src/actions/auth/ownerSignup.ts contains "emailRedirectTo"
    - src/actions/auth/ownerSignup.ts contains "refreshSession"
    - src/actions/auth/ownerSignup.ts contains "SLUG_TAKEN"
    - src/actions/auth/checkSlugAvailability.ts contains "'use server'"
    - src/actions/auth/checkSlugAvailability.ts contains "validateSlug"
    - src/actions/auth/checkSlugAvailability.ts contains "maybeSingle"
    - src/actions/auth/retryProvisioning.ts contains "'use server'"
    - src/actions/auth/retryProvisioning.ts contains "rpc('provision_store'"
    - src/actions/auth/retryProvisioning.ts contains "refreshSession"
    - src/actions/auth/resendVerification.ts contains "/api/auth/callback"
    - npx tsc --noEmit exits 0 (or only pre-existing type errors)
  </acceptance_criteria>
  <done>All four Server Actions compile. ownerSignup validates input with Zod + SlugSchema, rate-limits by IP, creates auth user with emailRedirectTo, calls provision_store RPC, deletes orphaned auth user on RPC failure, refreshes session on success. checkSlugAvailability validates slug format + reserved list + DB uniqueness. retryProvisioning re-runs RPC for authenticated users. resendVerification points to root-domain callback.</done>
</task>

<task type="auto">
  <name>Task 2: Auth callback route + middleware email verification gate + test stubs</name>
  <files>
    src/app/api/auth/callback/route.ts,
    src/middleware.ts,
    src/actions/auth/__tests__/ownerSignup.test.ts,
    src/middleware.test.ts
  </files>
  <read_first>
    src/middleware.ts,
    src/app/(store)/account/callback/route.ts,
    src/lib/supabase/server.ts,
    src/lib/supabase/admin.ts,
    src/lib/supabase/middleware.ts,
    .planning/phases/13-merchant-self-serve-signup/13-RESEARCH.md,
    .planning/phases/13-merchant-self-serve-signup/13-VALIDATION.md
  </read_first>
  <action>
    CREATE src/app/api/auth/callback/route.ts (per D-08, Pattern 2 from RESEARCH.md):
    - Import: createSupabaseServerClient, createSupabaseAdminClient, NextRequest, NextResponse
    - Export async function GET(request: NextRequest):
      1. Extract `code` from searchParams
      2. If no code, redirect to `/signup?error=verification_failed`
      3. Exchange code for session: `const supabase = await createSupabaseServerClient(); const { data, error } = await supabase.auth.exchangeCodeForSession(code)`
      4. If error or no user, redirect to `/signup?error=verification_failed`
      5. Look up store slug: `const admin = createSupabaseAdminClient(); const { data: store } = await admin.from('stores').select('slug').eq('owner_auth_id', data.user.id).single()`
      6. If store?.slug: construct redirect URL:
         - `const rootDomain = process.env.ROOT_DOMAIN ?? 'lvh.me:3000'`
         - `const protocol = rootDomain.includes('localhost') || rootDomain.includes('lvh.me') ? 'http' : 'https'`
         - `return NextResponse.redirect(\`\${protocol}://\${store.slug}.\${rootDomain}/admin/dashboard\`)`
      7. If no store slug found (edge case), redirect to `/signup?error=no_store`

    UPDATE src/middleware.ts (per D-07, Pattern 3 from RESEARCH.md):
    - In the admin routes branch (line 58, `if (pathname.startsWith('/admin'))`), AFTER the `if (!user)` redirect check and BEFORE the session/role check:
    - Add email verification gate:
      ```
      // D-07: Email verification gate — unverified owners cannot access admin
      const emailVerified = user.email_confirmed_at != null
      if (!emailVerified) {
        const rootDomain = process.env.ROOT_DOMAIN ?? 'lvh.me:3000'
        const protocol = rootDomain.includes('localhost') || rootDomain.includes('lvh.me') ? 'http' : 'https'
        const verifyUrl = new URL(`${protocol}://${rootDomain}/signup/verify-email`)
        verifyUrl.searchParams.set('email', user.email ?? '')
        return NextResponse.redirect(verifyUrl)
      }
      ```
    - This goes between the `if (!user)` block (line 64-68) and the `await supabase.auth.getSession()` call (line 70)
    - IMPORTANT: Only in the /admin branch. Do NOT add to POS, storefront, or API routes (per Pitfall 7)

    CREATE src/actions/auth/__tests__/ownerSignup.test.ts (Wave 0 test stub per VALIDATION.md):
    - Import ownerSignup (will need to mock Supabase clients)
    - describe('ownerSignup') with test stubs:
      - 'rejects invalid email' — safeParse fails
      - 'rejects password under 8 chars'
      - 'rejects reserved slug' — returns slug field error
      - 'rejects rate-limited IP' — mock checkRateLimit to return { allowed: false }
      - 'calls provision_store RPC on successful signup' — mock admin.rpc
      - 'deletes auth user if RPC fails' — mock rpc to throw, verify deleteUser called
    - Use vi.mock for '@/lib/supabase/server', '@/lib/supabase/admin', '@/lib/signupRateLimit'
    - Tests should be implementable (not just stubs) — mock the imports and assert behavior

    CREATE src/middleware.test.ts (Wave 0 test stub per VALIDATION.md):
    - Test the email verification gate logic
    - describe('middleware email verification gate'):
      - 'redirects unverified user on /admin to /signup/verify-email'
      - 'allows verified user through to /admin'
      - 'does not affect storefront routes'
    - Mock createSupabaseMiddlewareClient to return a mock supabase with getUser returning { user: { email_confirmed_at: null } } or { email_confirmed_at: '2026-01-01' }
    - These are unit-level tests of the middleware logic
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30 && npm run test -- --reporter=verbose src/middleware.test.ts src/actions/auth/__tests__/ownerSignup.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/auth/callback/route.ts contains "export async function GET"
    - src/app/api/auth/callback/route.ts contains "exchangeCodeForSession"
    - src/app/api/auth/callback/route.ts contains "owner_auth_id"
    - src/app/api/auth/callback/route.ts contains "/admin/dashboard"
    - src/middleware.ts contains "email_confirmed_at"
    - src/middleware.ts contains "/signup/verify-email"
    - src/middleware.ts contains the email_confirmed_at check INSIDE the pathname.startsWith('/admin') block
    - src/actions/auth/__tests__/ownerSignup.test.ts contains "describe('ownerSignup'"
    - src/actions/auth/__tests__/ownerSignup.test.ts contains "provision_store"
    - src/actions/auth/__tests__/ownerSignup.test.ts contains "deleteUser"
    - src/middleware.test.ts contains "email_confirmed_at"
    - src/middleware.test.ts contains "verify-email"
    - npx tsc --noEmit exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>Auth callback route exchanges PKCE code and redirects to store subdomain. Middleware blocks unverified users from /admin and redirects to /signup/verify-email on root domain. Test stubs cover ownerSignup (validation, rate limiting, RPC call, orphan cleanup) and middleware email gate (verified/unverified/storefront bypass). All new code compiles.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Server Action tests pass: `npm run test -- src/actions/auth/__tests__/ownerSignup.test.ts`
- Middleware tests pass: `npm run test -- src/middleware.test.ts`
- Full test suite: `npm run test`
- Auth callback route exists at src/app/api/auth/callback/route.ts
- Middleware contains email_confirmed_at check in /admin branch only
</verification>

<success_criteria>
- ownerSignup creates auth user, calls provision_store RPC, handles SLUG_TAKEN, deletes orphaned auth user on failure, rate-limits by IP
- checkSlugAvailability validates format + reserved list + DB uniqueness
- retryProvisioning re-runs RPC for authenticated users
- resendVerification redirects to root-domain callback
- Auth callback exchanges PKCE code and redirects to {slug}.{domain}/admin/dashboard
- Middleware blocks unverified users from /admin routes only
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-merchant-self-serve-signup/13-02-SUMMARY.md`
</output>
