---
phase: 13
slug: merchant-self-serve-signup
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-03
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for Phase 13: Merchant Self-Serve Signup.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind v4 with custom @theme tokens |
| Preset | not applicable |
| Component library | none (custom components, Radix not in use) |
| Icon library | none established — use inline SVG or Heroicons (consistent with existing admin components) |
| Font | Satoshi (display) + DM Sans (body/UI) — loaded via Bunny Fonts CDN |

Source: DESIGN.md + globals.css

---

## Spacing Scale

Declared values from globals.css (--space-* custom properties):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline element padding |
| sm | 8px | Compact element spacing, label-to-input gap |
| md | 16px | Default form field spacing, card internal padding |
| lg | 24px | Card padding, section spacing within a panel |
| xl | 32px | Between major form sections |
| 2xl | 48px | Page vertical centering padding |
| 3xl | 64px | Not used on this phase (no full-page layouts with large breaks) |

Exceptions:
- Minimum touch target for interactive elements: 44x44px (DESIGN.md POS-specific rule does not apply here — desktop form context, but maintain 44px height on all buttons and inputs for mobile usability on the signup flow)
- Slug availability indicator inline spacing: 8px left of the input trailing icon area

---

## Typography

Source: DESIGN.md full type scale, confirmed in globals.css @theme tokens.

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Body | 16px (base) | 400 (regular) | 1.5 | DM Sans |
| Label | 14px (sm) | 600 (semibold) | 1.4 | DM Sans |
| Heading | 24px (2xl) | 600 (semibold) | 1.2 | Satoshi |
| Display (page title / brand mark) | 30px (3xl) | 600 (semibold) | 1.1 | Satoshi |

Additional type rules for this phase:
- Helper text beneath form inputs: 14px (sm), weight 400, color `--color-text-muted` — visually distinguished from labels by muted color alone, not by a separate size step
- Slug availability status text: 14px (sm), weight 600, color `--color-success` (available) or `--color-error` (taken/invalid)
- Provisioning step labels: 14px (sm), weight 600 (active) or weight 400 (pending/complete), color `--color-text` (active) or `--color-text-muted` (pending/complete)
- Error alert body: 14px (sm), weight 400, color `--color-error`
- Rate-limit copy: 14px (sm), weight 400, color `--color-text-muted`

Declared sizes (4 total): 14px, 16px, 24px, 30px.
Declared weights (2 total): 400 (regular), 600 (semibold).

---

## Color

Source: DESIGN.md + globals.css @theme tokens.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #FAFAF9 (`--color-bg`) | Page background, full-screen loading screen background |
| Secondary (30%) | #FFFFFF (`--color-card`) | Signup card, verification card, provisioning panel — all white elevated surfaces |
| Accent (10%) | #E67E22 (`--color-amber`) | Primary CTA button only ("Create my store") |
| Destructive | #DC2626 (`--color-error`) | Inline field validation errors, provisioning failure alert |

Accent reserved for:
- "Create my store" primary submit button (and its loading state)
- No other elements on this phase use amber

Additional semantic usage on this phase:
- `--color-success` (#059669): Slug available checkmark indicator
- `--color-navy` (#1E293B): Secondary action button ("Resend email"), card headings, link text
- `--color-text-muted` (#78716C): Helper text, placeholder text, provisioning step labels (inactive)
- `--color-border` (#E7E5E4): Input borders (default), card borders
- Focus ring: `--color-navy` at 1px ring — consistent with login page pattern

---

## Screens & Interaction States

This phase introduces 3 screens. Each has distinct states the executor must implement.

### Screen 1: Signup Form (`/signup`)

Layout: Full-screen centered card. Same pattern as existing `/login` page.
Card width: max-w-sm (384px). Card padding: 32px (xl).

**Fields (in order):**
1. Email — type=email, autocomplete=email
2. Password — type=password, autocomplete=new-password, minimum 8 chars helper text
3. Store Name — type=text, autocomplete=organization, placeholder "My Supply Store"
4. Store Slug — type=text, autocomplete=off, pattern validation inline

**Slug field behaviour (D-01, D-02):**
- Auto-populates from store name on blur (slugify: lowercase, replace spaces with hyphens, strip non-alphanumeric)
- User can manually edit after auto-population
- Debounce: 400ms after last keystroke before triggering availability API call
- States while typing: neutral (no indicator)
- State while checking: spinner icon trailing the input
- State available: green checkmark trailing icon + "slug-name is available" text below (14px, `--color-success`)
- State taken: red X trailing icon + "slug-name is taken — try another" text below (14px, `--color-error`)
- State reserved/invalid: red X trailing icon + "This name is reserved" or "Slug must be 3–30 characters, letters and hyphens only" (14px, `--color-error`)

**Form-level error (above submit button):** Tinted error alert, `--color-error` text, 1px error-tinted border, icon. Copy defined in Copywriting Contract below.

**Submit button:** Full width, amber background (`--color-amber`), white text, 14px semibold, 44px min height, border-radius md (8px). Loading state: "Creating your store..." with spinner, disabled, 50% opacity.

**Footer link:** "Already have an account? Sign in" — 14px, `--color-text-muted`, link portion `--color-navy` underline.

---

### Screen 2: Provisioning Loading (`/signup/provisioning`)

Layout: Full-screen centered panel. No card chrome — just centered content on bg.

**Structure:**
- NZPOS brand mark (Satoshi 30px semibold, navy) — top center
- Heading: "Setting up your store" — Satoshi 24px semibold, navy, below brand mark, margin-top 32px
- Sub-heading: "This takes about 10 seconds." — DM Sans 14px, text-muted, margin-top 8px
- Progress step list (3 steps, vertical, 16px gap between items):
  - "Creating your account" — step 1
  - "Provisioning your store" — step 2
  - "Preparing your dashboard" — step 3
- Step visual: 20px circle indicator left of label. State: pending = hollow `--color-border` circle, active = navy spinner, complete = `--color-success` filled circle with white checkmark
- Active step label: `--color-text`, weight 600. Pending/complete step label: `--color-text-muted`, weight 400
- Below steps, after all complete: "Redirecting to your dashboard..." — 14px, text-muted, fade-in at 250ms

**Animation rules:**
- Step transitions: 150ms ease-out (DESIGN.md micro/short rule)
- Spinner rotation: 800ms linear infinite (functional, not decorative)
- No step should animate longer than 250ms for the state transition itself

---

### Screen 3: Email Verification Gate (`/signup/verify-email`)

Layout: Full-screen centered card. Same card pattern as Screen 1 (max-w-sm, white, border, shadow-sm).

**Structure:**
- NZPOS brand mark (Satoshi 30px semibold, navy)
- Icon: envelope SVG — 48px, `--color-navy`, centered, margin-bottom 16px
- Heading: "Check your email" — Satoshi 24px semibold, navy
- Body: "We sent a verification link to {email}. Click it to activate your store." — DM Sans 16px, text, 1.5 line-height, margin-top 8px
- Resend button: full-width, ghost variant (border `--color-border`, navy text, white background), 44px height, border-radius md. Loading state: "Sending..." disabled 50% opacity. Post-send state: "Email sent" with success icon, temporarily disabled (30-second cooldown per D-09)
- "Wrong email? Sign out" link — 14px, `--color-text-muted`, centered below resend button, margin-top 8px
- Rate-limit copy (shown only if resend is rate-limited): "Please wait before requesting another email." — 14px, `--color-text-muted`

**Unverified redirect:** Middleware redirects unverified sessions hitting any admin route to `/signup/verify-email?email={encoded_email}`. The email param pre-populates the body copy.

---

### Screen 4: Provisioning Failure State (shown on `/signup/provisioning`)

Replaces the progress step list when provisioning fails (D-06).

**Structure:**
- Error icon: 48px, `--color-error`, centered
- Heading: "Something went wrong" — Satoshi 24px semibold, navy
- Body: "We couldn't finish setting up your store. Your account has been created — just retry below." — DM Sans 16px, text, 1.5 line-height
- "Retry provisioning" button: full-width, navy background, white text, 44px height — primary action
- "Contact support" link: 14px, `--color-text-muted`, centered below, margin-top 8px

---

## Copywriting Contract

Source: CONTEXT.md (Claude's Discretion) + REQUIREMENTS.md success criteria.

| Element | Copy |
|---------|------|
| Primary CTA | "Create my store" |
| Signup page heading | "Start your free store" |
| Signup page sub-heading | "Your POS and online store, ready in minutes." |
| Slug field label | "Store address" |
| Slug field helper | "Your store will be at {slug}.nzpos.co.nz" |
| Slug available | "{slug} is available" |
| Slug taken | "{slug} is taken — try another" |
| Slug reserved | "This address is reserved — choose another" |
| Slug format invalid | "3–30 characters, letters and hyphens only, must start with a letter" |
| Provisioning heading | "Setting up your store" |
| Provisioning sub-heading | "This takes about 10 seconds." |
| Provisioning step 1 | "Creating your account" |
| Provisioning step 2 | "Provisioning your store" |
| Provisioning step 3 | "Preparing your dashboard" |
| Provisioning failure heading | "Something went wrong" |
| Provisioning failure body | "We couldn't finish setting up your store. Your account has been created — just retry below." |
| Provisioning retry CTA | "Retry provisioning" |
| Verification heading | "Check your email" |
| Verification body | "We sent a verification link to {email}. Click it to activate your store." |
| Resend CTA | "Resend verification email" |
| Resend success | "Email sent — check your inbox" |
| Resend rate-limited | "Please wait before requesting another email." |
| Wrong email link | "Wrong email? Sign out" |
| Reserved slug error (form-level) | "That store address is reserved. Choose something unique to your business." |
| Duplicate email error (form-level) | "An account already exists for this email." |
| Rate limit error (form-level) | "Too many signup attempts. Please wait an hour and try again." |
| Generic server error | "Something went wrong. Please try again." |
| Existing login link | "Already have an account? Sign in" |

Destructive actions on this phase: none. No delete/cancel flows in scope.

---

## Component Inventory

New components to build (named for executor):

| Component | Type | Description |
|-----------|------|-------------|
| `SignupForm` | Client Component | 4-field form with slug auto-generation, live availability check, submit |
| `SlugInput` | Client Component | Controlled input with debounced availability fetch, trailing indicator |
| `ProvisioningScreen` | Client Component | 3-step progress indicator, polls/awaits provisioning, redirects on success |
| `ProvisioningStepList` | Client Component | Animated step list (pending / active / complete states) |
| `VerifyEmailCard` | Client Component | Verification gate card with resend button and cooldown state |
| `AuthCard` | Shared layout | White card, max-w-sm, border, shadow-sm, padding xl — reusable shell |

Reuse from existing codebase:
- Input pattern: from `src/app/login/page.tsx` — exact same class string for consistency
- Button pattern: from `src/app/login/page.tsx` — same primary button pattern, swap `bg-navy` to `bg-amber` for CTA
- Brand mark: replicate NZPOS heading from login page (Satoshi 2xl semibold navy)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No external component registries. All components are custom Tailwind + React.

---

## Accessibility Notes

- All form inputs have associated `<label>` elements with `htmlFor`/`id` pairing
- Slug availability status announced via `aria-live="polite"` region — screen readers hear "my-store is available" without focus move
- Provisioning step list: `role="list"`, each step `role="listitem"`, active step `aria-current="step"`
- Error messages: linked to inputs via `aria-describedby`
- Resend button disabled state communicates cooldown via `aria-label="Resend verification email — wait 30 seconds"` during cooldown

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
