---
phase: 29-backend-billing-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/031_free_email_notifications.sql
  - src/lib/email.ts
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04]

must_haves:
  truths:
    - "All existing stores have has_email_notifications = true after migration"
    - "New stores created via provision_store get has_email_notifications = true by default"
    - "Auth hook JWT no longer contains email_notifications claim"
    - "Email sends without any feature gate check"
  artifacts:
    - path: "supabase/migrations/031_free_email_notifications.sql"
      provides: "Data migration, column default change, auth hook rewrite"
      contains: "UPDATE public.store_plans SET has_email_notifications = true"
    - path: "src/lib/email.ts"
      provides: "Email sending without feature gate"
      min_lines: 25
  key_links:
    - from: "supabase/migrations/031_free_email_notifications.sql"
      to: "store_plans table"
      via: "ALTER TABLE SET DEFAULT true"
      pattern: "ALTER TABLE.*has_email_notifications.*SET DEFAULT true"
    - from: "src/lib/email.ts"
      to: "Resend API"
      via: "direct send without gate"
      pattern: "resend\\.emails\\.send"
---

<objective>
Create the SQL migration that makes email notifications free for all stores, and remove the feature gate from email sending.

Purpose: Ensure every store -- existing and new -- has email notifications enabled at the database level, the JWT auth hook stops injecting the email_notifications claim, and the email sending function no longer checks any feature gate.

Output: One SQL migration file and one updated TypeScript file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-backend-billing-cleanup/29-CONTEXT.md
@.planning/phases/29-backend-billing-cleanup/29-RESEARCH.md

<interfaces>
<!-- Current auth hook function from migration 024 (lines 32-105) -->
<!-- The new migration must CREATE OR REPLACE this function, removing only email_notifications -->
<!-- Keep: v_has_xero, v_has_custom_domain, v_has_inventory variables and claims -->
<!-- Remove: v_has_email_notifications variable, sp.has_email_notifications from SELECT, email_notifications claim injection -->

From src/lib/email.ts (current, lines 4, 25-30 to remove):
```typescript
import { requireFeature } from '@/lib/requireFeature'
// ...
  const gate = await requireFeature('email_notifications', { requireDbCheck: true })
  if (!gate.authorized) {
    console.log('[email] Email notifications not active — skipping send')
    return { success: false }
  }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration -- data, default, auth hook</name>
  <files>supabase/migrations/031_free_email_notifications.sql</files>
  <read_first>
    supabase/migrations/024_service_product_type.sql
    supabase/migrations/014_multi_tenant_schema.sql
  </read_first>
  <action>
Create `supabase/migrations/031_free_email_notifications.sql` with three sections (per D-01, D-02):

**Section 1: Data migration (GATE-03)**
```sql
-- Make email notifications free for all existing stores
UPDATE public.store_plans SET has_email_notifications = true WHERE has_email_notifications = false;
```

**Section 2: Column default change (GATE-04)**
```sql
-- New stores get email notifications enabled by default
ALTER TABLE public.store_plans ALTER COLUMN has_email_notifications SET DEFAULT true;
```

**Section 3: Auth hook rewrite (GATE-02, per D-02)**
`CREATE OR REPLACE FUNCTION public.custom_access_token_hook(event JSONB) RETURNS JSONB LANGUAGE plpgsql AS $$`

Copy the function body from migration 024 (lines 32-105) with these removals ONLY:
- Remove `v_has_email_notifications BOOLEAN := false;` from DECLARE block
- Remove `sp.has_email_notifications` from the SELECT statement (line 86 of 024) -- change to `SELECT sp.has_xero, sp.has_custom_domain, sp.has_inventory`
- Remove `, v_has_email_notifications` from the INTO clause (line 87 of 024) -- change to `INTO v_has_xero, v_has_custom_domain, v_has_inventory`
- Remove the entire line `claims := jsonb_set(claims, '{app_metadata,email_notifications}', to_jsonb(COALESCE(v_has_email_notifications, false)));` (line 97 of 024)

Keep ALL other variables, claims, and logic unchanged: v_has_xero, v_has_custom_domain, v_has_inventory, store_id, role, is_super_admin, customer lookup, etc.

IMPORTANT (Pitfall 2 from research): Diff the new function against the original 024 version to verify ONLY email_notifications lines were removed. The xero, custom_domain, and inventory claims must remain intact.
  </action>
  <verify>
    <automated>grep -c "email_notifications" supabase/migrations/031_free_email_notifications.sql | grep -q "0" && echo "FAIL: migration should not reference email_notifications in auth hook" || true; grep "has_email_notifications = true" supabase/migrations/031_free_email_notifications.sql && grep "SET DEFAULT true" supabase/migrations/031_free_email_notifications.sql && grep "v_has_xero" supabase/migrations/031_free_email_notifications.sql && grep "v_has_inventory" supabase/migrations/031_free_email_notifications.sql && echo "Migration structure OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/031_free_email_notifications.sql` exists
    - Contains `UPDATE public.store_plans SET has_email_notifications = true WHERE has_email_notifications = false`
    - Contains `ALTER TABLE public.store_plans ALTER COLUMN has_email_notifications SET DEFAULT true`
    - Contains `CREATE OR REPLACE FUNCTION public.custom_access_token_hook`
    - Auth hook function contains `v_has_xero`, `v_has_custom_domain`, `v_has_inventory` variables
    - Auth hook function does NOT contain `v_has_email_notifications`
    - Auth hook function does NOT contain `email_notifications` in any jsonb_set claim line
    - Auth hook function still contains `claims := jsonb_set(claims, '{app_metadata,xero}'`
    - Auth hook function still contains `claims := jsonb_set(claims, '{app_metadata,custom_domain}'`
    - Auth hook function still contains `claims := jsonb_set(claims, '{app_metadata,inventory}'`
  </acceptance_criteria>
  <done>Migration file creates all three sections: data update, default change, and auth hook rewrite without email_notifications</done>
</task>

<task type="auto">
  <name>Task 2: Remove feature gate from email.ts</name>
  <files>src/lib/email.ts</files>
  <read_first>
    src/lib/email.ts
    src/lib/requireFeature.ts
  </read_first>
  <action>
Edit `src/lib/email.ts` (per D-04, GATE-01):

1. Remove line 4: `import { requireFeature } from '@/lib/requireFeature'`
2. Remove lines 25-30 (the entire gate block):
```typescript
  // Gate: email_notifications subscription required (DB check for mutation)
  const gate = await requireFeature('email_notifications', { requireDbCheck: true })
  if (!gate.authorized) {
    console.log('[email] Email notifications not active — skipping send')
    return { success: false }
  }
```

The function should go directly from the parameter destructuring to checking `getResend()`. No other changes needed -- the function signature, Resend client, error handling, and return types all stay the same.
  </action>
  <verify>
    <automated>! grep -q "requireFeature" src/lib/email.ts && ! grep -q "gate" src/lib/email.ts && grep -q "getResend" src/lib/email.ts && echo "Feature gate removed OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/email.ts` does NOT contain `requireFeature`
    - `src/lib/email.ts` does NOT contain `gate.authorized`
    - `src/lib/email.ts` does NOT contain `import { requireFeature }`
    - `src/lib/email.ts` still contains `export async function sendEmail`
    - `src/lib/email.ts` still contains `resend.emails.send`
    - `src/lib/email.ts` still contains `getResend()`
  </acceptance_criteria>
  <done>Email sending function has no feature gate -- sends directly when called</done>
</task>

</tasks>

<verification>
- `grep -rn "requireFeature" src/lib/email.ts` returns no matches
- `supabase/migrations/031_free_email_notifications.sql` exists with all three sections
- Auth hook in migration preserves xero, custom_domain, inventory claims
- Auth hook in migration does not reference email_notifications
</verification>

<success_criteria>
1. SQL migration file handles data fix, default change, and auth hook rewrite
2. email.ts sends without any feature gate check
3. No compile errors in email.ts (run `npx tsc --noEmit src/lib/email.ts` or full build check)
</success_criteria>

<output>
After completion, create `.planning/phases/29-backend-billing-cleanup/29-01-SUMMARY.md`
</output>
