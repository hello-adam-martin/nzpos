---
phase: 29-backend-billing-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/addons.ts
  - src/actions/billing/createSubscriptionCheckoutSession.ts
  - src/actions/super-admin/activateAddon.ts
  - src/actions/super-admin/deactivateAddon.ts
  - .env.example
  - docs/env-vars.md
  - docs/deploy.md
  - src/lib/__tests__/requireFeature.test.ts
  - src/actions/billing/__tests__/createSubscriptionCheckoutSession.test.ts
  - src/app/api/webhooks/stripe/billing/__tests__/billing.test.ts
  - src/lib/stripe/syncStripeSnapshot.test.ts
  - src/lib/__tests__/schema.test.ts
  - src/actions/super-admin/__tests__/activateAddon.test.ts
  - src/actions/super-admin/__tests__/deactivateAddon.test.ts
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03]

must_haves:
  truths:
    - "ADDONS config contains only xero, custom_domain, and inventory -- no email_notifications"
    - "Stripe checkout session creation cannot accept email_notifications as a feature"
    - "Stripe webhook handler has no mapping for email_notifications price ID"
    - "Super admin activate/deactivate actions cannot target email_notifications"
    - "All backend tests pass with email_notifications removed"
  artifacts:
    - path: "src/config/addons.ts"
      provides: "Central add-on config without email_notifications"
      contains: "xero.*custom_domain.*inventory"
    - path: "src/actions/billing/createSubscriptionCheckoutSession.ts"
      provides: "Checkout session without email_notifications option"
      contains: "z.enum"
  key_links:
    - from: "src/config/addons.ts"
      to: "src/actions/billing/createSubscriptionCheckoutSession.ts"
      via: "SubscriptionFeature type import"
      pattern: "import.*SubscriptionFeature.*from.*config/addons"
    - from: "src/config/addons.ts"
      to: "src/app/api/webhooks/stripe/billing/"
      via: "PRICE_TO_FEATURE map"
      pattern: "PRICE_TO_FEATURE"
---

<objective>
Remove email_notifications from all TypeScript config, billing actions, super admin actions, env files, docs, and update all affected backend tests.

Purpose: Ensure the Stripe billing system, add-on config, and super admin actions no longer reference email_notifications as a purchasable/toggleable feature. All backend tests updated to reflect this.

Output: Updated config, actions, env, docs, and 7 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-backend-billing-cleanup/29-CONTEXT.md
@.planning/phases/29-backend-billing-cleanup/29-RESEARCH.md

<interfaces>
From src/config/addons.ts (current state -- to be modified):
```typescript
export type SubscriptionFeature = 'xero' | 'email_notifications' | 'custom_domain' | 'inventory'

interface FeatureFlags {
  has_xero: boolean
  has_email_notifications: boolean
  has_custom_domain: boolean
  has_inventory: boolean
}

export const PRICE_ID_MAP: Record<SubscriptionFeature, string>
export const PRICE_TO_FEATURE: Record<string, keyof FeatureFlags>
export const FEATURE_TO_COLUMN: Record<SubscriptionFeature, keyof FeatureFlags>
export const ADDONS: readonly AddonEntry[]
```

From src/actions/billing/createSubscriptionCheckoutSession.ts line 13:
```typescript
const featureSchema = z.enum(['xero', 'email_notifications', 'custom_domain', 'inventory'])
```

From src/actions/super-admin/activateAddon.ts line 11:
```typescript
feature: z.enum(['xero', 'email_notifications', 'custom_domain', 'inventory']),
```

From src/actions/super-admin/deactivateAddon.ts line 11:
```typescript
feature: z.enum(['xero', 'email_notifications', 'custom_domain', 'inventory']),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove email_notifications from config, actions, env, and docs</name>
  <files>
    src/config/addons.ts
    src/actions/billing/createSubscriptionCheckoutSession.ts
    src/actions/super-admin/activateAddon.ts
    src/actions/super-admin/deactivateAddon.ts
    .env.example
    docs/env-vars.md
    docs/deploy.md
  </files>
  <read_first>
    src/config/addons.ts
    src/actions/billing/createSubscriptionCheckoutSession.ts
    src/actions/super-admin/activateAddon.ts
    src/actions/super-admin/deactivateAddon.ts
    .env.example
    docs/env-vars.md
    docs/deploy.md
  </read_first>
  <action>
**Per D-03, BILL-01, BILL-02:**

**1. `src/config/addons.ts`** -- Remove all email_notifications references:
- Line 4: Change `'xero' | 'email_notifications' | 'custom_domain' | 'inventory'` to `'xero' | 'custom_domain' | 'inventory'`
- Line 8: Remove `has_email_notifications: boolean` from `FeatureFlags` interface
- Line 16: Remove `email_notifications: process.env.STRIPE_PRICE_EMAIL_NOTIFICATIONS!,` from `PRICE_ID_MAP`
- Line 24: Remove `[process.env.STRIPE_PRICE_EMAIL_NOTIFICATIONS!]: 'has_email_notifications',` from `PRICE_TO_FEATURE`
- Line 35: Remove `email_notifications: 'has_email_notifications',` from `FEATURE_TO_COLUMN`
- Lines 48-55: Remove the entire email_notifications object from `ADDONS` array:
  ```typescript
  {
    feature: 'email_notifications' as SubscriptionFeature,
    name: 'Email Notifications',
    benefitLine: 'Send order confirmations, pickup reminders, and daily summaries automatically.',
    gatedHeadline: 'Email notifications require an upgrade',
    gatedBody: 'Automatically email customers when orders are confirmed, ready, or shipped.',
  },
  ```

**2. `src/actions/billing/createSubscriptionCheckoutSession.ts`** (BILL-02):
- Line 13: Change `z.enum(['xero', 'email_notifications', 'custom_domain', 'inventory'])` to `z.enum(['xero', 'custom_domain', 'inventory'])`

**3. `src/actions/super-admin/activateAddon.ts`**:
- Line 11: Change `z.enum(['xero', 'email_notifications', 'custom_domain', 'inventory'])` to `z.enum(['xero', 'custom_domain', 'inventory'])`

**4. `src/actions/super-admin/deactivateAddon.ts`**:
- Line 11: Change `z.enum(['xero', 'email_notifications', 'custom_domain', 'inventory'])` to `z.enum(['xero', 'custom_domain', 'inventory'])`

**5. `.env.example`**:
- Remove line 31: `STRIPE_PRICE_EMAIL_NOTIFICATIONS=price_your_email_addon_price_id`

**6. `docs/env-vars.md`**:
- Remove the row containing `STRIPE_PRICE_EMAIL_NOTIFICATIONS`

**7. `docs/deploy.md`**:
- Remove the row/line referencing `Email Notifications` product and `STRIPE_PRICE_EMAIL_NOTIFICATIONS` in the Stripe products table (around line 147)
- Remove `STRIPE_PRICE_EMAIL_NOTIFICATIONS` from the env vars table (around line 248)

IMPORTANT (Pitfall 3 from research): Remove from BOTH `PRICE_ID_MAP` and `PRICE_TO_FEATURE` -- if one references the env var but the other doesn't, you get an `undefined` key in the reverse map.
  </action>
  <verify>
    <automated>! grep -rn "email_notifications" src/config/addons.ts && ! grep -rn "email_notifications" src/actions/billing/createSubscriptionCheckoutSession.ts && ! grep -rn "email_notifications" src/actions/super-admin/activateAddon.ts && ! grep -rn "email_notifications" src/actions/super-admin/deactivateAddon.ts && ! grep -n "STRIPE_PRICE_EMAIL" .env.example && echo "All email_notifications references removed from source"</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/addons.ts` SubscriptionFeature type is `'xero' | 'custom_domain' | 'inventory'`
    - `src/config/addons.ts` FeatureFlags interface has 3 properties: has_xero, has_custom_domain, has_inventory
    - `src/config/addons.ts` ADDONS array has 3 entries (xero, custom_domain, inventory)
    - `src/config/addons.ts` does NOT contain string `email_notifications` anywhere
    - `src/config/addons.ts` does NOT contain string `STRIPE_PRICE_EMAIL_NOTIFICATIONS` anywhere
    - `createSubscriptionCheckoutSession.ts` featureSchema is `z.enum(['xero', 'custom_domain', 'inventory'])`
    - `activateAddon.ts` z.enum is `['xero', 'custom_domain', 'inventory']`
    - `deactivateAddon.ts` z.enum is `['xero', 'custom_domain', 'inventory']`
    - `.env.example` does NOT contain `STRIPE_PRICE_EMAIL_NOTIFICATIONS`
    - `docs/env-vars.md` does NOT contain `STRIPE_PRICE_EMAIL_NOTIFICATIONS`
    - `docs/deploy.md` does NOT contain `STRIPE_PRICE_EMAIL_NOTIFICATIONS`
  </acceptance_criteria>
  <done>All source code, env, and docs references to email_notifications as a paid add-on are removed</done>
</task>

<task type="auto">
  <name>Task 2: Update all 7 backend test files</name>
  <files>
    src/lib/__tests__/requireFeature.test.ts
    src/actions/billing/__tests__/createSubscriptionCheckoutSession.test.ts
    src/app/api/webhooks/stripe/billing/__tests__/billing.test.ts
    src/lib/stripe/syncStripeSnapshot.test.ts
    src/lib/__tests__/schema.test.ts
    src/actions/super-admin/__tests__/activateAddon.test.ts
    src/actions/super-admin/__tests__/deactivateAddon.test.ts
  </files>
  <read_first>
    src/lib/__tests__/requireFeature.test.ts
    src/actions/billing/__tests__/createSubscriptionCheckoutSession.test.ts
    src/app/api/webhooks/stripe/billing/__tests__/billing.test.ts
    src/lib/stripe/syncStripeSnapshot.test.ts
    src/lib/__tests__/schema.test.ts
    src/actions/super-admin/__tests__/activateAddon.test.ts
    src/actions/super-admin/__tests__/deactivateAddon.test.ts
  </read_first>
  <action>
**Per D-05:** Update all 7 test files to remove email_notifications references. Each file has specific changes per the research:

**1. `src/lib/__tests__/requireFeature.test.ts`**:
- Remove Test 3 (approximately lines 78-91) which tests `email_notifications` feature specifically. The generic requireFeature function is unchanged and still works for xero, custom_domain, inventory.

**2. `src/actions/billing/__tests__/createSubscriptionCheckoutSession.test.ts`**:
- Remove `email_notifications: 'price_email_test'` from the `PRICE_ID_MAP` mock (around line 58-59)
- Remove or delete the test case that calls `createSubscriptionCheckoutSession('email_notifications')` and checks for `price_email_test` (around lines 182-188)

**3. `src/app/api/webhooks/stripe/billing/__tests__/billing.test.ts`**:
- Remove `price_email_test: 'has_email_notifications'` from the `PRICE_TO_FEATURE` mock (around line 63)
- No test case removal needed -- existing tests use xero price

**4. `src/lib/stripe/syncStripeSnapshot.test.ts`**:
- Remove `price_email_monthly: 'has_email_notifications'` from the `PRICE_TO_FEATURE` mock (around line 36)
- Remove the test `'resolves addon_type for email_notifications price ID'` (approximately lines 285-332). This test creates a subscription with `price_email_monthly` price and asserts `addon_type === 'email_notifications'`

**5. `src/lib/__tests__/schema.test.ts`**:
- Update the assertion on line ~44 from `expect(data![0].has_email_notifications).toBe(false)` to `expect(data![0].has_email_notifications).toBe(true)` (column stays in DB but migration sets it to true)

**6. `src/actions/super-admin/__tests__/activateAddon.test.ts`**:
- Remove `has_email_notifications: false` and `has_email_notifications_manual_override: false` from mock data objects in `setupAdminClient` (around lines 104-111)

**7. `src/actions/super-admin/__tests__/deactivateAddon.test.ts`**:
- Remove `has_email_notifications: false` and `has_email_notifications_manual_override: false` from mock data objects in `setupAdminClient` (around lines 104-111)

After all edits, run `npx vitest run --reporter=verbose` to confirm all tests pass.

IMPORTANT: Do NOT edit `src/types/database.ts` -- it is auto-generated and the column remains in the DB.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `requireFeature.test.ts` does NOT contain a test case for `email_notifications` feature
    - `createSubscriptionCheckoutSession.test.ts` does NOT contain `email_notifications` or `price_email_test`
    - `billing.test.ts` PRICE_TO_FEATURE mock does NOT contain `has_email_notifications`
    - `syncStripeSnapshot.test.ts` does NOT contain `price_email_monthly` or `email_notifications` test case
    - `schema.test.ts` has `expect(data![0].has_email_notifications).toBe(true)` (not false)
    - `activateAddon.test.ts` mock data does NOT contain `has_email_notifications`
    - `deactivateAddon.test.ts` mock data does NOT contain `has_email_notifications`
    - `npx vitest run` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>All 7 backend test files updated and full test suite passes green</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `grep -rn "email_notifications" src/config/addons.ts src/actions/billing/ src/actions/super-admin/activateAddon.ts src/actions/super-admin/deactivateAddon.ts` returns no matches
- `grep -rn "STRIPE_PRICE_EMAIL_NOTIFICATIONS" .env.example docs/` returns no matches
- TypeScript compiles without errors: `npx tsc --noEmit`
</verification>

<success_criteria>
1. Zero references to email_notifications in source config, billing actions, or super admin actions
2. Zero references to STRIPE_PRICE_EMAIL_NOTIFICATIONS in env or docs
3. All 7 test files updated, full test suite passes
4. TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/29-backend-billing-cleanup/29-02-SUMMARY.md`
</output>
