---
phase: 32-demo-store-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - supabase/migrations/032_demo_store_seed.sql
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
must_haves:
  truths:
    - "A demo store named 'Aroha Home & Gift' exists in the stores table with NZ business details"
    - "The store has exactly 20 products across 5 categories with NZD tax-inclusive prices"
    - "Every product has a placeholder image URL and a unique SKU"
    - "Running supabase db reset twice produces the same result with zero duplicate records"
    - "DEMO_STORE_ID constant is importable from src/lib/constants.ts"
  artifacts:
    - path: "supabase/migrations/032_demo_store_seed.sql"
      provides: "Idempotent seed migration for demo store, categories, and products"
      contains: "ON CONFLICT"
    - path: "src/lib/constants.ts"
      provides: "DEMO_STORE_ID and DEV_STORE_ID constants"
      exports: ["DEMO_STORE_ID", "DEV_STORE_ID"]
  key_links:
    - from: "src/lib/constants.ts"
      to: "supabase/migrations/032_demo_store_seed.sql"
      via: "shared UUID value 00000000-0000-4000-a000-000000000099"
      pattern: "00000000-0000-4000-a000-000000000099"
---

<objective>
Create the demo store seed data: an idempotent SQL migration that inserts a realistic NZ retail store with 20 products across 5 categories, plus the TypeScript constant for the demo store ID.

Purpose: Phase 33 needs a real store in the database to power the `/demo/pos` route. This phase creates that data foundation.
Output: One SQL migration file, one TypeScript constants file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-demo-store-seed/32-CONTEXT.md
@.planning/phases/32-demo-store-seed/32-RESEARCH.md
@supabase/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration and TypeScript constants for demo store seed</name>
  <files>supabase/migrations/032_demo_store_seed.sql, src/lib/constants.ts</files>
  <read_first>
    supabase/seed.ts (DEV_STORE_ID pattern and product insertion structure)
    supabase/migrations/001_initial_schema.sql (stores, categories, products table definitions)
    supabase/migrations/014_multi_tenant_schema.sql (slug, logo_url, store_description, primary_color, is_active columns)
    supabase/migrations/028_customer_disable_settings.sql (business_address, ird_gst_number, receipt_header, receipt_footer)
    supabase/migrations/024_service_product_type.sql (product_type column)
    .planning/phases/32-demo-store-seed/32-RESEARCH.md (complete migration skeleton and patterns)
  </read_first>
  <action>
Create two files:

**1. `src/lib/constants.ts`** — Export two UUID constants (per D-10, D-11, D-12):

```typescript
/** Fixed UUID for the demo store. Matches 032_demo_store_seed.sql. */
export const DEMO_STORE_ID = '00000000-0000-4000-a000-000000000099'

/** Fixed UUID for the local dev store. Matches supabase/seed.ts DEV_STORE_ID. */
export const DEV_STORE_ID = '00000000-0000-4000-a000-000000000001'
```

**2. `supabase/migrations/032_demo_store_seed.sql`** — Idempotent migration with the following sections, all using `ON CONFLICT DO NOTHING` (per D-07, D-08):

**Section 1: Synthetic auth user** (satisfies stores.owner_auth_id FK constraint per research Pattern 2):
- INSERT INTO `auth.users` with fixed UUID `00000000-0000-4000-a000-000000000099`
- email: `demo@nzpos.internal`, encrypted_password: `''`, role: `authenticated`, aud: `authenticated`
- `ON CONFLICT (id) DO NOTHING`

**Section 2: Demo store** (per D-01, D-02, D-03 for DEMO-01):
- id: `00000000-0000-4000-a000-000000000099`
- name: `Aroha Home & Gift` (per D-01 — generic NZ retail/gift/homeware)
- slug: `aroha-demo`
- owner_auth_id: `00000000-0000-4000-a000-000000000099` (synthetic user)
- is_active: `true`
- logo_url: `/demo/store-logo.svg`
- store_description: `A curated collection of NZ-made gifts and homewares`
- primary_color: `#1E293B` (matches design system deep navy)
- business_address: `12 Lambton Quay, Wellington 6011`
- ird_gst_number: `123-456-789` (per D-03 — sample format)
- receipt_header: `Aroha Home & Gift | 12 Lambton Quay, Wellington 6011`
- receipt_footer: `Thank you for shopping local. GST included in all prices.`
- setup_wizard_dismissed: `true`
- setup_completed_steps: `'{}'::jsonb`
- `ON CONFLICT (id) DO NOTHING`

**Section 3: store_plans row** (per research Pattern 6 — avoids NULL feature flag lookups):
- INSERT INTO `public.store_plans (store_id)` VALUES `('00000000-0000-4000-a000-000000000099')`
- `ON CONFLICT (store_id) DO NOTHING`

**Section 4: Categories** — 5 categories with fixed UUIDs (per D-04 for DEMO-02):
- `00000000-0000-4000-b000-000000000001` — `Candles & Fragrance`, sort_order 0
- `00000000-0000-4000-b000-000000000002` — `Homewares`, sort_order 1
- `00000000-0000-4000-b000-000000000003` — `Prints & Art`, sort_order 2
- `00000000-0000-4000-b000-000000000004` — `Kitchen & Dining`, sort_order 3
- `00000000-0000-4000-b000-000000000005` — `Jewellery & Accessories`, sort_order 4
- All with store_id `00000000-0000-4000-a000-000000000099`
- `ON CONFLICT (id) DO NOTHING`

**Section 5: Products** — 20 products across the 5 categories (for DEMO-02, DEMO-03). Use the exact product data from the RESEARCH.md code example (Section "Complete Migration Structure"). Key rules:
- Product UUIDs: `00000000-0000-4000-c000-000000000001` through `...000000000020`
- All prices in NZD cents (per D-05), tax-inclusive, range $5.99–$89.99 (599–8999 cents)
- SKU prefixes by category: CAN-001..004, HOM-001..004, PRT-001..004, KIT-001..004, JWL-001..004
- image_url: `/demo/placeholder-{category}.svg` (candles, homewares, prints, kitchen, jewellery)
- product_type: `physical` for all
- is_active: `true` for all
- stock_quantity and reorder_threshold: realistic non-zero values
- `ON CONFLICT (id) DO NOTHING`

Use the exact product list from 32-RESEARCH.md Pattern "Complete Migration Structure (Skeleton)" section — it has all 20 products with correct UUIDs, names, SKUs, prices, and image URLs.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "ON CONFLICT" supabase/migrations/032_demo_store_seed.sql | grep -q "^[5-9]" && grep -q "DEMO_STORE_ID" src/lib/constants.ts && grep -q "00000000-0000-4000-a000-000000000099" src/lib/constants.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/032_demo_store_seed.sql exists and contains all 5 sections (auth.users, stores, store_plans, categories, products)
    - Every INSERT statement has `ON CONFLICT ... DO NOTHING` (at least 5 ON CONFLICT clauses)
    - Migration contains exactly 20 product INSERT rows (20 product UUIDs c000-000000000001 through c000-000000000020)
    - Migration contains exactly 5 category INSERT rows (b000-000000000001 through b000-000000000005)
    - Store UUID is `00000000-0000-4000-a000-000000000099` throughout
    - All prices are integers (no decimal points in price_cents values)
    - SKU format: CAN-001 through CAN-004, HOM-001 through HOM-004, PRT-001 through PRT-004, KIT-001 through KIT-004, JWL-001 through JWL-004
    - src/lib/constants.ts exports `DEMO_STORE_ID` with value `'00000000-0000-4000-a000-000000000099'`
    - src/lib/constants.ts exports `DEV_STORE_ID` with value `'00000000-0000-4000-a000-000000000001'`
    - `npm run test` passes (existing tests unaffected)
  </acceptance_criteria>
  <done>SQL migration file and TypeScript constants file exist with all demo store data. Migration has auth.users placeholder, store, store_plans, 5 categories, 20 products — all idempotent via ON CONFLICT DO NOTHING. Constants file exports DEMO_STORE_ID.</done>
</task>

<task type="auto">
  <name>Task 2: Create SVG placeholder images for demo product categories</name>
  <files>public/demo/store-logo.svg, public/demo/placeholder-candles.svg, public/demo/placeholder-homewares.svg, public/demo/placeholder-prints.svg, public/demo/placeholder-kitchen.svg, public/demo/placeholder-jewellery.svg</files>
  <read_first>
    DESIGN.md (design system colors — deep navy #1E293B, amber #E67E22)
    supabase/migrations/032_demo_store_seed.sql (confirm image_url paths match SVG filenames)
    .planning/phases/32-demo-store-seed/32-RESEARCH.md (SVG template and color scheme)
  </read_first>
  <action>
Create the `public/demo/` directory and 6 SVG files (per D-06 — placeholder SVGs, no external dependencies):

**Store logo** — `public/demo/store-logo.svg`:
- 200x200 SVG with deep navy `#1E293B` background, rounded corners (rx="16")
- White text "AHG" (Aroha Home & Gift initials) centered, font-size ~60, font-weight 700, font-family system-ui

**5 category placeholders** — each is a 400x400 SVG with a colored background (rx="12"), a large white single letter centered (font-size 160, font-weight 700):

| File | Background Color | Letter |
|------|-----------------|--------|
| `public/demo/placeholder-candles.svg` | `#E67E22` (amber) | C |
| `public/demo/placeholder-homewares.svg` | `#1E293B` (deep navy) | H |
| `public/demo/placeholder-prints.svg` | `#7C3AED` (purple) | P |
| `public/demo/placeholder-kitchen.svg` | `#059669` (green) | K |
| `public/demo/placeholder-jewellery.svg` | `#DC2626` (red) | J |

SVG template for each category placeholder:
```xml
<svg xmlns="http://www.w3.org/2000/svg" width="400" height="400" viewBox="0 0 400 400">
  <rect width="400" height="400" fill="{COLOR}" rx="12"/>
  <text x="200" y="230" font-family="system-ui, sans-serif" font-size="160" font-weight="700" fill="rgba(255,255,255,0.9)" text-anchor="middle">{LETTER}</text>
</svg>
```

These paths must match the `image_url` values in the migration: `/demo/store-logo.svg` and `/demo/placeholder-{category}.svg`.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && ls public/demo/store-logo.svg public/demo/placeholder-candles.svg public/demo/placeholder-homewares.svg public/demo/placeholder-prints.svg public/demo/placeholder-kitchen.svg public/demo/placeholder-jewellery.svg && echo "ALL_EXIST" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - public/demo/ directory exists with exactly 6 SVG files
    - public/demo/store-logo.svg contains `fill="#1E293B"` and text `AHG`
    - public/demo/placeholder-candles.svg contains `fill="#E67E22"` and letter `C`
    - public/demo/placeholder-homewares.svg contains `fill="#1E293B"` and letter `H`
    - public/demo/placeholder-prints.svg contains `fill="#7C3AED"` and letter `P`
    - public/demo/placeholder-kitchen.svg contains `fill="#059669"` and letter `K`
    - public/demo/placeholder-jewellery.svg contains `fill="#DC2626"` and letter `J`
    - All SVG files are valid XML (contain `xmlns="http://www.w3.org/2000/svg"`)
    - Filenames match the image_url values in 032_demo_store_seed.sql exactly
  </acceptance_criteria>
  <done>Six SVG placeholder files exist in public/demo/, matching the image_url paths in the migration. Store logo uses design system deep navy with "AHG" initials. Five category placeholders use distinct colors with category initial letters.</done>
</task>

</tasks>

<verification>
Phase 32 is complete when:
1. `supabase db reset` runs without error (migration applies cleanly)
2. SQL queries confirm: 1 store, 5 categories, 20 products, 1 store_plans row — all for DEMO_STORE_ID
3. Running `supabase db reset` a second time produces identical results (idempotency via ON CONFLICT DO NOTHING)
4. All 6 SVG files in public/demo/ load correctly (valid XML, visible in browser)
5. `src/lib/constants.ts` exports DEMO_STORE_ID matching the migration UUID
6. `npm run test` passes (existing tests unaffected by new files)
</verification>

<success_criteria>
- Demo store "Aroha Home & Gift" queryable from the database with store_id = DEMO_STORE_ID
- 20 products across 5 categories with NZD tax-inclusive prices, placeholder images, and unique SKUs
- Migration is idempotent — re-running produces zero duplicates
- DEMO_STORE_ID constant importable by Phase 33's /demo/pos route
</success_criteria>

<output>
After completion, create `.planning/phases/32-demo-store-seed/32-01-SUMMARY.md`
</output>
