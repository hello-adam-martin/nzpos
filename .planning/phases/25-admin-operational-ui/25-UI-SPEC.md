---
phase: 25
slug: admin-operational-ui
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-05
---

# Phase 25 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — manual Tailwind v4 tokens | CLAUDE.md / globals.css |
| Preset | not applicable | — |
| Component library | none (custom Radix-free components) | Codebase scan |
| Icon library | Inline SVG (heroicons-style, 24x24 stroke-1.5) | Existing component patterns |
| Display font | Satoshi (Bunny Fonts CDN, weight 700/900) | DESIGN.md |
| Body/UI font | DM Sans (Bunny Fonts CDN, weight 400/700) | DESIGN.md |
| Mono font | Geist Mono (Bunny Fonts CDN) | DESIGN.md — order numbers, promo codes, SKUs |

No shadcn. No third-party registry. All tokens defined in `src/app/globals.css` `@theme` block — use CSS custom properties via `var()` throughout.

---

## Spacing Scale

Declared values (multiples of 4 only). Defined in `globals.css :root`.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`--space-xs`) | Icon gaps, badge padding, inline input padding |
| sm | 8px (`--space-sm`) | Compact element spacing, between label and input |
| md | 16px (`--space-md`) | Default card padding, form field gap |
| lg | 24px (`--space-lg`) | Section padding, between form sections |
| xl | 32px (`--space-xl`) | Layout gaps between major page sections |
| 2xl | 48px (`--space-2xl`) | Major section breaks, empty state vertical padding |
| 3xl | 64px (`--space-3xl`) | Page-level top/bottom spacing (rare) |

Exceptions:
- Table row minimum height: 40px (admin density — consistent with existing PromoList rows)
- Touch targets (buttons): minimum 44px height (matches existing BrandingForm save button)
- Stat card minimum height: 96px (matches existing DashboardHeroCard `min-h-[96px]`)
- Recharts chart height: 240px for all charts (matches SalesBarChart `height={240}`)

---

## Typography

All text uses `font-sans` (DM Sans) except page-level headings (`font-display` Satoshi) and data values. Source: DESIGN.md + confirmed in DashboardHeroCard, StaffPageClient.

Two weights only: 400 (regular) and 700 (bold). All previous 600/semibold uses promoted to 700.

| Role | Size | Weight | Font | Line Height | Usage |
|------|------|--------|------|-------------|-------|
| Page heading (h1) | 24px (text-2xl) | 700 (bold) | Satoshi (`font-display`) | 1.2 | Dashboard, Settings, Customers, Promos page titles |
| Section heading (h2) | 16px (text-base) | 700 (bold) | DM Sans (`font-sans`) | 1.2 | Card section headers (e.g. "All Promo Codes", "Business Details") — visual distinction from body via weight 700 |
| Body / label | 16px (text-base) | 400 (normal) | DM Sans (`font-sans`) | 1.5 | Form labels, description text |
| Small / table | 14px (text-sm) | 400 / 700 | DM Sans (`font-sans`) | 1.5 | Table cells (400), table headers (700 + uppercase + tracking-wide), badge text, meta text, delta badge text |
| Stat value | 30px (text-3xl) | 700 (bold) | Satoshi (`font-display`) | 1.0 | DashboardHeroCard primary metric value |
| Mono / code | 14px (text-sm) | 400 | Geist Mono (`font-mono`) | 1.5 | Promo codes, order numbers, IRD/GST number field |

Table column headers: 14px (text-sm) uppercase, tracking-wide, weight 700 — distinct from data rows via uppercase + tracking-wide treatment (not a separate font size).

Delta badge text: 14px (text-sm), weight 700.

**Size scale: 14px, 16px, 24px, 30px. Weight scale: 400, 700.**

---

## Color

All tokens defined in `globals.css @theme`. Use `var(--color-*)` or Tailwind utilities where mapped.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `--color-bg` | #FAFAF9 | Main page background |
| Secondary (30%) | `--color-surface` + `--color-card` | #F5F5F4 / #FFFFFF | Table row background, card backgrounds, sidebar, modal background, settings section cards |
| Accent (10%) | `--color-amber` | #E67E22 | Reserved — see list below |
| Accent hover | `--color-amber-hover` | #D35400 | Amber button hover state only |
| Primary | `--color-navy` | #1E293B | Primary buttons, table header text, active states, link text |
| Destructive | `--color-error` | #DC2626 | Destructive buttons (Delete promo confirm), error inline messages, error toasts |

**Accent (#E67E22) reserved for:**
1. Primary CTA buttons: "Save Settings" (all settings sections), "Add Customer" (if applicable)
2. Period toggle active state (7 days / 30 days selector)
3. Chart area fill gradient (top opacity 0.15 to bottom opacity 0.0 amber, line stroke #E67E22)

**Accent must NOT be used for:** table row hover, edit actions, search bar focus, general interactive states.

**Semantic colors (from DESIGN.md):**
- Success badge background: `--color-success` (#059669) at 10% opacity, text `--color-success` — "Active" status
- Warning badge background: `--color-warning` (#D97706) at 10% opacity, text `--color-warning` — "Maxed Out" status, down-delta badges
- Error badge background: `--color-error` (#DC2626) at 10% opacity, text `--color-error` — "Expired", "Disabled" status
- Up-delta badge: `--color-success` with up-arrow icon
- Down-delta badge: `--color-warning` with down-arrow icon

**Border:** `--color-border` (#E7E5E4) for all card and table borders. `--color-border-light` (#F0EFED) for within-card dividers.

---

## Focal Points

Primary focal point declarations for key screens — establishes visual hierarchy priority.

| Screen | Primary Focal Point | Rationale |
|--------|--------------------|-----------|
| Customers page | Search bar at the top of the page | Operators arrive on this page to find a specific customer; the search bar is the primary action and should be the first visual anchor (prominent width, autofocused on mount) |
| Enhanced Dashboard | Sales trend chart (SalesTrendChart AreaChart) | The chart communicates business health at a glance; it occupies the widest layout area and uses the amber accent color to draw the eye before stat cards or the orders widget |

---

## Component Inventory

All components that must be built or extended in this phase. Reference existing patterns listed in CONTEXT.md.

### New Components

| Component | Path | Description |
|-----------|------|-------------|
| `CustomersPageClient` | `src/components/admin/customers/CustomersPageClient.tsx` | Client wrapper with search state, pagination |
| `CustomerTable` | `src/components/admin/customers/CustomerTable.tsx` | Paginated table: name, email, order count, status badge. Click row = navigate to detail. |
| `CustomerDetailPage` | `src/app/admin/customers/[id]/page.tsx` | Profile header + paginated order history table |
| `DisableCustomerModal` | `src/components/admin/customers/DisableCustomerModal.tsx` | Confirmation modal for disable/enable toggle |
| `SalesTrendChart` | `src/components/admin/dashboard/SalesTrendChart.tsx` | Recharts AreaChart with amber area fill and navy stroke |
| `PeriodToggle` | `src/components/admin/dashboard/PeriodToggle.tsx` | Two-button toggle: "7 days" / "30 days" |
| `ComparisonStatCard` | `src/components/admin/dashboard/ComparisonStatCard.tsx` | Extends DashboardHeroCard with delta badge (up/down arrow + % change) |
| `RecentOrdersWidget` | `src/components/admin/dashboard/RecentOrdersWidget.tsx` | Compact table: last 5 orders — order#, date, total, status |
| `EditPromoModal` | `src/components/admin/EditPromoModal.tsx` | Pre-filled PromoForm in modal overlay. Triggered by "Edit" row action. |
| `BusinessDetailsForm` | `src/app/admin/settings/BusinessDetailsForm.tsx` | Form section: address, phone, IRD/GST number. Per-section Save button. |
| `ReceiptForm` | `src/app/admin/settings/ReceiptForm.tsx` | Form section: receipt header textarea, receipt footer textarea. Per-section Save button. |

### Extended Components

| Component | Path | Change |
|-----------|------|--------|
| `PromoList` | `src/components/admin/PromoList.tsx` | Add Edit and Delete action buttons to each row. Add "Show deleted" toggle. Deleted rows: greyed out, "Deleted" badge. |
| `DashboardPage` | `src/app/admin/dashboard/page.tsx` | Add SalesTrendChart, PeriodToggle, ComparisonStatCard row, RecentOrdersWidget below hero cards |
| `SettingsPage` | `src/app/admin/settings/page.tsx` | Add BusinessDetailsForm and ReceiptForm sections below BrandingForm |
| `AdminSidebar` | (existing) | Verify "Customers" link is present; add if missing |

---

## Interaction Contracts

### Customer Table

- **Search bar:** Top of page, full-width on mobile / max-w-sm on desktop, 300ms debounce, autofocused on mount (supports focal point). Placeholder: "Search by name or email..."
- **Pagination:** 20 rows per page. Show "Showing 1–20 of N customers" below table. Previous / Next buttons. Disabled when at bounds.
- **Row click:** Entire row is clickable, navigates to `/admin/customers/[id]`. Hover: `bg-surface/50` (matches PromoList row hover pattern).
- **Status column:** "Active" badge (success) or "Disabled" badge (error). Pill style: `rounded-full text-xs font-bold px-2 py-0.5`.
- **Empty search result:** Show inline empty state within table area (not full page replacement). Message: "No customers match your search."

### Customer Detail Page

- **Profile header:** Name (text-2xl font-bold font-display), email (text-sm text-muted), status badge, created date (text-sm text-muted). "Disable" button (navy, right-aligned) or "Enable" button (navy outline) depending on current state.
- **Order history table:** Columns — Order #, Date, Total (NZD), Status. Order # in Geist Mono. 10 orders per page.
- **Empty order history:** Inline empty state: "No orders yet." (no icon required — compact context).

### Disable Customer Modal

- Trigger: "Disable" button on customer detail page header
- Title: "Disable [Customer Name]?"
- Body: "They won't be able to log in to the storefront."
- Actions: "Disable Account" (amber/error button) + "Keep Account Active" (ghost button)
- Re-enable flow: same page, button reads "Enable Account", no confirmation modal required (non-destructive action)
- Loading: button shows "Disabling..." with `disabled` + `opacity-60` during `useTransition` pending

### Sales Trend Chart

- Type: Recharts `AreaChart` (area fill, not bar — per D-04)
- Dimensions: `width="100%"` `height={240}` inside `ResponsiveContainer`
- X axis: date labels `MM-DD` format, `fontSize: 14`, `fontFamily: 'DM Sans'`, no axis line, no tick line
- Y axis: `$N` formatter (divide cents by 100, round to 0 dp), `fontSize: 14`, `fontFamily: 'DM Sans'`, `width={60}`, no axis line, no tick line
- Line stroke: `#1E293B` (navy), `strokeWidth={2}`
- Area fill: gradient — `#E67E22` at opacity 0.15 (top) to opacity 0 (bottom). Use `<defs><linearGradient>`.
- Grid: horizontal grid lines only, `stroke="#E7E5E4"` (border color), `strokeDasharray="4 4"`
- Tooltip: same pattern as existing `CustomTooltip` in SalesBarChart — white card, navy text, Geist Mono value
- Dot: no dots on line (cleaner at 7-day density)
- Cursor: `fill: rgba(30,41,59,0.08)` (matches SalesBarChart cursor)

### Period Toggle (7 days / 30 days)

- Position: top-right of chart card, same row as chart section heading
- Style: two buttons in a `<div>` with shared rounded border (`rounded-full border border-[var(--color-border)]`)
- Active state: `bg-amber text-white font-bold`
- Inactive state: `bg-transparent text-text-muted font-normal hover:text-text`
- Size: `px-3 py-1 text-sm` — compact, not full CTA size
- State stored in `useState`, no URL param (dashboard is not deep-linkable per phase scope)

### Comparison Stat Cards

- Layout: same 3-column grid as existing DashboardHeroCard row (`grid grid-cols-1 md:grid-cols-3 gap-[var(--space-lg)]`)
- Card structure: extends DashboardHeroCard — adds a delta badge below the primary value
- Delta badge: inline-flex, rounded-full, `px-2 py-0.5 text-sm font-bold`
  - Positive delta: success/10 bg, success text, up-arrow SVG icon (16x16)
  - Negative delta: warning/10 bg, warning text, down-arrow SVG icon (16x16)
  - Zero/no data: text-muted, `—`
- Metrics: "Today vs Yesterday" (sales cents), "This Week vs Last Week" (sales cents)
- Value format: NZD using `formatNZD()` for primary value; `+12%` / `-8%` for delta

### Recent Orders Widget

- Layout: white card, same border/shadow as DashboardHeroCard
- Title: "Recent Orders" (text-base font-bold font-sans, section heading level)
- Format: compact table (not card list) — 5 rows, no pagination, "View all" link at footer pointing to `/admin/orders`
- Columns: Order # (Geist Mono, text-sm), Date (text-sm text-muted, locale date `dd MMM`), Total (text-sm, `formatNZD`), Status badge
- Status badges: same pill pattern as PromoList StatusBadge — `completed` → success, `pending_pickup`/`ready` → warning, `refunded` → error
- Empty state: "No orders yet." centered, text-sm text-muted, `py-8`

### Edit Promo Modal

- Trigger: "Edit" button on PromoList row (new action column, rightmost)
- Style: same modal overlay pattern as staff modals (fixed inset-0 bg-black/50, centered white card, rounded-lg, `max-w-md w-full`)
- Content: PromoForm reused with pre-filled values passed as props
- Title: "Edit Promo Code" (text-2xl font-bold)
- CTA: "Save Changes" (amber button, full width)
- Cancel: "Discard Changes" (ghost button, below or beside CTA)
- Loading: "Saving..." during transition

### Soft-Delete Promo Modal (D-10)

- Trigger: "Delete" button on PromoList row
- Title: "Delete [CODE]?"
- Body: "It will stop working immediately but order history is preserved."
- Actions: "Delete Promo" (error-colored button: `bg-error text-white`) + "Keep Promo" (ghost)
- Loading: "Deleting..." during transition

### "Show Deleted" Toggle (D-09)

- Position: top-right of PromoList section header row (same row as "All Promo Codes" heading)
- Style: checkbox-style toggle with label "Show deleted" — `text-sm font-sans text-text-muted`
- When active: deleted promos appear at bottom of list, greyed out (`opacity-50`), with a "Deleted" badge (error/10 bg, error text)
- "Deleted" badge reuses StatusBadge pattern

### Settings Page — Section Cards (D-11)

- Each section is a white card (`bg-card border border-border shadow-sm rounded-[var(--radius-lg)] p-[var(--space-xl)]`)
- Section heading: h2, text-base font-bold font-sans, bottom margin `var(--space-lg)` — matches BrandingForm heading, visually distinct via weight 700
- Field layout: label above input, `mb-[var(--space-md)]` between fields
- Input style: matches existing BrandingForm inputs — `h-10 px-3 rounded-md border border-border bg-white text-sm focus:ring-2 focus:ring-navy/20 focus:border-navy/40`
- Textarea (receipt header/footer): same border/radius/focus as input, `min-h-[80px] py-2 resize-y`
- IRD/GST number input: text type (not number), Geist Mono font (`font-mono`), placeholder "e.g. 123-456-789"
- Per-section "Save Settings" button: matches BrandingForm save button — amber, full-width, `min-h-[44px]`, "Saving..." loading state
- Inline success message: text-sm text-success "Settings saved." (same BrandingForm pattern)
- Inline error message: text-sm text-error, role="alert"

---

## Loading States

All new async components must implement skeleton loaders matching existing admin patterns.

| Component | Skeleton Pattern |
|-----------|-----------------|
| CustomerTable | 5 rows of grey rectangles: `bg-surface animate-pulse rounded` at table row heights |
| SalesTrendChart | Full-height grey rectangle (`h-[240px] bg-surface animate-pulse rounded-lg`) |
| ComparisonStatCards | Same 3-column grid, each card with grey block replacing value (same DashboardHeroCard min-height) |
| RecentOrdersWidget | 5 rows of grey rectangles inside the card |
| Customer detail order table | 5 skeleton rows |

Use Tailwind's `animate-pulse` on `bg-surface` (`#F5F5F4`) blocks. No external skeleton library.

---

## Copywriting Contract

### Customer Management

| Element | Copy |
|---------|------|
| Page heading | Customers |
| Page subheading | Manage your customer accounts. |
| Search placeholder | Search by name or email... |
| Empty state (no customers yet) | No customers yet. Orders placed on your online store will appear here. |
| Empty state (no search match) | No customers match your search. |
| Customer table — status active | Active |
| Customer table — status disabled | Disabled |
| Customer detail — disable button | Disable Account |
| Customer detail — enable button | Enable Account |
| Disable modal title | Disable [Name]? |
| Disable modal body | They won't be able to log in to the storefront. |
| Disable modal confirm | Disable Account |
| Disable modal cancel | Keep Account Active |
| Load error | Couldn't load customers. Refresh the page to try again. |
| Success toast — disabled | [Name] disabled. |
| Success toast — enabled | [Name] re-enabled. |

### Dashboard

| Element | Copy |
|---------|------|
| Chart section heading | Sales Trend |
| Period toggle options | 7 days / 30 days |
| Comparison card — today | Today's Sales |
| Comparison card — today subLabel | vs. yesterday |
| Comparison card — this week | This Week |
| Comparison card — this week subLabel | vs. last week |
| Recent orders section heading | Recent Orders |
| Recent orders footer link | View all orders |
| Chart empty state | No sales data for this period. |
| Recent orders empty state | No orders yet. |

### Promo Management

| Element | Copy |
|---------|------|
| Edit modal title | Edit Promo Code |
| Edit CTA | Save Changes |
| Edit modal cancel | Discard Changes |
| Delete modal title | Delete [CODE]? |
| Delete modal body | It will stop working immediately but order history is preserved. |
| Delete modal confirm | Delete Promo |
| Delete modal cancel | Keep Promo |
| Show deleted toggle label | Show deleted |
| Deleted badge | Deleted |
| Success toast — edit | Promo updated. |
| Success toast — delete | [CODE] deleted. |
| Error (edit/delete) | Something went wrong. Check your connection and try again. |

### Store Settings

| Element | Copy |
|---------|------|
| Business Details section heading | Business Details |
| Address field label | Business Address |
| Address placeholder | e.g. 12 Example St, Auckland 1010 |
| Phone field label | Phone Number |
| Phone placeholder | e.g. 09 123 4567 |
| IRD/GST field label | IRD / GST Number |
| IRD/GST placeholder | e.g. 123-456-789 |
| Receipt section heading | Receipt Customisation |
| Receipt header label | Receipt Header |
| Receipt header placeholder | e.g. Thanks for shopping with us! |
| Receipt footer label | Receipt Footer |
| Receipt footer placeholder | e.g. Returns accepted within 7 days with receipt. |
| Save button (all sections) | Save Settings |
| Save loading | Saving... |
| Success inline | Settings saved. |
| Error inline | Couldn't save settings. Check your connection and try again. |

---

## Destructive Actions

| Action | Trigger | Confirmation | Button Color |
|--------|---------|-------------|-------------|
| Disable Customer | "Disable Account" button on customer detail | Modal: "Disable [Name]? They won't be able to log in to the storefront." | `bg-error text-white` |
| Delete Promo | "Delete" row action in PromoList | Modal: "Delete [CODE]? It will stop working immediately but order history is preserved." | `bg-error text-white` |

Note: "Enable Account" (customer re-enable) is non-destructive. No confirmation modal required. Immediate action.

---

## Motion Contract

From DESIGN.md — unchanged for admin context.

| Interaction | Duration | Easing |
|-------------|----------|--------|
| Modal open/close | 250ms | ease-out (open) / ease-in (close) |
| Button hover/focus | 150ms | ease-out |
| Toast slide-up | 150ms | ease-out (uses existing `.animate-slide-up`) |
| Table row hover | 75ms | ease-out (matches existing `transition-colors duration-75`) |
| Skeleton pulse | continuous | built-in Tailwind `animate-pulse` |

No decorative animations in admin. Nothing exceeds 250ms.

---

## Registry Safety

No shadcn. No third-party registries. Not applicable.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | n/a | not applicable — no shadcn, no third-party registry |

---

## Responsive Behaviour

Admin layout: sidebar nav (240px fixed) + main content. Sidebar collapses to hamburger on tablet (existing behaviour — do not change).

| Component | Mobile (< 768px) | Desktop (>= 768px) |
|-----------|-----------------|---------------------|
| Customer table | Horizontal scroll | Full columns |
| Dashboard grid (hero + comparison cards) | 1 column | 3 columns (`md:grid-cols-3`) |
| Chart + period toggle | Stack vertically | Toggle floats right of heading |
| Settings sections | Full-width card | `max-w-md` card (matches BrandingForm) |
| Recent orders widget | Full-width | Full-width |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
