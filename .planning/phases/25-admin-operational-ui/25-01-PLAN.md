---
phase: 25-admin-operational-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/028_customer_disable_settings.sql
  - src/types/database.ts
  - src/actions/settings/updateBusinessDetails.ts
  - src/actions/settings/updateReceiptSettings.ts
  - src/app/admin/settings/page.tsx
  - src/app/admin/settings/BusinessDetailsForm.tsx
  - src/app/admin/settings/ReceiptForm.tsx
  - src/actions/promos/updatePromoCode.ts
  - src/actions/promos/deletePromoCode.ts
  - src/components/admin/PromoList.tsx
  - src/components/admin/EditPromoModal.tsx
  - src/components/admin/AdminSidebar.tsx
autonomous: true
requirements:
  - SETTINGS-01
  - SETTINGS-02
  - SETTINGS-03
  - PROMO-01
  - PROMO-02

must_haves:
  truths:
    - "Admin can save business address, phone, and IRD/GST number from settings page"
    - "Admin can save receipt header and footer text from settings page"
    - "Admin can edit an existing promo code's discount, min order, max uses, and expiry"
    - "Admin can soft-delete a promo code so it stops working but history is preserved"
    - "Deleted promos are hidden by default and visible via Show deleted toggle"
    - "Customers link appears in AdminSidebar for owner role"
  artifacts:
    - path: "supabase/migrations/028_customer_disable_settings.sql"
      provides: "DB columns for customer disable + store settings"
      contains: "ALTER TABLE public.customers"
    - path: "src/app/admin/settings/BusinessDetailsForm.tsx"
      provides: "Business details form section"
      exports: ["BusinessDetailsForm"]
    - path: "src/app/admin/settings/ReceiptForm.tsx"
      provides: "Receipt customization form section"
      exports: ["ReceiptForm"]
    - path: "src/actions/promos/updatePromoCode.ts"
      provides: "Promo update Server Action"
      exports: ["updatePromoCode"]
    - path: "src/actions/promos/deletePromoCode.ts"
      provides: "Promo soft-delete Server Action"
      exports: ["deletePromoCode"]
    - path: "src/components/admin/EditPromoModal.tsx"
      provides: "Edit promo modal component"
      exports: ["EditPromoModal"]
  key_links:
    - from: "src/app/admin/settings/BusinessDetailsForm.tsx"
      to: "src/actions/settings/updateBusinessDetails.ts"
      via: "Server Action form submit"
      pattern: "updateBusinessDetails"
    - from: "src/components/admin/PromoList.tsx"
      to: "src/actions/promos/deletePromoCode.ts"
      via: "Server Action on delete confirm"
      pattern: "deletePromoCode"
    - from: "src/components/admin/EditPromoModal.tsx"
      to: "src/actions/promos/updatePromoCode.ts"
      via: "Server Action on edit form submit"
      pattern: "updatePromoCode"
---

<objective>
Create the DB migration for Phase 25, expand store settings with business details and receipt customization forms, add promo edit/delete functionality, and add Customers link to sidebar.

Purpose: Establishes DB schema changes needed by the entire phase, delivers settings and promo management requirements, and wires sidebar navigation for customers.
Output: Migration file, 2 new settings form components, 2 new promo Server Actions, extended PromoList with edit/delete, EditPromoModal, updated AdminSidebar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-admin-operational-ui/25-CONTEXT.md
@.planning/phases/25-admin-operational-ui/25-RESEARCH.md
@.planning/phases/25-admin-operational-ui/25-UI-SPEC.md

<interfaces>
<!-- Existing patterns the executor needs -->

From src/components/admin/dashboard/DashboardHeroCard.tsx:
```typescript
interface DashboardHeroCardProps {
  label: string
  value: string
  subLabel?: string
}
export function DashboardHeroCard({ label, value, subLabel }: DashboardHeroCardProps)
```

From src/components/admin/PromoList.tsx:
```typescript
type PromoCode = Database['public']['Tables']['promo_codes']['Row']
interface PromoListProps {
  promos: PromoCode[]
}
type PromoStatus = 'active' | 'expired' | 'maxed_out'
export default function PromoList({ promos }: PromoListProps)
```

From src/components/admin/PromoForm.tsx:
```typescript
type FormState = {
  error?: string
  success?: boolean
  errors?: Record<string, string[]>
}
export default function PromoForm()
// Uses useActionState with createPromoCode baked in
```

From src/components/admin/AdminSidebar.tsx:
```typescript
const BASE_NAV_LINKS = [
  { href: '/admin/dashboard', label: 'Dashboard' },
  { href: '/admin/products', label: 'Products' },
  { href: '/admin/promos', label: 'Promos' },
  { href: '/admin/orders', label: 'Orders' },
  { href: '/admin/reports', label: 'Reports' },
  { href: '/admin/cash-up', label: 'Cash-Up' },
  { href: '/admin/staff', label: 'Staff' },
  { href: '/admin/integrations', label: 'Integrations' },
  { href: '/admin/settings', label: 'Settings' },
  { href: '/admin/billing', label: 'Billing' },
]
// "Customers" link is MISSING — must be added between Orders and Reports
```

From src/app/admin/settings/page.tsx:
```typescript
// Currently only renders BrandingForm with store name, slug, logo_url, primary_color
// Must be extended to also fetch and pass business_address, phone, ird_gst_number, receipt_header, receipt_footer
```

From src/actions/promos/createPromoCode.ts:
```typescript
// Pattern: 'use server' + import 'server-only' + Zod safeParse + createSupabaseServerClient + revalidatePath
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration, types regen, settings forms, and settings Server Actions</name>
  <files>
    supabase/migrations/028_customer_disable_settings.sql,
    src/types/database.ts,
    src/actions/settings/updateBusinessDetails.ts,
    src/actions/settings/updateReceiptSettings.ts,
    src/app/admin/settings/BusinessDetailsForm.tsx,
    src/app/admin/settings/ReceiptForm.tsx,
    src/app/admin/settings/page.tsx
  </files>
  <read_first>
    supabase/migrations/027_staff_manager_role.sql,
    supabase/migrations/012_customer_accounts.sql,
    supabase/migrations/001_initial_schema.sql,
    src/app/admin/settings/page.tsx,
    src/app/admin/settings/BrandingForm.tsx,
    src/actions/setup/updateBranding.ts,
    src/types/database.ts,
    DESIGN.md,
    .planning/phases/25-admin-operational-ui/25-UI-SPEC.md
  </read_first>
  <action>
    **Migration 028** (`supabase/migrations/028_customer_disable_settings.sql`):
    ```sql
    -- Add is_active to customers table (CUST-04 disable flow)
    ALTER TABLE public.customers
      ADD COLUMN is_active BOOLEAN NOT NULL DEFAULT true;

    -- Expand stores table for business details and receipt customization (SETTINGS-01/02/03)
    ALTER TABLE public.stores
      ADD COLUMN business_address TEXT,
      ADD COLUMN phone TEXT,
      ADD COLUMN ird_gst_number TEXT,
      ADD COLUMN receipt_header TEXT,
      ADD COLUMN receipt_footer TEXT;
    ```
    Run: `npx supabase db push` (or `npx supabase migration up` if local dev).

    **Type regeneration**: Run `npx supabase gen types typescript --local > src/types/database.ts` to pick up new columns. If supabase CLI is not connected, manually add the 6 new fields to the existing type definitions:
    - `customers` Row/Insert/Update: `is_active: boolean` (Row), `is_active?: boolean` (Insert/Update)
    - `stores` Row/Insert/Update: `business_address: string | null`, `phone: string | null`, `ird_gst_number: string | null`, `receipt_header: string | null`, `receipt_footer: string | null`

    **Server Action: `updateBusinessDetails.ts`** (per D-11, D-13, SETTINGS-01, SETTINGS-03):
    - `'use server'` + `import 'server-only'`
    - Zod schema: `business_address: z.string().max(500).optional().default('')`, `phone: z.string().max(50).optional().default('')`, `ird_gst_number: z.string().max(50).optional().default('')`
    - Auth check: `createSupabaseServerClient()`, verify `user.app_metadata?.role === 'owner'`, get `store_id`
    - Update: `supabase.from('stores').update(parsed.data).eq('id', storeId)`
    - On error return `{ error: "Couldn't save settings. Check your connection and try again." }` (per UI-SPEC copywriting)
    - On success: `revalidatePath('/admin/settings')`, return `{ success: true }`

    **Server Action: `updateReceiptSettings.ts`** (per D-12, SETTINGS-02):
    - Same pattern as above
    - Zod schema: `receipt_header: z.string().max(500).optional().default('')`, `receipt_footer: z.string().max(500).optional().default('')`
    - Same auth, update, error/success pattern

    **BusinessDetailsForm.tsx** (per D-11, UI-SPEC "Settings Page - Section Cards"):
    - `'use client'` component
    - Props: `{ businessAddress: string, phone: string, irdGstNumber: string }`
    - White card: `bg-card border border-border shadow-sm rounded-[var(--radius-lg)] p-[var(--space-xl)]`
    - Section heading: `<h2 className="text-base font-bold font-sans mb-[var(--space-lg)]">Business Details</h2>`
    - Three fields stacked: "Business Address" (text input, placeholder "e.g. 12 Example St, Auckland 1010"), "Phone Number" (text input, placeholder "e.g. 09 123 4567"), "IRD / GST Number" (text input, `font-mono`, placeholder "e.g. 123-456-789")
    - Input style: `h-10 px-3 rounded-md border border-border bg-white text-sm focus:ring-2 focus:ring-navy/20 focus:border-navy/40`
    - Label above input, `mb-[var(--space-md)]` between fields
    - Per-section "Save Settings" button: amber, full-width, `min-h-[44px]`
    - Use `useTransition` for loading state: "Saving..." text, disabled + opacity-60
    - Inline success: `<p className="text-sm text-[var(--color-success)]">Settings saved.</p>`
    - Inline error: `<p className="text-sm text-[var(--color-error)]" role="alert">{error message}</p>`

    **ReceiptForm.tsx** (per D-12, UI-SPEC):
    - Same card pattern as BusinessDetailsForm
    - Section heading: `<h2 className="text-base font-bold font-sans mb-[var(--space-lg)]">Receipt Customisation</h2>`
    - Two textarea fields: "Receipt Header" (placeholder "e.g. Thanks for shopping with us!"), "Receipt Footer" (placeholder "e.g. Returns accepted within 7 days with receipt.")
    - Textarea style: same border/radius/focus as input, `min-h-[80px] py-2 resize-y`
    - Same save button and inline message pattern

    **Settings page.tsx** update:
    - Expand the existing `.select()` to include: `name, slug, logo_url, primary_color, business_address, phone, ird_gst_number, receipt_header, receipt_footer`
    - Add `<BusinessDetailsForm businessAddress={store.business_address ?? ''} phone={store.phone ?? ''} irdGstNumber={store.ird_gst_number ?? ''} />` after BrandingForm
    - Add `<ReceiptForm receiptHeader={store.receipt_header ?? ''} receiptFooter={store.receipt_footer ?? ''} />` after BusinessDetailsForm
    - Settings sections use `max-w-md` card on desktop (per UI-SPEC responsive table)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/028_customer_disable_settings.sql` contains `ALTER TABLE public.customers` and `ADD COLUMN is_active BOOLEAN`
    - `supabase/migrations/028_customer_disable_settings.sql` contains `ALTER TABLE public.stores` and `ADD COLUMN business_address TEXT`
    - `src/actions/settings/updateBusinessDetails.ts` contains `'use server'` and `import 'server-only'` and `z.object` and `business_address` and `ird_gst_number`
    - `src/actions/settings/updateReceiptSettings.ts` contains `'use server'` and `import 'server-only'` and `receipt_header` and `receipt_footer`
    - `src/app/admin/settings/BusinessDetailsForm.tsx` contains `'use client'` and `Business Details` and `IRD / GST Number` and `updateBusinessDetails`
    - `src/app/admin/settings/ReceiptForm.tsx` contains `'use client'` and `Receipt Customisation` and `updateReceiptSettings`
    - `src/app/admin/settings/page.tsx` contains `BusinessDetailsForm` and `ReceiptForm` and `business_address` in the select query
  </acceptance_criteria>
  <done>Migration 028 exists with all 6 column additions. Settings page renders 3 sections (Branding, Business Details, Receipt Customisation) each with its own Save button. Server Actions validate with Zod and update the stores table.</done>
</task>

<task type="auto">
  <name>Task 2: Promo edit/delete Server Actions, EditPromoModal, extended PromoList, AdminSidebar Customers link</name>
  <files>
    src/actions/promos/updatePromoCode.ts,
    src/actions/promos/deletePromoCode.ts,
    src/components/admin/EditPromoModal.tsx,
    src/components/admin/PromoList.tsx,
    src/components/admin/AdminSidebar.tsx
  </files>
  <read_first>
    src/actions/promos/createPromoCode.ts,
    src/actions/promos/validatePromoCode.ts,
    src/components/admin/PromoForm.tsx,
    src/components/admin/PromoList.tsx,
    src/components/admin/AdminSidebar.tsx,
    src/types/database.ts,
    DESIGN.md,
    .planning/phases/25-admin-operational-ui/25-UI-SPEC.md
  </read_first>
  <action>
    **Server Action: `updatePromoCode.ts`** (per D-08, PROMO-01):
    - `'use server'` + `import 'server-only'`
    - Zod schema: `id: z.string().uuid()`, `discount_type: z.enum(['percentage', 'fixed'])`, `discount_value: z.number().positive()`, `min_order_cents: z.number().int().min(0).optional()`, `max_uses: z.number().int().positive().optional().nullable()`, `expires_at: z.string().datetime().optional().nullable()`
    - Auth: owner-only via `createSupabaseServerClient()`, verify `user.app_metadata?.role === 'owner'`, get `store_id`
    - Update: `supabase.from('promo_codes').update({ discount_type, discount_value, min_order_cents, max_uses, expires_at }).eq('id', promoId).eq('store_id', storeId)`
    - Revalidate `/admin/promos`, return `{ success: true }`
    - Error: return `{ error: "Something went wrong. Check your connection and try again." }`

    **Server Action: `deletePromoCode.ts`** (per D-09, D-10, PROMO-02):
    - `'use server'` + `import 'server-only'`
    - Zod schema: `id: z.string().uuid()`
    - Auth: owner-only, same pattern
    - Soft-delete: `supabase.from('promo_codes').update({ is_active: false }).eq('id', promoId).eq('store_id', storeId).eq('is_active', true)` — optimistic lock prevents double-delete
    - Revalidate `/admin/promos`, return `{ success: true }`

    **EditPromoModal.tsx** (per D-08, UI-SPEC "Edit Promo Modal"):
    - `'use client'` component
    - Props: `{ promo: PromoCode, onClose: () => void }` where PromoCode is the database Row type
    - Modal overlay: `fixed inset-0 bg-black/50 z-50 flex items-center justify-center` with inner `bg-white rounded-lg max-w-md w-full p-[var(--space-xl)]`
    - Title: `<h2 className="font-display font-bold text-2xl mb-[var(--space-lg)]">Edit Promo Code</h2>`
    - Form fields matching PromoForm layout but pre-filled with `promo` values: discount_type (select), discount_value (number input), min_order_cents (number input, display as dollars), max_uses (number input, nullable), expires_at (date input, nullable)
    - DO NOT reuse PromoForm directly (its action is hardcoded to create). Duplicate the field JSX wired to `updatePromoCode`.
    - CTA: "Save Changes" amber button, full-width. Cancel: "Discard Changes" ghost button.
    - Use `useTransition` for "Saving..." loading state
    - On success: call `onClose()`. Show toast via existing toast pattern if available, otherwise inline success.

    **PromoList.tsx extension** (per D-08, D-09, D-10, UI-SPEC):
    - Add state: `const [editingPromo, setEditingPromo] = useState<PromoCode | null>(null)`
    - Add state: `const [deletingPromo, setDeletingPromo] = useState<PromoCode | null>(null)`
    - Add state: `const [showDeleted, setShowDeleted] = useState(false)`
    - Change props to accept ALL promos (including inactive): `promos: PromoCode[]`
    - Filter display: `const visiblePromos = showDeleted ? promos : promos.filter(p => p.is_active !== false)`
    - Add "Show deleted" checkbox toggle top-right of section header row (same row as "All Promo Codes" heading): `<label className="text-sm font-sans text-[var(--color-text-muted)] flex items-center gap-2"><input type="checkbox" checked={showDeleted} onChange={...} /> Show deleted</label>`
    - Add action column (rightmost) to each row: "Edit" button (text style, navy) and "Delete" button (text style, error color). Both hidden for inactive promos.
    - Deleted promo rows: `opacity-50` class, show "Deleted" badge (error/10 bg, error text, rounded-full px-2 py-0.5 text-xs font-bold)
    - Render `<EditPromoModal>` when `editingPromo !== null`
    - Render delete confirmation modal when `deletingPromo !== null`: Title "Delete [CODE]?", body "It will stop working immediately but order history is preserved.", confirm "Delete Promo" (`bg-[var(--color-error)] text-white`), cancel "Keep Promo" (ghost). On confirm call `deletePromoCode({ id: deletingPromo.id })`.
    - The promos page.tsx may need updating to pass all promos (including inactive) — read it and adjust the query if it currently filters by `is_active`.

    **AdminSidebar.tsx** — add Customers link:
    - Insert `{ href: '/admin/customers', label: 'Customers' }` into `BASE_NAV_LINKS` between Orders and Reports (after index 3, before index 4 in current array)
    - Do NOT add to `MANAGER_NAV_LINKS` — customer management is owner-only
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/actions/promos/updatePromoCode.ts` contains `'use server'` and `import 'server-only'` and `z.object` and `discount_type` and `discount_value`
    - `src/actions/promos/deletePromoCode.ts` contains `'use server'` and `import 'server-only'` and `is_active: false` and `eq('is_active', true)`
    - `src/components/admin/EditPromoModal.tsx` contains `'use client'` and `Edit Promo Code` and `Save Changes` and `updatePromoCode`
    - `src/components/admin/PromoList.tsx` contains `showDeleted` and `setEditingPromo` and `setDeletingPromo` and `EditPromoModal` and `deletePromoCode`
    - `src/components/admin/PromoList.tsx` contains `Show deleted` and `Delete Promo` and `Keep Promo` and `opacity-50`
    - `src/components/admin/AdminSidebar.tsx` contains `{ href: '/admin/customers', label: 'Customers' }`
    - AdminSidebar `MANAGER_NAV_LINKS` does NOT contain `customers`
  </acceptance_criteria>
  <done>Promo codes can be edited via modal and soft-deleted via confirmation modal. Deleted promos hidden by default, visible with toggle. Customers link appears in owner sidebar nav.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Settings page renders 3 sections: Branding, Business Details, Receipt Customisation
- PromoList shows Edit and Delete action buttons on each active promo row
- "Show deleted" toggle reveals soft-deleted promos with greyed-out styling
- AdminSidebar shows "Customers" link for owner role, not for manager role
- Migration 028 contains all 6 column additions (1 on customers, 5 on stores)
</verification>

<success_criteria>
- All 5 requirements (SETTINGS-01, SETTINGS-02, SETTINGS-03, PROMO-01, PROMO-02) have working Server Actions and UI
- Migration 028 is ready to run
- Types are updated to include new columns
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-admin-operational-ui/25-01-SUMMARY.md`
</output>
