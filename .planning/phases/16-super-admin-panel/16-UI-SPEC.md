---
phase: 16
slug: super-admin-panel
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-03
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for the Super Admin Panel. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Internal operations tool — not merchant-facing. Platform operator only.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | No components.json found; project uses Tailwind v4 with CSS custom properties |
| Preset | not applicable | Manual design system via globals.css @theme |
| Component library | none (custom components) | Existing codebase pattern |
| Icon library | Inline SVG (hand-rolled, per AdminSidebar.tsx) | CONTEXT.md D-03 + codebase |
| Font (display) | Satoshi, weight 700 | DESIGN.md |
| Font (body/UI) | DM Sans, weight 400–600 | DESIGN.md |
| Font (mono/data) | Geist Mono | DESIGN.md |

Registry safety gate: not applicable (no shadcn, no third-party component registries).

---

## Spacing Scale

Declared values from globals.css (multiples of 4, base unit 8px):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inner padding |
| sm | 8px | Compact element spacing, table cell padding |
| md | 16px | Default element spacing, card inner padding |
| lg | 24px | Section padding, form field gaps |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (not used in dense admin layout) |

Source: globals.css `:root` spacing custom properties.

Exceptions:
- Sidebar nav links: `px-3 py-3` (12px/12px) — matches AdminSidebar.tsx exactly, tighter than md
- Table rows: minimum 40px height for clickable row affordance
- Modal action area: `p-6` (24px) padding — consistent with existing admin modals
- No POS-specific touch targets (this is a desktop-only admin tool)

---

## Typography

| Role | Size | Weight | Line Height | Font | Source |
|------|------|--------|-------------|------|--------|
| Body | 16px (base) | 400 (regular) | 1.5 | DM Sans | DESIGN.md |
| Label / Table cell | 14px (sm) | 500 (medium) | 1.4 | DM Sans | DESIGN.md |
| Heading (page title) | 24px (2xl) | 600 (semibold) | 1.2 | Satoshi | Existing billing/page.tsx pattern |
| Section heading | 18px (lg) | 600 (semibold) | 1.2 | Satoshi | DESIGN.md |

Additional:
- Store slugs, IDs, order numbers: Geist Mono, 14px, weight 400 — enables at-a-glance scanning in tenant table
- Muted secondary text (timestamps, descriptions): DM Sans 14px weight 400, color `var(--color-text-muted)` (#78716C)
- Badge text: DM Sans 12px (xs) weight 600

---

## Color

| Role | Value | Usage | Source |
|------|-------|-------|--------|
| Dominant (60%) | #FAFAF9 (`--color-bg`) | Page background, main content area | globals.css |
| Secondary (30%) | #1E293B (`--color-navy`) | SuperAdminSidebar background, nav items | DESIGN.md / AdminSidebar.tsx |
| Accent (10%) | #E67E22 (`--color-amber`) | Active nav indicator (left border), primary CTA buttons, "Activate" add-on action | DESIGN.md D-03 |
| Surface | #F5F5F4 (`--color-surface`) | Cards, table row hover, detail panels | globals.css |
| Card | #FFFFFF (`--color-card`) | Tenant detail cards, modals | globals.css |
| Destructive | #DC2626 (`--color-error`) | Suspend button, suspension confirmation modal | DESIGN.md semantic |

Accent reserved for:
1. Active sidebar nav item — left border `border-l-4 border-amber` (matches AdminSidebar.tsx pattern)
2. Primary CTA buttons — "Activate Add-on" button (amber bg, white text)
3. "Active (Manual)" badge background (amber tint at 10% opacity with amber text)

Accent is NOT used for: table headers, pagination controls, filter dropdowns, search inputs, read-only status badges.

Status badge colors (semantic, not accent):
- Active tenant: `--color-success` (#059669) pill, white text — "Active"
- Suspended tenant: `--color-error` (#DC2626) pill, white text — "Suspended"
- Add-on Active (Stripe): navy (`--color-navy`) pill, white text — "Active (Stripe)"
- Add-on Active (Manual): amber (`--color-amber`) at 15% tint bg, amber text — "Active (Manual)"
- Add-on Inactive: `--color-surface` bg, `--color-text-muted` text — "Inactive"

---

## Layout Contract

Source: CONTEXT.md D-03, D-08; existing admin layout pattern.

### SuperAdminSidebar

- Width: 240px fixed (matches AdminSidebar.tsx)
- Background: `--color-navy` (#1E293B)
- Text: white, white/60 for inactive, white/40 for footer
- Nav items:
  1. "Tenants" — `/super-admin/tenants` (primary view)
  2. "Actions Log" — `/super-admin/actions` (audit trail list, if a dedicated page is added — Claude's discretion)
- Active state: `bg-white/10 text-white border-l-4 border-amber pl-2` (exact match to AdminSidebar.tsx)
- Logo area: "NZPOS Super Admin" — Satoshi semibold 16px
- Footer: super admin email + sign out link — DM Sans 12px white/50
- Context label: Small "SUPER ADMIN" badge or subtitle below logo to visually distinguish from tenant admin panel — DM Sans 10px uppercase tracking-wider white/40

### Super Admin Layout

- Structure: `flex min-h-screen bg-bg` — sidebar left, main content right (matches admin/layout.tsx)
- Main content: `flex-1 p-6` — 24px padding all sides
- No horizontal max-width constraint (full-width table layout for tenant list)
- No StripeTestModeBanner or XeroDisconnectBanner (those are tenant-specific)
- No mobile hamburger requirement — super admin panel is desktop-only (internal ops tool)

### Tenant List Page (`/super-admin/tenants`)

- Page title: "Tenants" — Satoshi semibold 24px
- Subtitle: "All provisioned stores on the platform." — DM Sans 16px `--color-text-muted`
- Controls row: `flex items-center gap-4 mb-6`
  - Search input: left-aligned, 280px wide, placeholder "Search name or slug…"
  - Status filter: `<select>` dropdown, options: All / Active / Suspended
  - Results count: right-aligned, DM Sans 14px muted — "42 stores"
- Table: full-width, no card wrapper, thin top/bottom borders only
  - Column order: Store Name | Slug | Status | Plan (active add-ons) | Created | Actions
  - Row height: 48px minimum
  - Row hover: `bg-surface` (#F5F5F4)
  - Row is clickable → navigates to tenant detail
  - "Actions" column: "View" text link, DM Sans 14px navy
- Pagination: `flex items-center justify-between mt-6`
  - Left: "Showing 1–20 of 42 stores" — DM Sans 14px muted
  - Right: Previous / [page numbers] / Next — DM Sans 14px, active page navy bg white text

### Tenant Detail Page (`/super-admin/tenants/[id]`)

Source: CONTEXT.md D-08 — all on one screen, no tabs.

Layout: Two-column on desktop (`grid grid-cols-3 gap-6`), left column 2/3 width main content, right column 1/3 width actions.

**Left column — Store Info card:**
- Card: white bg, `--shadow-sm`, `--radius-md`, `p-6`
- Section heading: "Store Information" — Satoshi 18px semibold
- Fields (label + value pairs, `space-y-4`):
  - Store Name — DM Sans 16px text
  - Slug — Geist Mono 14px
  - Created — DM Sans 14px muted, formatted as "3 Apr 2026"
  - Last Order — DM Sans 14px muted (uses latest order created_at, labelled "Last order" per RESEARCH.md recommendation)
  - Status badge — Active/Suspended pill

**Left column — Add-ons card (below store info):**
- Section heading: "Add-ons"
- One row per add-on (Xero, Email Notifications, Custom Domain)
- Row: `flex items-center justify-between py-3 border-b border-border-light`
- Add-on name: DM Sans 14px weight 500
- Status badge (see Color section above)
- Action: "Activate" button (amber, only if inactive) or "Deactivate" button (ghost/destructive, only if Active (Manual))
- No action button shown for "Active (Stripe)" — read-only, cannot deactivate Stripe-paid add-ons

**Left column — Audit Log card (below add-ons):**
- Section heading: "Recent Actions"
- Max 10 entries, scrollable if more
- Each entry: `flex items-start gap-3 py-2`
  - Icon circle: 32px, semantic color per action type
  - Action description: DM Sans 14px — e.g., "Suspended — billing dispute"
  - Timestamp: DM Sans 12px muted, relative then absolute on hover — "2 hours ago"

**Right column — Actions card:**
- Card: white bg, `--shadow-sm`, `--radius-md`, `p-6`
- Section heading: "Actions" — Satoshi 18px semibold
- Suspend button: full-width, destructive style (`bg-error text-white`), only shown when store is Active
- Unsuspend button: full-width, primary style (`bg-navy text-white`), only shown when store is Suspended
- Back to list link: DM Sans 14px, `--color-text-muted`, arrow left icon

---

## Component Inventory

| Component | File Path | Description |
|-----------|-----------|-------------|
| SuperAdminSidebar | `src/components/super-admin/SuperAdminSidebar.tsx` | Clone of AdminSidebar, different navLinks + "SUPER ADMIN" context label |
| TenantTable | `src/components/super-admin/TenantTable.tsx` | Full-width table with search/filter/pagination |
| TenantStatusBadge | `src/components/super-admin/TenantStatusBadge.tsx` | Active/Suspended pill, reusable |
| AddonStatusBadge | `src/components/super-admin/AddonStatusBadge.tsx` | Active (Stripe) / Active (Manual) / Inactive pill |
| PlanOverrideRow | `src/components/super-admin/PlanOverrideRow.tsx` | Single add-on row with status badge + action button |
| SuspendModal | `src/components/super-admin/SuspendModal.tsx` | Two-step confirmation modal with reason field |
| AuditLogList | `src/components/super-admin/AuditLogList.tsx` | Ordered list of super_admin_actions entries |
| SuspendedPage | `src/app/suspended/page.tsx` | Branded suspension notice (storefront + admin redirect target) |

---

## Interaction Contracts

### Search (Tenant List)
- Input: plain `<input type="text">`, DM Sans 14px, 280px wide
- Debounce: 300ms before URL param update (router.push with searchParams)
- Mechanism: server-side (URL search param `?q=`), not client-side filter
- Clear: show "×" icon button inside input when query is non-empty

### Status Filter (Tenant List)
- `<select>` element, not a custom dropdown
- Options: All | Active | Suspended
- URL param: `?status=active` / `?status=suspended` / (omitted for all)
- Updates synchronously (no debounce)

### Pagination
- Page size: 20 per page (Claude's discretion — balances density and performance)
- URL param: `?page=2`
- Previous/Next: disabled when at first/last page, DM Sans 14px, `--color-text-muted`
- Page number buttons: up to 5 visible, ellipsis (`…`) for gaps

### Suspend Flow (D-12 two-step modal)
1. Super admin clicks "Suspend" button on tenant detail page
2. Modal opens (250ms ease-out): `fixed inset-0 bg-black/50 z-50` overlay + centered card
3. Modal card: white bg, `--radius-lg`, `--shadow-lg`, `p-6`, max-w-md
4. Modal title: "Suspend [Store Name]?" — Satoshi 20px semibold
5. Impact warning: amber-tinted alert box — "This store's storefront and admin dashboard will be immediately inaccessible."
6. Reason field: `<textarea>` required, min 1 char, max 500 chars, placeholder "Reason for suspension…", DM Sans 14px
7. Footer: Cancel (ghost button) + "Confirm Suspension" (destructive button, disabled until reason is filled)
8. On confirm: Server Action call → loading state (button disabled + spinner) → on success modal closes, page revalidates
9. On error: inline error below form — "Suspension failed. Please try again." DM Sans 14px `--color-error`

### Add-on Activate Flow (D-13 direct toggle)
1. Super admin clicks "Activate" on an inactive add-on row
2. No modal — immediate Server Action call with optimistic UI (button disabled + spinner)
3. On success: badge updates to "Active (Manual)" via revalidatePath — no toast, inline feedback only
4. On error: inline error below the add-on row — "Failed to activate. Please try again."
5. Guard: if add-on is already "Active (Stripe)", "Activate" button is not shown — read-only state

### Add-on Deactivate Flow (D-15 symmetric)
1. Super admin clicks "Deactivate" on an "Active (Manual)" add-on row
2. Confirmation: inline confirm row expands below the add-on row (not a modal) — "Deactivate [name]? This will remove access." + "Confirm Deactivate" button
3. On confirm: Server Action call → loading → success shows "Inactive" badge
4. Guard: "Deactivate" button only appears on `Active (Manual)` add-ons, never on `Active (Stripe)`

### Suspension Page (D-09 branded)
- Route: `/suspended` — rendered on both storefront and admin subdomains via middleware rewrite
- Layout: centered, single column, no sidebar
- Background: `--color-bg` (#FAFAF9)
- Content card: white bg, `--radius-lg`, `--shadow-md`, `p-8`, max-w-md, centered vertically
- Logo: "NZPOS" — Satoshi 700 navy, 24px
- Heading: "Store Suspended" — Satoshi 700 navy, 28px
- Body: "This store has been temporarily suspended. If you believe this is an error, please contact support." — DM Sans 16px `--color-text-muted`
- Support contact: `support@nzpos.co.nz` — DM Sans 16px amber, mailto link
- No navigation, no sign-in form

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Page title — Tenant list | "Tenants" | Short, consistent with admin nav naming |
| Page subtitle — Tenant list | "All provisioned stores on the platform." | Contextualises scope |
| Search placeholder | "Search name or slug…" | Specifies both search fields |
| Empty state — no tenants | "No stores yet." / "No stores match your search. Try a different name or slug." | Context-dependent |
| Empty state body | "Stores will appear here once merchants sign up." | Only on zero-result all-stores view |
| Primary CTA — Activate add-on | "Activate" | Verb only — target (add-on name) is adjacent |
| Primary CTA — Suspend | "Suspend Store" | Verb + noun, full clarity |
| Primary CTA — Unsuspend | "Unsuspend Store" | Symmetric with suspend |
| Suspension modal title | "Suspend [Store Name]?" | Dynamic — includes store name for confirmation |
| Suspension modal warning | "This store's storefront and admin dashboard will be immediately inaccessible to the owner and their customers." | Impact is explicit |
| Suspension reason label | "Reason for suspension" | DM Sans label 14px |
| Suspension reason placeholder | "e.g. Billing dispute, Terms of Service violation…" | Guides without prescribing |
| Confirm suspension button | "Confirm Suspension" | Full label — no ambiguity |
| Deactivate inline confirm | "Deactivate [Add-on Name]? This will remove the store's access immediately." | Inline expansion, not modal |
| Error — suspend failed | "Failed to suspend store. Please try again or check logs." | Problem + next action |
| Error — activate failed | "Failed to activate add-on. Please try again." | Brief, actionable |
| Suspension page heading | "Store Suspended" | Neutral — not accusatory |
| Suspension page body | "This store has been temporarily suspended. If you believe this is an error, please contact support." | Gives merchant a path forward |
| Audit log — suspend action | "Suspended — [reason]" | Action + reason inline |
| Audit log — unsuspend action | "Unsuspended — [reason or 'No reason given']" | Consistent format |
| Audit log — activate action | "Activated [Add-on Name] (manual override)" | Clear it's a manual comp |
| Audit log — deactivate action | "Deactivated [Add-on Name] (manual override)" | Symmetric |
| Badge — Active tenant | "Active" | Success green |
| Badge — Suspended tenant | "Suspended" | Error red |
| Badge — Active Stripe | "Active (Stripe)" | Navy pill |
| Badge — Active Manual | "Active (Manual)" | Amber tint pill |
| Badge — Inactive add-on | "Inactive" | Muted, surface bg |
| Last order label | "Last order" | Not "Last active" — accurate to what's being shown |
| Pagination info | "Showing 1–20 of [N] stores" | Standard pagination copy |
| Context label in sidebar | "SUPER ADMIN" | Uppercase, tracking-wider, white/40 — distinguishes from tenant admin |

---

## Motion Contract

Source: DESIGN.md motion rules.

| Interaction | Duration | Easing |
|-------------|----------|--------|
| Modal open | 250ms | ease-out |
| Modal close | 200ms | ease-in |
| Button hover state | 150ms | ease-out |
| Sidebar nav hover | 150ms | ease-out |
| Row hover background | 100ms | ease-out |
| Inline confirm expand | 150ms | ease-out |
| Loading spinner | continuous | linear |

No page transition animations — admin panels feel instant.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn installed |
| Third-party | none | not applicable |

No component registries declared or used in this phase. All components are hand-rolled following existing project patterns.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Deep navy + amber palette, all hex values | DESIGN.md + globals.css (HIGH confidence) |
| Spacing scale (4/8/16/24/32/48/64) | DESIGN.md + globals.css custom properties (HIGH confidence) |
| Typography (Satoshi display, DM Sans body, Geist Mono data) | DESIGN.md (HIGH confidence) |
| Sidebar 240px, navy bg, white text, amber active border | AdminSidebar.tsx (HIGH confidence — direct clone) |
| Layout: sidebar + flex-1 main content | admin/layout.tsx (HIGH confidence — direct clone) |
| Card pattern: white bg, shadow-sm, radius-md, p-6 | billing/page.tsx + admin patterns (HIGH confidence) |
| Two-step suspension modal requirement | CONTEXT.md D-12 (LOCKED) |
| Add-on Stripe vs Manual badge distinction | CONTEXT.md D-14 (LOCKED) |
| Audit log on tenant detail | CONTEXT.md D-17 (LOCKED) |
| Server-side pagination (Previous/Next + page numbers) | CONTEXT.md D-07 (LOCKED) |
| Single search box (name + slug) + status filter | CONTEXT.md D-06 (LOCKED) |
| Suspension page — branded, both storefront and admin | CONTEXT.md D-09 (LOCKED) |
| Page size 20 | Claude's discretion (RESEARCH.md notes 10/20/25 as options) |
| Search debounce 300ms | Claude's discretion |
| "Last order" label (not "Last active") | RESEARCH.md recommendation — avoids middleware write overhead |
| Inline confirm for deactivate (not modal) | Claude's discretion — modals reserved for destructive suspend only |
| SuperAdminSidebar nav items (Tenants, Actions Log) | Claude's discretion |
| Suspension page support email placeholder | Claude's discretion |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
