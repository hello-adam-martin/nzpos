---
phase: 16-super-admin-panel
plan: 04
type: execute
wave: 3
depends_on: ["16-02", "16-03"]
files_modified: []
autonomous: false
requirements: [SADMIN-01, SADMIN-02, SADMIN-03, SADMIN-04]
must_haves:
  truths:
    - "All super admin flows work end-to-end in a browser"
    - "Suspension enforcement blocks storefront and admin for suspended tenants"
    - "Add-on overrides activate and deactivate correctly"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete super admin panel — tenant list, tenant detail, suspension flow, add-on overrides, and audit trail.

Purpose: Verify visual correctness and end-to-end functionality that automated tests cannot fully cover (middleware routing, real auth sessions, UI rendering).
Output: Human approval or issue list for iteration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-super-admin-panel/16-CONTEXT.md
@.planning/phases/16-super-admin-panel/16-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify super admin panel end-to-end</name>
  <files>src/app/super-admin/tenants/page.tsx</files>
  <action>
    Present the human verification checklist to the user. No automated work — this is a manual verification checkpoint for the complete super admin panel built in Plans 01-03.
  </action>
  <what-built>
    Complete super admin panel: layout with sidebar, tenant list with search/filter/pagination, tenant detail with store info/add-ons/audit log, suspend/unsuspend with confirmation modal, manual add-on activation/deactivation, branded suspension page, and middleware routing.
  </what-built>
  <how-to-verify>
    **Prerequisites:** Run `npx supabase db reset` to apply migration 020. Ensure a super admin user exists in the `super_admins` table. Start dev server with `npm run dev`.

    **1. Login + Redirect (D-02)**
    - Visit `http://lvh.me:3000/login`
    - Log in with super admin credentials
    - EXPECTED: Redirects to `/super-admin/tenants` (not `/admin/dashboard`)

    **2. Sidebar (D-03)**
    - EXPECTED: Left sidebar 240px, deep navy background, "NZPOS" logo + "SUPER ADMIN" subtitle
    - "Tenants" nav link highlighted with amber left border
    - Footer shows super admin email + sign out link

    **3. Tenant List (D-05, D-06, D-07)**
    - EXPECTED: Page title "Tenants", subtitle "All provisioned stores on the platform."
    - Table shows store name, slug (monospace), status badge, add-ons, created date, "View" link
    - Type in search box — results filter after 300ms debounce
    - Select "Suspended" from filter dropdown — only suspended stores shown
    - Check pagination: Previous/Next buttons, page numbers, "Showing 1-20 of N stores"

    **4. Tenant Detail (D-08)**
    - Click "View" on any tenant
    - EXPECTED: Two-column layout. Left: Store Information card, Add-ons card, Recent Actions card. Right: Actions card.
    - Store info shows name, slug (mono), created date, last order date, status badge
    - Each add-on row shows correct badge (Inactive/Active Stripe/Active Manual)

    **5. Suspend Flow (D-09, D-10, D-11, D-12)**
    - On an active tenant, click "Suspend Store"
    - EXPECTED: Modal opens with store name in title, amber warning box, required reason textarea, disabled "Confirm Suspension" button
    - Type a reason — "Confirm Suspension" button enables
    - Click "Confirm Suspension"
    - EXPECTED: Modal closes, status badge changes to "Suspended", audit log shows suspend entry
    - Visit the suspended tenant's subdomain (e.g., `http://demo.lvh.me:3000/`)
    - EXPECTED: Branded suspension page with "Store Suspended" heading and support email

    **6. Unsuspend Flow (D-11)**
    - Back on tenant detail, click "Unsuspend Store"
    - EXPECTED: Status changes to Active, audit log shows unsuspend entry
    - Revisit subdomain — storefront works again

    **7. Add-on Override (D-13, D-14, D-15)**
    - On a tenant with an inactive add-on, click "Activate Add-on"
    - EXPECTED: Badge changes to "Active (Manual)", audit log shows activate entry
    - Click "Deactivate" on the manually activated add-on
    - EXPECTED: Inline confirm appears. Click "Confirm Deactivate".
    - EXPECTED: Badge changes to "Inactive", audit log shows deactivate entry

    **8. Stripe Badge Protection (D-14)**
    - If a tenant has a Stripe-active add-on, verify no deactivate button appears — read-only "Active (Stripe)" badge
  </how-to-verify>
  <verify>Human approval of all 8 verification steps</verify>
  <done>User types "approved" confirming all super admin panel flows work correctly</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human verifies all 8 checkpoints above pass.
</verification>

<success_criteria>
- All super admin panel flows work end-to-end in a real browser session
- UI matches deep navy + amber design system
- Typography follows Satoshi/DM Sans/Geist Mono spec
- Suspension enforcement works on subdomain routing
</success_criteria>

<output>
After completion, create `.planning/phases/16-super-admin-panel/16-04-SUMMARY.md`
</output>
