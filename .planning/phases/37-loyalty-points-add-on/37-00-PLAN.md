---
phase: 37-loyalty-points-add-on
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/loyalty-utils.test.ts
  - src/lib/__tests__/pos-cart-loyalty.test.ts
  - src/actions/loyalty/__tests__/saveLoyaltySettings.test.ts
  - src/actions/loyalty/__tests__/quickAddCustomer.test.ts
  - src/actions/loyalty/__tests__/lookupCustomerForPOS.test.ts
autonomous: true
requirements: [LOYAL-02, LOYAL-03, LOYAL-04, LOYAL-09, LOYAL-11]

must_haves:
  truths:
    - "RED test stubs exist for loyalty utility functions"
    - "RED test stubs exist for cart state machine loyalty actions"
    - "RED test stubs exist for loyalty server actions"
  artifacts:
    - path: "src/lib/__tests__/loyalty-utils.test.ts"
      provides: "RED stubs for calculatePointsEarned, calculateRedemptionDiscount"
      contains: "expect(true).toBe(false)"
    - path: "src/lib/__tests__/pos-cart-loyalty.test.ts"
      provides: "RED stubs for ATTACH_CUSTOMER, DETACH_CUSTOMER, APPLY_LOYALTY_DISCOUNT"
      contains: "expect(true).toBe(false)"
    - path: "src/actions/loyalty/__tests__/saveLoyaltySettings.test.ts"
      provides: "RED stubs for loyalty settings validation"
      contains: "expect(true).toBe(false)"
    - path: "src/actions/loyalty/__tests__/quickAddCustomer.test.ts"
      provides: "RED stubs for consent_given validation"
      contains: "expect(true).toBe(false)"
    - path: "src/actions/loyalty/__tests__/lookupCustomerForPOS.test.ts"
      provides: "RED stubs for customer search result shape"
      contains: "expect(true).toBe(false)"
  key_links: []
---

<objective>
Create RED test stubs for all loyalty utility functions, cart state machine extensions, and server actions.

Purpose: Establish the Nyquist validation baseline before any implementation begins. All stubs must fail (RED) so that implementation plans turn them GREEN.
Output: 5 test files with RED stubs covering loyalty utils, cart state machine, settings, quick-add, and customer lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-loyalty-points-add-on/37-RESEARCH.md
@.planning/phases/37-loyalty-points-add-on/37-VALIDATION.md

<interfaces>
From src/lib/cart.ts:
```typescript
export type CartState = {
  items: CartItem[]
  paymentMethod: 'eftpos' | 'cash' | 'gift_card' | null
  cashTenderedCents: number | null
  cartDiscountCents: number
  cartDiscountType: 'percentage' | 'fixed' | null
  phase: 'idle' | 'eftpos_confirm' | 'cash_entry' | 'gift_card_entry' | 'gift_card_confirmed' | 'processing' | 'sale_complete' | 'sale_void'
  completedOrderId: string | null
  giftCardCode: string | null
  giftCardBalanceCents: number | null
  giftCardAmountCents: number | null
  giftCardRemainingAfterCents: number | null
  giftCardExpiresAt: string | null
  splitRemainderMethod: 'eftpos' | 'cash' | null
}

export type CartAction =
  | { type: 'ADD_PRODUCT'; product: { id: string; name: string; price_cents: number; stock_quantity: number } }
  | { type: 'SET_QUANTITY'; productId: string; quantity: number }
  // ... other existing actions
  | { type: 'NEW_SALE' }

export function cartReducer(state: CartState, action: CartAction): CartState
export const initialCartState: CartState
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RED test stubs for loyalty-utils and cart state machine</name>
  <files>src/lib/__tests__/loyalty-utils.test.ts, src/lib/__tests__/pos-cart-loyalty.test.ts</files>
  <read_first>
    - src/lib/__tests__/pos-cart-gift-card.test.ts (reference RED stub pattern from Phase 35)
    - src/lib/cart.ts (current CartState and CartAction types)
    - .planning/phases/37-loyalty-points-add-on/37-RESEARCH.md (loyalty-utils function signatures)
  </read_first>
  <action>
Create `src/lib/__tests__/loyalty-utils.test.ts` with RED stubs:

```typescript
import { describe, it, expect } from 'vitest'

describe('loyalty-utils', () => {
  describe('calculatePointsEarned', () => {
    it('earns 1 point per 100 cents at earn_rate_cents=100', () => {
      expect(true).toBe(false) // RED: calculatePointsEarned not implemented
    })
    it('floors fractional points (150 cents at rate 100 = 1 pt)', () => {
      expect(true).toBe(false)
    })
    it('returns 0 when netAmountCents <= 0', () => {
      expect(true).toBe(false)
    })
    it('returns 0 when earnRateCents <= 0', () => {
      expect(true).toBe(false)
    })
  })

  describe('calculateRedemptionDiscount', () => {
    it('calculates discount from points at redeemRateCents=1 (100pts = $1)', () => {
      expect(true).toBe(false) // RED: calculateRedemptionDiscount not implemented
    })
    it('returns 0 when pointsToRedeem <= 0', () => {
      expect(true).toBe(false)
    })
    it('returns 0 when redeemRateCents <= 0', () => {
      expect(true).toBe(false)
    })
  })

  describe('formatLoyaltyDisplay', () => {
    it('formats as "450 pts ($4.50 available)" per D-05', () => {
      expect(true).toBe(false) // RED: formatLoyaltyDisplay not implemented
    })
  })
})
```

Create `src/lib/__tests__/pos-cart-loyalty.test.ts` with RED stubs:

```typescript
import { describe, it, expect } from 'vitest'

describe('cart state machine — loyalty customer attachment', () => {
  it('attaches customer with ATTACH_CUSTOMER action', () => {
    expect(true).toBe(false) // RED: CartState does not have attachedCustomerId yet
  })
  it('stores customer name and points balance on ATTACH_CUSTOMER', () => {
    expect(true).toBe(false)
  })
  it('clears customer on DETACH_CUSTOMER', () => {
    expect(true).toBe(false)
  })
  it('preserves cart items when attaching customer', () => {
    expect(true).toBe(false)
  })
})

describe('cart state machine — loyalty discount', () => {
  it('applies loyalty discount with APPLY_LOYALTY_DISCOUNT', () => {
    expect(true).toBe(false) // RED: APPLY_LOYALTY_DISCOUNT action not implemented
  })
  it('stores loyaltyDiscountCents and loyaltyPointsRedeemed', () => {
    expect(true).toBe(false)
  })
  it('removes loyalty discount with REMOVE_LOYALTY_DISCOUNT', () => {
    expect(true).toBe(false)
  })
  it('clears loyalty discount on DETACH_CUSTOMER', () => {
    expect(true).toBe(false)
  })
  it('clears loyalty discount on NEW_SALE', () => {
    expect(true).toBe(false)
  })
})
```

Follow the exact `expect(true).toBe(false)` RED stub pattern from `pos-cart-gift-card.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npx vitest run src/lib/__tests__/loyalty-utils.test.ts src/lib/__tests__/pos-cart-loyalty.test.ts 2>&1 | grep -E "FAIL|Tests|failed"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/loyalty-utils.test.ts contains `expect(true).toBe(false)` in every `it()` block
    - src/lib/__tests__/loyalty-utils.test.ts contains `calculatePointsEarned` describe block
    - src/lib/__tests__/loyalty-utils.test.ts contains `calculateRedemptionDiscount` describe block
    - src/lib/__tests__/loyalty-utils.test.ts contains `formatLoyaltyDisplay` describe block
    - src/lib/__tests__/pos-cart-loyalty.test.ts contains `ATTACH_CUSTOMER` in test description
    - src/lib/__tests__/pos-cart-loyalty.test.ts contains `APPLY_LOYALTY_DISCOUNT` in test description
    - src/lib/__tests__/pos-cart-loyalty.test.ts contains `REMOVE_LOYALTY_DISCOUNT` in test description
    - Both files fail (RED) when run with vitest
  </acceptance_criteria>
  <done>Both test files exist, contain RED stubs, and fail when run with vitest. 8 stubs in loyalty-utils, 9 stubs in pos-cart-loyalty.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED test stubs for loyalty server actions</name>
  <files>src/actions/loyalty/__tests__/saveLoyaltySettings.test.ts, src/actions/loyalty/__tests__/quickAddCustomer.test.ts, src/actions/loyalty/__tests__/lookupCustomerForPOS.test.ts</files>
  <read_first>
    - src/lib/__tests__/pos-cart-gift-card.test.ts (reference RED stub pattern)
    - .planning/phases/37-loyalty-points-add-on/37-VALIDATION.md (test file locations and task IDs)
    - .planning/phases/37-loyalty-points-add-on/37-RESEARCH.md (server action signatures)
  </read_first>
  <action>
Create `src/actions/loyalty/__tests__/` directory and three test files.

`src/actions/loyalty/__tests__/saveLoyaltySettings.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'

describe('saveLoyaltySettings', () => {
  it('validates earn_rate_cents is a positive integer', () => {
    expect(true).toBe(false) // RED: saveLoyaltySettings not implemented
  })
  it('validates redeem_rate_cents is a positive integer', () => {
    expect(true).toBe(false)
  })
  it('rejects zero earn_rate_cents', () => {
    expect(true).toBe(false)
  })
  it('rejects negative redeem_rate_cents', () => {
    expect(true).toBe(false)
  })
  it('accepts valid settings with is_active boolean', () => {
    expect(true).toBe(false)
  })
})
```

`src/actions/loyalty/__tests__/quickAddCustomer.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'

describe('quickAddCustomer', () => {
  it('requires consent_given to be true per D-13/D-14 IPP 3A', () => {
    expect(true).toBe(false) // RED: quickAddCustomer not implemented
  })
  it('rejects when consent_given is false', () => {
    expect(true).toBe(false)
  })
  it('requires name field', () => {
    expect(true).toBe(false)
  })
  it('requires email field', () => {
    expect(true).toBe(false)
  })
  it('rejects duplicate email for same store', () => {
    expect(true).toBe(false)
  })
})
```

`src/actions/loyalty/__tests__/lookupCustomerForPOS.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'

describe('lookupCustomerForPOS', () => {
  it('returns results matching name query', () => {
    expect(true).toBe(false) // RED: lookupCustomerForPOS not implemented
  })
  it('returns results matching email query', () => {
    expect(true).toBe(false)
  })
  it('returns customer id, name, email, and points_balance in result shape', () => {
    expect(true).toBe(false)
  })
  it('returns empty array when no match', () => {
    expect(true).toBe(false)
  })
  it('requires minimum 2 character query per UI-SPEC debounce rule', () => {
    expect(true).toBe(false)
  })
})
```

All stubs use `expect(true).toBe(false)` pattern.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npx vitest run src/actions/loyalty/__tests__/ 2>&1 | grep -E "FAIL|Tests|failed"</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/loyalty/__tests__/saveLoyaltySettings.test.ts contains `expect(true).toBe(false)` in every `it()` block
    - src/actions/loyalty/__tests__/saveLoyaltySettings.test.ts contains `earn_rate_cents` in test description
    - src/actions/loyalty/__tests__/quickAddCustomer.test.ts contains `consent_given` in test description
    - src/actions/loyalty/__tests__/quickAddCustomer.test.ts contains `IPP 3A` in test description
    - src/actions/loyalty/__tests__/lookupCustomerForPOS.test.ts contains `points_balance` in test description
    - All three files fail (RED) when run with vitest
    - Directory src/actions/loyalty/__tests__/ exists
  </acceptance_criteria>
  <done>Three server action test files exist with RED stubs: 5 stubs for saveLoyaltySettings, 5 stubs for quickAddCustomer, 5 stubs for lookupCustomerForPOS. All fail when run.</done>
</task>

</tasks>

<verification>
All 5 test files fail (RED) when run individually and together:
```bash
npx vitest run src/lib/__tests__/loyalty-utils.test.ts src/lib/__tests__/pos-cart-loyalty.test.ts src/actions/loyalty/__tests__/
```
</verification>

<success_criteria>
- 5 test files created, all RED (failing)
- 27 total RED stubs across all files
- No production code created — stubs only
- Follows exact `expect(true).toBe(false)` pattern from Phase 35/36 Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/37-loyalty-points-add-on/37-00-SUMMARY.md`
</output>
