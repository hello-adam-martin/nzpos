---
phase: 37-loyalty-points-add-on
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(pos)/pos/page.tsx
  - src/app/(store)/layout.tsx
autonomous: true
gap_closure: true
requirements:
  - LOYAL-04
  - LOYAL-08
  - LOYAL-09

must_haves:
  truths:
    - "Staff can see Add Customer button and CustomerLookupSheet when merchant has loyalty subscription"
    - "Staff can apply loyalty points discount during POS checkout via LoyaltyRedemptionRow"
    - "Authenticated customer sees LoyaltyRedeemControl in CartDrawer during online checkout"
  artifacts:
    - path: "src/app/(pos)/pos/page.tsx"
      provides: "hasLoyalty and redeemRateCents props passed to POSClientShell"
      contains: "hasLoyalty={hasLoyalty}"
    - path: "src/app/(store)/layout.tsx"
      provides: "isAuthenticated prop passed to CartDrawer"
      contains: "isAuthenticated={isCustomer}"
  key_links:
    - from: "src/app/(pos)/pos/page.tsx"
      to: "src/components/pos/POSClientShell.tsx"
      via: "hasLoyalty and redeemRateCents props"
      pattern: "hasLoyalty=\\{hasLoyalty\\}"
    - from: "src/app/(store)/layout.tsx"
      to: "src/components/store/CartDrawer.tsx"
      via: "isAuthenticated prop"
      pattern: "isAuthenticated=\\{isCustomer\\}"
---

<objective>
Fix two prop-wiring gaps that block LOYAL-04, LOYAL-08, and LOYAL-09. The underlying components, actions, reducers, and RPCs are all complete and tested — only the server-component mount sites fail to pass required props to their child client components.

Purpose: Unblock all three failed verification truths so POS loyalty (customer lookup + points redemption) and online loyalty redemption work in production.
Output: Two modified server components with correct prop passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-loyalty-points-add-on/37-VERIFICATION.md

<interfaces>
<!-- POSClientShell props (from src/components/pos/POSClientShell.tsx) -->
```typescript
// Lines 47-48: Optional loyalty props with defaults
hasLoyalty?: boolean      // defaults to false
redeemRateCents?: number  // defaults to 1
```

<!-- CartDrawer props (from src/components/store/CartDrawer.tsx) -->
```typescript
interface CartDrawerProps {
  storeId: string
  hasGiftCards?: boolean
  isAuthenticated?: boolean  // defaults to false — controls loyalty data fetch
}
```

<!-- store_plans table columns available -->
<!-- has_inventory: boolean -->
<!-- has_gift_cards: boolean -->
<!-- has_loyalty_points: boolean -->

<!-- loyalty_settings table columns -->
<!-- store_id: uuid -->
<!-- earn_rate_cents: integer -->
<!-- redeem_rate_cents: integer -->
<!-- is_paused: boolean -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire loyalty props from POS page to POSClientShell</name>
  <files>src/app/(pos)/pos/page.tsx</files>
  <read_first>
    - src/app/(pos)/pos/page.tsx (current state — line 63-67 store_plans query, line 78-88 JSX)
    - src/components/pos/POSClientShell.tsx (lines 47-48 for prop interface, lines 63-64 for defaults)
    - .planning/phases/37-loyalty-points-add-on/37-VERIFICATION.md (Gap 1 details)
  </read_first>
  <action>
Modify `src/app/(pos)/pos/page.tsx` to fetch loyalty data and pass it to POSClientShell:

1. **Expand the store_plans SELECT on line 65** from `'has_inventory'` to `'has_inventory, has_loyalty_points'`. This adds the loyalty subscription flag to the existing query — no additional DB round-trip.

2. **Add a conditional loyalty_settings query** after the Promise.all block (after line 68). When `has_loyalty_points` is true, fetch `redeem_rate_cents` from `loyalty_settings` table:
```typescript
const hasLoyalty = storePlanResult.data?.has_loyalty_points === true

let redeemRateCents = 1
if (hasLoyalty) {
  const { data: loyaltySettings } = await supabase
    .from('loyalty_settings')
    .select('redeem_rate_cents')
    .eq('store_id', storeId)
    .single()
  redeemRateCents = loyaltySettings?.redeem_rate_cents ?? 1
}
```

3. **Pass both props to POSClientShell** in the JSX return (after the existing `hasInventory` prop):
```
hasLoyalty={hasLoyalty}
redeemRateCents={redeemRateCents}
```

The loyalty_settings query is only executed when the merchant has an active loyalty subscription (`has_loyalty_points === true`), so non-subscribers pay zero additional query cost. The `redeemRateCents` defaults to 1 if loyalty_settings row is missing (matches POSClientShell default).
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -n "has_loyalty_points" src/app/\(pos\)/pos/page.tsx && grep -n "hasLoyalty={hasLoyalty}" src/app/\(pos\)/pos/page.tsx && grep -n "redeemRateCents={redeemRateCents}" src/app/\(pos\)/pos/page.tsx && grep -n "loyalty_settings" src/app/\(pos\)/pos/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "has_loyalty_points" src/app/(pos)/pos/page.tsx returns at least 2 matches (SELECT string + boolean derivation)
    - grep "hasLoyalty={hasLoyalty}" src/app/(pos)/pos/page.tsx returns exactly 1 match (JSX prop)
    - grep "redeemRateCents={redeemRateCents}" src/app/(pos)/pos/page.tsx returns exactly 1 match (JSX prop)
    - grep "loyalty_settings" src/app/(pos)/pos/page.tsx returns at least 1 match (conditional query)
    - grep "redeem_rate_cents" src/app/(pos)/pos/page.tsx returns at least 1 match (column select)
    - npx tsc --noEmit exits 0 with no errors in this file
  </acceptance_criteria>
  <done>POS page fetches has_loyalty_points from store_plans, conditionally fetches redeem_rate_cents from loyalty_settings, and passes hasLoyalty + redeemRateCents to POSClientShell. Merchants with loyalty subscription see Add Customer button and can apply loyalty discounts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire isAuthenticated prop from storefront layout to CartDrawer</name>
  <files>src/app/(store)/layout.tsx</files>
  <read_first>
    - src/app/(store)/layout.tsx (current state — line 19 isCustomer variable, line 68 CartDrawer mount)
    - src/components/store/CartDrawer.tsx (lines 20-26 for props interface and defaults)
    - .planning/phases/37-loyalty-points-add-on/37-VERIFICATION.md (Gap 2 details)
  </read_first>
  <action>
Modify `src/app/(store)/layout.tsx` line 68 to pass the `isAuthenticated` prop to CartDrawer:

Change:
```tsx
<CartDrawer storeId={storeId ?? ''} hasGiftCards={hasGiftCards} />
```

To:
```tsx
<CartDrawer storeId={storeId ?? ''} hasGiftCards={hasGiftCards} isAuthenticated={isCustomer} />
```

The `isCustomer` variable already exists on line 19 (`const isCustomer = user?.app_metadata?.role === 'customer'`). It is a boolean — `true` when the user is an authenticated customer, `false` otherwise. This is exactly what CartDrawer needs to gate the `getCustomerLoyaltyForCheckout` fetch and render `LoyaltyRedeemControl`.

No new imports, no new queries, no new variables. Single prop addition.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -n "isAuthenticated={isCustomer}" src/app/\(store\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "isAuthenticated={isCustomer}" src/app/(store)/layout.tsx returns exactly 1 match on the CartDrawer line
    - The CartDrawer JSX line contains all three props: storeId, hasGiftCards, isAuthenticated
    - npx tsc --noEmit exits 0 with no errors in this file
  </acceptance_criteria>
  <done>Storefront layout passes isAuthenticated={isCustomer} to CartDrawer. Authenticated customers see loyalty balance and LoyaltyRedeemControl in cart drawer during online checkout.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. TypeScript compilation: `npx tsc --noEmit` — zero errors
2. POS page grep: `grep -c "hasLoyalty\|redeemRateCents\|has_loyalty_points\|loyalty_settings" src/app/(pos)/pos/page.tsx` returns 6+
3. Layout grep: `grep "isAuthenticated={isCustomer}" src/app/(store)/layout.tsx` returns match
4. Build check: `npm run build` completes without errors (confirms server component validity)
</verification>

<success_criteria>
- LOYAL-04 unblocked: POS page passes hasLoyalty=true for subscribed merchants, enabling Add Customer button and CustomerLookupSheet
- LOYAL-09 unblocked: Same prop fix enables LoyaltyRedemptionRow visibility in CartPanel
- LOYAL-08 unblocked: Storefront layout passes isAuthenticated to CartDrawer, enabling loyalty data fetch and LoyaltyRedeemControl rendering
- Zero new TypeScript errors introduced
- No changes to any component internals — only server-component prop wiring fixed
</success_criteria>

<output>
After completion, create `.planning/phases/37-loyalty-points-add-on/37-07-SUMMARY.md`
</output>
