---
phase: 35-gift-cards-add-on
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/gift-card-utils.test.ts
  - src/lib/__tests__/pos-cart-gift-card.test.ts
autonomous: true
requirements: [GIFT-01, GIFT-03, GIFT-06]

must_haves:
  truths:
    - "Test stubs exist for gift card code generation, expiry validation, and balance operations"
    - "Test stubs exist for cart state machine gift card payment method transitions"
    - "All test stubs fail (RED state) before implementation"
  artifacts:
    - path: "src/lib/__tests__/gift-card-utils.test.ts"
      provides: "Test stubs for gift card utility functions"
      contains: "describe.*gift card"
    - path: "src/lib/__tests__/pos-cart-gift-card.test.ts"
      provides: "Test stubs for cart state machine gift card support"
      contains: "describe.*gift card"
  key_links:
    - from: "src/lib/__tests__/gift-card-utils.test.ts"
      to: "src/actions/gift-cards/issueGiftCard.ts"
      via: "tests for code generation utility"
      pattern: "generateUniqueCode"
    - from: "src/lib/__tests__/pos-cart-gift-card.test.ts"
      to: "src/lib/cart.ts"
      via: "tests for cart reducer gift card actions"
      pattern: "GIFT_CARD_VALIDATED"
---

<objective>
Create test stub files for gift card functionality — code generation, expiry validation, balance operations, and cart state machine gift card support.

Purpose: Establishes the Nyquist-compliant test scaffolding that later plans will make pass. All stubs should define expected behaviors but fail (RED state) until implementation arrives.

Output: Two test files with stub tests covering core gift card logic.
</objective>

<execution_context>
@/Users/adam-personal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam-personal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-gift-cards-add-on/35-CONTEXT.md
@.planning/phases/35-gift-cards-add-on/35-VALIDATION.md

<interfaces>
From src/lib/__tests__/pos-cart.test.ts (existing cart tests):
```typescript
// Pattern: import cartReducer, create initial state, dispatch actions, assert state
import { cartReducer, initialCartState } from '../cart'
// Tests use describe/it/expect from vitest
```

From vitest.config.ts:
// Existing vitest config — no new setup needed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gift card utility test stubs</name>
  <files>src/lib/__tests__/gift-card-utils.test.ts</files>
  <read_first>
    - src/lib/__tests__/pos-cart.test.ts (existing test pattern, imports, describe/it structure)
    - vitest.config.ts (test config)
    - .planning/phases/35-gift-cards-add-on/35-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
Create `src/lib/__tests__/gift-card-utils.test.ts` with test stubs that will fail until implementation exists.

```typescript
import { describe, it, expect } from 'vitest'

describe('gift card code generation', () => {
  it('generates an 8-digit numeric string', () => {
    // Will import from gift card utils once created
    // For now, stub with a placeholder that fails
    expect(true).toBe(false) // RED — replace with real import in Plan 02
  })

  it('pads codes shorter than 8 digits with leading zeros', () => {
    expect(true).toBe(false) // RED
  })

  it('formats code as XXXX-XXXX with dash', () => {
    // Test: formatGiftCardCode('48271593') === '4827-1593'
    expect(true).toBe(false) // RED
  })
})

describe('gift card expiry validation', () => {
  it('treats active card past expires_at as expired', () => {
    // Pitfall 2: effective status computation
    expect(true).toBe(false) // RED
  })

  it('accepts active card within expiry window', () => {
    expect(true).toBe(false) // RED
  })

  it('rejects voided card regardless of expiry', () => {
    expect(true).toBe(false) // RED
  })
})

describe('gift card balance operations', () => {
  it('computes auto-split: full cover when balance >= total', () => {
    // D-06: giftCardAmount = Math.min(balance, total)
    // balance=5000, total=3000 -> giftCardAmount=3000, remainder=0
    expect(true).toBe(false) // RED
  })

  it('computes auto-split: partial cover when balance < total', () => {
    // balance=3000, total=5000 -> giftCardAmount=3000, remainder=2000
    expect(true).toBe(false) // RED
  })

  it('strips non-numeric characters from code input', () => {
    // Pitfall 3: '4827-1593' -> '48271593'
    expect(true).toBe(false) // RED
  })
})
```

These stubs are deliberately RED. Plans 02 and 03 will extract utility functions and make these tests pass.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/bin:$PATH" && cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "expect.*false" src/lib/__tests__/gift-card-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/gift-card-utils.test.ts exists
    - File has 3 describe blocks: code generation, expiry validation, balance operations
    - File has at least 8 test stubs (it blocks)
    - All stubs contain expect(true).toBe(false) — they WILL fail
    - File uses vitest imports (describe, it, expect)
  </acceptance_criteria>
  <done>Gift card utility test stubs created in RED state. 8+ test stubs covering code generation (8-digit, padding, formatting), expiry validation (effective status, active/voided), and balance operations (auto-split, code stripping).</done>
</task>

<task type="auto">
  <name>Task 2: Create cart state machine gift card test stubs</name>
  <files>src/lib/__tests__/pos-cart-gift-card.test.ts</files>
  <read_first>
    - src/lib/__tests__/pos-cart.test.ts (existing cart test pattern — how cartReducer is tested, initialCartState usage)
    - src/lib/cart.ts (current CartState, CartAction types — will be extended in Plan 03)
  </read_first>
  <action>
Create `src/lib/__tests__/pos-cart-gift-card.test.ts` with test stubs for gift card cart state machine transitions.

```typescript
import { describe, it, expect } from 'vitest'

describe('cart state machine — gift card payment', () => {
  it('transitions to gift_card_entry phase when gift_card payment method selected', () => {
    // SET_PAYMENT_METHOD { method: 'gift_card' } -> INITIATE_PAYMENT -> phase: 'gift_card_entry'
    expect(true).toBe(false) // RED — Plan 03 adds gift_card to cart
  })

  it('stores gift card code on ENTER_GIFT_CARD_CODE action', () => {
    // Strips non-digits, stores raw 8-digit code
    expect(true).toBe(false) // RED
  })

  it('computes auto-split on GIFT_CARD_VALIDATED when balance covers full total', () => {
    // giftCardAmountCents = totalCents, phase -> 'gift_card_confirmed'
    expect(true).toBe(false) // RED
  })

  it('remains in gift_card_entry on GIFT_CARD_VALIDATED when balance < total (needs split method)', () => {
    // giftCardAmountCents = balanceCents (< total), splitRemainderMethod needed
    expect(true).toBe(false) // RED
  })

  it('transitions to gift_card_confirmed after SET_SPLIT_REMAINDER_METHOD', () => {
    expect(true).toBe(false) // RED
  })

  it('resets gift card fields on GIFT_CARD_VALIDATION_FAILED', () => {
    expect(true).toBe(false) // RED
  })

  it('resets all gift card fields on NEW_SALE', () => {
    expect(true).toBe(false) // RED
  })
})
```

These stubs are deliberately RED. Plan 03 Task 1 extends cart.ts and will make these tests pass.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/bin:$PATH" && cd /Users/adam-personal/CODER/IDEAS/NZPOS && grep -c "expect.*false" src/lib/__tests__/pos-cart-gift-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/pos-cart-gift-card.test.ts exists
    - File has describe block for cart gift card payment
    - File has at least 7 test stubs (it blocks)
    - All stubs contain expect(true).toBe(false) — they WILL fail
    - Stubs cover: payment method selection, code entry, full cover validation, partial cover, split method, validation failure reset, new sale reset
  </acceptance_criteria>
  <done>Cart state machine gift card test stubs created in RED state. 7 test stubs covering all gift card cart transitions that Plan 03 will implement.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/gift-card-utils.test.ts` runs and shows failing tests (expected)
- `npx vitest run src/lib/__tests__/pos-cart-gift-card.test.ts` runs and shows failing tests (expected)
- Test files exist with correct structure
</verification>

<success_criteria>
- Both test stub files exist and are valid vitest test files
- All stubs are in RED state (failing by design)
- Stubs cover: code generation, expiry validation, balance operations, cart state machine transitions
- Later plans (02, 03) will make these tests pass during implementation
</success_criteria>

<output>
After completion, create `.planning/phases/35-gift-cards-add-on/35-00-SUMMARY.md`
</output>
