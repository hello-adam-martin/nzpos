---
phase: 36-advanced-reporting-cogs-add-on
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cogs.ts
  - src/lib/cogs.test.ts
autonomous: true
requirements:
  - COGS-04
  - COGS-05
  - COGS-06

must_haves:
  truths:
    - "aggregateCOGS correctly aggregates revenue, cost, and margin across order items"
    - "Products without cost prices are excluded from margin totals"
    - "Category grouping produces correct aggregated totals"
    - "CSV data shape includes all required columns with correct types"
    - "Margin percentage calculation handles zero revenue gracefully"
  artifacts:
    - path: "src/lib/cogs.ts"
      provides: "COGS aggregation logic, margin calculation, CSV data formatting, category grouping"
      exports: ["aggregateCOGS", "calculateMarginPercent", "productMarginPercent", "groupByCategory", "formatCogsCSV"]
    - path: "src/lib/cogs.test.ts"
      provides: "Unit tests for all COGS calculation functions"
      contains: "describe.*aggregateCOGS"
  key_links:
    - from: "src/lib/cogs.test.ts"
      to: "src/lib/cogs.ts"
      via: "import { aggregateCOGS, calculateMarginPercent, productMarginPercent, groupByCategory, formatCogsCSV }"
      pattern: "aggregateCOGS"
---

<objective>
Create the COGS calculation library with all pure functions for aggregation, margin calculation, category grouping, and CSV formatting — with comprehensive TDD-style tests.

Purpose: Isolates all business logic into a testable pure-function module. Plan 03 (reports UI) will import these functions.
Output: `src/lib/cogs.ts` with exported functions, `src/lib/cogs.test.ts` with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-advanced-reporting-cogs-add-on/36-CONTEXT.md
@.planning/phases/36-advanced-reporting-cogs-add-on/36-RESEARCH.md

<interfaces>
<!-- Types consumed by this module — defined inline in cogs.ts (no external dependency) -->

```typescript
// Input types (match Supabase query shapes)
interface OrderItem {
  product_id: string | null
  product_name: string
  quantity: number
  line_total_cents: number
  gst_cents: number
}

interface ProductCostData {
  id: string
  cost_price_cents: number | null
  category_id: string | null
  categories: { name: string } | null
}

// Output types
export interface CogsLineItem {
  productId: string | null
  productName: string
  sku: string | null
  categoryName: string | null
  unitsSold: number
  revenueExclGstCents: number
  costCents: number
  hasCostPrice: boolean
  marginCents: number | null
  marginPercent: number | null
}

export interface CogsCategoryGroup {
  categoryName: string
  products: CogsLineItem[]
  totalUnitsSold: number
  totalRevenueExclGstCents: number
  totalCostCents: number
  totalMarginCents: number | null
  marginPercent: number | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: COGS calculation library with tests</name>
  <files>src/lib/cogs.ts, src/lib/cogs.test.ts</files>
  <read_first>
    src/app/admin/reports/page.tsx,
    src/components/admin/reports/ExportCSVButton.tsx,
    vitest.config.mts
  </read_first>
  <behavior>
    **aggregateCOGS tests:**
    - Test 1: Single item with cost price returns correct revenue excl GST, cost, margin, margin %
      - Input: 1 item, product_id="p1", quantity=2, line_total_cents=2300, gst_cents=300; cost data: cost_price_cents=800
      - Expected: revenueExclGstCents=2000, costCents=1600, marginCents=400, marginPercent=20.0, hasCostPrice=true
    - Test 2: Item with NULL cost_price_cents has hasCostPrice=false, marginCents=null, marginPercent=null
    - Test 3: Multiple items for same product_id are aggregated (summed quantities, revenue, cost)
    - Test 4: Items with null product_id use product_name as grouping key
    - Test 5: Results sorted by revenueExclGstCents descending
    - Test 6: Empty items array returns empty array

    **calculateMarginPercent tests:**
    - Test 7: Normal case: (2000 - 1600) / 2000 * 100 = 20.0
    - Test 8: Zero revenue returns null
    - Test 9: Negative margin returns negative percentage

    **productMarginPercent tests:**
    - Test 10: price_cents=1150, cost_price_cents=500 => sell excl GST = 1000, margin = 50.0%
    - Test 11: price_cents=1150, cost_price_cents=1000 => margin = 0.0%

    **groupByCategory tests:**
    - Test 12: Groups items by categoryName, aggregates totals per category
    - Test 13: Items with null categoryName grouped under "Uncategorized"
    - Test 14: Category with all null-cost products has totalMarginCents=null

    **formatCogsCSV tests:**
    - Test 15: Returns array of objects with keys: product_name, sku, category, units_sold, revenue_excl_gst_cents, cost_cents, margin_cents, margin_percent
    - Test 16: Null margin shows "—" for margin_percent
    - Test 17: Null sku shows empty string
  </behavior>
  <action>
    **Write tests first (RED)** in `src/lib/cogs.test.ts`, then implement in `src/lib/cogs.ts`.

    **`src/lib/cogs.ts` must export these functions:**

    1. `aggregateCOGS(items: OrderItem[], productCosts: ProductCostData[]): CogsLineItem[]`
       - Build a Map from productCosts: product id -> cost data
       - Iterate items, group by `product_id ?? product_name`
       - For each group: sum quantity, sum `line_total_cents - gst_cents` as revenueExclGstCents, sum `cost_price_cents * quantity` as costCents
       - Set `hasCostPrice = costData?.cost_price_cents != null`
       - After aggregation, compute marginCents and marginPercent for items with cost prices
       - Sort by revenueExclGstCents descending
       - Per D-03: products with NULL cost price have `hasCostPrice: false`, `marginCents: null`, `marginPercent: null`

    2. `calculateMarginPercent(revenueExclGstCents: number, totalCostCents: number): number | null`
       - Guard: if revenueExclGstCents <= 0, return null
       - Return `((revenueExclGstCents - totalCostCents) / revenueExclGstCents) * 100`

    3. `productMarginPercent(priceCents: number, costPriceCents: number): number`
       - `const sellExclGst = priceCents / 1.15`
       - Return `((sellExclGst - costPriceCents) / sellExclGst) * 100`

    4. `groupByCategory(items: CogsLineItem[]): CogsCategoryGroup[]`
       - Group CogsLineItem[] by categoryName (null -> "Uncategorized")
       - Per category: sum unitsSold, revenueExclGstCents, costCents
       - Compute totalMarginCents only if at least one product hasCostPrice
       - Sort categories alphabetically

    5. `formatCogsCSV(items: CogsLineItem[]): Array<Record<string, string | number>>`
       - Map each item to: `{ product_name, sku: sku ?? '', category: categoryName ?? '', units_sold, revenue_excl_gst_cents, cost_cents, margin_cents: marginCents ?? 0, margin_percent: marginPercent !== null ? marginPercent.toFixed(1) + '%' : '—' }`
       - Per D-10: one row per product with all specified columns

    Export all types: `CogsLineItem`, `CogsCategoryGroup`, `OrderItem`, `ProductCostData`
  </action>
  <verify>
    <automated>npx vitest run src/lib/cogs.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/cogs.ts exports `aggregateCOGS` function
    - src/lib/cogs.ts exports `calculateMarginPercent` function
    - src/lib/cogs.ts exports `productMarginPercent` function
    - src/lib/cogs.ts exports `groupByCategory` function
    - src/lib/cogs.ts exports `formatCogsCSV` function
    - src/lib/cogs.ts exports `CogsLineItem` and `CogsCategoryGroup` types
    - src/lib/cogs.test.ts contains at least 15 test cases
    - src/lib/cogs.test.ts contains `describe('aggregateCOGS'`
    - src/lib/cogs.test.ts contains `describe('calculateMarginPercent'`
    - src/lib/cogs.test.ts contains `describe('groupByCategory'`
    - src/lib/cogs.test.ts contains `describe('formatCogsCSV'`
    - `npx vitest run src/lib/cogs.test.ts` exits with code 0 (all tests pass)
    - aggregateCOGS test verifies hasCostPrice=false for null cost products
    - calculateMarginPercent test verifies null return for zero revenue
    - formatCogsCSV test verifies "—" for null margin_percent
  </acceptance_criteria>
  <done>All COGS pure functions are implemented and tested. 15+ tests pass covering aggregation, margin calculation, category grouping, and CSV formatting. Null cost prices handled per D-03.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/cogs.test.ts --reporter=verbose` — all tests pass
- `grep -c "test\|it(" src/lib/cogs.test.ts` — at least 15 test cases
- `grep "export function" src/lib/cogs.ts` — 5 exported functions
</verification>

<success_criteria>
COGS calculation library exists with 5 exported pure functions. All functions have comprehensive tests that pass. Margin math is correct per D-02 (GST-exclusive cost). Null cost prices handled per D-03.
</success_criteria>

<output>
After completion, create `.planning/phases/36-advanced-reporting-cogs-add-on/36-02-SUMMARY.md`
</output>
