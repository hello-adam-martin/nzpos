---
phase: 13-merchant-self-serve-signup
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/app/signup/page.tsx
  - src/app/signup/provisioning/page.tsx
  - src/app/signup/verify-email/page.tsx
  - src/components/signup/SignupForm.tsx
  - src/components/signup/SlugInput.tsx
  - src/components/signup/ProvisioningScreen.tsx
  - src/components/signup/ProvisioningStepList.tsx
  - src/components/signup/VerifyEmailCard.tsx
  - src/components/signup/AuthCard.tsx
autonomous: false
requirements: [SIGNUP-01, SIGNUP-03]

must_haves:
  truths:
    - "Merchant can fill out 4 fields (email, password, store name, slug) and submit the signup form"
    - "Slug auto-populates from store name and can be manually edited"
    - "Live slug availability check shows green checkmark (available) or red X (taken/reserved) with debounced API call"
    - "After successful signup, merchant sees a provisioning progress screen with 3 animated steps"
    - "If provisioning fails, merchant sees error state with retry button"
    - "Unverified merchant sees the verification screen with resend button and 30-second cooldown"
    - "After email verification, merchant lands on their store's admin dashboard"
  artifacts:
    - path: "src/app/signup/page.tsx"
      provides: "Signup page shell"
      min_lines: 5
    - path: "src/components/signup/SignupForm.tsx"
      provides: "4-field signup form with slug auto-generation"
      contains: "ownerSignup"
    - path: "src/components/signup/SlugInput.tsx"
      provides: "Slug input with debounced availability check"
      contains: "checkSlugAvailability"
    - path: "src/app/signup/provisioning/page.tsx"
      provides: "Provisioning loading page"
      min_lines: 5
    - path: "src/components/signup/ProvisioningScreen.tsx"
      provides: "Progress steps + retry on failure"
      contains: "retryProvisioning"
    - path: "src/app/signup/verify-email/page.tsx"
      provides: "Email verification gate page"
      min_lines: 5
    - path: "src/components/signup/VerifyEmailCard.tsx"
      provides: "Verification card with resend + cooldown"
      contains: "resendVerification"
    - path: "src/components/signup/AuthCard.tsx"
      provides: "Reusable white card shell"
      contains: "max-w-sm"
  key_links:
    - from: "src/components/signup/SignupForm.tsx"
      to: "src/actions/auth/ownerSignup.ts"
      via: "form action / useActionState"
      pattern: "ownerSignup"
    - from: "src/components/signup/SlugInput.tsx"
      to: "src/actions/auth/checkSlugAvailability.ts"
      via: "debounced call in useEffect"
      pattern: "checkSlugAvailability"
    - from: "src/components/signup/ProvisioningScreen.tsx"
      to: "src/actions/auth/retryProvisioning.ts"
      via: "retry button action"
      pattern: "retryProvisioning"
    - from: "src/components/signup/VerifyEmailCard.tsx"
      to: "src/actions/auth/resendVerification.ts"
      via: "resend button action"
      pattern: "resendVerification"
---

<objective>
Build the three signup screens (form, provisioning, verification) with all interactive components following the UI-SPEC visual contract.

Purpose: Give merchants a complete self-service signup experience — from form entry through provisioning to email verification — that connects to all the Server Actions built in Plan 02.
Output: Three working pages at /signup, /signup/provisioning, and /signup/verify-email with all specified visual states and interactions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-merchant-self-serve-signup/13-CONTEXT.md
@.planning/phases/13-merchant-self-serve-signup/13-UI-SPEC.md
@.planning/phases/13-merchant-self-serve-signup/13-02-SUMMARY.md
@DESIGN.md
@src/app/login/page.tsx

<interfaces>
<!-- From Plan 02 (src/actions/auth/ownerSignup.ts): -->
export async function ownerSignup(formData: FormData): Promise<{
  error?: Record<string, string[]>;
  success?: boolean;
  slug?: string;
}>

<!-- From Plan 02 (src/actions/auth/checkSlugAvailability.ts): -->
export async function checkSlugAvailability(slug: string): Promise<{
  available: boolean;
  reason?: string;
}>

<!-- From Plan 02 (src/actions/auth/retryProvisioning.ts): -->
export async function retryProvisioning(formData: FormData): Promise<{
  success?: boolean;
  slug?: string;
  error?: string;
}>

<!-- From Plan 02 (src/actions/auth/resendVerification.ts): -->
export async function resendVerification(formData: FormData): Promise<{
  error?: string;
  success?: boolean;
}>

<!-- From Plan 01 (src/lib/slugValidation.ts): -->
export function slugify(name: string): string
export function validateSlug(slug: string): { valid: boolean; reason?: string }

<!-- Login page pattern (src/app/login/page.tsx): -->
<!-- Card: w-full max-w-sm rounded-lg bg-white p-8 shadow-md border border-[var(--color-border)] -->
<!-- Input: w-full rounded-md border border-[var(--color-border)] bg-white px-3 py-2 font-sans text-sm text-[var(--color-text)] placeholder:text-[var(--color-text-light)] focus:border-[var(--color-navy)] focus:outline-none focus:ring-1 focus:ring-[var(--color-navy)] -->
<!-- Label: block font-sans text-sm font-bold text-[var(--color-text)] mb-1 -->
<!-- Brand: font-display text-2xl font-bold text-[var(--color-navy)] mb-2 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthCard shell + SignupForm + SlugInput components + signup page</name>
  <files>
    src/components/signup/AuthCard.tsx,
    src/components/signup/SignupForm.tsx,
    src/components/signup/SlugInput.tsx,
    src/app/signup/page.tsx
  </files>
  <read_first>
    src/app/login/page.tsx,
    src/actions/auth/ownerSignup.ts,
    src/actions/auth/checkSlugAvailability.ts,
    src/lib/slugValidation.ts,
    DESIGN.md,
    .planning/phases/13-merchant-self-serve-signup/13-UI-SPEC.md,
    src/app/globals.css
  </read_first>
  <action>
    CREATE src/components/signup/AuthCard.tsx:
    - Reusable layout shell matching login page card pattern
    - 'use client' is NOT needed — this is a presentational wrapper, can be server component
    - Props: { children: React.ReactNode }
    - Render: `<main className="flex min-h-screen items-center justify-center bg-[var(--color-bg)]"><div className="w-full max-w-sm rounded-lg bg-white p-8 shadow-md border border-[var(--color-border)]">{children}</div></main>`
    - Per UI-SPEC: max-w-sm (384px), card padding 32px (p-8), white bg, border, shadow-sm (use shadow-md to match login)

    CREATE src/components/signup/SlugInput.tsx:
    - 'use client' directive
    - Props: { value: string; onChange: (slug: string) => void; error?: string }
    - Internal state: availabilityStatus: 'idle' | 'checking' | 'available' | 'taken' | 'invalid', reason: string
    - Debounced availability check using useEffect + setTimeout (400ms per UI-SPEC):
      - On value change, clear previous timeout, set new 400ms timeout
      - When fired: if value.length < 3, set idle; else call checkSlugAvailability(value)
      - On result: set status to 'available' or 'taken'/'invalid' based on response
    - Render:
      - Label: "Store address" (per Copywriting Contract), font-sans text-sm font-semibold text-[var(--color-text)] mb-1
      - Input with same classes as login page inputs + trailing icon area
      - Trailing icon: spinner (checking), green checkmark SVG (available), red X SVG (taken/invalid)
      - Below input: helper text "{slug}.nzpos.co.nz" in 14px text-muted when idle
      - Below input: "{slug} is available" in 14px font-semibold text-[var(--color-success)] when available
      - Below input: "{slug} is taken - try another" in 14px font-semibold text-[var(--color-error)] when taken
      - Below input: reason text in 14px font-semibold text-[var(--color-error)] when invalid
      - aria-live="polite" region for status announcements
      - aria-describedby linking input to status text
    - Hidden input name="slug" with current value (for formData)

    CREATE src/components/signup/SignupForm.tsx:
    - 'use client' directive
    - Import: ownerSignup, slugify, SlugInput, useActionState from 'react', useRouter from 'next/navigation'
    - State: slug (string), storeNameTouched (boolean to track if slug was auto-set)
    - useActionState wrapping ownerSignup:
      - On success (result.success && result.slug): router.push(`/signup/provisioning?slug=${result.slug}`)
      - On error: display field errors
    - Form fields (per UI-SPEC Screen 1, D-01):
      1. Email: type=email, autocomplete=email, required
      2. Password: type=password, autocomplete=new-password, required, helper text "Minimum 8 characters" (14px text-muted)
      3. Store Name: type=text, autocomplete=organization, placeholder="My Supply Store"
         - onBlur: if slug is empty or was auto-generated (not manually edited), set slug = slugify(e.target.value)
      4. SlugInput component with value={slug} onChange={setSlug}
    - Form-level error (state.error?._form): tinted error alert above submit button, text-[var(--color-error)], 1px border, icon
    - Field-level errors (state.error?.email, etc.): 14px text-[var(--color-error)] below each field
    - Submit button: full width, bg-[var(--color-amber)] text-white, font-sans text-sm font-semibold, min-h-[44px], rounded-md
      - Normal: "Create my store"
      - Pending: "Creating your store..." with spinner, disabled, opacity-50
    - Footer: "Already have an account? Sign in" link — 14px, text-muted, link in text-navy underline, href="/login"
    - All inputs use the login page class pattern: w-full rounded-md border border-[var(--color-border)] bg-white px-3 py-2 font-sans text-sm text-[var(--color-text)] placeholder:text-[var(--color-text-light)] focus:border-[var(--color-navy)] focus:outline-none focus:ring-1 focus:ring-[var(--color-navy)]
    - All labels: block font-sans text-sm font-semibold text-[var(--color-text)] mb-1 (use semibold per UI-SPEC, not bold)
    - All inputs have htmlFor/id pairing per accessibility notes

    REWRITE src/app/signup/page.tsx:
    - Import AuthCard and SignupForm
    - Render:
      ```
      <AuthCard>
        <h1 className="font-display text-3xl font-semibold text-[var(--color-navy)] mb-1">NZPOS</h1>
        <h2 className="font-display text-2xl font-semibold text-[var(--color-navy)] mb-1">Start your free store</h2>
        <p className="font-sans text-sm text-[var(--color-text-muted)] mb-6">Your POS and online store, ready in minutes.</p>
        <SignupForm />
      </AuthCard>
      ```
    - Per UI-SPEC: brand mark is Satoshi 30px (3xl) semibold navy, heading is 24px (2xl) semibold navy
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30 && test -f src/components/signup/AuthCard.tsx && test -f src/components/signup/SignupForm.tsx && test -f src/components/signup/SlugInput.tsx && echo "All files exist"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/signup/AuthCard.tsx contains "max-w-sm"
    - src/components/signup/AuthCard.tsx contains "min-h-screen"
    - src/components/signup/AuthCard.tsx contains "color-bg"
    - src/components/signup/SignupForm.tsx contains "'use client'"
    - src/components/signup/SignupForm.tsx contains "ownerSignup"
    - src/components/signup/SignupForm.tsx contains "slugify"
    - src/components/signup/SignupForm.tsx contains "SlugInput"
    - src/components/signup/SignupForm.tsx contains "Create my store"
    - src/components/signup/SignupForm.tsx contains "Creating your store..."
    - src/components/signup/SignupForm.tsx contains "color-amber"
    - src/components/signup/SignupForm.tsx contains "Already have an account"
    - src/components/signup/SignupForm.tsx contains "/signup/provisioning"
    - src/components/signup/SlugInput.tsx contains "'use client'"
    - src/components/signup/SlugInput.tsx contains "checkSlugAvailability"
    - src/components/signup/SlugInput.tsx contains "aria-live"
    - src/components/signup/SlugInput.tsx contains "Store address"
    - src/components/signup/SlugInput.tsx contains "nzpos.co.nz"
    - src/components/signup/SlugInput.tsx contains "color-success"
    - src/components/signup/SlugInput.tsx contains "color-error"
    - src/components/signup/SlugInput.tsx contains "setTimeout" (debounce)
    - src/app/signup/page.tsx contains "Start your free store"
    - src/app/signup/page.tsx contains "SignupForm"
    - src/app/signup/page.tsx contains "AuthCard"
    - npx tsc --noEmit exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>Signup page renders at /signup with brand mark, heading, subheading, and 4-field form. Slug auto-populates from store name on blur. Live slug availability check fires 400ms after last keystroke with green/red indicator. Form submits to ownerSignup and navigates to /signup/provisioning on success. All visual styles match UI-SPEC and login page pattern.</done>
</task>

<task type="auto">
  <name>Task 2: ProvisioningScreen + VerifyEmailCard + remaining pages</name>
  <files>
    src/components/signup/ProvisioningScreen.tsx,
    src/components/signup/ProvisioningStepList.tsx,
    src/components/signup/VerifyEmailCard.tsx,
    src/app/signup/provisioning/page.tsx,
    src/app/signup/verify-email/page.tsx
  </files>
  <read_first>
    src/actions/auth/retryProvisioning.ts,
    src/actions/auth/resendVerification.ts,
    src/components/signup/AuthCard.tsx,
    DESIGN.md,
    .planning/phases/13-merchant-self-serve-signup/13-UI-SPEC.md
  </read_first>
  <action>
    CREATE src/components/signup/ProvisioningStepList.tsx:
    - 'use client' directive
    - Props: { currentStep: number; failed: boolean } where currentStep is 0-3 (0 = not started, 3 = all complete)
    - Steps array: ["Creating your account", "Provisioning your store", "Preparing your dashboard"] (per Copywriting Contract)
    - Each step renders:
      - 20px circle indicator: pending = hollow border-[var(--color-border)], active = navy animated spinner (animate-spin, 800ms), complete = bg-[var(--color-success)] with white checkmark SVG
      - Label: active step = text-[var(--color-text)] font-semibold, pending/complete = text-[var(--color-text-muted)] font-normal
      - Vertical layout with 16px gap
    - role="list" on container, role="listitem" on each step, aria-current="step" on active step
    - Transitions: 150ms ease-out on state changes (per DESIGN.md micro/short rule)

    CREATE src/components/signup/ProvisioningScreen.tsx:
    - 'use client' directive
    - Props: { slug: string; storeName: string }
    - State: step (number 0-3), failed (boolean), error (string)
    - On mount (useEffect):
      - Since ownerSignup already completed provisioning before redirecting here, immediately animate through steps 1 -> 2 -> 3 with short delays (500ms each) as a UX affordance
      - After step 3 completes, show "Redirecting to your dashboard..." text (fade-in 250ms)
      - Then redirect: `const rootDomain = process.env.NEXT_PUBLIC_ROOT_DOMAIN ?? 'lvh.me:3000'; const protocol = rootDomain.includes('lvh.me') || rootDomain.includes('localhost') ? 'http' : 'https'; router.push(\`\${protocol}://\${slug}.\${rootDomain}/admin/dashboard\`)`
    - NOTE: The provisioning already happened in the ownerSignup Server Action. This page is a UX transition, not a real polling loop. The slug in the URL param tells us it succeeded.
    - Failure state (if slug param is missing or "failed" param is present):
      - Show ProvisioningStepList with failed=true at step 2
      - Show error icon (48px, color-error), heading "Something went wrong", body per Copywriting Contract
      - "Retry provisioning" button: full-width, bg-[var(--color-navy)] text-white, 44px height, rounded-md
        - On click: call retryProvisioning via formData (pass storeName and slug from URL params)
        - On retry success: animate steps to completion and redirect
        - On retry failure: show error message
      - "Contact support" link: 14px, text-muted, centered below

    For retry case: The provisioning page URL will be `/signup/provisioning?slug=xxx&storeName=xxx&status=failed` when provisioning fails. The SignupForm should redirect here with status=failed if ownerSignup returns an error that contains _form (not field-level), AND the error is provisioning-related. For field-level errors (email, password, slug), stay on the form page.

    CREATE src/components/signup/VerifyEmailCard.tsx:
    - 'use client' directive
    - Props: { email: string }
    - State: resendStatus: 'idle' | 'sending' | 'sent' | 'rate-limited', cooldownRemaining: number
    - Resend button handler:
      - Call resendVerification via formData (pass email)
      - On success: set status to 'sent', start 30-second cooldown timer
      - On error containing "wait": set status to 'rate-limited'
      - During cooldown: button disabled, aria-label="Resend verification email - wait {seconds} seconds"
    - Render (per UI-SPEC Screen 3):
      - Envelope SVG icon: 48px, text-[var(--color-navy)], centered, mb-4
      - Heading: "Check your email" — font-display text-2xl font-semibold text-[var(--color-navy)]
      - Body: "We sent a verification link to {email}. Click it to activate your store." — font-sans text-base text-[var(--color-text)], mt-2, leading-relaxed
      - Resend button: full-width, border border-[var(--color-border)] bg-white text-[var(--color-navy)], 44px height, rounded-md (ghost variant)
        - Normal: "Resend verification email"
        - Sending: "Sending..." disabled opacity-50
        - Sent: "Email sent - check your inbox" with success icon
        - Rate-limited: disabled, show "Please wait before requesting another email." below in 14px text-muted
      - "Wrong email? Sign out" link: 14px, text-muted, centered, mt-2 — clicking signs out and redirects to /signup

    CREATE src/app/signup/provisioning/page.tsx:
    - Read slug, storeName, status from searchParams
    - Render ProvisioningScreen with props from searchParams
    - No AuthCard wrapper — provisioning uses full-screen centered layout per UI-SPEC Screen 2

    CREATE src/app/signup/verify-email/page.tsx:
    - Read email from searchParams
    - Render inside AuthCard:
      - NZPOS brand mark (font-display text-3xl font-semibold text-navy)
      - VerifyEmailCard with email prop
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30 && test -f src/app/signup/provisioning/page.tsx && test -f src/app/signup/verify-email/page.tsx && test -f src/components/signup/ProvisioningScreen.tsx && test -f src/components/signup/VerifyEmailCard.tsx && echo "All files exist"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/signup/ProvisioningStepList.tsx contains "role=\"list\""
    - src/components/signup/ProvisioningStepList.tsx contains "aria-current"
    - src/components/signup/ProvisioningStepList.tsx contains "Creating your account"
    - src/components/signup/ProvisioningStepList.tsx contains "Provisioning your store"
    - src/components/signup/ProvisioningStepList.tsx contains "Preparing your dashboard"
    - src/components/signup/ProvisioningStepList.tsx contains "animate-spin"
    - src/components/signup/ProvisioningScreen.tsx contains "'use client'"
    - src/components/signup/ProvisioningScreen.tsx contains "retryProvisioning"
    - src/components/signup/ProvisioningScreen.tsx contains "Setting up your store"
    - src/components/signup/ProvisioningScreen.tsx contains "Retry provisioning"
    - src/components/signup/ProvisioningScreen.tsx contains "admin/dashboard"
    - src/components/signup/VerifyEmailCard.tsx contains "'use client'"
    - src/components/signup/VerifyEmailCard.tsx contains "resendVerification"
    - src/components/signup/VerifyEmailCard.tsx contains "Check your email"
    - src/components/signup/VerifyEmailCard.tsx contains "Resend verification email"
    - src/components/signup/VerifyEmailCard.tsx contains "Wrong email? Sign out"
    - src/components/signup/VerifyEmailCard.tsx contains "30" (cooldown seconds)
    - src/app/signup/provisioning/page.tsx contains "ProvisioningScreen"
    - src/app/signup/verify-email/page.tsx contains "VerifyEmailCard"
    - src/app/signup/verify-email/page.tsx contains "AuthCard"
    - npx tsc --noEmit exits 0 (or only pre-existing errors)
  </acceptance_criteria>
  <done>Provisioning page at /signup/provisioning shows animated 3-step progress, auto-redirects to store dashboard on success, shows error state with retry button on failure. Verification page at /signup/verify-email shows email confirmation prompt with resend button, 30-second cooldown, and sign-out link. All visual states match UI-SPEC screens 2, 3, and 4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete signup flow</name>
  <files>none</files>
  <action>Human verification of the complete signup flow across all three screens. No code changes — visual and functional inspection only.</action>
  <what-built>Complete merchant self-serve signup flow: 4-field form with live slug check, provisioning progress screen, email verification gate, and subdomain redirect after verification.</what-built>
  <how-to-verify>
    1. Start local dev: `npm run dev` and ensure Supabase is running (`supabase start`)
    2. Visit http://lvh.me:3000/signup
    3. Verify: Page shows "NZPOS" brand mark, "Start your free store" heading, "Your POS and online store, ready in minutes." subheading
    4. Verify: 4 fields visible — Email, Password, Store Name, Store Slug ("Store address")
    5. Type a store name (e.g. "Test Supplies") — verify slug field auto-populates with "test-supplies" on blur
    6. Edit slug to "admin" — verify red X and "This address is reserved" message
    7. Edit slug to "ab" — verify error about 3-30 characters
    8. Edit slug to something unique (e.g. "teststore123") — verify green checkmark and "teststore123 is available"
    9. Fill in email and password, click "Create my store"
    10. Verify: Button shows "Creating your store..." spinner
    11. Verify: Redirected to /signup/provisioning page with animated progress steps
    12. Verify: Steps animate through "Creating your account" -> "Provisioning your store" -> "Preparing your dashboard"
    13. Verify: After steps complete, redirected to teststore123.lvh.me:3000/admin/dashboard (but will hit verification gate)
    14. Verify: Redirected to /signup/verify-email with "Check your email" message showing your email
    15. Visit http://127.0.0.1:54324 (Inbucket) — find verification email, click the link
    16. Verify: After clicking verification link, redirected to teststore123.lvh.me:3000/admin/dashboard
    17. Verify: Admin dashboard loads correctly for the new store
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — human confirms end-to-end signup flow works"</automated>
  </verify>
  <done>Human has confirmed: signup form renders correctly, slug validation works, provisioning screen animates, email verification gate blocks unverified users, and post-verification redirect lands on the correct store admin dashboard.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Full test suite passes: `npm run test`
- Signup page renders at /signup with correct layout and form
- Provisioning page renders at /signup/provisioning
- Verification page renders at /signup/verify-email
- Complete end-to-end flow works from signup to verified dashboard access
</verification>

<success_criteria>
- All 3 screens render per UI-SPEC visual contract
- Signup form has 4 fields with slug auto-generation and live availability check
- Provisioning screen shows animated progress and handles failure with retry
- Verification screen shows email prompt with resend cooldown
- Complete flow: signup -> provisioning -> email verify -> store admin dashboard
- Accessibility: labels linked to inputs, aria-live on slug status, aria-current on step list
</success_criteria>

<output>
After completion, create `.planning/phases/13-merchant-self-serve-signup/13-03-SUMMARY.md`
</output>
