---
phase: 14-store-setup-wizard-marketing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/(marketing)/components/LandingNav.tsx
  - src/app/(marketing)/components/LandingHero.tsx
  - src/app/(marketing)/components/LandingFeatures.tsx
  - src/app/(marketing)/components/LandingPricing.tsx
  - src/app/(marketing)/components/LandingCTA.tsx
  - src/app/(marketing)/components/LandingFooter.tsx
autonomous: true
requirements:
  - MKTG-01
  - MKTG-02

must_haves:
  truths:
    - "Landing page renders at root domain / with hero, features, pricing, and CTAs"
    - "Landing page is statically rendered (no server-side data fetching, no dynamic opt-in)"
    - "Hero CTA links directly to /signup in one click"
    - "Pricing section shows free core and paid add-ons with NZD prices"
    - "Page is mobile-optimised with responsive typography and layout"
    - "All copy uses NZ spelling and friendly NZ-focused tone"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Root domain landing page entry"
      contains: "force-static"
    - path: "src/app/(marketing)/components/LandingNav.tsx"
      provides: "Sticky nav with NZPOS wordmark, Sign in, Get started"
      contains: "NZPOS"
    - path: "src/app/(marketing)/components/LandingHero.tsx"
      provides: "Hero section with headline, sub-headline, CTA"
      contains: "The POS built for Kiwi retailers"
    - path: "src/app/(marketing)/components/LandingPricing.tsx"
      provides: "Pricing section with free core and add-ons"
      contains: "$9/month NZD"
    - path: "src/app/(marketing)/components/LandingFooter.tsx"
      provides: "Footer with copyright and links"
      contains: "Built in New Zealand"
  key_links:
    - from: "src/app/(marketing)/components/LandingHero.tsx"
      to: "/signup"
      via: "Link href"
      pattern: "/signup"
    - from: "src/app/(marketing)/components/LandingCTA.tsx"
      to: "/signup"
      via: "Link href"
      pattern: "/signup"
---

<objective>
Build the static marketing landing page at the root domain with hero, features, pricing, and signup CTAs.

Purpose: Drives merchant signups by clearly communicating the product value proposition to NZ small businesses.
Output: 1 page route + 6 section components, all statically rendered with no data fetching.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-store-setup-wizard-marketing/14-CONTEXT.md
@.planning/phases/14-store-setup-wizard-marketing/14-UI-SPEC.md
@DESIGN.md

<interfaces>
<!-- No dependencies on other plans — landing page is self-contained -->

From src/middleware.ts (root domain handling, lines 20-26):
```typescript
const isRoot = host === rootDomain || host === `www.${rootDomain}` || host.startsWith('localhost')
if (isRoot) {
  const { response } = await createSupabaseMiddlewareClient(request)
  return response  // passes through — no tenant resolution
}
```
Note: Root domain requests pass through middleware untouched. No changes needed.

From src/app/layout.tsx (root layout):
```typescript
// Contains: Bunny Fonts CDN links for Satoshi + DM Sans + Geist Mono
// Contains: globals.css import with CSS custom properties
// The landing page will use this root layout — fonts and tokens are available
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Landing page route and all section components</name>
  <files>
    src/app/page.tsx,
    src/app/(marketing)/components/LandingNav.tsx,
    src/app/(marketing)/components/LandingHero.tsx,
    src/app/(marketing)/components/LandingFeatures.tsx,
    src/app/(marketing)/components/LandingPricing.tsx,
    src/app/(marketing)/components/LandingCTA.tsx,
    src/app/(marketing)/components/LandingFooter.tsx
  </files>
  <read_first>
    src/app/layout.tsx,
    src/app/globals.css,
    src/middleware.ts,
    DESIGN.md,
    .planning/phases/14-store-setup-wizard-marketing/14-UI-SPEC.md
  </read_first>
  <action>
    **CRITICAL: All components are pure JSX Server Components. No `'use client'`, no `useState`, no `useEffect`, no `cookies()`, no `headers()`, no Supabase calls. This ensures static rendering per MKTG-02.**

    Exception: `LandingNav` needs a mobile hamburger menu toggle — use a `<details>/<summary>` HTML pattern or a tiny client component for the toggle only. Prefer the HTML approach to keep the page fully static.

    **Root page (src/app/page.tsx):**
    ```typescript
    import LandingNav from './(marketing)/components/LandingNav'
    import LandingHero from './(marketing)/components/LandingHero'
    import LandingFeatures from './(marketing)/components/LandingFeatures'
    import LandingPricing from './(marketing)/components/LandingPricing'
    import LandingCTA from './(marketing)/components/LandingCTA'
    import LandingFooter from './(marketing)/components/LandingFooter'
    import type { Metadata } from 'next'

    export const dynamic = 'force-static'

    export const metadata: Metadata = {
      title: 'NZPOS — The POS built for Kiwi retailers',
      description: 'Ring up sales in-store, take orders online, and get GST handled correctly — all from one dashboard. Free to start.',
    }

    export default function LandingPage() {
      return (
        <main>
          <LandingNav />
          <LandingHero />
          <LandingFeatures />
          <LandingPricing />
          <LandingCTA />
          <LandingFooter />
        </main>
      )
    }
    ```

    **LandingNav (src/app/(marketing)/components/LandingNav.tsx):**
    - Sticky header: `sticky top-0 z-50 bg-[var(--color-navy)]`.
    - Inner container: `max-w-[1200px] mx-auto px-[var(--space-lg)] py-[var(--space-sm)]` (mobile: `px-[var(--space-md)]`).
    - Left: "NZPOS" — Satoshi 700, white, text-xl. Use `<Link href="/">`.
    - Right (desktop): "Sign in" ghost button (`border border-white/50 text-white px-4 py-2 rounded-md text-sm hover:bg-white/10`) linking to `/login`. "Get started" amber button (`bg-[var(--color-amber)] text-white px-4 py-2 rounded-md text-sm font-bold hover:opacity-90`) linking to `/signup`.
    - Mobile: hamburger icon button (`md:hidden`). On click: toggle a full-screen overlay with "Sign in" and "Get started" links. Use `<details>` + `<summary>` for no-JS toggle: `<summary>` is the hamburger icon, `<details>[open]` reveals a fixed overlay with nav links.
    - Desktop nav hidden on mobile (`hidden md:flex`), mobile hamburger hidden on desktop.

    **LandingHero (src/app/(marketing)/components/LandingHero.tsx):**
    - Full-bleed: `bg-[var(--color-navy)]` (use navy, not navy-dark — same token).
    - Inner: `max-w-[1200px] mx-auto px-[var(--space-lg)] py-[var(--space-3xl)]` (mobile: `px-[var(--space-md)] py-[var(--space-2xl)]`).
    - Two-column layout on desktop (`md:grid md:grid-cols-2 md:gap-[var(--space-xl)] items-center`).
    - Left column:
      - Headline: "The POS built for Kiwi retailers." — `font-display text-[48px] md:text-[48px] text-[28px] font-bold leading-[1.1] text-white`. Actually for mobile-first: `text-[28px] md:text-[48px]`.
      - Sub-headline: "Ring up sales in-store, take orders online, and get GST handled correctly -- all from one dashboard." — `text-base leading-relaxed text-white/70 mt-[var(--space-lg)]`.
      - CTA button: "Get started free" — `inline-block bg-[var(--color-amber)] text-white px-[var(--space-xl)] py-[var(--space-sm)] rounded-md text-sm font-bold mt-[var(--space-lg)] hover:opacity-90`. `<Link href="/signup">`.
      - Below CTA: "No credit card required. NZD pricing." — `text-sm text-white/50 mt-[var(--space-sm)]`.
    - Right column (desktop only, `hidden md:block`): A placeholder div with a stylised mockup. Use a simple CSS illustration (rounded rect representing an iPad with the NZPOS colors) or leave as an empty placeholder with a subtle border. Keep it simple — no external images.

    **LandingFeatures (src/app/(marketing)/components/LandingFeatures.tsx):**
    - Background: `bg-[var(--color-bg)]`.
    - Inner: `max-w-[1200px] mx-auto px-[var(--space-lg)] py-[var(--space-3xl)]`.
    - Heading: "Everything a Kiwi shop needs" — DM Sans 20px / 700, `text-[var(--color-text)]`, centered.
    - Grid: `grid grid-cols-1 md:grid-cols-2 gap-[var(--space-lg)] mt-[var(--space-xl)]`.
    - 4 feature cards, each: `bg-white border border-[var(--color-border)] shadow-sm rounded-lg p-[var(--space-xl)]`.
      - Inline SVG icon (24x24, `text-[var(--color-navy)]`). Use simple geometric shapes:
        1. POS icon (rectangle/tablet shape) + "POS on iPad" + "Tap to sell. Scan barcodes. EFTPOS confirmed manually."
        2. Store icon (shopping bag) + "Online store included" + "Your storefront is live from day one. Click-and-collect ready."
        3. Receipt/calculator icon + "GST done right" + "15% GST calculated per line, IRD-compliant on every receipt."
        4. Sync/refresh icon + "Stock stays in sync" + "Sell in-store or online -- inventory updates instantly."
      - Title: DM Sans 14px / 700, `text-[var(--color-text)]`, `mt-[var(--space-md)]`.
      - Description: DM Sans 16px / 400, `text-[var(--color-text-muted)]`, `mt-[var(--space-xs)]`.

    **LandingPricing (src/app/(marketing)/components/LandingPricing.tsx):**
    - Background: `bg-[var(--color-surface)]`.
    - Inner: `max-w-[1200px] mx-auto px-[var(--space-lg)] py-[var(--space-3xl)]`.
    - Heading: "Transparent pricing" — DM Sans 20px / 700, centered.
    - Sub-heading: "Free to start. Pay only for what you use." — 16px, muted, centered, `mt-[var(--space-sm)]`.
    - Layout: centered flex column. Free core card on top, 2 add-on pills side by side below.
    - Free core card: `bg-white border-2 border-[var(--color-navy)] rounded-lg p-[var(--space-xl)] max-w-md mx-auto mt-[var(--space-xl)]`.
      - "Free" — `font-display text-[48px] md:text-[48px] text-[28px] font-bold text-[var(--color-text)]`.
      - Includes list (bullet points, DM Sans 16px):
        - POS checkout
        - Online storefront
        - Inventory management
        - GST-compliant receipts
    - Add-on pills: `grid grid-cols-1 md:grid-cols-2 gap-[var(--space-lg)] max-w-lg mx-auto mt-[var(--space-lg)]`.
      - Each: `bg-white border border-[var(--color-border)] rounded-lg p-[var(--space-xl)]`.
      - Xero Integration: title + "$9/month NZD" (DM Sans 14px 700) + 2-3 bullet points (auto-sync invoices, credit notes for refunds).
      - Email Notifications: title + "$5/month NZD" (DM Sans 14px 700) + 2-3 bullet points (order confirmations, pickup-ready alerts, daily summary).
    - Note below: "All prices include GST. No hidden fees." — 14px, muted, centered, `mt-[var(--space-lg)]`.

    **LandingCTA (src/app/(marketing)/components/LandingCTA.tsx):**
    - Background: `bg-[var(--color-navy)]`.
    - Inner: `max-w-[1200px] mx-auto px-[var(--space-lg)] py-[var(--space-3xl)] text-center`.
    - Heading: "Ready to run a better shop?" — DM Sans 20px / 700, white.
    - CTA button: "Get started free" — same amber button as hero. `<Link href="/signup">`. `aria-label="Get started free — final call to action"` (distinct from hero per accessibility contract).
    - Sub-text: "Set up in under 5 minutes." — 14px, `text-white/60`, `mt-[var(--space-sm)]`.

    **LandingFooter (src/app/(marketing)/components/LandingFooter.tsx):**
    - Background: `bg-[var(--color-navy)]` (slightly darker area — use same token, the CTA section above provides visual separation via content).
    - Inner: `max-w-[1200px] mx-auto px-[var(--space-lg)] py-[var(--space-xl)]`.
    - Flex row: `flex flex-col md:flex-row justify-between items-center gap-[var(--space-md)]`.
    - Left: "© 2026 NZPOS. Built in New Zealand." — 14px, `text-white/40`.
    - Right: links "Sign in" | "Privacy" | "Terms" — 14px, `text-white/50 hover:text-white`, separated by `|` or spacing.
    - "Sign in" links to `/login`, "Privacy" and "Terms" link to `#` for now (placeholder).

    **No scroll animations** per motion contract and MKTG-02.
    **No `<Image>` components** — the landing page has no images (icons are inline SVG).
  </action>
  <verify>
    <automated>npx next build 2>&1 | grep -E "^(○|λ|ƒ).*\/$" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `export const dynamic = 'force-static'` and imports all 6 landing components
    - src/app/(marketing)/components/LandingNav.tsx contains `NZPOS` and `/signup` and `/login` and `sticky`
    - src/app/(marketing)/components/LandingHero.tsx contains `The POS built for Kiwi retailers` and `Get started free` and `/signup`
    - src/app/(marketing)/components/LandingFeatures.tsx contains `Everything a Kiwi shop needs` and `POS on iPad` and `GST done right` and `Stock stays in sync`
    - src/app/(marketing)/components/LandingPricing.tsx contains `Transparent pricing` and `$9/month NZD` and `$5/month NZD` and `All prices include GST`
    - src/app/(marketing)/components/LandingCTA.tsx contains `Ready to run a better shop?` and `/signup` and `Set up in under 5 minutes`
    - src/app/(marketing)/components/LandingFooter.tsx contains `Built in New Zealand` and `2026`
    - `npx next build` shows `/` route as static (`○`) not dynamic (`λ`)
    - No `'use client'` directive in any landing component (except optional nav toggle)
    - No `cookies()`, `headers()`, or `createSupabase` in any landing component
  </acceptance_criteria>
  <done>Landing page renders statically at root domain with hero, features, pricing, CTAs, and footer. Mobile-responsive. NZ-focused copy. One-click to /signup from hero CTA.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds and shows `/` as static (`○`)
- Landing page has no dynamic data fetching
- All copy matches the copywriting contract from UI-SPEC
- Hero CTA links to /signup
- Responsive: mobile layout stacks to single column
</verification>

<success_criteria>
- Landing page renders at root domain with all 6 sections
- Page is statically rendered (build output confirms `○` for `/`)
- Hero CTA links to /signup in one click
- Pricing shows free core + $9/month Xero + $5/month Email add-ons in NZD
- Mobile-optimised: responsive typography (48px -> 28px headline), stacked layout
- NZ spelling throughout (colour, not color)
</success_criteria>

<output>
After completion, create `.planning/phases/14-store-setup-wizard-marketing/14-03-SUMMARY.md`
</output>
