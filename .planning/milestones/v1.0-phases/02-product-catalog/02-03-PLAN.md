---
phase: 02-product-catalog
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/app/(admin)/products/page.tsx
  - src/app/(admin)/products/loading.tsx
  - src/components/admin/products/ProductDataTable.tsx
  - src/components/admin/products/ProductSearchBar.tsx
  - src/components/admin/products/ProductFilterBar.tsx
  - src/components/admin/products/ProductStatusBadge.tsx
  - src/components/admin/products/ProductFormDrawer.tsx
  - src/components/admin/products/ProductImagePicker.tsx
  - src/components/admin/products/PriceInput.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-05

must_haves:
  truths:
    - "Owner sees a sortable data table of products with thumbnail, name, SKU, price, stock, category, status"
    - "Owner can search products by name or SKU"
    - "Owner can filter by category, stock status, and active/inactive"
    - "Owner can click Add Product to open a drawer and create a product with all fields including image"
    - "Owner can click a product row to edit it in the drawer"
    - "Owner can deactivate a product from the edit drawer"
    - "Product images display as thumbnails in the table"
  artifacts:
    - path: "src/app/(admin)/products/page.tsx"
      provides: "Product list page (Server Component)"
      min_lines: 30
    - path: "src/components/admin/products/ProductDataTable.tsx"
      provides: "Sortable product data table"
      min_lines: 60
    - path: "src/components/admin/products/ProductFormDrawer.tsx"
      provides: "Slide-in drawer for create/edit product"
      min_lines: 80
  key_links:
    - from: "src/components/admin/products/ProductFormDrawer.tsx"
      to: "src/actions/products/createProduct.ts"
      via: "form submit calls createProduct"
      pattern: "createProduct"
    - from: "src/components/admin/products/ProductFormDrawer.tsx"
      to: "src/app/api/products/image/route.ts"
      via: "fetch POST to /api/products/image"
      pattern: "fetch.*api/products/image"
    - from: "src/app/(admin)/products/page.tsx"
      to: "supabase.from('products')"
      via: "Server Component data fetch"
      pattern: "from\\('products'\\)"
    - from: "src/app/(admin)/products/page.tsx"
      to: "src/components/admin/categories/CategorySidebarPanel.tsx"
      via: "Renders CategorySidebarPanel with categories for filtering"
      pattern: "CategorySidebarPanel"

# NOTE: PROD-05 is *partially* satisfied by this plan (admin product list thumbnails only).
# Full PROD-05 delivery (POS grid images + online store images) is completed in Phase 3 and Phase 4.
---

<objective>
Build the product list page with sortable data table, search/filter bar, and the product form drawer for create/edit/deactivate, including image upload integration.

Purpose: This is the primary admin workspace (per D-01). The owner manages their entire product catalog from this single page. It consumes the Server Actions from Plan 02 and the category sidebar from Plan 01.
Output: Fully functional product list page at /admin/products with data table, search, filters, and slide-in form drawer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-product-catalog/02-CONTEXT.md
@.planning/phases/02-product-catalog/02-UI-SPEC.md
@.planning/phases/02-product-catalog/02-RESEARCH.md
@DESIGN.md
@src/schemas/product.ts
@src/types/database.ts
@src/lib/supabase/server.ts
@src/lib/money.ts

<interfaces>
<!-- From Plan 01 outputs — read these files before implementing -->
From src/components/admin/categories/CategorySidebarPanel.tsx:
```typescript
// Client Component — receives categories from server, handles drag-and-drop reorder
interface CategorySidebarPanelProps {
  initialCategories: Category[]
  selectedCategoryId?: string | null
  onCategorySelect: (categoryId: string | null) => void
}
```

<!-- From Plan 02 outputs -->
From src/actions/products/createProduct.ts:
```typescript
export async function createProduct(formData: FormData): Promise<{ success: true } | { error: Record<string, string[]> }>
```

From src/actions/products/updateProduct.ts:
```typescript
export async function updateProduct(id: string, formData: FormData): Promise<{ success: true } | { error: Record<string, string[]> }>
```

From src/actions/products/deactivateProduct.ts:
```typescript
export async function deactivateProduct(id: string): Promise<{ success: true } | { error: Record<string, string[]> }>
```

From src/app/api/products/image/route.ts:
```typescript
// POST /api/products/image
// Request: FormData with 'image' file field
// Response: { url: string } on success, { error: string } on failure
```

From src/lib/money.ts:
```typescript
export function formatNZD(cents: number): string  // e.g. 899 -> "$8.99"
export function parsePriceToCents(raw: string): number | null  // e.g. "8.99" -> 899
```

From src/schemas/product.ts:
```typescript
export const CreateProductSchema = z.object({
  name: z.string().min(1).max(200),
  sku: z.string().max(50).optional(),
  barcode: z.string().max(50).optional(),
  price_cents: z.number().int().min(0),
  category_id: z.string().uuid().optional(),
  stock_quantity: z.number().int().min(0).default(0),
  reorder_threshold: z.number().int().min(0).default(0),
  image_url: z.string().url().optional(),
})
export type CreateProductInput = z.infer<typeof CreateProductSchema>
```

From src/types/database.ts (products Row):
```typescript
{
  id: string; store_id: string; category_id: string | null;
  name: string; sku: string | null; barcode: string | null;
  price_cents: number; stock_quantity: number; reorder_threshold: number;
  image_url: string | null; is_active: boolean;
  created_at: string; updated_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product list page with data table, search, and filters</name>
  <files>
    src/app/(admin)/products/page.tsx,
    src/app/(admin)/products/loading.tsx,
    src/components/admin/products/ProductDataTable.tsx,
    src/components/admin/products/ProductSearchBar.tsx,
    src/components/admin/products/ProductFilterBar.tsx,
    src/components/admin/products/ProductStatusBadge.tsx
  </files>
  <read_first>
    DESIGN.md,
    .planning/phases/02-product-catalog/02-UI-SPEC.md,
    src/types/database.ts,
    src/lib/supabase/server.ts,
    src/lib/money.ts,
    src/app/(admin)/layout.tsx,
    src/components/admin/categories/CategorySidebarPanel.tsx
  </read_first>
  <action>
1. Create `src/app/(admin)/products/page.tsx` (Server Component):
   - Page title: "Products" in Satoshi 30px/3xl weight 600, per UI-SPEC typography
   - Fetch products: `const { data: products } = await supabase.from('products').select('*, categories(name)').order('name')`
   - Fetch categories: `const { data: categories } = await supabase.from('categories').select('*').order('sort_order')`
   - Layout: two-column — main content area (flex-1) + CategorySidebarPanel (240px, right side)
   - Main content: ProductSearchBar + ProductFilterBar + ProductDataTable + "Add Product" amber button + "Import CSV" amber-outline button
   - Pass products and categories as props to client components
   - "Add Product" button: amber bg (#E67E22), white text, triggers ProductFormDrawer open (per UI-SPEC accent reserved for CTAs)
   - "Import CSV" button: amber border, amber text, links/navigates to CSV import flow (Plan 04)
   - Empty state: if no products, show "No products yet" heading + "Add your first product to start building your catalog." body (per UI-SPEC copywriting)
   - Filtered empty state: "No products match your filters" + "Try adjusting your search or clearing the filters." (per UI-SPEC)

2. Create `src/app/(admin)/products/loading.tsx`:
   - Skeleton loading state matching table layout
   - Use `bg-surface animate-pulse` for skeleton rows
   - 5 skeleton rows with placeholder columns matching data table structure

3. Create `src/components/admin/products/ProductDataTable.tsx` (`'use client'`):
   - Receives `products` prop (Product[] with joined category name) and `categories` for filter state
   - Per D-01: Columns are: thumbnail (40x40, object-cover), name, SKU (Geist Mono font), price (NZD formatted via formatNZD, Geist Mono, `font-feature-settings: 'tnum' 1`), stock quantity, category name, status badge
   - Per D-03: No barcode, reorder threshold, or dates in default view
   - Sortable columns (per UI-SPEC): name, SKU, price, stock. Click header = ascending, click again = descending, third click = clear (default name A-Z). Chevron up/down icon in navy color
   - Row min-height: 48px (per UI-SPEC spacing)
   - Row hover: `bg-surface` (#F5F5F4) per UI-SPEC states reference
   - Row click: opens ProductFormDrawer in edit mode (calls `onProductSelect(product)`)
   - Thumbnail: use `next/image` with width=40 height=40 className="object-cover rounded" for products with image_url. For products without image: show navy silhouette placeholder icon on surface background
   - Client-side filtering: filter products by `searchQuery` (matches name or SKU, case-insensitive), `selectedCategoryId`, `stockStatus` ('all' | 'in_stock' | 'low' | 'out'), `activeStatus` ('all' | 'active' | 'inactive')
   - Stock status logic: `out` = stock_quantity === 0, `low` = stock_quantity > 0 && stock_quantity <= reorder_threshold, `in_stock` = stock_quantity > reorder_threshold
   - Client-side sorting: maintain `sortColumn` and `sortDirection` state. Sort products array before rendering
   - Deactivated product rows: reduced opacity (60%) per UI-SPEC

4. Create `src/components/admin/products/ProductSearchBar.tsx` (`'use client'`):
   - Text input with placeholder "Search by name or SKU" (per UI-SPEC copywriting)
   - Debounced 300ms via `setTimeout`/`clearTimeout` (no external debounce library needed)
   - Clear button (x icon) appears when input has text
   - Calls `onSearchChange(query)` prop
   - Input styling: border, navy 2px ring on focus (per UI-SPEC states reference)

5. Create `src/components/admin/products/ProductFilterBar.tsx` (`'use client'`):
   - Three inline filters per D-02:
     a. Category dropdown: populated from categories prop. Options: "All Categories" + each category name
     b. Stock status select: "All" / "In Stock" / "Low Stock" / "Out of Stock"
     c. Status toggle: "All" / "Active" / "Inactive"
   - Active filters shown as dismissible chips below (per UI-SPEC interaction contract)
   - "Clear all filters" text link when any filter is active
   - Calls `onFilterChange({ categoryId, stockStatus, activeStatus })` prop

6. Create `src/components/admin/products/ProductStatusBadge.tsx`:
   - Pill badge (rounded-full) per UI-SPEC
   - Badge padding vertical: 4px (xs token), horizontal: 8px (sm token)
   - Font: DM Sans 14px/sm weight 600
   - Variants based on product state:
     - Active (is_active=true, stock > reorder_threshold): success green (#059669) fill, white text
     - Low Stock (is_active=true, stock > 0 && stock <= reorder_threshold): warning amber (#D97706) fill, white text
     - Out of Stock (is_active=true, stock === 0): error red (#DC2626) fill, white text
     - Inactive (is_active=false): border only + muted text (no fill)
  </action>
  <verify>
    <automated>test -f src/app/\(admin\)/products/page.tsx && test -f src/components/admin/products/ProductDataTable.tsx && test -f src/components/admin/products/ProductSearchBar.tsx && test -f src/components/admin/products/ProductFilterBar.tsx && test -f src/components/admin/products/ProductStatusBadge.tsx && grep -q "formatNZD" src/components/admin/products/ProductDataTable.tsx && grep -q "next/image" src/components/admin/products/ProductDataTable.tsx && grep -q "CategorySidebarPanel" src/app/\(admin\)/products/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(admin)/products/page.tsx is a Server Component that fetches products with `.select('*, categories(name)')` and categories
    - Page renders "Products" heading in Satoshi font
    - Page renders CategorySidebarPanel (from Plan 01) with categories and filtering props
    - ProductDataTable renders columns: thumbnail (40x40), name, SKU (mono font), price (via formatNZD, tnum), stock, category, status badge
    - ProductDataTable supports sort on name, SKU, price, stock columns (ascending/descending/clear cycle)
    - ProductSearchBar debounces at 300ms and has clear button
    - ProductFilterBar has category dropdown, stock status select, active/inactive toggle
    - ProductStatusBadge renders 4 variants: active (green), low stock (amber), out of stock (red), inactive (border-only)
    - Empty state shows "No products yet" copy per UI-SPEC
    - Deactivated rows render at 60% opacity
    - Product thumbnails use next/image component
  </acceptance_criteria>
  <done>Product list page renders at /admin/products with sortable data table, debounced search, three-filter bar, status badges, CategorySidebarPanel integration, and proper empty states. Products display with thumbnails, formatted NZD prices, and mono-spaced SKUs.</done>
</task>

<task type="auto">
  <name>Task 2: Product form drawer (create/edit/deactivate with image upload)</name>
  <files>
    src/components/admin/products/ProductFormDrawer.tsx,
    src/components/admin/products/ProductImagePicker.tsx,
    src/components/admin/products/PriceInput.tsx
  </files>
  <read_first>
    DESIGN.md,
    .planning/phases/02-product-catalog/02-UI-SPEC.md,
    src/schemas/product.ts,
    src/actions/products/createProduct.ts,
    src/actions/products/updateProduct.ts,
    src/actions/products/deactivateProduct.ts,
    src/lib/money.ts,
    src/types/database.ts
  </read_first>
  <action>
1. Create `src/components/admin/products/ProductFormDrawer.tsx` (`'use client'`):
   - Slide-in drawer from right side, 480px width on desktop. Full-screen on viewport < 640px (`w-full sm:w-[480px]`)
   - Backdrop: `rgba(0,0,0,0.4)` covering main content. Click backdrop to close (if no unsaved changes)
   - Unsaved changes guard: track `isDirty` state. If dirty and user clicks backdrop or Escape, show inline confirmation: "You have unsaved changes. Discard?" with "Keep editing" (ghost button) and "Discard" (ghost button) per UI-SPEC
   - Two modes via `product` prop: if null = create mode (title "Add Product", button "Save Product"), if product object = edit mode (title "Edit Product", button "Save Changes")
   - Form fields in order:
     a. ProductImagePicker (image upload)
     b. Name: text input, required. Label "Name"
     c. SKU: text input with Geist Mono font, auto-uppercase (via `onChange` toUpperCase). Label "SKU"
     d. Barcode: text input. Label "Barcode" (hidden from default view per D-03 but available in form)
     e. PriceInput: prefixed "NZD $", accepts decimal. Label "Price"
     f. CategorySelect: dropdown from categories. Include "New category" option at bottom that opens inline text input for new category creation. Label "Category"
     g. StockInput: integer input with +/- stepper buttons. Label "Stock Quantity"
     h. ReorderThresholdInput: integer input. Label "Reorder Threshold". Helper text: "Alert when stock falls below this number."
     i. ActiveToggle: toggle switch. Label "Active" / "Inactive". Radius-full, navy bg when on (per UI-SPEC states)
   - Form validation on submit (not on blur per UI-SPEC). Error messages below each field in 14px/sm caption style, error red (#DC2626)
   - On submit create: build FormData, call `createProduct(formData)`. If image was uploaded, image_url is already set from ProductImagePicker
   - On submit edit: build FormData, call `updateProduct(product.id, formData)`
   - On success: close drawer, table updates (via revalidatePath from Server Action)
   - Save button: navy bg (#1E293B), white text. "Save Product" (create) or "Save Changes" (edit). States per UI-SPEC: hover (navy-light), disabled (40% opacity), loading (spinner replaces label)
   - Deactivate button (edit mode only): ghost button, error text color (#DC2626), "Deactivate Product". Click opens confirmation dialog per UI-SPEC: heading "Deactivate {product.name}?", body "This product will no longer appear in the POS or online store.", buttons "Keep Product Active" (ghost) + "Deactivate" (red bg). On confirm: call `deactivateProduct(product.id)`, close drawer
   - Error messages per UI-SPEC copywriting:
     - Required: "This field is required."
     - Invalid price: "Enter a valid price (e.g. 8.99)."
     - Duplicate SKU: "This SKU is already in use."

2. Create `src/components/admin/products/ProductImagePicker.tsx` (`'use client'`):
   - Click-to-browse file input (no drag-and-drop per D-05)
   - Accept: JPEG, PNG, WebP (`accept="image/jpeg,image/png,image/webp"`)
   - Hidden `<input type="file">` triggered by clicking the picker area
   - Placeholder state: navy silhouette icon (package/image placeholder) on surface bg, text "Click to choose a photo" (per UI-SPEC)
   - After selection: show 200x200px preview, `object-cover` crop center
   - Helper text below picker: "JPEG, PNG or WebP. Any size — we'll resize it for you." (per UI-SPEC)
   - On file select: validate client-side that file.size <= 10MB (show error "File too large. Maximum 10MB." if exceeded)
   - Upload to `/api/products/image` via `fetch` with `FormData` containing the file
   - Progress: subtle progress bar below picker during upload (use `fetch` with standard loading state, not actual progress tracking — keep simple)
   - On upload success: call `onImageUrl(url)` prop to pass the URL to the parent form
   - On upload error: show inline error "Upload failed. Check your connection and try again." (per UI-SPEC)
   - Edit mode: if product already has image_url, show existing image as preview. Allow replacing by clicking to choose new file

3. Create `src/components/admin/products/PriceInput.tsx` (`'use client'`):
   - Input prefixed with "NZD $" (prefix text, not part of input value)
   - Accepts decimal values like "8.99"
   - On change: store raw string value in local state
   - Exposes `valueCents` to parent via `onPriceChange(cents: number)` callback
   - Conversion uses `parsePriceToCents` from `src/lib/money.ts`
   - In edit mode: receives `initialCents` prop, converts to display dollars on mount (e.g. 899 -> "8.99")
   - Input type: "text" (not "number" — to allow formatting control)
   - Pattern attribute: `[0-9]*\.?[0-9]{0,2}` for mobile numeric keyboard
   - Font: Geist Mono (matches SKU/data typography per UI-SPEC)
  </action>
  <verify>
    <automated>test -f src/components/admin/products/ProductFormDrawer.tsx && test -f src/components/admin/products/ProductImagePicker.tsx && test -f src/components/admin/products/PriceInput.tsx && grep -q "createProduct" src/components/admin/products/ProductFormDrawer.tsx && grep -q "deactivateProduct" src/components/admin/products/ProductFormDrawer.tsx && grep -q "/api/products/image" src/components/admin/products/ProductImagePicker.tsx && grep -q "parsePriceToCents" src/components/admin/products/PriceInput.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ProductFormDrawer.tsx is `'use client'`, 480px wide (`w-[480px]` or equivalent), slides in from right, has backdrop at 40% opacity
    - ProductFormDrawer has unsaved changes guard that shows "You have unsaved changes. Discard?" confirmation
    - Create mode shows "Save Product" button, edit mode shows "Save Changes" button (per UI-SPEC copywriting)
    - Deactivate button only visible in edit mode, ghost style with error color, opens confirmation dialog
    - Form calls `createProduct(formData)` on create submit and `updateProduct(id, formData)` on edit submit
    - ProductImagePicker.tsx uses `accept="image/jpeg,image/png,image/webp"`, no drag-and-drop
    - ProductImagePicker uploads to `/api/products/image` via fetch POST with FormData
    - ProductImagePicker shows "Click to choose a photo" placeholder and 200x200 preview after selection
    - PriceInput.tsx prefixed with "NZD $", uses parsePriceToCents from money.ts, exposes cents value
    - PriceInput uses Geist Mono font
    - All error messages match UI-SPEC copywriting contract exactly
  </acceptance_criteria>
  <done>Product form drawer handles create and edit with all fields, image upload with preview and progress, price input with dollar-to-cents conversion, and deactivation with confirmation dialog. All copy matches UI-SPEC.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npx vitest run` passes all tests
- /admin/products page renders product table with all specified columns
- Product form drawer opens on "Add Product" click and on row click
- Image upload sends to /api/products/image and shows preview
- Price input converts dollars to cents correctly
- CategorySidebarPanel renders on the products page with filtering support
</verification>

<success_criteria>
- Product list page at /admin/products shows sortable table with thumbnail, name, SKU (mono), price (NZD, tnum), stock, category, status badge
- Search debounces at 300ms, filters by name + SKU
- Three filters (category, stock status, active/inactive) work with AND logic
- Form drawer slides in at 480px, full-screen on mobile
- Create and edit modes use correct Server Actions
- Image picker uploads to /api/products/image, shows preview, handles errors
- Deactivate confirmation follows UI-SPEC dialog pattern
- CategorySidebarPanel from Plan 01 is integrated for category filtering
- PROD-05 partially satisfied: product image thumbnails display in admin table (POS grid + online store images delivered in Phase 3/4)
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-catalog/02-03-SUMMARY.md`
</output>
