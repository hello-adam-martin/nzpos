---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - .env.local.example
  - vitest.config.mts
  - .gitignore
autonomous: true
requirements:
  - FOUND-01
  - FOUND-07

must_haves:
  truths:
    - "Next.js 16 app starts with `npm run dev` and renders a page"
    - "Tailwind v4 utility classes apply correctly (no tailwind.config.js)"
    - "Design system colors and fonts are available as Tailwind tokens"
    - "Vitest runs with `npm test` (even if no tests yet)"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies installed"
      contains: "next"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-native config with design system tokens"
      contains: "@import"
    - path: "postcss.config.mjs"
      provides: "PostCSS config for Tailwind v4"
      contains: "@tailwindcss/postcss"
    - path: "vitest.config.mts"
      provides: "Vitest test runner configuration"
      contains: "defineConfig"
    - path: ".env.local.example"
      provides: "Required env vars documented"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "postcss.config.mjs"
      to: "src/app/globals.css"
      via: "PostCSS processes Tailwind directives"
      pattern: "@tailwindcss/postcss"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import in root layout"
      pattern: "import.*globals.css"
---

<objective>
Scaffold the Next.js 16 project with all Phase 1 dependencies, Tailwind v4 CSS-native configuration with NZPOS design system tokens, Vitest test runner, and environment variable documentation.

Purpose: Every subsequent plan and phase builds on this scaffold. Nothing can run without it.
Output: Working Next.js 16 app with Tailwind v4 design system, all dependencies installed, test runner configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .env.local.example, .gitignore</files>
  <read_first>
    - DESIGN.md (design system tokens for Tailwind config)
    - .planning/phases/01-foundation/01-RESEARCH.md (exact versions + install commands)
    - CLAUDE.md (stack constraints)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --eslint --app --tailwind --src-dir --import-alias "@/*"` in the project root.
       If the project root already has files (like .planning), use a temp directory and move scaffold files, or use `--yes` flag.
       The scaffold must use App Router (not Pages Router). Per D-01, no Supabase hosting needed yet.

    2. Install all Phase 1 dependencies (per RESEARCH.md verified versions):
       ```
       npm install @supabase/supabase-js @supabase/ssr
       npm install zod jose bcryptjs
       npm install server-only date-fns
       npm install stripe @stripe/stripe-js
       npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/dom vite-tsconfig-paths
       npm install -D @playwright/test
       npm install -D tsx
       npm install -D @types/bcryptjs
       ```

    3. Replace `postcss.config.mjs` with Tailwind v4 CSS-native config (NOT tailwind.config.js — per D-06 and Tailwind v4 pattern):
       ```js
       const config = { plugins: { "@tailwindcss/postcss": {} } };
       export default config;
       ```
       Delete `tailwind.config.ts` or `tailwind.config.js` if create-next-app generated one.

    4. Create `.env.local.example` with all required env vars:
       ```
       # Supabase (from supabase start output or Supabase Dashboard)
       NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
       SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

       # Staff PIN JWT (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
       STAFF_JWT_SECRET=your-32-byte-hex-secret

       # Stripe (from Stripe Dashboard)
       STRIPE_SECRET_KEY=sk_test_...
       NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
       STRIPE_WEBHOOK_SECRET=whsec_...
       ```

    5. Add scripts to package.json:
       ```json
       "test": "vitest run",
       "test:watch": "vitest",
       "test:e2e": "playwright test"
       ```

    6. Ensure .gitignore includes: `.env.local`, `.env*.local`, `node_modules/`, `.next/`, `.vercel/`

    Per FOUND-07: all monetary values will be integer cents throughout the codebase. This is enforced at schema level (Plan 02) and Zod level (Plan 03). No action needed here beyond noting the convention.
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" dependency
    - package.json contains "@supabase/supabase-js" dependency
    - package.json contains "@supabase/ssr" dependency
    - package.json contains "zod" dependency
    - package.json contains "jose" dependency
    - package.json contains "bcryptjs" dependency
    - package.json contains "stripe" dependency
    - package.json contains "vitest" in devDependencies
    - postcss.config.mjs contains "@tailwindcss/postcss"
    - NO file named tailwind.config.ts or tailwind.config.js exists
    - .env.local.example contains "NEXT_PUBLIC_SUPABASE_URL"
    - .env.local.example contains "STAFF_JWT_SECRET"
    - .env.local.example contains "STRIPE_SECRET_KEY"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Next.js 16 app builds cleanly with all Phase 1 dependencies installed, Tailwind v4 PostCSS config in place, env vars documented</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind v4 design system tokens, root layout with fonts, and Vitest</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, vitest.config.mts</files>
  <read_first>
    - DESIGN.md (exact hex values, font names, spacing scale)
    - src/app/globals.css (current state after scaffold)
    - src/app/layout.tsx (current state after scaffold)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Tailwind v4, Pattern 8: Vitest)
  </read_first>
  <action>
    1. Replace `src/app/globals.css` with Tailwind v4 CSS-native config using DESIGN.md tokens:
       ```css
       @import "tailwindcss";

       @theme {
         /* Primary */
         --color-navy: #1E293B;
         --color-navy-light: #334155;
         --color-navy-dark: #0F172A;

         /* Accent */
         --color-amber: #E67E22;
         --color-amber-hover: #D35400;

         /* Backgrounds */
         --color-bg: #FAFAF9;
         --color-surface: #F5F5F4;
         --color-card: #FFFFFF;

         /* Text */
         --color-text: #1C1917;
         --color-text-muted: #78716C;
         --color-text-light: #A8A29E;

         /* Borders */
         --color-border: #E7E5E4;
         --color-border-light: #F0EFED;

         /* Semantic */
         --color-success: #059669;
         --color-warning: #D97706;
         --color-error: #DC2626;
         --color-info: #3B82F6;

         /* Typography */
         --font-sans: 'DM Sans', sans-serif;
         --font-display: 'Satoshi', sans-serif;
         --font-mono: 'Geist Mono', monospace;

         /* Border radius */
         --radius-sm: 4px;
         --radius-md: 8px;
         --radius-lg: 12px;
         --radius-full: 9999px;
       }
       ```

    2. Update `src/app/layout.tsx`:
       - Import globals.css
       - Add Bunny Fonts CDN link in `<head>`: `https://fonts.bunny.net/css?family=satoshi:400,500,700,900|dm-sans:400,500,600,700`
       - Set `<html lang="en">` with `className="bg-bg text-text font-sans"`
       - Set metadata: `title: 'NZPOS'`, `description: 'NZ Retail POS System'`
       - Add viewport meta for iPad: `viewport: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no'` (prevents pinch zoom on POS)

    3. Replace `src/app/page.tsx` with a minimal placeholder that confirms design system is working:
       - Show "NZPOS" in `font-display text-4xl font-bold text-navy`
       - Show "Foundation ready" in `text-text-muted`
       - An amber button: `bg-amber text-white px-4 py-2 rounded-md hover:bg-amber-hover`
       This serves as a visual smoke test that Tailwind tokens work.

    4. Create `vitest.config.mts`:
       ```typescript
       import { defineConfig } from 'vitest/config'
       import react from '@vitejs/plugin-react'
       import tsconfigPaths from 'vite-tsconfig-paths'

       export default defineConfig({
         plugins: [tsconfigPaths(), react()],
         test: {
           environment: 'jsdom',
         },
       })
       ```
  </action>
  <verify>
    <automated>cd /Users/adam-personal/CODER/IDEAS/NZPOS && npm run build 2>&1 | tail -5 && npx vitest run --passWithNoTests 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains "@import" and "@theme"
    - src/app/globals.css contains "--color-navy: #1E293B"
    - src/app/globals.css contains "--color-amber: #E67E22"
    - src/app/globals.css contains "--font-display: 'Satoshi'"
    - src/app/globals.css contains "--font-sans: 'DM Sans'"
    - src/app/globals.css does NOT contain `@tailwind base` (that is v3 syntax)
    - src/app/layout.tsx contains "bunny.net" font link
    - src/app/layout.tsx contains "NZPOS" in metadata title
    - src/app/layout.tsx contains "user-scalable=no" or "maximum-scale=1"
    - vitest.config.mts contains "defineConfig" and "tsconfigPaths"
    - `npm run build` exits 0
    - `npx vitest run --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>Tailwind v4 renders NZPOS design system tokens (navy, amber, DM Sans, Satoshi), root layout loads fonts, Vitest configured and runnable</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `npm run dev` starts on localhost:3000 and shows the placeholder page
- `npx vitest run --passWithNoTests` completes without errors
- No `tailwind.config.js` or `tailwind.config.ts` file exists
- `.env.local.example` documents all required env vars
</verification>

<success_criteria>
Next.js 16 project builds, runs, and renders with NZPOS design system colors and fonts. All Phase 1 npm dependencies installed. Vitest configured and runnable. Environment variables documented. No Tailwind v3 config files present.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
