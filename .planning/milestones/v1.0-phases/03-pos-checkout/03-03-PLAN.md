---
phase: 03-pos-checkout
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/pos/CartPanel.tsx
  - src/components/pos/CartLineItem.tsx
  - src/components/pos/QuantityControl.tsx
  - src/components/pos/CartSummary.tsx
  - src/components/pos/PayButton.tsx
  - src/components/pos/PaymentMethodToggle.tsx
autonomous: true
requirements: [POS-03, POS-04, POS-05, DISC-03]
must_haves:
  truths:
    - "Cart panel shows line items with product name, quantity controls, line total, and remove button"
    - "Cart summary shows subtotal, GST breakdown (per-line aggregate), and total in correct NZD format"
    - "Staff can adjust quantity via +/- buttons with 44px touch targets"
    - "Staff can select EFTPOS or Cash payment method via pill toggle"
    - "Pay button shows 'Charge $X.XX' with dynamic total and is disabled when cart is empty"
    - "Cart line items show discount indicator text when discount is applied"
    - "Empty cart shows 'No items yet' message with instruction text"
  artifacts:
    - path: "src/components/pos/CartPanel.tsx"
      provides: "Full-height right panel rendering line items, summary, payment toggle, pay button"
      contains: "CartLineItem"
    - path: "src/components/pos/CartLineItem.tsx"
      provides: "Single cart row: name, qty controls, total, remove, discount indicator"
      contains: "QuantityControl"
    - path: "src/components/pos/QuantityControl.tsx"
      provides: "-/+ buttons with 44px tap targets"
    - path: "src/components/pos/CartSummary.tsx"
      provides: "Subtotal, GST, total rows with tabular-nums"
      contains: "formatNZD"
    - path: "src/components/pos/PayButton.tsx"
      provides: "Amber CTA with dynamic total, disabled state"
      contains: "Charge"
    - path: "src/components/pos/PaymentMethodToggle.tsx"
      provides: "EFTPOS / Cash pill toggle"
  key_links:
    - from: "src/components/pos/CartPanel.tsx"
      to: "src/lib/cart.ts"
      via: "receives CartState and dispatch from POSClientShell"
      pattern: "CartState"
    - from: "src/components/pos/CartSummary.tsx"
      to: "src/lib/cart.ts"
      via: "calls calcCartTotals to compute subtotal/GST/total"
      pattern: "calcCartTotals"
---

<objective>
Build the POS right panel: cart line items with quantity controls, cart summary with GST breakdown, payment method toggle, and pay button.

Purpose: This creates the order assembly experience where staff see what is being purchased, adjust quantities, view totals with GST, and initiate payment.
Output: Complete cart panel that displays items, calculates totals correctly, and has a functional pay button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pos-checkout/03-CONTEXT.md
@.planning/phases/03-pos-checkout/03-UI-SPEC.md

@src/lib/cart.ts
@src/lib/gst.ts
@src/lib/money.ts
@src/types/database.ts
@DESIGN.md
@src/app/globals.css

<interfaces>
From src/lib/cart.ts (created in Plan 01):
```typescript
export type CartItem = {
  productId: string; productName: string; unitPriceCents: number;
  quantity: number; discountCents: number; discountType?: 'percentage' | 'fixed';
  discountReason?: string; lineTotalCents: number; gstCents: number;
}
export type CartState = {
  items: CartItem[]; paymentMethod: 'eftpos' | 'cash' | null;
  cashTenderedCents: number | null; cartDiscountCents: number;
  cartDiscountType: 'percentage' | 'fixed' | null;
  phase: 'idle' | 'eftpos_confirm' | 'cash_entry' | 'processing' | 'sale_complete' | 'sale_void';
  completedOrderId: string | null;
}
export type CartAction =
  | { type: 'SET_QUANTITY'; productId: string; quantity: number }
  | { type: 'REMOVE_ITEM'; productId: string }
  | { type: 'SET_PAYMENT_METHOD'; method: 'eftpos' | 'cash' }
  | { type: 'INITIATE_PAYMENT' }
  | ...
export function calcCartTotals(items: CartItem[]): { subtotalCents: number; gstCents: number; totalCents: number }
```

From src/lib/money.ts:
```typescript
export function formatNZD(cents: number): string  // e.g. "$42.50"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartPanel + CartLineItem + QuantityControl</name>
  <files>src/components/pos/CartPanel.tsx, src/components/pos/CartLineItem.tsx, src/components/pos/QuantityControl.tsx</files>
  <read_first>
    - src/lib/cart.ts (CartItem, CartState, CartAction types — these are the data contracts)
    - src/lib/money.ts (formatNZD for price display)
    - DESIGN.md (spacing, typography, touch targets)
    - .planning/phases/03-pos-checkout/03-UI-SPEC.md (cart panel spec: 400px width, line item 48px min height, quantity buttons 36px visible / 44px touch target, remove button spec)
    - .planning/phases/03-pos-checkout/03-CONTEXT.md (D-02 re-tap increments, D-07 discount reason display)
  </read_first>
  <action>
    **1. Create `src/components/pos/QuantityControl.tsx`:**
    - Props: `{ quantity: number, onIncrement: () => void, onDecrement: () => void }`
    - Layout: horizontal flex with minus button, quantity display, plus button
    - Minus button: `<button>` with Minus icon (lucide-react), `w-9 h-9 min-w-[36px] min-h-[36px]` visible size, padding to 44px touch target via `p-1`, `rounded` border, `border border-border bg-card`
    - Plus button: same styling as minus with Plus icon
    - Quantity display: `text-base font-bold tabular-nums` centered between buttons, min-width `w-8` for alignment
    - When quantity is 1, minus button changes to a remove/trash action (or stays as minus, going to 0 triggers removal in reducer)
    - All buttons: `aria-label="Decrease quantity"` / `aria-label="Increase quantity"`

    **2. Create `src/components/pos/CartLineItem.tsx`:**
    - Props: `{ item: CartItem, onSetQuantity: (qty: number) => void, onRemove: () => void, onOpenDiscount: () => void }`
    - Row layout: `flex items-center gap-2 px-3 py-2 min-h-[48px] bg-card`
    - Left side: product name (`text-base font-normal truncate flex-1`) + discount indicator below name if `item.discountCents > 0` (show `"${item.discountType === 'percentage' ? `${discountPercent}% off` : `${formatNZD(item.discountCents)} off`}"` in `text-sm text-muted`)
    - Center: `<QuantityControl quantity={item.quantity} onIncrement={() => onSetQuantity(item.quantity + 1)} onDecrement={() => onSetQuantity(item.quantity - 1)} />`
    - Right side: line total `text-base font-bold tabular-nums` showing `formatNZD(item.lineTotalCents)`
    - Remove button (X icon): `w-6 h-6` visible, padded to `min-w-[36px] min-h-[36px]` touch target, `text-muted hover:text-error`, `aria-label="Remove ${item.productName} from order"`
    - Tap on the product name area opens discount sheet: `onClick={onOpenDiscount}` on the name/discount area, with `cursor-pointer`

    **3. Create `src/components/pos/CartPanel.tsx`:**
    - Props: `{ cart: CartState, dispatch: React.Dispatch<CartAction>, staffRole: 'owner' | 'staff', onOpenDiscount: (productId: string) => void }`
    - Full-height right panel: `flex flex-col h-full bg-surface border-l border-border`
    - Header: `"Order"` heading (`text-xl font-bold px-4 py-3`) + item count badge (`text-sm text-muted ml-2`, e.g. "(3 items)")
    - Line items list: `<div className="flex-1 overflow-y-auto px-2 space-y-1">` containing CartLineItem for each `cart.items`
    - Each CartLineItem wired to dispatch: `onSetQuantity={(qty) => dispatch({ type: 'SET_QUANTITY', productId: item.productId, quantity: qty })}`, `onRemove={() => dispatch({ type: 'REMOVE_ITEM', productId: item.productId })}`
    - Empty state: when `cart.items.length === 0`, show centered empty state: heading "No items yet" (`text-xl font-bold text-muted`), body "Tap a product to add it to the order." (`text-sm text-muted`)
    - Bottom section (sticky): CartSummary + PaymentMethodToggle + PayButton (placeholders for Task 2 — render empty divs with IDs for now, or render the components if Task 2 is done)
    - Bottom section layout: `<div className="border-t border-border p-4 space-y-3">`
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pos/QuantityControl.tsx contains `min-w-[36px]` or `w-9`
    - src/components/pos/QuantityControl.tsx contains `aria-label`
    - src/components/pos/CartLineItem.tsx contains `min-h-[48px]`
    - src/components/pos/CartLineItem.tsx contains `formatNZD`
    - src/components/pos/CartLineItem.tsx contains `tabular-nums`
    - src/components/pos/CartLineItem.tsx contains `Remove`
    - src/components/pos/CartLineItem.tsx contains `discountCents`
    - src/components/pos/CartPanel.tsx contains `No items yet`
    - src/components/pos/CartPanel.tsx contains `Tap a product to add it to the order`
    - src/components/pos/CartPanel.tsx contains `overflow-y-auto`
    - src/components/pos/CartPanel.tsx contains `CartLineItem`
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Cart panel renders line items with quantity controls (44px touch targets), remove buttons, discount indicators, and empty state. All monetary values formatted via formatNZD with tabular-nums.</done>
</task>

<task type="auto">
  <name>Task 2: CartSummary + PaymentMethodToggle + PayButton</name>
  <files>src/components/pos/CartSummary.tsx, src/components/pos/PaymentMethodToggle.tsx, src/components/pos/PayButton.tsx, src/components/pos/CartPanel.tsx</files>
  <read_first>
    - src/components/pos/CartPanel.tsx (created in Task 1 — will add summary/toggle/button to bottom section)
    - src/lib/cart.ts (calcCartTotals function, CartState shape)
    - src/lib/money.ts (formatNZD)
    - .planning/phases/03-pos-checkout/03-UI-SPEC.md (cart summary spec: subtotal/GST/total rows, payment toggle spec, pay button spec with 56px height)
    - .planning/phases/03-pos-checkout/03-CONTEXT.md (D-05 payment methods, D-09 charge button copy)
    - DESIGN.md (amber color for pay button)
  </read_first>
  <action>
    **1. Create `src/components/pos/CartSummary.tsx`:**
    - Props: `{ items: CartItem[] }`
    - Calls `calcCartTotals(items)` to get `{ subtotalCents, gstCents, totalCents }`
    - Layout: `<div className="space-y-1">` with 3 rows:
      - Subtotal row: `<div className="flex justify-between text-sm">` — label "Subtotal" (text-sm font-normal), value `formatNZD(subtotalCents)` (text-sm tabular-nums)
      - GST row: `<div className="flex justify-between text-sm text-muted">` — label "Incl. GST" (muted), value `formatNZD(gstCents)` (muted tabular-nums)
      - Divider: `<div className="border-t border-border my-2" />`
      - Total row: `<div className="flex justify-between items-baseline">` — label "Total" (text-xl font-bold), value `formatNZD(totalCents)` in `text-3xl font-display font-bold tabular-nums` (30px Satoshi 700 per UI-SPEC)
    - Apply `font-feature-settings: 'tnum' 1` via the `tabular-nums` Tailwind class on all monetary values

    **2. Create `src/components/pos/PaymentMethodToggle.tsx`:**
    - Props: `{ selected: 'eftpos' | 'cash' | null, onSelect: (method: 'eftpos' | 'cash') => void }`
    - Two pill buttons side-by-side: `<div className="flex gap-2">`
    - Each pill: `flex-1 min-h-[44px] rounded-full text-sm font-bold text-center`
    - Active pill: `bg-navy text-white`
    - Inactive pill: `bg-card text-navy border border-border`
    - Labels: "EFTPOS" and "Cash" (per copywriting contract)
    - Clicking dispatches `onSelect('eftpos')` or `onSelect('cash')`

    **3. Create `src/components/pos/PayButton.tsx`:**
    - Props: `{ totalCents: number, disabled: boolean, onClick: () => void }`
    - Full-width amber button: `w-full min-h-[56px] bg-amber text-white text-base font-bold rounded-lg`
    - Label: `Charge ${formatNZD(totalCents)}` — e.g. "Charge $42.50" (per copywriting contract)
    - Disabled state: when `disabled` is true (cart empty or no payment method), apply `opacity-50 pointer-events-none`
    - `aria-disabled={disabled}`

    **4. Update `src/components/pos/CartPanel.tsx`:**
    - Replace bottom section placeholders with actual components:
    - `<CartSummary items={cart.items} />`
    - `<PaymentMethodToggle selected={cart.paymentMethod} onSelect={(method) => dispatch({ type: 'SET_PAYMENT_METHOD', method })} />`
    - `<PayButton totalCents={totals.totalCents} disabled={cart.items.length === 0 || cart.paymentMethod === null} onClick={() => dispatch({ type: 'INITIATE_PAYMENT' })} />`
    - Compute `totals` via `calcCartTotals(cart.items)` at top of CartPanel
    - Only show summary/toggle/button when `cart.items.length > 0`
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pos/CartSummary.tsx contains `calcCartTotals`
    - src/components/pos/CartSummary.tsx contains `formatNZD`
    - src/components/pos/CartSummary.tsx contains `Incl. GST`
    - src/components/pos/CartSummary.tsx contains `tabular-nums`
    - src/components/pos/CartSummary.tsx contains `font-display` (Satoshi for total)
    - src/components/pos/PaymentMethodToggle.tsx contains `min-h-[44px]`
    - src/components/pos/PaymentMethodToggle.tsx contains `EFTPOS`
    - src/components/pos/PaymentMethodToggle.tsx contains `Cash`
    - src/components/pos/PaymentMethodToggle.tsx contains `bg-navy`
    - src/components/pos/PayButton.tsx contains `min-h-[56px]`
    - src/components/pos/PayButton.tsx contains `bg-amber`
    - src/components/pos/PayButton.tsx contains `Charge`
    - src/components/pos/PayButton.tsx contains `formatNZD`
    - src/components/pos/PayButton.tsx contains `opacity-50`
    - src/components/pos/CartPanel.tsx contains `CartSummary`
    - src/components/pos/CartPanel.tsx contains `PaymentMethodToggle`
    - src/components/pos/CartPanel.tsx contains `PayButton`
    - Next.js build succeeds
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>Cart summary shows subtotal, GST (per-line aggregate), and total with correct formatting. Payment toggle offers EFTPOS/Cash. Pay button shows "Charge $X.XX" in amber, disabled when cart empty or no payment method selected. All integrated into CartPanel bottom section.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- `npx vitest run` — all tests still pass
- Cart panel renders with line items, summary, toggle, and pay button
- Empty cart shows "No items yet" message
- All touch targets minimum 44px
- All monetary values use tabular-nums
</verification>

<success_criteria>
- CartPanel receives CartState and dispatch from POSClientShell
- Line items show product name, quantity controls, line total, discount indicator
- CartSummary uses calcCartTotals from cart.ts (not manual calculation)
- GST displayed as "Incl. GST" (tax-inclusive NZ pattern)
- Total uses Satoshi 700 at 30px (font-display class)
- Pay button label is "Charge $X.XX" with dynamic total
- All monetary values use formatNZD from money.ts
</success_criteria>

<output>
After completion, create `.planning/phases/03-pos-checkout/03-03-SUMMARY.md`
</output>
