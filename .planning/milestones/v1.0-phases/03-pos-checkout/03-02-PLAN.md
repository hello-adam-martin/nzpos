---
phase: 03-pos-checkout
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/(pos)/pos/page.tsx
  - src/app/(pos)/layout.tsx
  - src/components/pos/POSClientShell.tsx
  - src/components/pos/POSTopBar.tsx
  - src/components/pos/CategoryFilterBar.tsx
  - src/components/pos/ProductGrid.tsx
  - src/components/pos/ProductCard.tsx
  - src/components/pos/StockBadge.tsx
autonomous: true
requirements: [POS-01, POS-02, POS-08]
must_haves:
  truths:
    - "Staff sees a product grid with images, names, prices, and stock badges on iPad"
    - "Staff can filter products by category using the horizontal pill bar"
    - "Staff can search products by name or SKU using the persistent search bar"
    - "Tapping a product adds it to the cart via dispatch ADD_PRODUCT"
    - "Re-tapping a product already in cart increments quantity by 1 (per D-02)"
    - "Stock badges show exact count with color coding: green/amber/red per D-13"
    - "Out-of-stock products show red badge and are disabled for staff role"
    - "Product grid refreshes stock on page focus via visibilitychange + router.refresh()"
  artifacts:
    - path: "src/app/(pos)/pos/page.tsx"
      provides: "Server Component fetching products + categories via admin client with store_id filter"
      contains: "createSupabaseAdminClient"
    - path: "src/components/pos/POSClientShell.tsx"
      provides: "Client Component root with useReducer(cartReducer), manages all POS state"
      contains: "useReducer"
    - path: "src/components/pos/POSTopBar.tsx"
      provides: "Navy top bar with store name and staff name + logout"
    - path: "src/components/pos/CategoryFilterBar.tsx"
      provides: "Horizontal scroll pill buttons for category filtering"
    - path: "src/components/pos/ProductGrid.tsx"
      provides: "Auto-fill CSS grid of ProductCard components with search filtering"
    - path: "src/components/pos/ProductCard.tsx"
      provides: "Product card with image, name, price, stock badge, tap handler"
    - path: "src/components/pos/StockBadge.tsx"
      provides: "Green/amber/red badge with stock count text"
  key_links:
    - from: "src/app/(pos)/pos/page.tsx"
      to: "src/components/pos/POSClientShell.tsx"
      via: "Server Component passes products + categories as props"
      pattern: "POSClientShell.*products.*categories"
    - from: "src/components/pos/POSClientShell.tsx"
      to: "src/lib/cart.ts"
      via: "useReducer(cartReducer, initialCartState)"
      pattern: "useReducer\\(cartReducer"
    - from: "src/components/pos/ProductCard.tsx"
      to: "src/components/pos/POSClientShell.tsx"
      via: "onAddToCart callback dispatches ADD_PRODUCT"
      pattern: "onAddToCart"
---

<objective>
Build the POS left panel: Server Component data fetching, client shell with cart state, top bar, category filter, product grid with search, and product cards with stock badges.

Purpose: This creates the product browsing and selection experience — the primary iPad interaction surface where staff find and tap products.
Output: Working product grid that responds to taps, filters by category, searches by name/SKU, and shows real-time stock status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pos-checkout/03-CONTEXT.md
@.planning/phases/03-pos-checkout/03-UI-SPEC.md
@.planning/phases/03-pos-checkout/03-RESEARCH.md

@src/lib/cart.ts
@src/app/(pos)/pos/page.tsx
@src/app/(pos)/layout.tsx
@src/lib/supabase/admin.ts
@src/actions/auth/staffPin.ts
@src/types/database.ts
@DESIGN.md
@src/app/globals.css

<interfaces>
<!-- From Plan 01 output — cart module -->
From src/lib/cart.ts (created in Plan 01):
```typescript
export type CartItem = {
  productId: string; productName: string; unitPriceCents: number;
  quantity: number; discountCents: number; discountType?: 'percentage' | 'fixed';
  discountReason?: string; lineTotalCents: number; gstCents: number;
}
export type CartState = {
  items: CartItem[]; paymentMethod: 'eftpos' | 'cash' | null;
  cashTenderedCents: number | null; cartDiscountCents: number;
  cartDiscountType: 'percentage' | 'fixed' | null;
  phase: 'idle' | 'eftpos_confirm' | 'cash_entry' | 'processing' | 'sale_complete' | 'sale_void';
  completedOrderId: string | null;
}
export type CartAction = { type: 'ADD_PRODUCT'; product: ProductRow } | ...
export function cartReducer(state: CartState, action: CartAction): CartState
export const initialCartState: CartState
```

From src/types/database.ts:
```typescript
// products.Row: { id, store_id, category_id, name, sku, barcode, price_cents, stock_quantity, reorder_threshold, image_url, is_active, created_at, updated_at }
// categories.Row: { id, store_id, name, sort_order, created_at, updated_at }
```

From src/lib/supabase/admin.ts:
```typescript
export function createSupabaseAdminClient(): SupabaseClient<Database>
```

From src/actions/auth/staffPin.ts (JWT payload):
```typescript
// staff_session cookie JWT payload: { role: 'owner' | 'staff', store_id: string, staff_id: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POS page Server Component + POSClientShell + POSTopBar</name>
  <files>src/app/(pos)/pos/page.tsx, src/app/(pos)/layout.tsx, src/components/pos/POSClientShell.tsx, src/components/pos/POSTopBar.tsx</files>
  <read_first>
    - src/app/(pos)/pos/page.tsx (current placeholder — will be replaced entirely)
    - src/app/(pos)/layout.tsx (existing POS layout — may need minor updates)
    - src/lib/supabase/admin.ts (admin client import path)
    - src/lib/cart.ts (cartReducer, initialCartState, CartState, CartAction imports)
    - src/types/database.ts (products.Row and categories.Row shapes for props)
    - src/actions/auth/staffPin.ts (JWT cookie name 'staff_session' and payload shape)
    - DESIGN.md (color tokens, font choices)
    - .planning/phases/03-pos-checkout/03-UI-SPEC.md (layout contract, top bar spec, spacing)
    - src/app/globals.css (@theme tokens for colors)
  </read_first>
  <action>
    **1. Update `src/app/(pos)/layout.tsx`:**
    Keep existing `touch-manipulation` and `bg-bg`. Add `h-dvh overflow-hidden` to the outer div to prevent page-level scrolling per UI-SPEC. Keep the viewport metadata export.

    **2. Replace `src/app/(pos)/pos/page.tsx`** with a Server Component that:
    - Imports `createSupabaseAdminClient` from `@/lib/supabase/admin`
    - Imports `cookies` from `next/headers` and `jwtVerify` from `jose` to extract `store_id` and staff name from `staff_session` cookie
    - Queries products: `supabase.from('products').select('*').eq('store_id', storeId).eq('is_active', true).order('name')`
    - Queries categories: `supabase.from('categories').select('*').eq('store_id', storeId).order('sort_order')`
    - Queries staff record for the current staff_id to get `name` for the top bar
    - Runs all 3 queries in `Promise.all`
    - Renders `<POSClientShell products={products} categories={categories} staffName={staffName} staffRole={staffRole} storeName={storeName} />`
    - If no staff session, redirect to `/pos/login` using `redirect()` from `next/navigation`
    - Mark file with `import 'server-only'` at top

    **3. Create `src/components/pos/POSClientShell.tsx`:**
    - `'use client'` directive
    - Props: `{ products: ProductRow[], categories: CategoryRow[], staffName: string, staffRole: 'owner' | 'staff', storeName: string }`
    - Uses `useReducer(cartReducer, initialCartState)` from `@/lib/cart`
    - Uses `useRouter()` from `next/navigation`
    - Implements `useStockRefreshOnFocus` custom hook inline: listens to `document.visibilitychange`, calls `router.refresh()` when visible (per D-14, POS-08)
    - Renders the two-column layout: `<div className="grid grid-cols-[1fr_400px] h-full">` with left panel (top bar + category bar + product grid) and right panel placeholder `<div className="border-l border-border bg-surface">Cart panel (Plan 03)</div>`
    - Manages search state: `const [search, setSearch] = useState('')`
    - Manages category filter: `const [selectedCategory, setSelectedCategory] = useState<string | null>(null)`
    - Filters products by search (name.toLowerCase includes search OR sku?.toLowerCase includes search) AND by selectedCategory (null = all)
    - Passes `onAddToCart={(product) => dispatch({ type: 'ADD_PRODUCT', product })}` to ProductGrid
    - Passes `cart` state to right panel (will be wired in Plan 03)

    **4. Create `src/components/pos/POSTopBar.tsx`:**
    - Props: `{ storeName: string, staffName: string, onLogout: () => void }`
    - Navy background (`bg-navy`), 56px height (`h-14`), horizontal flex layout
    - Left: store name in Satoshi 700 (text-white, `font-display font-bold`)
    - Right: staff name in DM Sans 400 (text-white/70, `font-sans`) + logout button (text-white/50 hover:text-white, `text-sm`)
    - Logout handler calls `onLogout` prop — POSClientShell implements by clearing staff_session cookie via a server action or client redirect to `/pos/login`
    - Padding: `px-4` (16px horizontal per UI-SPEC)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(pos)/pos/page.tsx contains `import 'server-only'`
    - src/app/(pos)/pos/page.tsx contains `createSupabaseAdminClient`
    - src/app/(pos)/pos/page.tsx contains `jwtVerify`
    - src/app/(pos)/pos/page.tsx contains `staff_session`
    - src/app/(pos)/pos/page.tsx contains `POSClientShell`
    - src/app/(pos)/layout.tsx contains `h-dvh`
    - src/components/pos/POSClientShell.tsx contains `'use client'`
    - src/components/pos/POSClientShell.tsx contains `useReducer(cartReducer`
    - src/components/pos/POSClientShell.tsx contains `visibilitychange`
    - src/components/pos/POSClientShell.tsx contains `router.refresh()`
    - src/components/pos/POSClientShell.tsx contains `grid grid-cols-[1fr_400px]`
    - src/components/pos/POSTopBar.tsx contains `bg-navy`
    - src/components/pos/POSTopBar.tsx contains `storeName`
    - src/components/pos/POSTopBar.tsx contains `staffName`
    - Next.js build succeeds without errors
  </acceptance_criteria>
  <done>POS page fetches products/categories/staff via admin client with store_id filter. POSClientShell manages cart state via useReducer. Stock refreshes on visibilitychange. Top bar shows store and staff name on navy background.</done>
</task>

<task type="auto">
  <name>Task 2: CategoryFilterBar + ProductGrid + ProductCard + StockBadge</name>
  <files>src/components/pos/CategoryFilterBar.tsx, src/components/pos/ProductGrid.tsx, src/components/pos/ProductCard.tsx, src/components/pos/StockBadge.tsx</files>
  <read_first>
    - src/components/pos/POSClientShell.tsx (created in Task 1 — understand props passed down)
    - src/types/database.ts (products.Row and categories.Row shapes)
    - src/lib/money.ts (formatNZD for price display)
    - DESIGN.md (design tokens, card styling, spacing)
    - .planning/phases/03-pos-checkout/03-UI-SPEC.md (product grid spec, card spec, stock badge spec, category bar spec)
    - src/app/globals.css (@theme block — exact color token names)
  </read_first>
  <action>
    **1. Create `src/components/pos/StockBadge.tsx`:**
    - Props: `{ quantity: number, reorderThreshold: number }`
    - Renders a small badge in the top-right corner of the product card
    - Color logic: `quantity === 0` → red bg (`bg-error text-white`, text "Out of Stock"), `quantity <= reorderThreshold` → amber bg (`bg-warning text-white`, text "Low Stock"), else → green bg (`bg-success text-white`, shows count e.g. "3 left")
    - When quantity is 0, show "Out of Stock". When low, show count + "Low Stock". When OK, show just the count (e.g. "12").
    - Text: 14px (`text-sm`), DM Sans, absolute positioned `absolute top-1.5 right-1.5`
    - Rounded: `rounded-full px-2 py-0.5`

    **2. Create `src/components/pos/ProductCard.tsx`:**
    - Props: `{ product: ProductRow, onAddToCart: (product: ProductRow) => void, isInCart: boolean, cartQuantity: number, staffRole: 'owner' | 'staff' }`
    - White background card: `bg-card rounded-lg border border-border shadow-warm`
    - Image: `next/image` component, 1:1 aspect ratio via `aspect-square` class, `object-cover`, Supabase Storage URL. Fallback: show product name initial in a colored placeholder div if `image_url` is null.
    - Product name: `text-sm font-normal` (14px DM Sans 400), 2-line clamp: `line-clamp-2`
    - Price: `text-sm font-bold` (14px DM Sans 700), formatted via `formatNZD(product.price_cents)`, tabular nums class `tabular-nums`
    - StockBadge: rendered inside the card, absolutely positioned
    - Entire card is the tap target: `role="button" tabIndex={0}` on the outer div
    - `onClick` calls `onAddToCart(product)` — BUT disabled if `product.stock_quantity === 0 && staffRole !== 'owner'`
    - When `product.stock_quantity === 0`: add `opacity-50 cursor-not-allowed` and `aria-disabled="true"`
    - When `isInCart`: show a subtle blue ring or badge indicating quantity in cart (e.g., small circle with cartQuantity overlaid)
    - Min size: `min-w-[120px] min-h-[140px]`
    - Padding: `p-2` (8px) for card content below image
    - Keyboard accessible: `onKeyDown` handler triggers onAddToCart on Enter/Space

    **3. Create `src/components/pos/CategoryFilterBar.tsx`:**
    - Props: `{ categories: CategoryRow[], selectedCategory: string | null, onSelectCategory: (categoryId: string | null) => void }`
    - Horizontal scrollable row: `flex overflow-x-auto gap-2 px-4 py-2` with `scrollbar-hide` (hide scrollbar CSS)
    - "All" pill always first: active when `selectedCategory === null`
    - Each category pill: `min-h-[44px]` (touch target), `px-4 rounded-full text-sm font-normal`
    - Active pill: `bg-navy text-white`
    - Inactive pill: `bg-card text-navy border border-border`
    - Background: `bg-surface` on the container row
    - Clicking a pill calls `onSelectCategory(category.id)` or `onSelectCategory(null)` for "All"

    **4. Create `src/components/pos/ProductGrid.tsx`:**
    - Props: `{ products: ProductRow[], cart: CartState, onAddToCart: (product: ProductRow) => void, staffRole: 'owner' | 'staff' }`
    - Search bar: rendered at top of the grid area. `<input type="search" inputMode="search" placeholder="Search products..." className="w-full text-base ...">`
    - SKU quick-entry field (per D-03): a second input next to search, `inputMode="text" placeholder="SKU #"`. On Enter, finds product by exact SKU match and calls `onAddToCart`. Width: narrower than search (~120px).
    - Both inputs minimum `text-base` (16px) to prevent iOS auto-zoom.
    - Grid: `grid grid-cols-[repeat(auto-fill,minmax(140px,1fr))] gap-4 p-4`
    - Maps filtered products to `<ProductCard>` components
    - Empty state: when no products match search/filter, show centered text: `No products match "[query]"` in muted color (per copywriting contract)
    - Loading state: not needed (Server Component pre-fetches)

    **5. Update `src/components/pos/POSClientShell.tsx`:**
    - Wire search state and category filter state from Task 1
    - Import and render `<POSTopBar>`, `<CategoryFilterBar>`, `<ProductGrid>` in the left panel
    - Left panel structure: `<div className="flex flex-col h-full overflow-hidden">` containing POSTopBar, CategoryFilterBar, then ProductGrid in a `<div className="flex-1 overflow-y-auto">` (product area scrolls independently)
    - Pass search/setSearch to ProductGrid (or manage filtering in POSClientShell and pass filtered products down)
    - Pass `onAddToCart` that dispatches `{ type: 'ADD_PRODUCT', product }`
    - NOTE: The search and SKU inputs can either be in ProductGrid or POSClientShell. Recommended: keep in ProductGrid for encapsulation, pass `onAddToCart` for SKU quick-entry too.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/pos/StockBadge.tsx contains `bg-error` and `bg-warning` and `bg-success`
    - src/components/pos/StockBadge.tsx contains `reorderThreshold`
    - src/components/pos/ProductCard.tsx contains `role="button"`
    - src/components/pos/ProductCard.tsx contains `aria-disabled`
    - src/components/pos/ProductCard.tsx contains `formatNZD`
    - src/components/pos/ProductCard.tsx contains `tabular-nums`
    - src/components/pos/ProductCard.tsx contains `line-clamp-2`
    - src/components/pos/ProductCard.tsx contains `onAddToCart`
    - src/components/pos/CategoryFilterBar.tsx contains `min-h-[44px]`
    - src/components/pos/CategoryFilterBar.tsx contains `bg-navy`
    - src/components/pos/CategoryFilterBar.tsx contains `overflow-x-auto`
    - src/components/pos/ProductGrid.tsx contains `grid-cols-[repeat(auto-fill,minmax(140px,1fr))]`
    - src/components/pos/ProductGrid.tsx contains `inputMode="search"`
    - src/components/pos/ProductGrid.tsx contains `text-base` (16px inputs to prevent iOS zoom)
    - src/components/pos/ProductGrid.tsx contains `No products match`
    - Next.js build succeeds without errors
  </acceptance_criteria>
  <done>Product grid renders with category filtering, search by name/SKU, SKU quick-entry (D-03), stock badges with color coding (D-13), disabled out-of-stock cards, and 44px+ touch targets. All components follow UI-SPEC layout contract.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- POS page at `/pos` renders two-column layout with product grid on left
- Products display with images, names, prices, and stock badges
- Category pills filter the grid
- Search filters by name and SKU
- `npx vitest run` — all tests still pass
</verification>

<success_criteria>
- POS page Server Component fetches data via admin client with store_id from staff JWT (not public read)
- POSClientShell uses useReducer with cartReducer from Plan 01
- Stock refreshes on visibilitychange via router.refresh()
- All touch targets are minimum 44x44px
- All inputs are minimum 16px font-size (no iOS auto-zoom)
- Product cards show stock count with green/amber/red color coding
- Out-of-stock products are visually disabled
</success_criteria>

<output>
After completion, create `.planning/phases/03-pos-checkout/03-02-SUMMARY.md`
</output>
