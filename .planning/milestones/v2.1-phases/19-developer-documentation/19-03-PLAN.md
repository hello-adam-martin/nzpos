---
phase: 19-developer-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/server-actions.md
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "All 48 Server Actions are catalogued in the inventory"
    - "Each action has name, auth requirement, Zod input schema summary, and description"
    - "Actions are grouped by domain matching the src/actions/ directory structure"
  artifacts:
    - path: "docs/server-actions.md"
      provides: "Complete 48-action inventory (DOC-04)"
      contains: "completeSale"
      min_lines: 100
  key_links:
    - from: "docs/server-actions.md"
      to: "src/actions/"
      via: "file path references"
      pattern: "src/actions/"
---

<objective>
Write the Server Action inventory documenting all 48 actions with name, auth requirement, Zod input schema summary, and description, grouped by domain.

Purpose: A developer can look up any Server Action's contract (what it needs, who can call it, what it does) without reading source code.

Output: docs/server-actions.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-developer-documentation/19-CONTEXT.md
@.planning/phases/19-developer-documentation/19-RESEARCH.md

Source directories to reference:
@src/actions/auth/
@src/actions/orders/
@src/actions/products/
@src/actions/categories/
@src/actions/billing/
@src/actions/super-admin/
@src/actions/setup/
@src/actions/xero/
@src/actions/promos/
@src/actions/cash-sessions/
@src/schemas/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/server-actions.md — Full 48-action inventory</name>
  <files>docs/server-actions.md</files>
  <read_first>
    - src/actions/auth/ (list all files, read each to extract: export name, Zod schema, auth check pattern)
    - src/actions/orders/ (same)
    - src/actions/products/ (same)
    - src/actions/categories/ (same)
    - src/actions/billing/ (same)
    - src/actions/super-admin/ (same)
    - src/actions/setup/ (same)
    - src/actions/xero/ (same)
    - src/actions/promos/ (same)
    - src/actions/cash-sessions/ (same)
    - src/schemas/ (Zod schema definitions shared across actions)
    - .planning/phases/19-developer-documentation/19-RESEARCH.md (complete inventory at lines 216-306, action pattern at lines 188-212)
  </read_first>
  <action>
    Write docs/server-actions.md with:

    **# Server Action Reference**

    Intro paragraph: "NZPOS has 48 Server Actions organized in `src/actions/{domain}/`. All follow a consistent pattern: `'use server'` directive, `server-only` import, Zod input validation, auth check, database operation, and `{ success, error?, data? }` response."

    **## Common Pattern**
    Code block showing the standard action structure:
    ```typescript
    'use server'
    import 'server-only'

    export async function actionName(input: unknown) {
      const auth = await resolveAuth()
      if (!auth) return { success: false, error: 'Not authenticated' }

      const parsed = Schema.safeParse(input)
      if (!parsed.success) return { success: false, error: 'Invalid input' }

      const supabase = createSupabaseAdminClient()
      // ... database operation
      return { success: true, data: result }
    }
    ```

    **## Action Inventory**
    One H3 section per domain (per D-07). Each section has a Markdown table with 4 columns (per D-08): **Name** | **Auth** | **Input Schema** | **Description**

    For each action, read the actual source file to get:
    - The exported function name
    - The auth check (resolveAuth, resolveStaffAuth, supabase.auth.getUser, or none for public)
    - The Zod schema shape — summarize as key fields with types, e.g. `{ email: string, password: string }`
    - One-line description of what it does

    The 10 domain tables (from D-07, verified in research):

    **### Auth** (~15 actions)
    Read every file in src/actions/auth/. Include ownerSignup, ownerSignin, signOut, changePassword, updateEmail, updateProfile, resetPassword, resendVerification, provisionStore, retryProvisioning, checkSlugAvailability, verifyStaffPin (staffPin.ts), customerSignup, customerSignin, customerSignOut.

    **### Orders** (6 actions)
    completeSale, createCheckoutSession, processRefund, processPartialRefund, updateOrderStatus, sendPosReceipt.

    **### Products** (5 actions)
    createProduct, updateProduct, deactivateProduct, importProducts, lookupBarcode.

    **### Categories** (4 actions)
    createCategory, updateCategory, deleteCategory, reorderCategories.

    **### Billing** (2 actions)
    createSubscriptionCheckoutSession, createBillingPortalSession.

    **### Super Admin** (4 actions)
    suspendTenant, unsuspendTenant, activateAddon, deactivateAddon.

    **### Setup** (5 actions)
    saveStoreNameStep, saveLogoStep, saveProductStep, dismissWizard, updateBranding.

    **### Xero** (3 actions)
    triggerManualSync, saveXeroSettings, disconnectXero.

    **### Promos** (2 actions)
    createPromoCode, validatePromoCode.

    **### Cash Sessions** (2 actions)
    openCashSession, closeCashSession.

    **Input Schema column format:** Summarize as the key fields. Examples:
    - `{ email: string, password: string }` for ownerSignin
    - `{ storeId: uuid, staffId: uuid, pin: string }` for verifyStaffPin
    - `{ items: CartItem[], paymentMethod: enum, ... }` for completeSale
    - `(none)` for actions with no input (e.g. signOut)

    **Auth column values:** Use short labels: `public`, `owner`, `staff/owner`, `customer`, `super-admin`

    **Footer section:**
    "## Maintenance"
    Note: "This inventory reflects 48 actions as of Phase 19. To verify the current count:"
    ```bash
    find src/actions -name "*.ts" ! -path "*__tests__*" ! -name "*.test.ts" -type f | wc -l
    ```
    "If the count has changed, update this document."
  </action>
  <verify>
    <automated>test -f docs/server-actions.md && grep -c "completeSale\|ownerSignup\|createProduct\|suspendTenant\|openCashSession\|validatePromoCode\|triggerManualSync\|createCategory\|createSubscriptionCheckoutSession\|dismissWizard" docs/server-actions.md | xargs test 10 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/server-actions.md exists: test -f docs/server-actions.md
    - All 10 domain sections present: grep "### Auth" docs/server-actions.md succeeds AND grep "### Orders" docs/server-actions.md succeeds AND grep "### Products" docs/server-actions.md succeeds AND grep "### Categories" docs/server-actions.md succeeds AND grep "### Billing" docs/server-actions.md succeeds AND grep "### Super Admin" docs/server-actions.md succeeds AND grep "### Setup" docs/server-actions.md succeeds AND grep "### Xero" docs/server-actions.md succeeds AND grep "### Promos" docs/server-actions.md succeeds AND grep "### Cash Sessions" docs/server-actions.md succeeds
    - Key actions from each domain documented: grep "completeSale" docs/server-actions.md succeeds, grep "ownerSignup" docs/server-actions.md succeeds, grep "createProduct" docs/server-actions.md succeeds, grep "suspendTenant" docs/server-actions.md succeeds
    - Four columns per table (per D-08): grep "Auth" docs/server-actions.md succeeds AND grep "Input Schema" docs/server-actions.md succeeds AND grep "Description" docs/server-actions.md succeeds
    - Common pattern section exists: grep "Common Pattern" docs/server-actions.md succeeds
    - Maintenance section with count verification command: grep "wc -l" docs/server-actions.md succeeds
    - 48 actions total across all tables (spot check — count rows): grep -c "|" docs/server-actions.md returns a high count (each action is a table row with pipes)
  </acceptance_criteria>
  <done>docs/server-actions.md inventories all 48 Server Actions across 10 domain groups. Each action has name, auth requirement, Zod input schema summary, and one-line description. Includes common pattern example and maintenance instructions for keeping the count current.</done>
</task>

</tasks>

<verification>
- docs/server-actions.md covers all 48 actions across 10 domains
- Each action row has 4 columns: Name, Auth, Input Schema, Description
- Action names match actual exported function names in src/actions/
- Maintenance section includes a verification command
</verification>

<success_criteria>
- docs/server-actions.md exists with all 48 actions documented
- All 10 domain groups have their own table
- Schema summaries are accurate (verified against actual Zod schemas in source)
- A developer can look up any action's contract without reading source code
</success_criteria>

<output>
After completion, create `.planning/phases/19-developer-documentation/19-03-SUMMARY.md`
</output>
