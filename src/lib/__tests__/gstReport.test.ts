import { describe, it } from 'vitest'

describe('GST report', () => {
  it.todo('computes total GST collected from order gst_cents')
  it.todo('computes GST-exclusive total as total minus GST')
  it.todo('excludes refunded orders from GST totals')
  it.todo('shows refunded GST amounts separately')
})
