import { describe, it } from 'vitest'

describe('Reports data aggregation', () => {
  it.todo('groups orders by date for daily totals')
  it.todo('computes channel breakdown (POS vs online)')
  it.todo('aggregates top products by revenue')
  it.todo('includes refunded orders in reporting queries')
})
