import { describe, it } from 'vitest'

describe('openCashSession', () => {
  it.todo('creates session with opening float')
  it.todo('rejects when session already open')
  it.todo('rejects unauthenticated request')
})
