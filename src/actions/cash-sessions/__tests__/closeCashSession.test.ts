import { describe, it } from 'vitest'

describe('closeCashSession', () => {
  it.todo('closes session with variance calculation')
  it.todo('rejects when session not found')
  it.todo('rejects when session already closed')
})
